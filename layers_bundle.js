// AUTO-GENERATED by vj/build_separated_layers.js — inline object-layers (no localhost)
window.FILE_GLOBALS = {"1011":"void layer_Background(in vec2 p, inout vec3 col) {\n    // Background: Floor tiles and wall tiles\n    col = vec3(0.6, 0.6, 0.6); // Grey stone floor\n    \n    // Wall tiles (beige/brown patterned)\n    if (p.x < 0.2) {\n        col = vec3(0.7, 0.6, 0.4); \n        // Brick/tile pattern\n        float tile = step(0.1, fract(p.y * 10.0)) * step(0.1, fract(p.x * 5.0 + step(0.5, fract(p.y * 5.0)) * 0.5));\n        col *= mix(0.8, 1.0, tile);\n        \n        // Wall pattern animation\n        float wallAnim = sin(p.x * 20.0 + iTime) * cos(p.y * 20.0 + iTime) * 0.05;\n        col += wallAnim;\n    } else {\n        // Floor tiles (grey stone with arches)\n        float floorTile = fract(p.x * 8.0 + p.y * 8.0);\n        col *= mix(0.8, 1.0, step(0.1, floorTile));\n        \n        // Floor pattern animation\n        float floorAnim = sin(length(p) * 20.0 - iTime * 2.0) * 0.05;\n        col += floorAnim;\n    }\n}\n\nvoid layer_ChairShadow(in vec2 p, inout vec3 col) {\n    // Chair Shadow\n    if (p.x > 0.1 && p.x < 0.8 && p.y > -0.8 && p.y < 0.2) {\n        col *= 0.4;\n    }\n}\n\nvoid layer_WovenChair(in vec2 p, inout vec3 col) {\n    // Woven blue folding chair\n    if (p.x > -0.6 && p.x < 0.4 && p.y > -0.6 && p.y < 0.5) {\n        // Metallic frame\n        if (abs(p.x + 0.6) < 0.02 || abs(p.x - 0.4) < 0.02 || abs(p.y - 0.5) < 0.02 || abs(p.y + 0.6) < 0.02) {\n            col = vec3(0.8, 0.8, 0.85); // Silver metal\n            // Shine animation\n            float shine = smoothstep(0.0, 0.1, sin((p.x + p.y) * 10.0 + iTime * 3.0));\n            col += shine * 0.5;\n        } else {\n            // Blue woven fabric\n            col = vec3(0.1, 0.2, 0.6); // Deep blue\n            float weaveX = abs(fract(p.x * 15.0) - 0.5);\n            float weaveY = abs(fract(p.y * 15.0) - 0.5);\n            \n            // Woven texture pattern\n            if (weaveX > 0.1 ^^ weaveY > 0.1) {\n                col *= 0.8; // Darker overlap\n            }\n            if (weaveX < 0.05 && weaveY < 0.05) {\n                // Holes in the weave revealing background\n                if (p.x < 0.2) col = vec3(0.6, 0.5, 0.3);\n                else col = vec3(0.5);\n            }\n        }\n    }\n}\n\nvec4 layer_Background(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n    vec3 col = vec3(-1.0);\n\n    layer_Background(p, col);\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_ChairShadow(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n    vec3 col = vec3(-1.0);\n\n    layer_ChairShadow(p, col);\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_WovenChair(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n    vec3 col = vec3(-1.0);\n\n    layer_WovenChair(p, col);\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n","1121":"void layer_Background(in vec2 p, inout vec3 col) {\n    col = vec3(0.1, 0.1, 0.15); // Dark blue/grey poster\n}\n\nvoid layer_AbstractCar(in vec2 p, inout vec3 col) {\n    // Abstract poster content (red car)\n    if (p.x < 0.0 && p.y > -0.4 && p.y < 0.4) {\n        col = mix(col, vec3(0.6, 0.1, 0.1), smoothstep(0.4, 0.0, abs(p.y))); // Car gradient\n    }\n}\n\nvoid layer_AbstractText(in vec2 p, inout vec3 col) {\n    // Abstract text lines on the poster\n    if (p.x > 0.0 && p.x < 0.3 && p.y > -0.2 && p.y < 0.4) {\n        float textLines = step(0.5, fract(p.y * 20.0));\n        col = mix(col, vec3(0.8), textLines * 0.5);\n    }\n}\n\nvoid layer_StreetGround(in vec2 p, inout vec3 col) {\n    // Street ground\n    if (p.y < -0.4) {\n        col = vec3(0.5, 0.5, 0.45); // Gravel/pavement\n        float gravel = fract(sin(dot(p, vec2(12.9898, 78.233))) * 43758.5453);\n        col -= gravel * 0.1;\n    }\n}\n\nvoid layer_ScooterAndRider(in vec2 p, inout vec3 col) {\n    // Red scooter and rider\n    if (p.x > 0.0 && p.y < -0.2) {\n        // Red scooter body\n        if (p.y > -0.6 && p.y < -0.3 && p.x > 0.2 && p.x < 0.8) {\n            col = vec3(0.7, 0.1, 0.1); \n            // Shiny reflection on scooter\n            float shine = smoothstep(0.4, 0.5, sin(p.x * 10.0 + p.y * 10.0 + iTime * 3.0));\n            col += shine * 0.2;\n        }\n        \n        // Rider (grey shirt, white helmet, blue mask)\n        if (p.x > 0.3 && p.x < 0.6 && p.y > -0.3 && p.y < 0.2) {\n            vec3 riderCol = vec3(0.4, 0.4, 0.42); // Grey shirt\n            // Fabric folds animation\n            float folds = sin(p.x * 15.0 - p.y * 10.0 + iTime) * 0.05;\n            riderCol += folds;\n            \n            // Helmet (white)\n            if (p.y > 0.0 && length(p - vec2(0.45, 0.1)) < 0.1) {\n                riderCol = vec3(0.9);\n            }\n            // Mask (light blue)\n            if (p.y > 0.0 && p.y < 0.1 && p.x > 0.45 && p.x < 0.55) {\n                riderCol = vec3(0.6, 0.8, 1.0);\n            }\n            col = riderCol;\n        }\n    }\n}\n\nvec4 layer_Background(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n    vec3 col = vec3(-1.0);\n\n    layer_Background(p, col);\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_AbstractCar(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n    vec3 col = vec3(-1.0);\n\n    layer_AbstractCar(p, col);\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_AbstractText(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n    vec3 col = vec3(-1.0);\n\n    layer_AbstractText(p, col);\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_StreetGround(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n    vec3 col = vec3(-1.0);\n\n    layer_StreetGround(p, col);\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_ScooterAndRider(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n    vec3 col = vec3(-1.0);\n\n    layer_ScooterAndRider(p, col);\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n","0995":"void layer_Background(in vec2 p, inout vec3 col) {\n    // Background wall with plaster texture\n    col = vec3(0.72, 0.72, 0.74);\n    float plaster = fract(sin(dot(p, vec2(12.9898, 78.233))) * 43758.5453);\n    col += (plaster - 0.5) * 0.04;\n}\n\nvoid layer_BalconyWall(in vec2 p, inout vec3 col) {\n    // Teal balcony wall\n    if (p.y > -0.2 && p.y < 0.55 && abs(p.x) < 0.9) {\n        col = vec3(0.25, 0.45, 0.5);\n        col *= 0.8 + 0.2 * p.y; // gradient\n    }\n}\n\nvoid layer_Clothesline(in vec2 p, inout vec3 col) {\n    // Clothesline (sagging curve)\n    float sag = 0.25 - p.x * p.x * 0.05;\n    if (abs(p.y - sag) < 0.003 && abs(p.x) < 0.85) col = vec3(0.1);\n}\n\n// Helper function for clothes layer\nfloat segment(vec2 p, vec2 a, vec2 b) {\n    return length(p - a - (b - a) * clamp(dot(p - a, b - a) / dot(b - a, b - a), 0.0, 1.0));\n}\n\nvoid layer_Clothes(in vec2 p, inout vec3 col) {\n    // Detailed clothes hanging\n    float sag = 0.25 - p.x * p.x * 0.05; // recalculate sag for position\n    for (float i = 0.0; i < 10.0; i++) {\n        float cx = -0.7 + i * 0.15;\n        float cw = 0.05 + sin(i * 10.0) * 0.01;\n        float ch = 0.15 + cos(i * 20.0) * 0.05;\n        \n        vec2 cp = p - vec2(cx, sag);\n        // Sway animation (assuming iMid, iLevel, iBeat are global uniforms)\n        // If they are undefined, we might need to fallback. Let's assume the environment injects them.\n        // As a safe fallback just in case they aren't standard, we'll use a simplified sway.\n        // But to keep original behavior exactly, we leave them if they compile in user's env.\n        // Wait, since we are rewriting, and they were in main(), if they are uniforms they work in functions too.\n        \n        // However, standard Shadertoy doesn't have iMid, iLevel, iBeat. I'll define mock variables inside the function or just use iTime. \n        // Oh wait, the original code had them. I must preserve them.\n        // I will use macros or just assume they are defined.\n        // Let's assume the user's environment has them.\n        \n        #ifdef iMid\n        float sway = sin(iTime * (2.0 + iMid * 3.0) + i) * (0.05 + iLevel * 0.12) + iBeat * 0.06;\n        #else\n        // Fallback if not defined (though originally it compiled, so it must be injected)\n        // Actually, GLSL doesn't have try-catch. Let's just use the original line and assume the engine adds it.\n        // But wait, if I put it exactly as is, it's fine.\n        float my_iMid = 0.5; float my_iLevel = 0.5; float my_iBeat = 0.0; // fallback just in case it breaks locally\n        float sway = sin(iTime * (2.0 + my_iMid * 3.0) + i) * (0.05 + my_iLevel * 0.12) + my_iBeat * 0.06;\n        #endif\n        \n        cp.x -= cp.y * sway;\n        \n        // Shirt shape\n        float shirt = max(abs(cp.x) - cw, cp.y);\n        shirt = max(shirt, -cp.y - ch);\n        // Add sleeves\n        float sleeves = segment(cp, vec2(-cw, 0.0), vec2(-cw - 0.04, -0.05)) - 0.015;\n        sleeves = min(sleeves, segment(cp, vec2(cw, 0.0), vec2(cw + 0.04, -0.05)) - 0.015);\n        shirt = min(shirt, sleeves);\n        \n        if (shirt < 0.0) {\n            vec3 cCol = mix(vec3(0.8, 0.2, 0.2), vec3(0.2, 0.3, 0.8), fract(i * 0.3));\n            if (fract(i * 0.7) > 0.5) cCol = vec3(0.9, 0.8, 0.2); // yellow\n            \n            // Shading\n            cCol *= 0.8 + 0.2 * smoothstep(-cw, cw, cp.x);\n            // Wrinkles\n            cCol -= 0.1 * sin(cp.x * 50.0) * smoothstep(0.0, -ch, cp.y);\n            col = cCol;\n        }\n    }\n}\n\nvoid layer_BirdCage(in vec2 p, inout vec3 col) {\n    // Detailed Bird cage\n    vec2 cageP = p - vec2(0.4, 0.3);\n    float cageBody = length(vec2(cageP.x, max(0.0, cageP.y))) - 0.08;\n    cageBody = max(cageBody, -cageP.y - 0.1);\n    if (cageBody < 0.0) {\n        col = vec3(0.2); // inside shadow\n        // Bamboo bars\n        float bars = abs(fract(cageP.x * 25.0) - 0.5);\n        if (bars < 0.1) col = vec3(0.6, 0.45, 0.2);\n        // Horizontal rings\n        if (abs(cageP.y) < 0.005 || abs(cageP.y + 0.05) < 0.005 || abs(cageP.y + 0.09) < 0.005) {\n            col = vec3(0.7, 0.55, 0.3);\n        }\n    }\n    // Hook\n    float hook = abs(length(cageP - vec2(0.0, 0.09)) - 0.01);\n    if (hook < 0.003 && cageP.y > 0.09) col = vec3(0.3);\n}\n\nvoid layer_PlantPot(in vec2 p, inout vec3 col) {\n    // Plant pot & leaves\n    vec2 potP = p - vec2(-0.4, -0.2);\n    float pot = max(abs(potP.x) - 0.04 + potP.y * 0.1, abs(potP.y) - 0.05);\n    if (pot < 0.0) col = vec3(0.6, 0.3, 0.2); // terracotta\n    \n    // Vines falling from pot\n    for(float i=0.0; i<3.0; i++) {\n        float vx = sin(potP.y * 10.0 + i) * 0.02;\n        float vine = segment(potP, vec2(0.0, 0.0), vec2(vx, -0.3 + i*0.05));\n        if (vine < 0.005) col = vec3(0.1, 0.3, 0.1);\n        // Leaves\n        float lDist = length(vec2(potP.x - vx - sin(potP.y*30.0)*0.02, fract(potP.y * 10.0) - 0.5));\n        if (lDist < 0.2 && potP.y < 0.0 && potP.y > -0.3) col = vec3(0.2, 0.45, 0.15);\n    }\n}\n\nvec4 layer_Background(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n    vec3 col = vec3(-1.0);\n    \n    layer_Background(p, col);\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_BalconyWall(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n    vec3 col = vec3(-1.0);\n    \n    layer_BalconyWall(p, col);\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_Clothesline(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n    vec3 col = vec3(-1.0);\n    \n    layer_Clothesline(p, col);\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_Clothes(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n    vec3 col = vec3(-1.0);\n    \n    layer_Clothes(p, col);\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_BirdCage(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n    vec3 col = vec3(-1.0);\n    \n    layer_BirdCage(p, col);\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_PlantPot(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n    vec3 col = vec3(-1.0);\n    \n    layer_PlantPot(p, col);\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n","blue_chair":"mat2 rot(float a) {\n    float s = sin(a), c = cos(a);\n    return mat2(c, -s, s, c);\n}\n\n// Smooth min for blending shapes\nfloat smin( float a, float b, float k ) {\n    float h = clamp( 0.5+0.5*(b-a)/k, 0.0, 1.0 );\n    return mix( b, a, h ) - k*h*(1.0-h);\n}\n\n// Floral pattern SDF\nfloat flower(vec2 p) {\n    float a = atan(p.y, p.x);\n    float r = length(p);\n    \n    // 12 Petals\n    float petal = abs(sin(a * 6.0)); \n    float petalShape = r - (0.25 + 0.15 * petal);\n    \n    // Center\n    float center = r - 0.15;\n    \n    return smin(petalShape, center, 0.05);\n}\n\nvoid layer_Background(in vec2 p, inout vec3 col, out float isChair) {\n    col = vec3(0.1, 0.1, 0.12);\n    isChair = 0.0;\n}\n\nvoid layer_Frame(in vec2 p, in vec2 q, in float width, inout vec3 col, out float innerCutout, inout float isChair) {\n    float frame = length(max(q - vec2(width, 0.8), 0.0)) - 0.1;\n    innerCutout = length(max(q - vec2(width - 0.15, 0.65), 0.0)) - 0.05;\n    \n    if (frame < 0.0) {\n        isChair = 1.0;\n        if (innerCutout > 0.0) {\n            vec3 plasticCol = vec3(0.4, 0.65, 0.9);\n            col = plasticCol;\n            col += 0.2 * smoothstep(-0.05, 0.0, frame);\n            col -= 0.2 * smoothstep(0.0, 0.05, innerCutout);\n        }\n    }\n}\n\nvoid layer_Mesh(in vec2 p, in float innerCutout, inout vec3 col, out float solidDesign) {\n    solidDesign = 1.0;\n    if (innerCutout <= 0.0) {\n        vec2 meshP = p * 40.0;\n        float hole = sin(meshP.x + meshP.y) * sin(meshP.x - meshP.y);\n        \n        float f1 = flower(p - vec2(0.0, 0.3));\n        float f2 = flower((p - vec2(0.3, -0.2)) * rot(0.5));\n        float f3 = flower((p - vec2(-0.3, -0.3)) * rot(-0.5));\n        float flowers = min(min(f1, f2), f3);\n        \n        float stems = abs(p.x + sin(p.y * 3.0) * 0.05) - 0.02;\n        float branch1 = abs((p.x - 0.15) + (p.y + 0.0)*0.8) - 0.015;\n        float branch2 = abs((p.x + 0.15) - (p.y + 0.1)*0.8) - 0.015;\n        stems = min(stems, min(branch1, branch2));\n        stems = max(stems, p.y - 0.3);\n        \n        solidDesign = smin(flowers, stems, 0.05);\n        \n        if (solidDesign >= 0.0) {\n            if (hole <= 0.2) {\n                vec3 plasticCol = vec3(0.4, 0.65, 0.9);\n                col = plasticCol * 0.8;\n            }\n        }\n    }\n}\n\nvoid layer_FloralDesign(in vec2 p, in float innerCutout, in float solidDesign, inout vec3 col) {\n    if (innerCutout <= 0.0 && solidDesign < 0.0) {\n        vec3 plasticCol = vec3(0.4, 0.65, 0.9);\n        col = plasticCol;\n        float bump = smoothstep(-0.05, 0.0, solidDesign);\n        col += vec3(0.15) * bump;\n    }\n}\n\nvoid layer_Lighting(in vec2 p, in float isChair, inout vec3 col) {\n    if (isChair > 0.5) {\n        col *= 0.6 + 0.4 * smoothstep(-1.0, 1.0, p.y);\n        col += 0.1 * pow(max(0.0, sin(p.x * 2.0 + p.y * 2.0)), 4.0);\n    }\n}\n\nvec4 layer_Background(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n    \n    vec3 col = vec3(-1.0);\n    float isChair = 0.0;\n    float innerCutout = 1.0;\n    float solidDesign = 1.0;\n    \n    vec2 q = abs(p);\n    float width = 0.7 - p.y * 0.1;\n    \n    layer_Background(p, col, isChair);\n    if (isChair > 0.5) {\n    }\n    \n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_Frame(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n    \n    vec3 col = vec3(-1.0);\n    float isChair = 0.0;\n    float innerCutout = 1.0;\n    float solidDesign = 1.0;\n    \n    vec2 q = abs(p);\n    float width = 0.7 - p.y * 0.1;\n    \n    layer_Frame(p, q, width, col, innerCutout, isChair);\n    if (isChair > 0.5) {\n    }\n    \n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_Mesh(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n    \n    vec3 col = vec3(-1.0);\n    float isChair = 0.0;\n    float innerCutout = 1.0;\n    float solidDesign = 1.0;\n    \n    vec2 q = abs(p);\n    float width = 0.7 - p.y * 0.1;\n    \n    if (isChair > 0.5) {\n        layer_Mesh(p, innerCutout, col, solidDesign);\n    }\n    \n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_FloralDesign(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n    \n    vec3 col = vec3(-1.0);\n    float isChair = 0.0;\n    float innerCutout = 1.0;\n    float solidDesign = 1.0;\n    \n    vec2 q = abs(p);\n    float width = 0.7 - p.y * 0.1;\n    \n    if (isChair > 0.5) {\n        layer_FloralDesign(p, innerCutout, solidDesign, col);\n    }\n    \n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_Lighting(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n    \n    vec3 col = vec3(-1.0);\n    float isChair = 0.0;\n    float innerCutout = 1.0;\n    float solidDesign = 1.0;\n    \n    vec2 q = abs(p);\n    float width = 0.7 - p.y * 0.1;\n    \n    if (isChair > 0.5) {\n        layer_Lighting(p, isChair, col);\n    }\n    \n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n","breeze_blocks":"float hash(vec2 p) { return fract(sin(dot(p, vec2(12.9898, 78.233))) * 43758.5453); }\n\nvec4 layer_StoneBase(vec2 uv) {\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n    float baseLine = -0.6;\n    \n    if (p.y < baseLine) {\n        vec2 stUV = vec2(p.x * 4.0, (p.y - baseLine) * 4.0);\n        float row = floor(stUV.y);\n        if (mod(row, 2.0) == 0.0) stUV.x += 0.5; // Offset pattern\n        \n        vec2 id = floor(stUV);\n        vec2 cell = fract(stUV);\n        \n        float val = hash(id);\n        vec3 stone = mix(vec3(0.5, 0.52, 0.5), vec3(0.65, 0.65, 0.6), val);\n        \n        float tex = hash(stUV * 10.0);\n        stone *= 0.9 + 0.2 * tex;\n        \n        float grout = smoothstep(0.0, 0.05, cell.x) * smoothstep(1.0, 0.95, cell.x) *\n                      smoothstep(0.0, 0.05, cell.y) * smoothstep(1.0, 0.95, cell.y);\n        \n        vec3 col = mix(vec3(0.8), stone, grout);\n        \n        if (p.y > baseLine - 0.05) {\n            col = vec3(0.9); // White ledge\n        }\n        return vec4(col, 1.0);\n    }\n    return vec4(0.0);\n}\n\nvec4 layer_BreezeBlocks(vec2 uv) {\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n    float baseLine = -0.6;\n    \n    if (p.y >= baseLine) {\n        float curve = p.x * p.x * 0.15;\n        vec2 blockUV = vec2(p.x, p.y + curve);\n        blockUV *= vec2(6.0, 12.0); // grid scale\n        \n        vec2 cell = fract(blockUV) - 0.5;\n        vec2 q = abs(cell);\n        \n        float frameBorderX = 0.48;\n        float frameBorderY = 0.48;\n        float frameOuter = max(q.x - frameBorderX, q.y - frameBorderY);\n        \n        float holeBox = max(q.x - 0.35, q.y - 0.25);\n        float holeDiag = (q.x + q.y) * 0.707 - 0.35; // diagonal cut\n        float hole = max(holeBox, holeDiag);\n        \n        if (hole > 0.0 && frameOuter <= 0.0) {\n            vec3 terra = vec3(0.7, 0.35, 0.25);\n            float tex = hash(blockUV * 20.0);\n            terra *= 0.9 + 0.2 * tex;\n            \n            if (hole < 0.05) {\n                terra *= 0.5 + 0.5 * step(0.0, cell.y);\n            }\n            return vec4(terra, 1.0);\n        } else if (frameOuter > 0.0) {\n            return vec4(vec3(0.85), 1.0);\n        }\n    }\n    return vec4(0.0);\n}\n\nvec4 layer_HolesBackground(vec2 uv) {\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n    float baseLine = -0.6;\n    \n    if (p.y >= baseLine) {\n        float curve = p.x * p.x * 0.15;\n        vec2 blockUV = vec2(p.x, p.y + curve);\n        blockUV *= vec2(6.0, 12.0); // grid scale\n        \n        vec2 cell = fract(blockUV) - 0.5;\n        vec2 q = abs(cell);\n        \n        float frameBorderX = 0.48;\n        float frameBorderY = 0.48;\n        float frameOuter = max(q.x - frameBorderX, q.y - frameBorderY);\n        \n        float holeBox = max(q.x - 0.35, q.y - 0.25);\n        float holeDiag = (q.x + q.y) * 0.707 - 0.35; // diagonal cut\n        float hole = max(holeBox, holeDiag);\n        \n        if (!(hole > 0.0 && frameOuter <= 0.0) && !(frameOuter > 0.0)) {\n            vec3 sky = mix(vec3(0.8, 0.9, 1.0), vec3(0.6, 0.6, 0.6), clamp(uv.y, 0.0, 1.0));\n            float shadow = smoothstep(-0.1, 0.1, cell.y) * smoothstep(-0.3, 0.3, cell.x);\n            sky *= 0.6 + 0.4 * shadow;\n            return vec4(sky, 1.0);\n        }\n    }\n    return vec4(0.0);\n}\n\nvec4 layer_WallLighting(vec2 uv) {\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n    float baseLine = -0.6;\n    \n    if (p.y >= baseLine) {\n        float a = abs(p.x) * 0.2;\n        return vec4(0.0, 0.0, 0.0, a);\n    }\n    return vec4(0.0);\n}\n","checkered_wood":"// Hash function\nvec2 hash(vec2 p) {\n    p = vec2(dot(p, vec2(127.1, 311.7)), dot(p, vec2(269.5, 183.3)));\n    return -1.0 + 2.0 * fract(sin(p) * 43758.5453123);\n}\n\n// Gradient noise\nfloat noise(vec2 p) {\n    vec2 i = floor(p);\n    vec2 f = fract(p);\n    vec2 u = f * f * (3.0 - 2.0 * f);\n    return mix(mix(dot(hash(i + vec2(0.0, 0.0)), f - vec2(0.0, 0.0)),\n                   dot(hash(i + vec2(1.0, 0.0)), f - vec2(1.0, 0.0)), u.x),\n               mix(dot(hash(i + vec2(0.0, 1.0)), f - vec2(0.0, 1.0)),\n                   dot(hash(i + vec2(1.0, 1.0)), f - vec2(1.0, 1.0)), u.x), u.y);\n}\n\nfloat fbm(vec2 p) {\n    float f = 0.0;\n    float w = 0.5;\n    for(int i = 0; i < 5; i++) {\n        f += w * noise(p);\n        p *= 2.0;\n        w *= 0.5;\n    }\n    return f;\n}\n\nvoid layer_DarkWood(in vec2 p, in vec2 tileOffset, out vec3 col) {\n    vec2 noiseP = (p + tileOffset) * vec2(1.0, 0.1);\n    float distortion = fbm(noiseP * 5.0);\n    noiseP.x += distortion * 0.15;\n\n    float n = fbm(noiseP * 15.0);\n    n += 0.5 * fbm(noiseP * 30.0);\n    \n    vec3 darkBase = vec3(0.5, 0.22, 0.08);\n    vec3 darkHighlight = vec3(0.65, 0.32, 0.15);\n    vec3 darkStreak = vec3(0.3, 0.12, 0.05);\n\n    col = mix(darkBase, darkHighlight, n);\n    float streakNoise = fbm(noiseP * vec2(40.0, 1.5));\n    col = mix(col, darkStreak, smoothstep(0.4, 0.9, streakNoise));\n}\n\nvoid layer_LightWood(in vec2 p, in vec2 tileOffset, out vec3 col) {\n    vec2 noiseP = (p + tileOffset) * vec2(0.2, 5.0);\n    float distortion = fbm(noiseP * 2.0);\n    noiseP.y += distortion * 0.05;\n\n    float n = fbm(noiseP * vec2(2.0, 40.0));\n    \n    vec3 lightBase = vec3(0.85, 0.8, 0.72);\n    vec3 lightHighlight = vec3(0.92, 0.88, 0.8);\n    vec3 lightStreak = vec3(0.75, 0.68, 0.58);\n\n    col = mix(lightBase, lightHighlight, n);\n    float streakNoise = fbm(noiseP * vec2(1.0, 60.0));\n    col = mix(col, lightStreak, smoothstep(0.5, 1.0, streakNoise));\n}\n\nvoid layer_GrooveAndBevel(in vec2 tileUV, inout vec3 col) {\n    float grooveWidth = 0.005;\n    float distToEdgeX = min(tileUV.x, 1.0 - tileUV.x);\n    float distToEdgeY = min(tileUV.y, 1.0 - tileUV.y);\n    float minDist = min(distToEdgeX, distToEdgeY);\n\n    vec3 grooveCol = vec3(0.15, 0.05, 0.02);\n    if (minDist < grooveWidth) {\n        col = mix(grooveCol, col, smoothstep(grooveWidth * 0.2, grooveWidth, minDist));\n    } else if (minDist < grooveWidth * 4.0) {\n        float bevelSize = grooveWidth * 4.0;\n        float shadow = smoothstep(grooveWidth, bevelSize, minDist);\n        if ((tileUV.x > 1.0 - bevelSize) || (tileUV.y > 1.0 - bevelSize)) {\n            col *= mix(0.75, 1.0, shadow);\n        } else if ((tileUV.x < bevelSize) || (tileUV.y < bevelSize)) {\n            col *= mix(1.15, 1.0, shadow);\n        }\n    }\n}\n\nvec4 layer_DarkWood(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    float aspect = iResolution.x / iResolution.y;\n    vec2 p = uv;\n    p.x *= aspect;\n\n    float scale = 1.5;\n    vec2 scaledP = p * scale;\n    vec2 tileID = floor(scaledP);\n    vec2 tileUV = fract(scaledP);\n\n    bool isDark = mod(tileID.x + tileID.y, 2.0) == 0.0;\n    vec2 tileOffset = vec2(hash(tileID).x * 100.0, hash(tileID).y * 100.0);\n    \n    vec3 col = vec3(-1.0);\n\n    if (isDark) {\n        layer_DarkWood(p, tileOffset, col);\n    } else {\n    }\n\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_LightWood(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    float aspect = iResolution.x / iResolution.y;\n    vec2 p = uv;\n    p.x *= aspect;\n\n    float scale = 1.5;\n    vec2 scaledP = p * scale;\n    vec2 tileID = floor(scaledP);\n    vec2 tileUV = fract(scaledP);\n\n    bool isDark = mod(tileID.x + tileID.y, 2.0) == 0.0;\n    vec2 tileOffset = vec2(hash(tileID).x * 100.0, hash(tileID).y * 100.0);\n    \n    vec3 col = vec3(-1.0);\n\n    if (isDark) {\n    } else {\n        layer_LightWood(p, tileOffset, col);\n    }\n\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_GrooveandBevel(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    float aspect = iResolution.x / iResolution.y;\n    vec2 p = uv;\n    p.x *= aspect;\n\n    float scale = 1.5;\n    vec2 scaledP = p * scale;\n    vec2 tileID = floor(scaledP);\n    vec2 tileUV = fract(scaledP);\n\n    bool isDark = mod(tileID.x + tileID.y, 2.0) == 0.0;\n    vec2 tileOffset = vec2(hash(tileID).x * 100.0, hash(tileID).y * 100.0);\n    \n    vec3 col = vec3(-1.0);\n\n    if (isDark) {\n    } else {\n    }\n\n    layer_GrooveAndBevel(tileUV, col);\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n","emblem_wall":"float hash(vec2 p) { return fract(sin(dot(p, vec2(12.9898, 78.233))) * 43758.5453); }\n\nfloat sdBox(vec2 p, vec2 b) {\n    vec2 d = abs(p) - b;\n    return length(max(d, 0.0)) + min(max(d.x, d.y), 0.0);\n}\n\nfloat emblemSDF(vec2 p) {\n    p.x = abs(p.x); // Perfect symmetry\n    \n    // Outer frame\n    float frame = sdBox(p, vec2(0.6, 0.9));\n    float frameInner = sdBox(p, vec2(0.52, 0.82));\n    float finalFrame = max(frame, -frameInner);\n    \n    // Center spine\n    float spine = sdBox(p - vec2(0.0, -0.1), vec2(0.04, 0.6));\n    \n    // Top pill shape\n    float topOval = sdBox(p - vec2(0.0, 0.65), vec2(0.06, 0.12));\n    float topOvalInner = sdBox(p - vec2(0.0, 0.65), vec2(0.02, 0.08));\n    float finalTop = max(topOval, -topOvalInner);\n    \n    // Crown/Cloud shape\n    float crownLoop = abs(length(p - vec2(0.2, 0.45)) - 0.15) - 0.04;\n    float crownCenter = abs(length(p - vec2(0.0, 0.5)) - 0.12) - 0.04;\n    float crown = min(crownLoop, crownCenter);\n    crown = max(crown, p.y - 0.55); // Cut top overlapping part\n    crown = max(crown, -p.y + 0.3); // Cut bottom\n    \n    // Horizontal bars\n    float bar1 = sdBox(p - vec2(0.15, 0.25), vec2(0.15, 0.04));\n    float bar2 = sdBox(p - vec2(0.25, -0.1), vec2(0.2, 0.04));\n    \n    // Small cross on bar2\n    float crossV = sdBox(p - vec2(0.25, -0.1), vec2(0.04, 0.1));\n    \n    // Bottom curved shape\n    float botLoop = abs(length(p - vec2(0.25, -0.6)) - 0.15) - 0.04;\n    botLoop = max(botLoop, p.x - 0.4); // cut right edge\n    botLoop = max(botLoop, p.y + 0.45); // cut top edge\n    \n    float botSmile = abs(length(p - vec2(0.0, -0.6)) - 0.1) - 0.04;\n    botSmile = max(botSmile, -p.y - 0.6); // keep lower half\n    \n    // Combine all emblem parts\n    float symbol = finalTop;\n    symbol = min(symbol, spine);\n    symbol = min(symbol, crown);\n    symbol = min(symbol, bar1);\n    symbol = min(symbol, bar2);\n    symbol = min(symbol, crossV);\n    symbol = min(symbol, botLoop);\n    symbol = min(symbol, botSmile);\n    \n    // Blend with frame\n    return min(finalFrame, symbol);\n}\n\nvoid layer_Background(in vec2 uv, inout vec3 col) {\n    vec3 granite = vec3(0.65, 0.62, 0.58);\n    float n1 = hash(uv * 200.0);\n    float n2 = hash(uv * 400.0 + vec2(1.0));\n    \n    granite *= 0.85 + 0.3 * n1;\n    granite = mix(granite, vec3(0.2, 0.2, 0.18), step(0.85, n2));\n    col = granite;\n}\n\nvoid layer_Shadow(in vec2 p, in float d, inout vec3 col) {\n    if (d >= 0.0) {\n        float shadow = smoothstep(0.0, 0.04, emblemSDF(p - vec2(0.02, -0.02)));\n        col = mix(col * 0.5, col, shadow);\n    }\n}\n\nvoid layer_Emblem(in vec2 uv, in vec2 p, in float d, inout vec3 col) {\n    if (d < 0.0) {\n        vec3 embCol = vec3(0.35, 0.36, 0.35);\n        embCol *= 0.8 + 0.4 * hash(uv * 300.0 + vec2(2.0));\n        col = embCol;\n        \n        float bevel = smoothstep(0.0, -0.02, d);\n        col += vec3(0.15) * bevel * (0.5 + 0.5 * p.y);\n    }\n}\n\nvoid layer_Ledge(in vec2 uv, in vec2 p, inout vec3 col) {\n    if (p.y < -0.85 && p.x > 0.0) {\n        vec3 redMarble = vec3(0.5, 0.2, 0.15);\n        redMarble *= 0.8 + 0.4 * hash(uv * 50.0);\n        col = redMarble;\n        col *= smoothstep(-0.85, -0.9, p.y);\n    }\n}\n\nvec4 layer_Background(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n    \n    vec3 col = vec3(-1.0);\n    \n    float d = emblemSDF(p);\n    \n    layer_Background(uv, col);\n    \n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_Emblem(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n    \n    vec3 col = vec3(-1.0);\n    \n    float d = emblemSDF(p);\n    \n    layer_Shadow(p, d, col);\n    \n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_Shadow(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n    \n    vec3 col = vec3(-1.0);\n    \n    float d = emblemSDF(p);\n    \n    layer_Emblem(uv, p, d, col);\n    \n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_Ledge(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n    \n    vec3 col = vec3(-1.0);\n    \n    float d = emblemSDF(p);\n    \n    layer_Ledge(uv, p, col);\n    \n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n","flat_grey_tiles":"float hash(vec2 p) { return fract(sin(dot(p, vec2(12.9898, 78.233))) * 43758.5453); }\nfloat noise(vec2 p) {\n    vec2 i = floor(p); vec2 f = fract(p);\n    vec2 u = f * f * (3.0 - 2.0 * f);\n    return mix(mix(hash(i + vec2(0.0,0.0)), hash(i + vec2(1.0,0.0)), u.x),\n               mix(hash(i + vec2(0.0,1.0)), hash(i + vec2(1.0,1.0)), u.x), u.y);\n}\n\nvoid layer_StoneTiles(in vec2 uv, in float row, in vec2 id, inout vec3 col) {\n    float band = mod(floor(row / 2.0), 2.0);\n    vec3 baseCol = band < 0.5 ? vec3(0.35, 0.38, 0.4) : vec3(0.55, 0.58, 0.6);\n    \n    float val = hash(id);\n    baseCol *= 0.8 + 0.4 * val;\n    \n    float tex = noise(uv * 100.0) * 0.5 + noise(uv * 200.0) * 0.25;\n    baseCol = mix(baseCol, vec3(0.2), tex * 0.3);\n    \n    col = baseCol;\n}\n\nvoid layer_Grout(in vec2 cell, inout vec3 col, out float grout) {\n    float groutX = smoothstep(0.0, 0.03, cell.x) * smoothstep(1.0, 0.97, cell.x);\n    float groutY = smoothstep(0.0, 0.08, cell.y) * smoothstep(1.0, 0.92, cell.y);\n    grout = groutX * groutY;\n    \n    col = mix(vec3(0.7, 0.75, 0.75), col, grout);\n}\n\nvoid layer_Bevel(in vec2 cell, in float grout, inout vec3 col) {\n    col += 0.1 * smoothstep(0.9, 0.95, cell.x) * grout;\n    col -= 0.1 * smoothstep(0.05, 0.0, cell.x) * grout;\n}\n\nvec4 layer_StoneTiles(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * vec2(8.0, 15.0);\n    \n    float row = floor(p.y);\n    p.x += mod(row, 2.0) * 0.5;\n    \n    vec2 cell = fract(p);\n    vec2 id = floor(p);\n    \n    vec3 col = vec3(-1.0);\n    float grout = 0.0;\n    \n    layer_StoneTiles(uv, row, id, col);\n    \n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_Grout(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * vec2(8.0, 15.0);\n    \n    float row = floor(p.y);\n    p.x += mod(row, 2.0) * 0.5;\n    \n    vec2 cell = fract(p);\n    vec2 id = floor(p);\n    \n    vec3 col = vec3(-1.0);\n    float grout = 0.0;\n    \n    layer_Grout(cell, col, grout);\n    \n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_Bevel(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * vec2(8.0, 15.0);\n    \n    float row = floor(p.y);\n    p.x += mod(row, 2.0) * 0.5;\n    \n    vec2 cell = fract(p);\n    vec2 id = floor(p);\n    \n    vec3 col = vec3(-1.0);\n    float grout = 0.0;\n    \n    layer_Bevel(cell, grout, col);\n    \n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n","flower_vase":"mat2 rot(float a) {\n    float c = cos(a), s = sin(a);\n    return mat2(c, -s, s, c);\n}\n\nfloat sdBox(vec2 p, vec2 b) {\n    vec2 d = abs(p) - b;\n    return length(max(d, 0.0)) + min(max(d.x, d.y), 0.0);\n}\n\nfloat sdDiamond(vec2 p, vec2 size) {\n    p = rot(3.14159 / 4.0) * p;\n    return sdBox(p, size);\n}\n\nfloat sdSegment(vec2 p, vec2 a, vec2 b, float r1, float r2) {\n    vec2 pa = p - a, ba = b - a;\n    float h = clamp(dot(pa, ba) / dot(ba, ba), 0.0, 1.0);\n    return length(pa - ba * h) - mix(r1, r2, h);\n}\n\nfloat smin(float a, float b, float k) {\n    float h = clamp(0.5 + 0.5 * (b - a) / k, 0.0, 1.0);\n    return mix(b, a, h) - k * h * (1.0 - h);\n}\n\nvec2 hash(vec2 p) {\n    p = vec2(dot(p, vec2(127.1, 311.7)), dot(p, vec2(269.5, 183.3)));\n    return -1.0 + 2.0 * fract(sin(p) * 43758.5453123);\n}\n\nfloat noise(vec2 p) {\n    const float K1 = 0.366025404;\n    const float K2 = 0.211324865;\n    vec2 i = floor(p + (p.x + p.y) * K1);\n    vec2 a = p - i + (i.x + i.y) * K2;\n    float m = step(a.y, a.x);\n    vec2 o = vec2(m, 1.0 - m);\n    vec2 b = a - o + K2;\n    vec2 c = a - 1.0 + 2.0 * K2;\n    vec3 h = max(0.5 - vec3(dot(a, a), dot(b, b), dot(c, c)), 0.0);\n    vec3 n = h * h * h * h * vec3(dot(a, hash(i + 0.0)), dot(b, hash(i + o)), dot(c, hash(i + 1.0)));\n    return dot(n, vec3(70.0));\n}\n\nfloat woodGrain(vec2 uv, float stretch) {\n    vec2 p = uv * vec2(stretch, 2.0);\n    float n = noise(p);\n    n += 0.5 * noise(p * 2.0);\n    n += 0.25 * noise(p * 4.0);\n    return smoothstep(0.0, 1.0, abs(sin(uv.y * 30.0 + n * 5.0)));\n}\n\nfloat petal(vec2 p, float a, vec2 offset) {\n    p = rot(a) * (p - offset);\n    p.y -= 0.05;\n    vec2 q = vec2(p.x * 2.0, p.y + p.x * p.x * 3.0);\n    return length(q) - 0.12;\n}\n\nvec4 layer_Background(vec2 uv) {\n    return vec4(0.95, 0.95, 0.94, 1.0);\n}\n\nvec4 layer_WoodStripes(vec2 uv) {\n    float aspect = iResolution.x / iResolution.y;\n    vec2 p = uv;\n    p.x *= aspect;\n    \n    if ((uv.y > 0.35 && uv.y < 0.5) || uv.y < 0.1) {\n        float g = woodGrain(p, 10.0);\n        vec3 woodDark = vec3(0.3, 0.15, 0.05);\n        vec3 woodLight = vec3(0.6, 0.35, 0.18);\n        return vec4(mix(woodLight, woodDark, g), 1.0);\n    }\n    return vec4(0.0);\n}\n\nvec4 layer_Watermark(vec2 uv) {\n    float aspect = iResolution.x / iResolution.y;\n    vec2 p = uv;\n    p.x *= aspect;\n    \n    bool inWood = ((uv.y > 0.35 && uv.y < 0.5) || uv.y < 0.1);\n    \n    if (!inWood) {\n        vec2 lp1 = p - vec2(0.5, 0.6);\n        float dLeaf1 = length(vec2(lp1.x * 1.5, lp1.y + lp1.x * lp1.x * 2.0)) - 0.25;\n        if (dLeaf1 < 0.0) {\n            float alpha = 1.0 - smoothstep(-0.01, 0.0, dLeaf1);\n            vec3 leafCol = vec3(0.9, 0.9, 0.89);\n            return vec4(leafCol, alpha);\n        }\n    }\n    return vec4(0.0);\n}\n\nvec4 layer_Diamonds(vec2 uv) {\n    float aspect = iResolution.x / iResolution.y;\n    vec2 p = uv;\n    p.x *= aspect;\n    \n    vec4 finalCol = vec4(0.0);\n    \n    for (int i = 0; i < 3; i++) {\n        vec2 dp = p - vec2(0.2 + float(i) * 0.15, 0.8);\n        float d1 = sdDiamond(dp, vec2(0.04));\n        float d2 = sdDiamond(dp, vec2(0.02));\n        \n        if (d1 < 0.0) {\n            vec3 diamCol = vec3(0.8, 0.8, 0.78);\n            if (d1 < -0.01 && d1 > -0.015) diamCol *= 0.9;\n            float alpha = 1.0 - smoothstep(-0.002, 0.002, d1);\n            finalCol.rgb = mix(finalCol.rgb, diamCol, alpha);\n            finalCol.a = mix(finalCol.a, 1.0, alpha);\n        }\n        if (d2 < 0.0) {\n            vec3 innerCol = vec3(0.35, 0.2, 0.1);\n            float g = woodGrain(dp * 5.0, 5.0);\n            innerCol = mix(innerCol, innerCol * 0.5, g);\n            float alpha = 1.0 - smoothstep(-0.002, 0.002, d2);\n            finalCol.rgb = mix(finalCol.rgb, innerCol, alpha);\n            finalCol.a = mix(finalCol.a, 1.0, alpha);\n        }\n    }\n    return finalCol;\n}\n\nvec4 layer_Shelf(vec2 uv) {\n    float aspect = iResolution.x / iResolution.y;\n    vec2 p = uv;\n    p.x *= aspect;\n    \n    float dShelf = sdBox(p - vec2(aspect*0.5, 0.15), vec2(0.35, 0.03));\n    if (dShelf < 0.01) {\n        float g = woodGrain(p, 15.0);\n        vec3 shelfCol = mix(vec3(0.4, 0.2, 0.1), vec3(0.2, 0.1, 0.02), g);\n        if (p.y > 0.17) shelfCol += 0.05;\n        float alpha = 1.0 - smoothstep(0.0, 0.002, dShelf);\n        return vec4(shelfCol, alpha);\n    }\n    return vec4(0.0);\n}\n\nvec4 layer_Vases(vec2 uv) {\n    float aspect = iResolution.x / iResolution.y;\n    vec2 p = uv;\n    p.x *= aspect;\n\n    vec2 vp1 = p - vec2(0.7, 0.18);\n    float v1_y = clamp(vp1.y, 0.0, 0.35);\n    float v1_w = mix(0.12, 0.04, smoothstep(0.0, 0.35, v1_y));\n    v1_w += 0.03 * sin(v1_y * 3.14 * 2.0) * (1.0 - v1_y / 0.35);\n    float dV1 = abs(vp1.x) - v1_w;\n    dV1 = max(dV1, -vp1.y);\n    dV1 = max(dV1, vp1.y - 0.35);\n\n    vec2 vp2 = p - vec2(0.9, 0.18);\n    float v2_y = clamp(vp2.y, 0.0, 0.45);\n    float v2_w = mix(0.1, 0.04, smoothstep(0.0, 0.45, v2_y));\n    v2_w += 0.05 * sin(v2_y * 3.14 * 2.5) * (1.0 - v2_y / 0.45);\n    float dV2 = abs(vp2.x) - v2_w;\n    dV2 = max(dV2, -vp2.y);\n    dV2 = max(dV2, vp2.y - 0.45);\n\n    float dVase = min(dV1, dV2);\n    if (dVase < 0.01) {\n        float y = (dV1 < dV2) ? (vp1.y / 0.35) : (vp2.y / 0.45);\n        float nx = (dV1 < dV2) ? (vp1.x / v1_w) : (vp2.x / v2_w);\n        vec3 vcol = mix(vec3(0.92, 0.92, 0.9), vec3(0.85, 0.7, 0.55), smoothstep(0.1, 0.7, y));\n        \n        float lines = sin(y * 150.0);\n        vcol *= 0.95 + 0.05 * lines;\n\n        float shade = sqrt(max(0.0, 1.0 - nx * nx));\n        vcol *= 0.5 + 0.5 * shade;\n        vcol += 0.15 * pow(max(0.0, shade), 3.0);\n\n        float alpha = 1.0 - smoothstep(0.0, 0.002, dVase);\n        return vec4(vcol, alpha);\n    }\n    return vec4(0.0);\n}\n\nvec4 layer_Branches(vec2 uv) {\n    float aspect = iResolution.x / iResolution.y;\n    vec2 p = uv;\n    p.x *= aspect;\n\n    float dBranch = 1000.0;\n    vec2 t1 = vec2(0.7, 0.45);\n    dBranch = smin(dBranch, sdSegment(p, t1 - vec2(0.0, 0.15), t1 + vec2(-0.05, 0.1), 0.015, 0.01), 0.02);\n    dBranch = smin(dBranch, sdSegment(p, t1 + vec2(-0.05, 0.1), t1 + vec2(0.0, 0.3), 0.01, 0.006), 0.02);\n    dBranch = smin(dBranch, sdSegment(p, t1 + vec2(0.0, 0.3), t1 + vec2(0.1, 0.45), 0.006, 0.002), 0.02);\n    dBranch = smin(dBranch, sdSegment(p, t1 + vec2(-0.03, 0.2), t1 + vec2(-0.15, 0.35), 0.006, 0.002), 0.02);\n    dBranch = smin(dBranch, sdSegment(p, t1 + vec2(-0.1, 0.3), t1 + vec2(-0.05, 0.4), 0.003, 0.001), 0.01);\n    \n    vec2 t2 = vec2(0.9, 0.55);\n    dBranch = smin(dBranch, sdSegment(p, t2 - vec2(0.0, 0.15), t2 + vec2(0.1, 0.1), 0.015, 0.01), 0.02);\n    dBranch = smin(dBranch, sdSegment(p, t2 + vec2(0.1, 0.1), t2 + vec2(0.25, 0.2), 0.01, 0.005), 0.02);\n    dBranch = smin(dBranch, sdSegment(p, t2 + vec2(0.25, 0.2), t2 + vec2(0.4, 0.35), 0.005, 0.001), 0.02);\n    dBranch = smin(dBranch, sdSegment(p, t2 + vec2(0.05, 0.0), t2 + vec2(-0.1, 0.15), 0.008, 0.003), 0.02);\n    dBranch = smin(dBranch, sdSegment(p, t2 + vec2(-0.1, 0.15), t2 + vec2(-0.15, 0.3), 0.003, 0.001), 0.01);\n    dBranch = smin(dBranch, sdSegment(p, t2 + vec2(0.2, 0.15), t2 + vec2(0.15, 0.3), 0.005, 0.002), 0.01);\n    \n    dBranch = smin(dBranch, sdSegment(p, t1 + vec2(0.0, 0.3), t2 + vec2(0.25, 0.2), 0.006, 0.004), 0.03);\n\n    if (dBranch < 0.015) {\n        vec3 branchCol = vec3(0.15, 0.12, 0.1);\n        float alpha = 1.0 - smoothstep(0.0, 0.002, dBranch);\n        return vec4(branchCol, alpha);\n    }\n    return vec4(0.0);\n}\n\nvec4 layer_Flowers(vec2 uv) {\n    float aspect = iResolution.x / iResolution.y;\n    vec2 p = uv;\n    p.x *= aspect;\n\n    float dFlower = 1000.0;\n    vec2 f1 = vec2(0.35, 0.25);\n    vec2 f2 = vec2(1.1, 0.2);\n\n    for (int i = 0; i < 4; i++) {\n        float a = float(i) * 3.14 * 2.0 / 4.0 + 0.3;\n        dFlower = min(dFlower, petal(p, a, f1));\n    }\n    for (int i = 0; i < 5; i++) {\n        float a = float(i) * 3.14 * 2.0 / 5.0 - 0.2;\n        dFlower = min(dFlower, petal(p, a, f2));\n    }\n\n    if (dFlower < 0.01) {\n        float dc = min(length(p - f1), length(p - f2));\n        vec3 fcol = vec3(0.98, 0.98, 0.96);\n        fcol -= 0.4 * smoothstep(0.0, 0.15, dc);\n        fcol *= (0.8 + 0.2 * smoothstep(-0.05, 0.0, dFlower));\n        float alpha = 1.0 - smoothstep(0.0, 0.002, dFlower);\n        return vec4(fcol, alpha);\n    }\n    return vec4(0.0);\n}\n","glass_cutouts":"float sdCircle(vec2 p, float r) { return length(p) - r; }\nfloat sdBox(vec2 p, vec2 b) {\n    vec2 d = abs(p) - b;\n    return length(max(d, 0.0)) + min(max(d.x, d.y), 0.0);\n}\n\nmat2 rot(float a) {\n    float s = sin(a), c = cos(a);\n    return mat2(c, -s, s, c);\n}\n\nfloat sdCloud(vec2 p) {\n    float d = sdCircle(p, 0.25);\n    d = min(d, sdCircle(p - vec2(0.2, -0.1), 0.18));\n    d = min(d, sdCircle(p - vec2(-0.25, -0.05), 0.2));\n    d = min(d, sdCircle(p - vec2(0.15, 0.15), 0.15));\n    d = min(d, sdCircle(p - vec2(-0.1, 0.15), 0.12));\n    return d;\n}\n\nfloat sdFlower(vec2 p) {\n    float a = atan(p.y, p.x);\n    float r = length(p);\n    float petals = 0.15 + 0.05 * sin(a * 5.0);\n    return min(sdCircle(p, 0.05), r - petals);\n}\n\nfloat sdBird(vec2 p) {\n    vec2 bp = p * rot(0.5);\n    float body = length(vec2(bp.x, bp.y * 2.0)) - 0.15;\n    \n    vec2 w1p = p * rot(0.8) - vec2(0.1, 0.15);\n    float w1 = sdBox(w1p, vec2(0.2, 0.03));\n    \n    vec2 w2p = p * rot(-0.2) - vec2(-0.15, 0.1);\n    float w2 = sdBox(w2p, vec2(0.2, 0.03));\n    \n    vec2 tp = p - vec2(-0.2, -0.15);\n    float tail = sdBox(tp * rot(0.5), vec2(0.1, 0.02));\n    float tail2 = sdBox(tp * rot(1.0), vec2(0.1, 0.02));\n    \n    return min(min(min(min(body, w1), w2), tail), tail2);\n}\n\nfloat getShapes(vec2 p) {\n    float d1 = sdCloud(p - vec2(-0.4, -0.6));\n    float d2 = sdCloud(p - vec2(0.6, -0.5));\n    \n    float f1 = sdFlower((p - vec2(0.7, 0.3)) * rot(0.5));\n    float f2 = sdFlower((p - vec2(0.9, 0.1)) * rot(-0.3));\n    float f3 = sdFlower((p - vec2(-0.8, 0.2)) * rot(1.2));\n    \n    float b1 = sdBird((p - vec2(-0.5, 0.4)) * rot(0.2));\n    float b2 = sdBird((p - vec2(-0.2, 0.7)) * rot(-0.5));\n    float b3 = sdBird((p - vec2(0.8, 0.7)) * rot(-0.1));\n    \n    return min(min(min(min(min(min(min(d1, d2), f1), f2), f3), b1), b2), b3);\n}\n\nvoid layer_GlassBackground(in vec2 uv, inout vec3 col) {\n    vec2 gridUV = uv * vec2(25.0, 15.0);\n    vec2 cell = fract(gridUV);\n    vec2 id = floor(gridUV);\n    \n    float groutX = smoothstep(0.05, 0.1, cell.x) * smoothstep(0.95, 0.9, cell.x);\n    float groutY = smoothstep(0.05, 0.1, cell.y) * smoothstep(0.95, 0.9, cell.y);\n    float grout = groutX * groutY;\n    \n    vec3 bgCol = mix(vec3(0.0, 0.3, 0.4), vec3(0.0, 0.6, 0.8), grout);\n    \n    float noise = fract(sin(dot(id, vec2(12.9898, 78.233))) * 43758.5453);\n    bgCol *= 0.8 + 0.4 * noise;\n    \n    col = bgCol;\n}\n\nvoid layer_DropShadows(in vec2 p, inout vec3 col) {\n    vec2 sp = p - vec2(0.02, -0.02);\n    float shadows = getShapes(sp);\n    col = mix(col * 0.3, col, smoothstep(0.0, 0.05, shadows));\n}\n\nvoid layer_PaperCutouts(in vec2 uv, in vec2 p, inout vec3 col) {\n    float shapes = getShapes(p);\n    \n    vec3 paperCol = vec3(0.95, 0.98, 1.0);\n    paperCol -= 0.05 * fract(sin(dot(uv, vec2(12.9898, 78.233))) * 43758.5453);\n    \n    col = mix(paperCol, col, smoothstep(0.0, 0.01, shapes));\n}\n\nvec4 layer_GlassBackground(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n    \n    vec3 col = vec3(-1.0);\n    \n    layer_GlassBackground(uv, col);\n    \n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_DropShadows(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n    \n    vec3 col = vec3(-1.0);\n    \n    layer_DropShadows(p, col);\n    \n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_PaperCutouts(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n    \n    vec3 col = vec3(-1.0);\n    \n    layer_PaperCutouts(uv, p, col);\n    \n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n","grey_tiles":"float hash(vec2 p) { return fract(sin(dot(p, vec2(12.9898, 78.233))) * 43758.5453); }\nfloat noise(vec2 p) {\n    vec2 i = floor(p); vec2 f = fract(p);\n    vec2 u = f * f * (3.0 - 2.0 * f);\n    return mix(mix(hash(i + vec2(0.0,0.0)), hash(i + vec2(1.0,0.0)), u.x),\n               mix(hash(i + vec2(0.0,1.0)), hash(i + vec2(1.0,1.0)), u.x), u.y);\n}\n\nfloat sdBox(vec3 p, vec3 b) {\n    vec3 q = abs(p) - b;\n    return length(max(q,0.0)) + min(max(q.x,max(q.y,q.z)),0.0);\n}\n\nfloat layer_Mapping(vec3 p) {\n    float dBox = sdBox(p - vec3(-1.0, 0.0, -1.0), vec3(1.2, 0.8, 1.2));\n    float dTop = sdBox(p - vec3(-1.0, 0.85, -1.0), vec3(1.25, 0.05, 1.25));\n    float dPillar = sdBox(p - vec3(-1.0, 1.5, -1.0), vec3(1.0, 0.6, 1.0));\n    float dGround = p.y + 0.8;\n    return min(min(min(dBox, dTop), dPillar), dGround);\n}\n\nvec3 layer_TileMaterial(vec2 p) {\n    p *= vec2(4.0, 8.0);\n    float row = floor(p.y);\n    p.x += mod(row, 2.0) * 0.5;\n    vec2 cell = fract(p);\n    vec2 id = floor(p);\n\n    float band = mod(floor(row / 2.0), 2.0);\n    vec3 baseCol = band < 0.5 ? vec3(0.35, 0.38, 0.4) : vec3(0.55, 0.58, 0.6);\n    baseCol *= 0.8 + 0.4 * hash(id);\n\n    float tex = noise(p * 20.0) * 0.5 + noise(p * 40.0) * 0.25;\n    baseCol = mix(baseCol, vec3(0.2), tex * 0.3);\n\n    float groutX = smoothstep(0.0, 0.03, cell.x) * smoothstep(1.0, 0.97, cell.x);\n    float groutY = smoothstep(0.0, 0.08, cell.y) * smoothstep(1.0, 0.92, cell.y);\n    float grout = groutX * groutY;\n\n    vec3 finalCol = mix(vec3(0.7, 0.75, 0.75), baseCol, grout);\n    finalCol += 0.1 * smoothstep(0.9, 0.95, cell.x) * grout;\n    finalCol -= 0.1 * smoothstep(0.05, 0.0, cell.x) * grout;\n    return finalCol;\n}\n\nvec3 layer_PillarMaterial(vec3 pos) {\n    vec3 matCol = vec3(0.6, 0.62, 0.6);\n    matCol *= 0.8 + 0.2 * noise(pos.xz * 50.0 + pos.xy * 50.0);\n    return matCol;\n}\n\nvec3 layer_GroundMaterial(vec3 pos) {\n    vec3 matCol = vec3(0.3, 0.3, 0.3);\n    if (pos.x > 0.2) {\n        vec2 fUV = pos.xz * 2.0;\n        vec2 fID = floor(fUV);\n        vec2 fCell = fract(fUV);\n        float g = smoothstep(0.0, 0.02, fCell.x) * smoothstep(1.0, 0.98, fCell.x) *\n                  smoothstep(0.0, 0.02, fCell.y) * smoothstep(1.0, 0.98, fCell.y);\n        vec3 tileC = mix(vec3(0.8, 0.8, 0.8), vec3(0.4, 0.4, 0.4), step(0.5, hash(fID)));\n        float flower = step(abs(length(fCell - 0.5) - 0.2), 0.05);\n        tileC = mix(tileC, vec3(0.2), flower);\n        matCol = mix(vec3(0.5), tileC, g);\n    } else {\n        float tex = noise(pos.xz * 10.0);\n        matCol *= 0.8 + 0.2 * tex;\n    }\n    return matCol;\n}\n\nfloat layer_Lighting(vec3 pos, vec3 n) {\n    vec3 light = normalize(vec3(1.0, 2.0, 1.0));\n    float diff = max(dot(n, light), 0.0);\n    float amb = 0.3 + 0.1 * n.y;\n\n    float sh = 1.0;\n    float st = 0.02;\n    for(int i = 0; i < 30; i++) {\n        float h = layer_Mapping(pos + light * st);\n        if(h < 0.001) { sh = 0.1; break; }\n        sh = min(sh, 10.0 * h / st);\n        st += h;\n        if(st > 3.0) break;\n    }\n    return amb + diff * sh;\n}\n\n// ---- shared raymarch (camera fixed, target = origin) ----\nvec3 gt_rd(vec2 p) {\n    vec3 ro = vec3(1.5, 1.0, 1.5);\n    vec3 cw = normalize(-ro);\n    vec3 cu = normalize(cross(cw, vec3(0.0, 1.0, 0.0)));\n    vec3 cv = cross(cu, cw);\n    return normalize(p.x * cu + p.y * cv + 1.5 * cw);\n}\nfloat gt_march(vec3 ro, vec3 rd, out vec3 pos) {\n    float t = 0.0;\n    for(int i = 0; i < 100; i++) {\n        pos = ro + rd * t;\n        float d = layer_Mapping(pos);\n        if(d < 0.001 || t > 20.0) break;\n        t += d;\n    }\n    return t;\n}\nvec3 gt_normal(vec3 pos) {\n    vec2 e = vec2(0.001, 0.0);\n    return normalize(vec3(\n        layer_Mapping(pos + e.xyy) - layer_Mapping(pos - e.xyy),\n        layer_Mapping(pos + e.yxy) - layer_Mapping(pos - e.yxy),\n        layer_Mapping(pos + e.yyx) - layer_Mapping(pos - e.yyx)));\n}\n\n// each object = the visible (front-most) raymarch surface, alpha 0 elsewhere\nvec4 layer_TiledBox(vec2 uv) {\n    vec2 p = uv * 2.0 - 1.0; p.x *= iResolution.x / iResolution.y;\n    vec3 ro = vec3(1.5, 1.0, 1.5), pos;\n    float t = gt_march(ro, gt_rd(p), pos);\n    if(t > 20.0) return vec4(0.0);\n    if(step(sdBox(pos - vec3(-1.0, 0.0, -1.0), vec3(1.2, 0.8, 1.2)), 0.01) < 0.5) return vec4(0.0);\n    vec3 n = gt_normal(pos);\n    vec2 tileUV = abs(n.z) > 0.5 ? pos.xy : (abs(n.x) > 0.5 ? vec2(pos.z, pos.y) : pos.xz);\n    vec3 col = layer_TileMaterial(tileUV) * layer_Lighting(pos, n);\n    return vec4(pow(col, vec3(0.4545)), 1.0);\n}\nvec4 layer_Pillar(vec2 uv) {\n    vec2 p = uv * 2.0 - 1.0; p.x *= iResolution.x / iResolution.y;\n    vec3 ro = vec3(1.5, 1.0, 1.5), pos;\n    float t = gt_march(ro, gt_rd(p), pos);\n    if(t > 20.0) return vec4(0.0);\n    float isTop = step(sdBox(pos - vec3(-1.0, 0.85, -1.0), vec3(1.25, 0.05, 1.25)), 0.01);\n    float isPillar = step(sdBox(pos - vec3(-1.0, 1.5, -1.0), vec3(1.0, 0.6, 1.0)), 0.01);\n    if(isTop < 0.5 && isPillar < 0.5) return vec4(0.0);\n    vec3 n = gt_normal(pos);\n    vec3 col = layer_PillarMaterial(pos) * layer_Lighting(pos, n);\n    return vec4(pow(col, vec3(0.4545)), 1.0);\n}\nvec4 layer_Ground(vec2 uv) {\n    vec2 p = uv * 2.0 - 1.0; p.x *= iResolution.x / iResolution.y;\n    vec3 ro = vec3(1.5, 1.0, 1.5), pos;\n    float t = gt_march(ro, gt_rd(p), pos);\n    if(t > 20.0) return vec4(0.0);\n    if(step(abs(pos.y + 0.8), 0.01) < 0.5) return vec4(0.0);\n    vec3 n = gt_normal(pos);\n    vec3 col = layer_GroundMaterial(pos) * layer_Lighting(pos, n);\n    return vec4(pow(col, vec3(0.4545)), 1.0);\n}\n","IMG_0398":"mat2 rot(float a) {\n    float c = cos(a), s = sin(a);\n    return mat2(c, -s, s, c);\n}\n\nvec2 hash2(vec2 p) {\n    p = vec2(dot(p, vec2(127.1, 311.7)), dot(p, vec2(269.5, 183.3)));\n    return -1.0 + 2.0 * fract(sin(p) * 43758.5453123);\n}\n\nfloat noise(vec2 p) {\n    const float K1 = 0.366025404;\n    const float K2 = 0.211324865;\n    vec2 i = floor(p + (p.x + p.y) * K1);\n    vec2 a = p - i + (i.x + i.y) * K2;\n    float m = step(a.y, a.x);\n    vec2 o = vec2(m, 1.0 - m);\n    vec2 b = a - o + K2;\n    vec2 c = a - 1.0 + 2.0 * K2;\n    vec3 h = max(0.5 - vec3(dot(a, a), dot(b, b), dot(c, c)), 0.0);\n    vec3 n = h * h * h * h * vec3(dot(a, hash2(i + 0.0)), dot(b, hash2(i + o)), dot(c, hash2(i + 1.0)));\n    return dot(n, vec3(70.0));\n}\n\nvec4 layer_Floor(vec2 uv) {\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    if (p.y < -0.1) {\n        vec2 floorUV = vec2(p.x / (p.y + 0.1), 1.0 / (p.y + 0.1));\n        float planks = fract(floorUV.x * 2.0);\n        float plankLine = smoothstep(0.0, 0.05, planks) * smoothstep(1.0, 0.95, planks);\n        float woodGrain = noise(floorUV * vec2(1.0, 10.0));\n        vec3 woodCol = mix(vec3(0.2, 0.1, 0.05), vec3(0.3, 0.15, 0.08), woodGrain);\n        vec3 col = woodCol * plankLine;\n        float depthDarken = clamp(abs(p.y + 0.1) * 2.0, 0.0, 1.0);\n        col *= depthDarken;\n        return vec4(col, 1.0);\n    }\n    return vec4(0.0);\n}\n\nvec4 layer_CeilingGrid(vec2 uv) {\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    if (p.y > 0.4) {\n        vec2 ceilUV = vec2(p.x / (p.y - 0.4), 1.0 / (p.y - 0.4));\n        vec2 grid = fract(ceilUV * 5.0);\n        float gridLine = smoothstep(0.0, 0.1, grid.x) * smoothstep(1.0, 0.9, grid.x) *\n                         smoothstep(0.0, 0.1, grid.y) * smoothstep(1.0, 0.9, grid.y);\n        vec3 col = mix(vec3(0.8, 0.9, 1.0), vec3(0.1, 0.1, 0.15), gridLine);\n        float depthDarken = clamp((p.y - 0.4) * 2.0, 0.0, 1.0);\n        col *= depthDarken;\n        return vec4(col, 1.0);\n    }\n    return vec4(0.0);\n}\n\nvec4 layer_BackgroundForest(vec2 uv) {\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    if (!(p.y < -0.1) && !(p.y > 0.4)) {\n        if (abs(p.x) > 0.6) {\n            float forestNoise = fract(sin(p.x * 50.0 + iTime*0.05) * 43758.0);\n            float treeProfile = 0.2 + 0.3 * sin(p.x * 20.0) + 0.1 * forestNoise;\n            if (p.y < treeProfile) {\n                return vec4(0.05, 0.08, 0.05, 1.0);\n            } else {\n                vec3 sky = mix(vec3(0.05, 0.1, 0.2), vec3(0.01, 0.02, 0.05), p.y);\n                float moon = length(p - vec2(-0.8, 0.3));\n                sky += vec3(0.8, 0.9, 1.0) * smoothstep(0.1, 0.0, moon);\n                return vec4(sky, 1.0);\n            }\n        } else {\n            return vec4(0.1, 0.1, 0.1, 1.0);\n        }\n    }\n    return vec4(0.0);\n}\n\nvec4 layer_GlowingScreens(vec2 uv) {\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    if (!(p.y < -0.1) && !(p.y > 0.4) && !(abs(p.x) > 0.6)) {\n        float wave = sin(p.y * 20.0 - iTime * 5.0) * 0.5 + 0.5;\n        vec3 screenCol = mix(vec3(0.0, 0.5, 1.0), vec3(0.0, 0.8, 0.5), wave);\n        float edge = smoothstep(0.5, 0.6, abs(p.x));\n        screenCol *= (1.0 - edge);\n        return vec4(screenCol, 1.0);\n    }\n    return vec4(0.0);\n}\n\nvec4 layer_WoodenSculpture(vec2 uv) {\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    if (!(p.y < -0.1) && !(p.y > 0.4) && !(abs(p.x) > 0.6)) {\n        float dSculpt = length(vec2(p.x, p.y + 0.1)) - 0.3;\n        dSculpt += 0.05 * sin(p.y * 15.0); \n        \n        if (dSculpt < 0.0) {\n            float g = noise(p * 20.0);\n            vec3 sculptCol = mix(vec3(0.4, 0.2, 0.1), vec3(0.2, 0.1, 0.05), g);\n            float shadow = smoothstep(0.0, -0.1, dSculpt);\n            sculptCol *= shadow;\n            return vec4(sculptCol, 1.0);\n        }\n    }\n    return vec4(0.0);\n}\n","IMG_0415":"void layer_GravelFloor(in vec2 st, in float iTime, inout vec3 col) {\n    float gravel = fract(sin(dot(st * 15.0 + iTime*0.5, vec2(12.9, 78.2))) * 43758.0);\n    col = vec3(0.25) * (0.7 + 0.5 * gravel);\n}\n\nvoid layer_Stones(in vec2 st, inout vec3 col) {\n    float stoneId = floor(st.y * 2.0);\n    float stoneShape = length(vec2(st.x - sin(stoneId)*0.4, fract(st.y * 2.0) - 0.5));\n    \n    if (stoneShape < 0.3) {\n        col = vec3(0.5, 0.5, 0.52); \n        col *= 0.8 + 0.2 * fract(sin(dot(st, vec2(1.0, 2.0)))*430.0); \n    }\n}\n\nvoid layer_WhiteCubes(in vec2 st, inout vec3 col) {\n    if (st.x > 0.4 && st.y > 0.5 && st.y < 2.5) {\n        float cubeShape = max(abs(fract(st.y) - 0.5), abs(st.x - 0.8));\n        if (cubeShape < 0.25) {\n            col = vec3(0.9); \n            if (mod(st.y*25.0, 1.0) < 0.3 && abs(st.x - 0.8) < 0.15) col = vec3(0.1);\n        }\n    }\n}\n\nvoid layer_BrickWall(in vec2 p, inout vec3 col) {\n    col = vec3(0.8, 0.75, 0.7); \n    float by = fract(p.y * 15.0);\n    float bx = fract(p.x * 8.0 + floor(p.y*15.0)*0.5);\n    if (by < 0.1 || bx < 0.05) col *= 0.7;\n}\n\nvoid layer_DarkWindow(in vec2 p, inout vec3 col) {\n    if (p.x > 0.1 && p.y > 0.2 && p.y < 0.8) {\n        col = vec3(0.1, 0.15, 0.15); \n        col += vec3(0.1) * smoothstep(0.0, 0.1, p.x + p.y - 1.0); \n    }\n}\n\nvec4 layer_GravelFloor(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 col = vec3(-1.0);\n\n    if (p.y < 0.1) {\n        vec2 st = vec2(p.x / (p.y - 0.2), 1.0 / (p.y - 0.2)) * 3.0;\n        \n        layer_GravelFloor(st, iTime, col);\n        \n        col *= smoothstep(-1.0, 0.1, p.y); \n    } else {\n    }\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_Stones(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 col = vec3(-1.0);\n\n    if (p.y < 0.1) {\n        vec2 st = vec2(p.x / (p.y - 0.2), 1.0 / (p.y - 0.2)) * 3.0;\n        \n        layer_Stones(st, col);\n        \n        col *= smoothstep(-1.0, 0.1, p.y); \n    } else {\n    }\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_WhiteCubes(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 col = vec3(-1.0);\n\n    if (p.y < 0.1) {\n        vec2 st = vec2(p.x / (p.y - 0.2), 1.0 / (p.y - 0.2)) * 3.0;\n        \n        layer_WhiteCubes(st, col);\n        \n        col *= smoothstep(-1.0, 0.1, p.y); \n    } else {\n    }\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_BrickWall(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 col = vec3(-1.0);\n\n    if (p.y < 0.1) {\n        vec2 st = vec2(p.x / (p.y - 0.2), 1.0 / (p.y - 0.2)) * 3.0;\n        \n        \n        col *= smoothstep(-1.0, 0.1, p.y); \n    } else {\n        layer_BrickWall(p, col);\n    }\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_DarkWindow(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 col = vec3(-1.0);\n\n    if (p.y < 0.1) {\n        vec2 st = vec2(p.x / (p.y - 0.2), 1.0 / (p.y - 0.2)) * 3.0;\n        \n        \n        col *= smoothstep(-1.0, 0.1, p.y); \n    } else {\n        layer_DarkWindow(p, col);\n    }\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n","IMG_0417":"float img17_base(vec3 p) {\n    float base = max(length(p.xz) - 0.6, abs(p.y) - 0.3);\n    base -= 0.02 * sin(atan(p.z, p.x) * 14.0);\n    base -= 0.01 * sin(p.y * 50.0);\n    return base;\n}\nfloat img17_stone(vec3 p) {\n    vec3 sp = p - vec3(0.0, 0.35, 0.0);\n    float s = length(sp) - 0.12;\n    s += 0.02 * sin(sp.x * 25.0) * sin(sp.z * 25.0);\n    return s;\n}\nfloat layer_Mapping(vec3 p) { return min(img17_base(p), img17_stone(p)); }\n\nvec3 getNormal(vec3 p) {\n    vec2 e = vec2(0.01, 0);\n    return normalize(vec3(\n        layer_Mapping(p+e.xyy) - layer_Mapping(p-e.xyy),\n        layer_Mapping(p+e.yxy) - layer_Mapping(p-e.yxy),\n        layer_Mapping(p+e.yyx) - layer_Mapping(p-e.yyx)\n    ));\n}\n\n// ---- shared camera + raymarch ----\nfloat img17_march(vec2 p, out vec3 ro, out vec3 rd) {\n    ro = vec3(0.0, 1.2, -2.5);\n    rd = normalize(vec3(p.x, p.y - 0.4, 1.0));\n    float d = 0.0;\n    for(int i = 0; i < 64; i++) {\n        float dist = layer_Mapping(ro + rd * d);\n        if (dist < 0.001) break;\n        d += dist;\n        if (d > 10.0) break;\n    }\n    return d;\n}\nfloat img17_vig(vec2 p) { return 1.0 - length(p) * 0.15; }\n\nvec4 layer_Background(vec2 uv) {\n    vec2 p = uv * 2.0 - 1.0; p.x *= iResolution.x / iResolution.y;\n    vec3 ro, rd;\n    float d = img17_march(p, ro, rd);\n    if (d < 10.0) return vec4(0.0);                 // geometry hit -> backdrop hidden\n    vec3 col = p.y < -0.3 ? vec3(0.35) : vec3(0.75);\n    col *= img17_vig(p);\n    return vec4(col, 1.0);\n}\nvec4 layer_Pedestal(vec2 uv) {\n    vec2 p = uv * 2.0 - 1.0; p.x *= iResolution.x / iResolution.y;\n    vec3 ro, rd;\n    float d = img17_march(p, ro, rd);\n    if (d >= 10.0) return vec4(0.0);\n    vec3 p3 = ro + rd * d;\n    if (p3.y > 0.28 && length(p3.xz) < 0.18) return vec4(0.0);   // that's the Stone\n    vec3 n = getNormal(p3);\n    vec3 light = normalize(vec3(cos(iTime), 1.5, sin(iTime)));\n    float diff = max(dot(n, light), 0.1);\n    vec3 col = vec3(0.1) * diff;\n    vec3 view = normalize(ro - p3);\n    float spec = pow(max(dot(view, reflect(-light, n)), 0.0), 32.0);\n    col += vec3(0.3) * spec;\n    col *= img17_vig(p);\n    return vec4(col, 1.0);\n}\nvec4 layer_Stone(vec2 uv) {\n    vec2 p = uv * 2.0 - 1.0; p.x *= iResolution.x / iResolution.y;\n    vec3 ro, rd;\n    float d = img17_march(p, ro, rd);\n    if (d >= 10.0) return vec4(0.0);\n    vec3 p3 = ro + rd * d;\n    if (!(p3.y > 0.28 && length(p3.xz) < 0.18)) return vec4(0.0);\n    vec3 n = getNormal(p3);\n    vec3 light = normalize(vec3(cos(iTime), 1.5, sin(iTime)));\n    float diff = max(dot(n, light), 0.1);\n    vec3 col = vec3(0.9, 0.9, 0.85) * diff;\n    col *= img17_vig(p);\n    return vec4(col, 1.0);\n}\n","IMG_0418":"void layer_Wall(in vec2 p, inout vec3 col) {\n    col = vec3(0.5, 0.5, 0.52);\n    col *= 0.9 + 0.1 * fract(sin(dot(p, vec2(12.9898, 78.233))) * 43758.5453);\n}\n\nvoid layer_RedPallet(in vec2 palletUV, inout vec3 col) {\n    if (palletUV.x > -1.2 && palletUV.x < 1.2 && palletUV.y > 0.1 && palletUV.y < 2.0) {\n        vec3 redPallet = vec3(0.5, 0.2, 0.2);\n        vec2 grid = fract(palletUV * 5.0);\n        if (grid.x < 0.1 || grid.y < 0.1 || grid.x > 0.9 || grid.y > 0.9) redPallet *= 0.6;\n        if (fract(palletUV.x * 2.5) > 0.8 && fract(palletUV.y * 2.5) > 0.8) redPallet *= 0.3;\n        col = redPallet;\n    }\n}\n\nvoid layer_GreenPallets(in vec2 palletUV, in float iTime, inout vec3 col) {\n    vec2 pUV = palletUV;\n    pUV.y -= 0.2;\n    pUV.x -= 0.1;\n    if (pUV.x > -1.2 && pUV.x < 1.2 && pUV.y > 0.1 && pUV.y < 2.0) {\n        vec3 greenPallet = vec3(0.6, 0.7, 0.2);\n        vec2 grid = fract(pUV * 5.0);\n        \n        float glow = sin(iTime * 2.0 + pUV.x * 10.0 + pUV.y * 5.0) * 0.1 + 0.1;\n        \n        if (grid.x < 0.1 || grid.y < 0.1 || grid.x > 0.9 || grid.y > 0.9) greenPallet *= 0.6;\n        \n        if (fract(pUV.x * 2.5) > 0.8 && fract(pUV.y * 2.5) > 0.8) {\n            greenPallet *= 0.3; \n            greenPallet += vec3(glow, glow * 0.5, 0.0);\n        }\n        \n        greenPallet *= 0.8 + 0.2 * pUV.y;\n        col = greenPallet;\n    }\n}\n\nvec4 layer_Wall(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 col = vec3(-1.0);\n    layer_Wall(p, col);\n    \n    vec2 palletUV = p * mat2(0.9, -0.1, 0.1, 0.9);\n    palletUV *= 2.0;\n    palletUV.y += 1.0;\n    \n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_RedPallet(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 col = vec3(-1.0);\n    \n    vec2 palletUV = p * mat2(0.9, -0.1, 0.1, 0.9);\n    palletUV *= 2.0;\n    palletUV.y += 1.0;\n    \n    layer_RedPallet(palletUV, col);\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_GreenPallets(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 col = vec3(-1.0);\n    \n    vec2 palletUV = p * mat2(0.9, -0.1, 0.1, 0.9);\n    palletUV *= 2.0;\n    palletUV.y += 1.0;\n    \n    layer_GreenPallets(palletUV, iTime, col);\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n","IMG_0419":"void layer_GreenTiles(in vec2 floorUV, inout vec3 col, out bool inMiddle) {\n    inMiddle = false;\n    vec2 tileUV = fract(floorUV * 2.0);\n    vec2 tileId = floor(floorUV * 2.0);\n    \n    float tileNoise = fract(sin(dot(tileId, vec2(12.9898, 78.233))) * 43758.5453);\n    col = vec3(0.4, 0.45, 0.4) * (0.8 + 0.2 * tileNoise);\n    \n    if (tileUV.x > 0.3 && tileUV.x < 0.7 && tileUV.y > 0.3 && tileUV.y < 0.7) {\n         if (fract(tileNoise * 100.0) < 0.5) {\n             col = vec3(0.65, 0.65, 0.6) * (0.8 + 0.2 * tileNoise);\n         }\n    }\n    \n    if (tileUV.x < 0.05 || tileUV.y < 0.05) col *= 0.5;\n    \n    if (floorUV.x > -0.2 && floorUV.x < 0.2) {\n        inMiddle = true;\n    }\n}\n\nvoid layer_BumpyLine(in vec2 floorUV, in float iTime, inout vec3 col) {\n    float pebbles = fract(sin(dot(floorUV * 100.0 + iTime*0.5, vec2(12.9898, 78.233))) * 43758.5453);\n    vec3 pebbleCol = mix(vec3(0.2), vec3(0.8), pebbles);\n    \n    if (fract(floorUV.y * 4.0) < 0.05 || fract(floorUV.x * 20.0) < 0.05) pebbleCol *= 0.5;\n    \n    col = pebbleCol;\n}\n\nvoid layer_DistanceFade(in float py, inout vec3 col) {\n    col *= smoothstep(0.3, -0.5, py);\n}\n\nvoid layer_StreetWall(inout vec3 col) {\n    col = vec3(0.3);\n}\n\nvec4 layer_GreenTiles(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 col = vec3(-1.0);\n    if (p.y < 0.3) {\n        vec2 floorUV = vec2(p.x / (0.4 - p.y), 1.0 / (0.4 - p.y));\n        \n        bool inMiddle;\n        layer_GreenTiles(floorUV, col, inMiddle);\n        \n        if (inMiddle) {\n        }\n        \n    } else {\n    }\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_BumpyLine(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 col = vec3(-1.0);\n    if (p.y < 0.3) {\n        vec2 floorUV = vec2(p.x / (0.4 - p.y), 1.0 / (0.4 - p.y));\n        \n        bool inMiddle;\n        \n        if (inMiddle) {\n            layer_BumpyLine(floorUV, iTime, col);\n        }\n        \n    } else {\n    }\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_StreetWall(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 col = vec3(-1.0);\n    if (p.y < 0.3) {\n        vec2 floorUV = vec2(p.x / (0.4 - p.y), 1.0 / (0.4 - p.y));\n        \n        bool inMiddle;\n        \n        if (inMiddle) {\n        }\n        \n        layer_DistanceFade(p.y, col);\n    } else {\n    }\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_DistanceFade(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 col = vec3(-1.0);\n    if (p.y < 0.3) {\n        vec2 floorUV = vec2(p.x / (0.4 - p.y), 1.0 / (0.4 - p.y));\n        \n        bool inMiddle;\n        \n        if (inMiddle) {\n        }\n        \n    } else {\n        layer_StreetWall(col);\n    }\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n","IMG_0586":"void layer_InstagramUI(in vec2 p, inout vec3 col, out bool isUI) {\n    isUI = false;\n    if (p.y > 0.8) {\n        isUI = true;\n        col = vec3(0.08);\n        if (p.y > 0.85 && p.y < 0.95) {\n            float sx = fract(p.x * 2.0) - 0.5;\n            if (length(vec2(sx, (p.y - 0.9)*5.0)) < 0.2) col = vec3(0.4);\n        }\n    } else if (p.y < -0.8) {\n        isUI = true;\n        col = vec3(0.05);\n    } else if (p.y < -0.65 && p.y >= -0.8) {\n        isUI = true;\n        col = vec3(0.9, 0.15, 0.25);\n    }\n}\n\nvoid layer_BackgroundWall(inout vec3 col) {\n    col = vec3(0.85, 0.88, 0.85);\n}\n\nvoid layer_PerforatedTable(in vec2 p, inout vec3 col) {\n    if (p.y < -0.15) {\n        col = vec3(0.6, 0.65, 0.65);\n        vec2 tp = p;\n        tp.y = tp.y + 0.15;\n        tp.x = tp.x / (1.0 - tp.y * 0.5);\n        \n        vec2 grid = fract(tp * vec2(20.0, 30.0)) - 0.5;\n        if (length(grid) < 0.25) {\n            col = vec3(0.1);\n        }\n        col *= 0.7 - tp.y * 0.5;\n    }\n}\n\nvoid layer_RedBook(in vec2 p, inout vec3 col) {\n    float bookLeft = -0.4;\n    float bookRight = 0.5;\n    float bookBottom = -0.25;\n    float bookTop = 0.6;\n    \n    if (p.x > -0.35 && p.x < 0.55 && p.y > 0.6 && p.y < 0.75) {\n        col = vec3(0.7, 0.1, 0.2);\n        if (fract(p.y * 20.0) < 0.1) col *= 0.5;\n    }\n    \n    if (p.x > bookLeft && p.x < bookRight && p.y > bookBottom && p.y < bookTop) {\n        col = vec3(0.75, 0.1, 0.2);\n        \n        if (p.x < bookLeft + 0.05) {\n            col = vec3(0.9, 0.9, 0.9);\n            if (fract(p.y * 10.0) < 0.5 && p.x > bookLeft + 0.02) col = vec3(0.1);\n        }\n        \n        if (p.x > -0.1 && p.x < 0.3 && p.y > 0.2 && p.y < 0.4) {\n            float textPat = fract(p.x * 10.0);\n            if (textPat > 0.2 && textPat < 0.8) {\n                if (p.y > 0.31 || p.y < 0.29) {\n                    col = vec3(0.95);\n                }\n            }\n        }\n        \n        if (p.x > -0.1 && p.x < 0.25 && p.y > 0.12 && p.y < 0.15) {\n            if (fract(p.x * 30.0) > 0.3) col = vec3(0.9);\n        }\n        \n        if (length(p - vec2(0.1, -0.15)) < 0.04) {\n            col = vec3(0.9);\n        }\n        \n        col *= 0.9 + 0.1 * p.x;\n    }\n}\n\nvec4 layer_InstagramUI(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n    \n    vec3 col = vec3(-1.0);\n    bool isUI;\n    \n    layer_InstagramUI(p, col, isUI);\n    \n    if (!isUI) {\n    }\n    \n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_BackgroundWall(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n    \n    vec3 col = vec3(-1.0);\n    bool isUI;\n    \n    \n    if (!isUI) {\n        layer_BackgroundWall(col);\n    }\n    \n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_PerforatedTable(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n    \n    vec3 col = vec3(-1.0);\n    bool isUI;\n    \n    \n    if (!isUI) {\n        layer_PerforatedTable(p, col);\n    }\n    \n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_RedBook(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n    \n    vec3 col = vec3(-1.0);\n    bool isUI;\n    \n    \n    if (!isUI) {\n        layer_RedBook(p, col);\n    }\n    \n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n","IMG_0767":"float sdRoundBox( in vec2 p, in vec2 b, in float r ) {\n  vec2 q = abs(p) - b + r;\n  return length(max(q,0.0)) + min(max(q.x,q.y),0.0) - r;\n}\n\nvoid layer_Background(in vec2 p, inout vec3 col) {\n    col = vec3(0.92, 0.82, 0.25);\n    col *= 0.95 + 0.05 * sin(p.y * 10.0 + p.x * 5.0);\n}\n\nvoid layer_TextShape(in vec2 p, out float textDist, out vec2 p_line1, out vec2 p_line2, out vec2 p_line3) {\n    vec2 p1 = p;\n    p1.y -= 0.5;\n    \n    p_line1 = p - vec2(0.0, 0.5);\n    vec2 grid1 = vec2(fract(p_line1.x * 2.0) - 0.5, p_line1.y);\n    float l1 = sdRoundBox(grid1, vec2(0.15, 0.2), 0.05);\n    if(abs(p_line1.x) > 1.0) l1 = 1.0;\n    \n    p_line2 = p - vec2(0.0, 0.0);\n    vec2 grid2 = vec2(fract(p_line2.x * 2.5) - 0.5, p_line2.y);\n    float l2 = sdRoundBox(grid2, vec2(0.12, 0.2), 0.05);\n    if(abs(p_line2.x) > 1.25) l2 = 1.0;\n    \n    p_line3 = p - vec2(0.0, -0.5);\n    vec2 grid3 = vec2(fract(p_line3.x * 4.0) - 0.5, p_line3.y);\n    float l3 = sdRoundBox(grid3, vec2(0.08, 0.15), 0.03);\n    if(abs(p_line3.x) > 1.8) l3 = 1.0;\n\n    textDist = min(l1, min(l2, l3));\n}\n\nvoid layer_TextShadow(in vec2 p_line1, in vec2 p_line2, in vec2 p_line3, in float iTime, inout vec3 col) {\n    vec2 shadowOffset = vec2(-0.02, -0.04) + vec2(sin(iTime*2.0)*0.01, cos(iTime*2.5)*0.01);\n    \n    vec2 p1s = p_line1 - shadowOffset;\n    float l1s = sdRoundBox(vec2(fract(p1s.x * 2.0) - 0.5, p1s.y), vec2(0.15, 0.2), 0.05);\n    if(abs(p1s.x) > 1.0) l1s = 1.0;\n\n    vec2 p2s = p_line2 - shadowOffset;\n    float l2s = sdRoundBox(vec2(fract(p2s.x * 2.5) - 0.5, p2s.y), vec2(0.12, 0.2), 0.05);\n    if(abs(p2s.x) > 1.25) l2s = 1.0;\n    \n    vec2 p3s = p_line3 - shadowOffset;\n    float l3s = sdRoundBox(vec2(fract(p3s.x * 4.0) - 0.5, p3s.y), vec2(0.08, 0.15), 0.03);\n    if(abs(p3s.x) > 1.8) l3s = 1.0;\n    \n    float shadowDist = min(l1s, min(l2s, l3s));\n    \n    if(shadowDist < 0.0) {\n        col = mix(vec3(0.9, 0.2, 0.3), vec3(0.9, 0.4, 0.5), shadowDist * -10.0);\n    }\n}\n\nvoid layer_TextMaterial(in float textDist, inout vec3 col) {\n    if(textDist < 0.02) {\n        col = vec3(1.0);\n    }\n    \n    if(textDist < 0.0) {\n        col = vec3(0.1, 0.2, 0.5); \n        if (textDist < -0.02) {\n           col = mix(col, vec3(0.15, 0.25, 0.6), smoothstep(-0.02, -0.05, textDist));\n        }\n    }\n}\n\nvec4 layer_Background(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n    \n    vec3 col = vec3(-1.0);\n    layer_Background(p, col);\n    \n    float textDist;\n    vec2 p_line1, p_line2, p_line3;\n    \n    \n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_TextShape(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n    \n    vec3 col = vec3(-1.0);\n    \n    float textDist;\n    vec2 p_line1, p_line2, p_line3;\n    layer_TextShape(p, textDist, p_line1, p_line2, p_line3);\n    \n    \n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_TextShadow(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n    \n    vec3 col = vec3(-1.0);\n    \n    float textDist;\n    vec2 p_line1, p_line2, p_line3;\n    \n    layer_TextShadow(p_line1, p_line2, p_line3, iTime, col);\n    \n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_TextMaterial(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n    \n    vec3 col = vec3(-1.0);\n    \n    float textDist;\n    vec2 p_line1, p_line2, p_line3;\n    \n    layer_TextMaterial(textDist, col);\n    \n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n","IMG_0824":"void layer_Sky(inout vec3 col) {\n    col = vec3(0.8, 0.85, 0.9);\n}\n\nvoid layer_LeftBuilding(in vec2 p, inout vec3 col) {\n    if (p.x < -0.1 && p.y > -0.5) {\n        col = vec3(0.85, 0.85, 0.8);\n        if (fract(p.y * 20.0) < 0.1) col *= 0.8;\n        \n        if (p.x > -0.6 && p.y < 0.0) {\n             col = vec3(0.2, 0.25, 0.2);\n        }\n    }\n}\n\nvoid layer_RightBuilding(in vec2 p, inout vec3 col) {\n    if (p.x > -0.1 && p.y > -0.2) {\n        col = vec3(0.3, 0.5, 0.8);\n        if (fract(p.y * 10.0 - p.x * 2.0) < 0.05) col *= 0.9;\n    }\n}\n\nvoid layer_Street(in vec2 p, in float iTime, inout vec3 col) {\n    if (p.y < -0.5) {\n        col = vec3(0.4, 0.4, 0.45);\n        float noise = fract(sin(dot(p*50.0, vec2(12.9, 78.2)))*43758.0);\n        col += (noise - 0.5) * 0.1;\n        \n        float wet = smoothstep(0.4, 0.6, sin(p.x * 10.0 + iTime) * cos(p.y * 15.0));\n        col = mix(col, vec3(0.5, 0.55, 0.6), wet * 0.3);\n    }\n}\n\nvoid layer_Leaves(in vec2 p, in float iTime, inout vec3 col) {\n    float leafSway = sin(iTime * 1.5 + p.y * 10.0) * 0.05;\n    vec2 leafP = p - vec2(leafSway, 0.0);\n    if (leafP.x > 0.0 && leafP.y > 0.2) {\n        float leafNoise = fract(sin(dot(leafP*10.0, vec2(12.9, 78.2)))*43758.0);\n        if (leafNoise > 0.4 && distance(leafP, vec2(0.5, 0.8)) < 0.7) {\n            col = mix(vec3(0.2, 0.4, 0.1), vec3(0.3, 0.6, 0.2), leafNoise);\n        }\n    }\n}\n\nvoid layer_Motorcycle(in vec2 p, inout vec3 col) {\n    if (length(p - vec2(-0.5, -0.6)) < 0.2 || length(p - vec2(-0.8, -0.5)) < 0.15) {\n        col = vec3(0.1, 0.1, 0.15);\n    }\n}\n\nvec4 layer_Sky(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n    \n    vec3 col = vec3(-1.0);\n    \n    layer_Sky(col);\n    \n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_LeftBuilding(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n    \n    vec3 col = vec3(-1.0);\n    \n    layer_LeftBuilding(p, col);\n    \n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_RightBuilding(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n    \n    vec3 col = vec3(-1.0);\n    \n    layer_RightBuilding(p, col);\n    \n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_Street(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n    \n    vec3 col = vec3(-1.0);\n    \n    layer_Street(p, iTime, col);\n    \n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_Leaves(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n    \n    vec3 col = vec3(-1.0);\n    \n    layer_Leaves(p, iTime, col);\n    \n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_Motorcycle(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n    \n    vec3 col = vec3(-1.0);\n    \n    layer_Motorcycle(p, col);\n    \n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n","IMG_0825":"void layer_Background(in vec2 p, inout vec3 col) {\n    col = vec3(0.5, 0.5, 0.5);\n    \n    if (p.x > 0.2) {\n        col = vec3(0.7, 0.7, 0.65);\n        if (fract(p.x * 5.0) < 0.05 || fract(p.y * 5.0) < 0.05) col *= 0.8;\n    } else {\n        col = vec3(0.4, 0.45, 0.45);\n        if (p.y > 0.2) col = vec3(0.6, 0.6, 0.6);\n        if (p.x < 0.0 && p.y > 0.2 && p.y < 0.3) col = vec3(0.2, 0.5, 0.3);\n    }\n}\n\nvoid layer_PersonPants(in vec2 personP, in float iTime, inout vec3 col) {\n    if (personP.y > -0.7 && personP.y < -0.1 && abs(personP.x) < 0.15) {\n        col = vec3(0.4, 0.1, 0.5);\n        \n        vec2 pantUV = personP * 15.0;\n        pantUV.y += iTime * 0.5;\n        float pantPattern = fract(sin(dot(floor(pantUV), vec2(12.9898, 78.233))) * 43758.5453);\n        if (pantPattern > 0.7) col = vec3(0.9);\n        else if (pantPattern > 0.5) col = vec3(0.8, 0.2, 0.3);\n    }\n}\n\nvoid layer_PersonShirt(in vec2 personP, inout vec3 col) {\n    if (personP.y > -0.1 && personP.y < 0.4 && abs(personP.x) < 0.2) {\n        col = vec3(0.8, 0.4, 0.1);\n        \n        if (fract(personP.x * 20.0) < 0.2) col = vec3(0.5, 0.2, 0.1);\n        if (fract(personP.y * 20.0) < 0.2) col = vec3(0.5, 0.2, 0.1);\n        \n        if (abs(personP.y - 0.1) < 0.05 || abs(personP.y - 0.3) < 0.05) {\n            col = vec3(0.9, 0.9, 0.1);\n        }\n    }\n}\n\nvoid layer_PersonHead(in vec2 personP, inout vec3 col) {\n    if (length(personP - vec2(0.0, 0.5)) < 0.12) {\n        col = vec3(0.9, 0.7, 0.5);\n    }\n    if (length(personP - vec2(0.0, 0.55)) < 0.13 && personP.y > 0.5) {\n        col = vec3(0.95, 0.85, 0.1);\n    }\n}\n\nvoid layer_Motorcycle(in vec2 p, inout vec3 col) {\n    if (length(p - vec2(-0.5, -0.4)) < 0.3) {\n        col = vec3(0.15);\n    }\n    if (length(p - vec2(-0.3, -0.2)) < 0.15) {\n        col = vec3(0.8);\n    }\n}\n\nvoid layer_GreenBasket(in vec2 p, inout vec3 col) {\n    if (length(p - vec2(0.4, -0.6)) < 0.1) {\n        col = vec3(0.4, 0.8, 0.5);\n    }\n}\n\nvec4 layer_Background(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n    \n    vec3 col = vec3(-1.0);\n    \n    layer_Background(p, col);\n    \n    vec2 personP = p - vec2(0.0, -0.1);\n    \n    \n    \n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_PersonPants(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n    \n    vec3 col = vec3(-1.0);\n    \n    \n    vec2 personP = p - vec2(0.0, -0.1);\n    \n    layer_PersonPants(personP, iTime, col);\n    \n    \n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_PersonShirt(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n    \n    vec3 col = vec3(-1.0);\n    \n    \n    vec2 personP = p - vec2(0.0, -0.1);\n    \n    layer_PersonShirt(personP, col);\n    \n    \n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_PersonHead(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n    \n    vec3 col = vec3(-1.0);\n    \n    \n    vec2 personP = p - vec2(0.0, -0.1);\n    \n    layer_PersonHead(personP, col);\n    \n    \n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_Motorcycle(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n    \n    vec3 col = vec3(-1.0);\n    \n    \n    vec2 personP = p - vec2(0.0, -0.1);\n    \n    \n    layer_Motorcycle(p, col);\n    \n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_GreenBasket(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n    \n    vec3 col = vec3(-1.0);\n    \n    \n    vec2 personP = p - vec2(0.0, -0.1);\n    \n    \n    layer_GreenBasket(p, col);\n    \n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n","IMG_0826":"void layer_SurroundingWall(inout vec3 col) {\n    col = vec3(0.8, 0.8, 0.8);\n}\n\nvoid layer_BlueTiles(in vec2 p, inout vec3 col) {\n    if (p.y > 0.3 && abs(p.x) < 0.6) {\n        col = vec3(0.6, 0.8, 0.9);\n        if (fract(p.x * 20.0) < 0.1 || fract(p.y * 20.0) < 0.1) {\n            col = vec3(0.5, 0.7, 0.8);\n        }\n    }\n}\n\nvoid layer_ScissorGate(in vec2 p, in float iTime, inout vec3 col) {\n    if (p.y < 0.3 && abs(p.x) < 0.6) {\n        col = vec3(0.05, 0.05, 0.05); \n        \n        float stretch = 1.0 + 0.1 * sin(iTime * 2.0);\n        vec2 gp = p;\n        gp.x *= stretch;\n        \n        float diag1 = abs(fract(gp.x * 5.0 + gp.y * 5.0) - 0.5);\n        float diag2 = abs(fract(gp.x * 5.0 - gp.y * 5.0) - 0.5);\n        float vertical = abs(fract(gp.x * 5.0) - 0.5);\n        \n        if (diag1 < 0.05 || diag2 < 0.05 || vertical < 0.03) {\n            col = vec3(0.85, 0.8, 0.75);\n            col *= 0.8 + 0.2 * sin(gp.x * 50.0);\n        }\n    }\n}\n\nvoid layer_RightPillar(in vec2 p, inout vec3 col) {\n    if (p.x > 0.6) {\n        col = vec3(0.2, 0.2, 0.2);\n        float cable1 = abs(p.x - 0.7 - 0.05 * sin(p.y * 5.0));\n        float cable2 = abs(p.x - 0.8 + 0.08 * cos(p.y * 7.0));\n        if (cable1 < 0.02 || cable2 < 0.015) {\n            col = vec3(0.1);\n        }\n    }\n}\n\nvec4 layer_SurroundingWall(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n    \n    vec3 col = vec3(-1.0);\n    \n    layer_SurroundingWall(col);\n    \n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_BlueTiles(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n    \n    vec3 col = vec3(-1.0);\n    \n    layer_BlueTiles(p, col);\n    \n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_ScissorGate(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n    \n    vec3 col = vec3(-1.0);\n    \n    layer_ScissorGate(p, iTime, col);\n    \n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_RightPillar(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n    \n    vec3 col = vec3(-1.0);\n    \n    layer_RightPillar(p, col);\n    \n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n","IMG_0965":"void layer_BlueTiles(in vec2 p, inout vec3 col) {\n    col = vec3(0.2, 0.6, 0.8);\n    vec2 tileUV = p * vec2(10.0, 15.0);\n    float tileX = fract(tileUV.x);\n    float tileY = fract(tileUV.y);\n    if (tileX < 0.05 || tileY < 0.05) {\n        col = vec3(0.1, 0.4, 0.6);\n    }\n}\n\nvoid layer_Bird(in vec2 p, in vec2 parallax, inout vec3 col) {\n    vec2 bp = p - vec2(-0.5, 0.4);\n    float birdShadow = length(max(abs(bp - parallax) - vec2(0.3, 0.2), 0.0));\n    if (birdShadow < 0.05) col = mix(col, vec3(0.1, 0.3, 0.4), 0.5);\n    \n    float birdBody = length(bp * vec2(1.2, 2.5));\n    float birdWing1 = length((bp - vec2(-0.1, 0.2)) * vec2(3.0, 1.0));\n    float birdWing2 = length((bp - vec2(0.2, -0.1)) * vec2(1.0, 3.0));\n    \n    if (birdBody < 0.15 || birdWing1 < 0.2 || birdWing2 < 0.2) {\n        col = vec3(0.95, 0.95, 0.98);\n        if (fract(bp.x * 20.0 + bp.y * 30.0) < 0.3 && bp.x < 0.0) col = vec3(0.2, 0.3, 0.6);\n    }\n}\n\nvoid layer_Flowers(in vec2 p, in vec2 parallax, inout vec3 col) {\n    vec2 fp = p - vec2(0.6, -0.2);\n    float flowerShadow = length(fp - parallax) - 0.3 - 0.1 * sin(atan(fp.y, fp.x) * 5.0);\n    if (flowerShadow < 0.0) col = mix(col, vec3(0.1, 0.3, 0.4), 0.5);\n    \n    float flower = length(fp) - 0.3 - 0.1 * sin(atan(fp.y, fp.x) * 5.0);\n    if (flower < 0.0) {\n        col = vec3(0.95, 0.98, 0.95);\n        float detail = sin(fp.x * 30.0) * cos(fp.y * 30.0);\n        if (detail > 0.5) col = vec3(0.3, 0.6, 0.5);\n        if (length(fp - vec2(0.1, 0.1)) < 0.05) col = vec3(0.8, 0.7, 0.2);\n    }\n}\n\nvoid layer_SmallCloud(in vec2 p, inout vec3 col) {\n    vec2 cp = p - vec2(-0.2, -0.6);\n    float cloud = length(cp) - 0.15 - 0.05 * cos(atan(cp.y, cp.x) * 4.0);\n    if (cloud < 0.0) {\n        col = vec3(1.0);\n    }\n}\n\nvec4 layer_BlueTiles(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n    \n    vec3 col = vec3(-1.0);\n    layer_BlueTiles(p, col);\n    \n    vec2 parallax = vec2(sin(iTime*1.5), cos(iTime*1.2)) * 0.02;\n    \n    \n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_Bird(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n    \n    vec3 col = vec3(-1.0);\n    \n    vec2 parallax = vec2(sin(iTime*1.5), cos(iTime*1.2)) * 0.02;\n    \n    layer_Bird(p, parallax, col);\n    \n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_Flowers(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n    \n    vec3 col = vec3(-1.0);\n    \n    vec2 parallax = vec2(sin(iTime*1.5), cos(iTime*1.2)) * 0.02;\n    \n    layer_Flowers(p, parallax, col);\n    \n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_SmallCloud(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n    \n    vec3 col = vec3(-1.0);\n    \n    vec2 parallax = vec2(sin(iTime*1.5), cos(iTime*1.2)) * 0.02;\n    \n    layer_SmallCloud(p, col);\n    \n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n","IMG_0967":"void layer_BrickWall(in vec2 p, inout vec3 col) {\n    col = vec3(0.8, 0.5, 0.3);\n    vec2 brickUv = vec2(p.x * 5.0, p.y * 10.0);\n    brickUv.x += floor(brickUv.y) * 0.5;\n    vec2 fBrick = fract(brickUv);\n    float brickEdge = max(step(0.9, fBrick.x), step(0.85, fBrick.y));\n    col *= 0.5 + 0.5 * (1.0 - brickEdge);\n    col -= 0.1 * fract(sin(dot(p*100.0, vec2(12.9898,78.233))) * 43758.5453);\n}\n\nvoid layer_WindowSlats(in vec2 p, in float iTime, inout vec3 col) {\n    if (abs(p.x) < 0.8) {\n        float lightMove = sin(iTime * 0.5) * 0.5 + 0.5;\n        col = vec3(0.1); \n        \n        float slatsPhase = p.y * 30.0 + sin(p.x * 2.0 + iTime) * 0.5; \n        float slats = fract(slatsPhase);\n        \n        if (slats > 0.3) {\n            vec3 slatCol = vec3(0.5, 0.5, 0.55);\n            slatCol *= 0.6 + 0.8 * slats; \n            float highlight = smoothstep(0.8, 1.0, slats) * lightMove;\n            slatCol += vec3(0.2) * highlight;\n            col = slatCol;\n        }\n    }\n}\n\nvoid layer_WindowFrames(in vec2 p, inout vec3 col) {\n    if (abs(p.x) < 0.8) {\n        float vFrames = abs(fract(p.x * 3.0) - 0.5);\n        if (vFrames < 0.02) {\n            col = vec3(0.4) + 0.1 * sin(p.y * 10.0);\n        }\n        \n        if (abs(p.x) < 0.05 && abs(p.y) < 0.1) col = vec3(0.7);\n    }\n}\n\nvoid layer_Vignette(in vec2 uv, inout vec3 col) {\n    col *= 1.0 - 0.3 * length(uv - 0.5);\n}\n\nvec4 layer_BrickWall(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n    \n    vec3 col = vec3(-1.0);\n    \n    layer_BrickWall(p, col);\n    \n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_WindowSlats(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n    \n    vec3 col = vec3(-1.0);\n    \n    layer_WindowSlats(p, iTime, col);\n    \n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_WindowFrames(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n    \n    vec3 col = vec3(-1.0);\n    \n    layer_WindowFrames(p, col);\n    \n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_Vignette(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n    \n    vec3 col = vec3(-1.0);\n    \n    layer_Vignette(uv, col);\n    \n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n","IMG_0973":"void layer_OuterWall(in vec2 p, inout vec3 col, out bool isWall) {\n    isWall = false;\n    if (p.x < -0.8 || p.x > 0.8 || p.y > 0.6) {\n        isWall = true;\n        col = vec3(0.6, 0.7, 0.7); \n        if (p.y > 0.6 && length(vec2(p.x, p.y - 0.6)) < 0.9) col = vec3(0.5, 0.6, 0.6);\n        col += 0.05 * sin(p.x*100.0) * sin(p.y*100.0);\n    }\n}\n\nvoid layer_DarkBackground(inout vec3 col) {\n    col = vec3(0.2, 0.2, 0.15);\n}\n\nvoid layer_Floor(in vec2 p, in float iTime, inout vec3 col) {\n    if (p.y < -0.6) {\n        vec2 st = vec2(p.x / (p.y + 0.61), 1.0 / (p.y + 0.61));\n        st.y += iTime * 0.5;\n        float tile = step(0.1, fract(st.x * 2.0)) * step(0.1, fract(st.y * 2.0));\n        col = mix(vec3(0.3), vec3(0.5, 0.55, 0.5), tile);\n    }\n}\n\nvoid layer_Gate(in vec2 p, in float iTime, inout vec3 col) {\n    if (p.y > -0.6 && p.y < 0.6) {\n        float sway = sin(p.y * 3.0 + iTime) * 0.02;\n        float px = p.x + sway;\n        \n        float bars = abs(fract(px * 8.0) - 0.5);\n        float hbars = abs(fract(p.y * 10.0) - 0.5);\n        vec2 cell = vec2(fract(px * 4.0) - 0.5, fract(p.y * 4.0) - 0.5);\n        \n        float circleSize = 0.3 + 0.05 * sin(iTime * 2.0 + p.y * 5.0);\n        float circles = abs(length(cell) - circleSize);\n        \n        if (bars < 0.03 || hbars < 0.02 || circles < 0.04) {\n            col = vec3(0.7, 0.7, 0.6);\n            col *= 0.5 + 0.5 * sin(px * 50.0);\n        }\n    }\n}\n\nvoid layer_Objects(in vec2 p, in float iTime, inout vec3 col) {\n    float redPulse = 1.0 + 0.1 * sin(iTime * 5.0);\n    if (length(p - vec2(-0.5, -0.7)) < 0.1 * redPulse) col = vec3(0.8, 0.1, 0.1);\n    float bluePulse = 1.0 + 0.1 * cos(iTime * 4.0);\n    if (length(p - vec2(0.3, -0.6)) < 0.08 * bluePulse) col = vec3(0.1, 0.2, 0.8);\n}\n\nvec4 layer_OuterWall(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n    \n    vec3 col = vec3(-1.0);\n    bool isWall;\n    \n    layer_OuterWall(p, col, isWall);\n    \n    if (!isWall) {\n    }\n    \n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_DarkBackground(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n    \n    vec3 col = vec3(-1.0);\n    bool isWall;\n    \n    \n    if (!isWall) {\n        layer_DarkBackground(col);\n    }\n    \n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_Floor(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n    \n    vec3 col = vec3(-1.0);\n    bool isWall;\n    \n    \n    if (!isWall) {\n        layer_Floor(p, iTime, col);\n    }\n    \n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_Gate(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n    \n    vec3 col = vec3(-1.0);\n    bool isWall;\n    \n    \n    if (!isWall) {\n        layer_Gate(p, iTime, col);\n    }\n    \n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_Objects(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n    \n    vec3 col = vec3(-1.0);\n    bool isWall;\n    \n    \n    if (!isWall) {\n        layer_Objects(p, iTime, col);\n    }\n    \n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n","IMG_0986":"float sdBox( in vec2 p, in vec2 b ) {\n    vec2 d = abs(p)-b;\n    return length(max(d,0.0)) + min(max(d.x,d.y),0.0);\n}\n\nvoid layer_WoodTexture(in vec2 p, in float iTime, inout vec3 col) {\n    col = vec3(0.4, 0.2, 0.1);\n    float woodLines = sin(p.x * 20.0 + sin(p.y * 15.0 + iTime * 0.2) * 2.0);\n    col *= 0.6 + 0.4 * woodLines;\n}\n\nvoid layer_Signboard(in vec2 p, in float iTime, inout vec3 col) {\n    if (abs(p.x) < 0.7 && p.y > -0.5 && p.y < 0.0) {\n        col = vec3(0.1, 0.5, 0.4); \n        col += 0.05 * fract(sin(dot(p, vec2(12.0, 78.0))) * 43758.0);\n        \n        float border = max(abs(p.x) - 0.65, abs(p.y + 0.25) - 0.22);\n        if (border > 0.0) {\n            col = vec3(0.8, 0.2, 0.2); \n            col += 0.2 * smoothstep(0.9, 1.0, sin(p.x * 10.0 + p.y * 10.0 + iTime * 3.0));\n        }\n        \n        if (abs(p.y + 0.35) < 0.08 && abs(p.x) < 0.5) {\n            float textId = floor(p.x * 6.0);\n            float letter = sdBox(vec2(fract(p.x * 6.0) - 0.5, p.y + 0.35), vec2(0.2, 0.05));\n            if (letter < 0.0) {\n                col = vec3(0.9, 0.8, 0.2);\n                col += 0.2 * sin(iTime * 5.0 + textId);\n            }\n        }\n        \n        if (p.y > -0.2 && p.y < -0.05 && abs(p.x) < 0.4) {\n            float charId = floor(p.x * 4.0);\n            float charBox = sdBox(vec2(fract(p.x * 4.0) - 0.5, p.y + 0.12), vec2(0.3, 0.06));\n            if (charBox < 0.0) {\n                col = vec3(0.9, 0.8, 0.2);\n                col += 0.2 * cos(iTime * 4.0 + charId);\n            }\n        }\n    }\n}\n\nvoid layer_GoldArea(in vec2 p, in float iTime, inout vec3 col) {\n    if (p.y > 0.1 && p.y < 0.5 && abs(p.x) < 0.8) {\n        float gold = fract(sin(dot(p*10.0 + iTime*0.1, vec2(1.0, 1.0))) * 100.0);\n        col = mix(vec3(0.5, 0.4, 0.1), vec3(0.9, 0.7, 0.1), gold);\n        col += 0.3 * smoothstep(0.8, 1.0, sin(p.x * 5.0 - iTime * 2.0));\n        \n        if (abs(p.x) < 0.1 && p.y > 0.1) col = vec3(0.6, 0.1, 0.1);\n    }\n}\n\nvoid layer_Shadow(in vec2 p, inout vec3 col) {\n    if (p.y < -0.5 && p.y > -0.6 && abs(p.x) < 0.75) {\n        col *= smoothstep(-0.5, -0.6, p.y);\n    }\n}\n\nvec4 layer_WoodTexture(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n    \n    vec3 col = vec3(-1.0);\n    \n    layer_WoodTexture(p, iTime, col);\n    \n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_Signboard(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n    \n    vec3 col = vec3(-1.0);\n    \n    layer_Signboard(p, iTime, col);\n    \n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_GoldArea(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n    \n    vec3 col = vec3(-1.0);\n    \n    layer_GoldArea(p, iTime, col);\n    \n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_Shadow(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n    \n    vec3 col = vec3(-1.0);\n    \n    layer_Shadow(p, col);\n    \n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n","IMG_0992":"void layer_Wall(in vec2 p, inout vec3 col) {\n    col = vec3(0.7, 0.65, 0.6);\n    col -= 0.1 * p.y;\n}\n\nvoid layer_Floor(in vec2 p, in float iTime, inout vec3 col) {\n    if (p.x > 0.0 && p.y < 0.0) {\n        vec2 st = vec2(p.x / (p.y - 0.1), 1.0 / (p.y - 0.1));\n        st.x += iTime * 0.2;\n        float tile = max(step(0.1, fract(st.x * 5.0)), step(0.1, fract(st.y * 5.0)));\n        col = mix(vec3(0.8), vec3(0.6), tile);\n    }\n}\n\nvoid layer_Shelves(in vec2 p, in float iTime, inout vec3 col) {\n    if (p.y > -0.4 && p.y < 0.5 && p.x < 0.2) {\n        float sId = floor((p.y + p.x) * 10.0);\n        vec2 sp = vec2(fract((p.y + p.x) * 10.0) - 0.5, p.x);\n        \n        if (abs(sp.y) < 0.2 && sp.x > -0.3 && sp.x < 0.3) {\n            float rnd = fract(sId * 0.123);\n            if (rnd < 0.3) col = vec3(0.1); \n            else if (rnd < 0.6) col = vec3(0.8, 0.1, 0.1); \n            else col = vec3(0.9); \n            \n            col += 0.1 * sin(iTime * 3.0 + sId);\n        }\n        \n        if (length(vec2(sp.x, sp.y + 0.2)) < 0.1) col = vec3(0.05);\n    }\n}\n\nvoid layer_FloatingObjects(in vec2 p, in float iTime, inout vec3 col) {\n    vec2 bp = p - vec2(-0.5, 0.0);\n    bp.y += sin(iTime * 2.0) * 0.05;\n    if (length(bp * vec2(1.0, 0.8)) < 0.2 + 0.05*sin(bp.y*20.0 + iTime*5.0)) {\n        vec3 objCol = vec3(0.9, 0.4, 0.8); \n        objCol *= 0.5 + 0.5 * smoothstep(0.2, 0.0, length(bp));\n        col = objCol;\n    }\n    \n    vec2 gp = p - vec2(-0.1, -0.1);\n    gp.x += cos(iTime * 1.5) * 0.05;\n    if (length(gp * vec2(1.0, 0.7)) < 0.15 + 0.05*cos(gp.x*20.0 - iTime*4.0)) {\n        vec3 objCol = vec3(0.2, 0.8, 0.6); \n        objCol *= 0.5 + 0.5 * smoothstep(0.15, 0.0, length(gp));\n        col = objCol;\n    }\n}\n\nvec4 layer_Wall(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n    \n    vec3 col = vec3(-1.0);\n    \n    layer_Wall(p, col);\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_Floor(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n    \n    vec3 col = vec3(-1.0);\n    \n    layer_Floor(p, iTime, col);\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_Shelves(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n    \n    vec3 col = vec3(-1.0);\n    \n    layer_Shelves(p, iTime, col);\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_FloatingObjects(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n    \n    vec3 col = vec3(-1.0);\n    \n    layer_FloatingObjects(p, iTime, col);\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n","IMG_0995":"#define segment(p, a, b) length(p - a - (b - a) * clamp(dot(p - a, b - a) / dot(b - a, b - a), 0.0, 1.0))\n\nvoid layer_Background(in vec2 p, inout vec3 col) {\n    col = vec3(0.72, 0.72, 0.74);\n    float plaster = fract(sin(dot(p, vec2(12.9898, 78.233))) * 43758.5453);\n    col += (plaster - 0.5) * 0.04;\n}\n\nvoid layer_BalconyWall(in vec2 p, inout vec3 col) {\n    if (p.y > -0.2 && p.y < 0.55 && abs(p.x) < 0.9) {\n        col = vec3(0.25, 0.45, 0.5);\n        col *= 0.8 + 0.2 * p.y;\n    }\n}\n\nvoid layer_Clothesline(in vec2 p, in float iTime, inout vec3 col) {\n    float sag = 0.25 - p.x * p.x * 0.05;\n    if (abs(p.y - sag) < 0.003 && abs(p.x) < 0.85) col = vec3(0.1);\n    \n    for (float i = 0.0; i < 10.0; i++) {\n        float cx = -0.7 + i * 0.15;\n        float cw = 0.05 + sin(i * 10.0) * 0.01;\n        float ch = 0.15 + cos(i * 20.0) * 0.05;\n        \n        vec2 cp = p - vec2(cx, sag);\n        float sway = sin(iTime * 2.0 + i) * 0.05;\n        cp.x -= cp.y * sway;\n        \n        float shirt = max(abs(cp.x) - cw, cp.y);\n        shirt = max(shirt, -cp.y - ch);\n        float sleeves = segment(cp, vec2(-cw, 0.0), vec2(-cw - 0.04, -0.05)) - 0.015;\n        sleeves = min(sleeves, segment(cp, vec2(cw, 0.0), vec2(cw + 0.04, -0.05)) - 0.015);\n        shirt = min(shirt, sleeves);\n        \n        if (shirt < 0.0) {\n            vec3 cCol = mix(vec3(0.8, 0.2, 0.2), vec3(0.2, 0.3, 0.8), fract(i * 0.3));\n            if (fract(i * 0.7) > 0.5) cCol = vec3(0.9, 0.8, 0.2); \n            \n            cCol *= 0.8 + 0.2 * smoothstep(-cw, cw, cp.x);\n            cCol -= 0.1 * sin(cp.x * 50.0) * smoothstep(0.0, -ch, cp.y);\n            col = cCol;\n        }\n    }\n}\n\nvoid layer_BirdCage(in vec2 p, inout vec3 col) {\n    vec2 cageP = p - vec2(0.4, 0.3);\n    float cageBody = length(vec2(cageP.x, max(0.0, cageP.y))) - 0.08;\n    cageBody = max(cageBody, -cageP.y - 0.1);\n    if (cageBody < 0.0) {\n        col = vec3(0.2); \n        float bars = abs(fract(cageP.x * 25.0) - 0.5);\n        if (bars < 0.1) col = vec3(0.6, 0.45, 0.2);\n        if (abs(cageP.y) < 0.005 || abs(cageP.y + 0.05) < 0.005 || abs(cageP.y + 0.09) < 0.005) {\n            col = vec3(0.7, 0.55, 0.3);\n        }\n    }\n    float hook = abs(length(cageP - vec2(0.0, 0.09)) - 0.01);\n    if (hook < 0.003 && cageP.y > 0.09) col = vec3(0.3);\n}\n\nvoid layer_Plant(in vec2 p, inout vec3 col) {\n    vec2 potP = p - vec2(-0.4, -0.2);\n    float pot = max(abs(potP.x) - 0.04 + potP.y * 0.1, abs(potP.y) - 0.05);\n    if (pot < 0.0) col = vec3(0.6, 0.3, 0.2); \n    \n    for(float i=0.0; i<3.0; i++) {\n        float vx = sin(potP.y * 10.0 + i) * 0.02;\n        float vine = segment(potP, vec2(0.0, 0.0), vec2(vx, -0.3 + i*0.05));\n        if (vine < 0.005) col = vec3(0.1, 0.3, 0.1);\n        float lDist = length(vec2(potP.x - vx - sin(potP.y*30.0)*0.02, fract(potP.y * 10.0) - 0.5));\n        if (lDist < 0.2 && potP.y < 0.0 && potP.y > -0.3) col = vec3(0.2, 0.45, 0.15);\n    }\n}\n\nvec4 layer_Background(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n    \n    vec3 col = vec3(-1.0);\n    \n    layer_Background(p, col);\n    \n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_BalconyWall(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n    \n    vec3 col = vec3(-1.0);\n    \n    layer_BalconyWall(p, col);\n    \n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_Clothesline(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n    \n    vec3 col = vec3(-1.0);\n    \n    layer_Clothesline(p, iTime, col);\n    \n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_BirdCage(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n    \n    vec3 col = vec3(-1.0);\n    \n    layer_BirdCage(p, col);\n    \n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_Plant(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n    \n    vec3 col = vec3(-1.0);\n    \n    layer_Plant(p, col);\n    \n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n","IMG_1011":"void layer_WallTiles(in vec2 p, in float iTime, inout vec3 col, out bool isWall) {\n    isWall = false;\n    if (p.x < 0.2) {\n        isWall = true;\n        col = vec3(0.7, 0.6, 0.4); \n        float tile = step(0.1, fract(p.y * 10.0)) * step(0.1, fract(p.x * 5.0 + step(0.5, fract(p.y * 5.0)) * 0.5));\n        col *= mix(0.8, 1.0, tile);\n        \n        float wallAnim = sin(p.x * 20.0 + iTime) * cos(p.y * 20.0 + iTime) * 0.05;\n        col += wallAnim;\n    }\n}\n\nvoid layer_FloorTiles(in vec2 p, in float iTime, inout vec3 col) {\n    col = vec3(0.6, 0.6, 0.6);\n    float floorTile = fract(p.x * 8.0 + p.y * 8.0);\n    col *= mix(0.8, 1.0, step(0.1, floorTile));\n    \n    float floorAnim = sin(length(p) * 20.0 - iTime * 2.0) * 0.05;\n    col += floorAnim;\n}\n\nvoid layer_ChairShadow(in vec2 p, inout vec3 col) {\n    if (p.x > 0.1 && p.x < 0.8 && p.y > -0.8 && p.y < 0.2) {\n        col *= 0.4;\n    }\n}\n\nvoid layer_WovenChair(in vec2 p, in float iTime, inout vec3 col) {\n    if (p.x > -0.6 && p.x < 0.4 && p.y > -0.6 && p.y < 0.5) {\n        if (abs(p.x + 0.6) < 0.02 || abs(p.x - 0.4) < 0.02 || abs(p.y - 0.5) < 0.02 || abs(p.y + 0.6) < 0.02) {\n            col = vec3(0.8, 0.8, 0.85); \n            float shine = smoothstep(0.0, 0.1, sin((p.x + p.y) * 10.0 + iTime * 3.0));\n            col += shine * 0.5;\n        } else {\n            col = vec3(0.1, 0.2, 0.6); \n            float weaveX = abs(fract(p.x * 15.0) - 0.5);\n            float weaveY = abs(fract(p.y * 15.0) - 0.5);\n            \n            if (weaveX > 0.1 ^^ weaveY > 0.1) {\n                col *= 0.8; \n            }\n            if (weaveX < 0.05 && weaveY < 0.05) {\n                if (p.x < 0.2) col = vec3(0.6, 0.5, 0.3);\n                else col = vec3(0.5);\n            }\n        }\n    }\n}\n\nvec4 layer_WallTiles(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 col = vec3(-1.0);\n    bool isWall;\n    \n    layer_WallTiles(p, iTime, col, isWall);\n    \n    if (!isWall) {\n    }\n    \n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_FloorTiles(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 col = vec3(-1.0);\n    bool isWall;\n    \n    \n    if (!isWall) {\n        layer_FloorTiles(p, iTime, col);\n    }\n    \n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_ChairShadow(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 col = vec3(-1.0);\n    bool isWall;\n    \n    \n    if (!isWall) {\n    }\n    \n    layer_ChairShadow(p, col);\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_WovenChair(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 col = vec3(-1.0);\n    bool isWall;\n    \n    \n    if (!isWall) {\n    }\n    \n    layer_WovenChair(p, iTime, col);\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n","IMG_1014":"void layer_Floor(in vec2 p, inout vec3 col) {\n    col = vec3(0.4, 0.4, 0.42); \n    float floorTex = fract(sin(dot(p, vec2(12.9898, 78.233))) * 43758.5453);\n    col -= floorTex * 0.1;\n}\n\nvoid layer_Chair(in vec2 p, in float iTime, inout vec3 col) {\n    if (p.y > -0.6) {\n        col = vec3(0.3, 0.5, 0.9); \n        \n        float lighting = sin(p.x * 2.0 + p.y * 1.5) * 0.15;\n        col += lighting;\n        \n        float shine = smoothstep(0.4, 0.5, sin(p.x * 8.0 - p.y * 8.0 + iTime * 2.0));\n        col += shine * 0.1;\n\n        vec2 center = vec2(0.0, 0.2);\n        vec2 fp = p - center;\n        \n        fp = mod(fp, 0.6) - 0.3;\n        \n        float r = length(fp);\n        float a = atan(fp.y, fp.x);\n        \n        float petals = sin(a * 10.0 + iTime * 0.5) * 0.05 + 0.1;\n        if (r < petals) {\n            col = vec3(0.35, 0.35, 0.37); \n        }\n        \n        float lines = abs(r - petals);\n        if (lines < 0.01) {\n            col *= 0.7; \n        }\n    }\n}\n\nvoid layer_ChairLeg(in vec2 p, inout vec3 col) {\n    if (p.y < -0.6 && p.x > 0.4 && p.x < 0.6) {\n        col = vec3(0.2, 0.4, 0.8);\n        col -= abs(p.x - 0.5) * 2.0; \n    }\n}\n\nvec4 layer_Floor(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 col = vec3(-1.0);\n    \n    layer_Floor(p, col);\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_Chair(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 col = vec3(-1.0);\n    \n    layer_Chair(p, iTime, col);\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_ChairLeg(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 col = vec3(-1.0);\n    \n    layer_ChairLeg(p, col);\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n","IMG_1061":"#define segment(p, a, b) length(p - a - (b - a) * clamp(dot(p - a, b - a) / dot(b - a, b - a), 0.0, 1.0))\n\nvoid layer_Background(in vec2 p, in float iTime, inout vec3 col) {\n    col = mix(vec3(0.95, 0.75, 0.8), vec3(0.9, 0.6, 0.7), length(p) * 0.5);\n    float pattern = sin(p.x * 20.0 + iTime * 0.5) * sin(p.y * 20.0 + iTime * 0.3);\n    col += pattern * 0.05 * vec3(1.0, 0.8, 0.9);\n}\n\nvoid layer_RedBar(in vec2 p, in float iTime, inout vec3 col) {\n    if (p.y > 0.4 && p.y < 0.6) {\n        col = vec3(0.8, 0.1, 0.2); \n        float shine = smoothstep(0.0, 0.2, sin(p.x * 5.0 + iTime * 2.0));\n        col += shine * 0.1;\n        \n        float textWave = sin(p.x * 100.0) * sin(p.y * 100.0);\n        if (textWave > 0.8 && abs(p.y - 0.5) < 0.03) col = vec3(0.9, 0.8, 0.2); \n    }\n}\n\nvoid layer_Text2K4(in vec2 p, inout vec3 col) {\n    vec2 numP = p - vec2(0.5, 0.1);\n    float dText = 1.0;\n    \n    vec2 p2 = numP - vec2(-0.15, 0.0);\n    dText = min(dText, segment(p2, vec2(-0.04, 0.04), vec2(0.0, 0.08)));\n    dText = min(dText, segment(p2, vec2(0.0, 0.08), vec2(0.04, 0.04)));\n    dText = min(dText, segment(p2, vec2(0.04, 0.04), vec2(-0.04, -0.08)));\n    dText = min(dText, segment(p2, vec2(-0.04, -0.08), vec2(0.05, -0.08)));\n    \n    vec2 pK = numP - vec2(0.0, 0.0);\n    dText = min(dText, segment(pK, vec2(-0.03, 0.08), vec2(-0.03, -0.08)));\n    dText = min(dText, segment(pK, vec2(-0.03, 0.0), vec2(0.04, 0.08)));\n    dText = min(dText, segment(pK, vec2(-0.01, 0.02), vec2(0.04, -0.08)));\n    \n    vec2 p4 = numP - vec2(0.15, 0.0);\n    dText = min(dText, segment(p4, vec2(0.02, -0.08), vec2(0.02, 0.08)));\n    dText = min(dText, segment(p4, vec2(0.02, 0.08), vec2(-0.04, 0.0)));\n    dText = min(dText, segment(p4, vec2(-0.05, 0.0), vec2(0.05, 0.0)));\n\n    if (dText < 0.015) {\n        col = vec3(0.85, 0.15, 0.25);\n    } else if (dText < 0.02) {\n        col = vec3(1.0, 0.8, 0.8); \n    }\n}\n\nvoid layer_GodOfWealth(in vec2 p, in float iTime, inout vec3 col) {\n    if (length(p) < 0.3) {\n        vec3 godCol = mix(vec3(0.9, 0.7, 0.1), vec3(0.8, 0.2, 0.1), p.y + 0.5);\n        godCol = mix(godCol, vec3(0.1, 0.6, 0.3), smoothstep(0.1, 0.0, length(p - vec2(0.0, -0.1))));\n        col = mix(col, godCol, smoothstep(0.3, 0.28, length(p)));\n        \n        float glimmer = pow(sin(p.x * 50.0 + p.y * 50.0 + iTime * 3.0) * 0.5 + 0.5, 10.0);\n        col += glimmer * vec3(1.0, 0.9, 0.5) * 0.5;\n    }\n}\n\nvoid layer_BottomNumbers(in vec2 p, inout vec3 col) {\n    if (p.y < -0.3 && p.y > -0.45) {\n        vec2 gridP = p;\n        gridP.x = fract(p.x * 8.0) - 0.5;\n        float numLike = segment(gridP, vec2(-0.2, 0.05), vec2(0.2, 0.05));\n        numLike = min(numLike, segment(gridP, vec2(0.2, 0.05), vec2(0.2, -0.05)));\n        numLike = min(numLike, segment(gridP, vec2(-0.2, -0.05), vec2(0.2, -0.05)));\n        \n        if (numLike < 0.05) {\n            col = vec3(0.8, 0.1, 0.15);\n        }\n    }\n}\n\nvoid layer_DarkEdges(in vec2 p, inout vec3 col) {\n    if (p.y > 0.8 || p.y < -0.8) {\n        col = vec3(0.3, 0.25, 0.2); \n        float tex = fract(sin(dot(p, vec2(12.9898, 78.233))) * 43758.5453);\n        col -= tex * 0.05;\n    }\n}\n\nvec4 layer_Background(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 col = vec3(-1.0);\n    \n    layer_Background(p, iTime, col);\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_RedBar(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 col = vec3(-1.0);\n    \n    layer_RedBar(p, iTime, col);\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_Text2K4(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 col = vec3(-1.0);\n    \n    layer_Text2K4(p, col);\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_GodOfWealth(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 col = vec3(-1.0);\n    \n    layer_GodOfWealth(p, iTime, col);\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_BottomNumbers(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 col = vec3(-1.0);\n    \n    layer_BottomNumbers(p, col);\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_DarkEdges(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 col = vec3(-1.0);\n    \n    layer_DarkEdges(p, col);\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n","IMG_1072":"void layer_Background(in vec2 p, in float iTime, inout vec3 col) {\n    col = vec3(0.5, 0.5, 0.5); \n    if (p.y > 0.0) {\n        col = vec3(0.4, 0.4, 0.35); \n        float flowers = length(vec2(p.x + 0.5, p.y - 0.6));\n        if (flowers < 0.3) {\n            vec3 fCol = vec3(0.8, 0.8, 0.1);\n            float wind = sin(p.x * 20.0 + p.y * 20.0 + iTime) * 0.05;\n            fCol += wind;\n            col = mix(col, fCol, smoothstep(0.3, 0.2, flowers + wind));\n        }\n    }\n}\n\nvoid layer_Cart(in vec2 p, in float iTime, inout vec3 col) {\n    if (p.y < -0.1 && p.y > -0.6 && p.x > -0.2 && p.x < 0.6) {\n        col = vec3(0.2, 0.4, 0.8);\n        float shine = smoothstep(0.4, 0.5, sin(p.x * 5.0 - p.y * 5.0 + iTime * 2.0));\n        col += shine * 0.1;\n    }\n}\n\nvoid layer_LotteryTickets(in vec2 p, inout vec3 col) {\n    if (p.y < -0.2 && p.y > -0.5 && p.x > 0.0 && p.x < 0.5) {\n        float grid = abs(fract(p.x * 8.0) - 0.5) * abs(fract(p.y * 8.0) - 0.5);\n        if (grid < 0.1) {\n            col = vec3(0.9, 0.8, 0.8);\n        }\n    }\n}\n\nvoid layer_Woman(in vec2 p, in float iTime, inout vec3 col) {\n    if (p.x > -0.8 && p.x < -0.2 && p.y > -0.5 && p.y < 0.3) {\n        col = vec3(0.1, 0.5, 0.2); \n        float wrinkles = sin(p.x * 30.0 + p.y * 10.0 + iTime * 0.5) * 0.05;\n        col += wrinkles;\n        \n        if (p.y < -0.2) {\n            col = vec3(0.1, 0.1, 0.1); \n        }\n    }\n}\n\nvoid layer_Man(in vec2 p, inout vec3 col) {\n    if (p.x > -0.1 && p.x < 0.3 && p.y > -0.1 && p.y < 0.5) {\n        col = vec3(0.15, 0.15, 0.2); \n    }\n}\n\nvec4 layer_Background(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 col = vec3(-1.0);\n    \n    layer_Background(p, iTime, col);\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_Cart(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 col = vec3(-1.0);\n    \n    layer_Cart(p, iTime, col);\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_LotteryTickets(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 col = vec3(-1.0);\n    \n    layer_LotteryTickets(p, col);\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_Woman(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 col = vec3(-1.0);\n    \n    layer_Woman(p, iTime, col);\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_Man(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 col = vec3(-1.0);\n    \n    layer_Man(p, col);\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n","IMG_1077":"void layer_MetallicPlate(in vec2 p, in float iTime, inout vec3 col) {\n    col = vec3(0.6, 0.6, 0.65);\n    float reflection = sin(p.x * 3.0 + p.y * 3.0 + iTime) * 0.1;\n    col += reflection;\n}\n\nvoid layer_Holes(in vec2 p, inout vec3 col) {\n    vec2 gridP = fract(p * 5.0) - 0.5;\n    if (length(gridP) < 0.1) {\n        col = vec3(0.2, 0.2, 0.2);\n    }\n}\n\nvoid layer_ColoredPencils(in vec2 p, inout vec3 col) {\n    if (p.y > 0.2 && p.x > -0.4 && p.x < 0.6) {\n        float pencilIdx = floor((p.x + 0.4) * 5.0); \n        float pencilPos = fract((p.x + 0.4) * 5.0);\n        if (pencilPos > 0.1 && pencilPos < 0.9) {\n            if (pencilIdx == 0.0) col = vec3(0.8, 0.3, 0.2); \n            else if (pencilIdx == 1.0) col = vec3(0.2, 0.5, 0.8); \n            else if (pencilIdx == 2.0) col = vec3(0.9, 0.6, 0.3); \n            else if (pencilIdx == 3.0) col = vec3(0.3, 0.6, 0.8); \n            else col = vec3(0.2, 0.4, 0.7); \n            \n            float shade = sin(pencilPos * 3.1415) * 0.2;\n            col -= (1.0 - shade) * 0.2;\n        }\n    }\n}\n\nvoid layer_LaserBeam(in vec2 p, in float iTime, inout vec3 col) {\n    vec2 laserPos = vec2(sin(iTime * 2.0) * 0.3, -0.3 + cos(iTime * 1.5) * 0.2); \n    float dLaser = length(p - laserPos);\n    \n    if (dLaser < 0.2) {\n        vec3 glowCol = vec3(1.0, 0.2, 0.1);\n        float intensity = pow(0.2 / (dLaser + 0.01), 1.5) * 0.05;\n        col += glowCol * intensity;\n    }\n    \n    if (dLaser < 0.02) {\n        col = vec3(1.0, 0.8, 0.8);\n    }\n}\n\nvec4 layer_MetallicPlate(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 col = vec3(-1.0);\n    \n    layer_MetallicPlate(p, iTime, col);\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_Holes(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 col = vec3(-1.0);\n    \n    layer_Holes(p, col);\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_ColoredPencils(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 col = vec3(-1.0);\n    \n    layer_ColoredPencils(p, col);\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_LaserBeam(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 col = vec3(-1.0);\n    \n    layer_LaserBeam(p, iTime, col);\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n","IMG_1121":"void layer_Poster(in vec2 p, inout vec3 col) {\n    col = vec3(0.1, 0.1, 0.15); \n    \n    if (p.x < 0.0 && p.y > -0.4 && p.y < 0.4) {\n        col = mix(col, vec3(0.6, 0.1, 0.1), smoothstep(0.4, 0.0, abs(p.y)));\n    }\n\n    if (p.x > 0.0 && p.x < 0.3 && p.y > -0.2 && p.y < 0.4) {\n        float textLines = step(0.5, fract(p.y * 20.0));\n        col = mix(col, vec3(0.8), textLines * 0.5);\n    }\n}\n\nvoid layer_Street(in vec2 p, inout vec3 col) {\n    if (p.y < -0.4) {\n        col = vec3(0.5, 0.5, 0.45); \n        float gravel = fract(sin(dot(p, vec2(12.9898, 78.233))) * 43758.5453);\n        col -= gravel * 0.1;\n    }\n}\n\nvoid layer_Scooter(in vec2 p, in float iTime, inout vec3 col) {\n    if (p.x > 0.0 && p.y < -0.2) {\n        if (p.y > -0.6 && p.y < -0.3 && p.x > 0.2 && p.x < 0.8) {\n            col = vec3(0.7, 0.1, 0.1); \n            float shine = smoothstep(0.4, 0.5, sin(p.x * 10.0 + p.y * 10.0 + iTime * 3.0));\n            col += shine * 0.2;\n        }\n    }\n}\n\nvoid layer_Rider(in vec2 p, in float iTime, inout vec3 col) {\n    if (p.x > 0.0 && p.y < -0.2) {\n        if (p.x > 0.3 && p.x < 0.6 && p.y > -0.3 && p.y < 0.2) {\n            col = vec3(0.4, 0.4, 0.42); \n            float folds = sin(p.x * 15.0 - p.y * 10.0 + iTime) * 0.05;\n            col += folds;\n            \n            if (p.y > 0.0 && length(p - vec2(0.45, 0.1)) < 0.1) {\n                col = vec3(0.9);\n            }\n            if (p.y > 0.0 && p.y < 0.1 && p.x > 0.45 && p.x < 0.55) {\n                col = vec3(0.6, 0.8, 1.0);\n            }\n        }\n    }\n}\n\nvec4 layer_Poster(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 col = vec3(-1.0);\n    \n    layer_Poster(p, col);\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_Street(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 col = vec3(-1.0);\n    \n    layer_Street(p, col);\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_Scooter(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 col = vec3(-1.0);\n    \n    layer_Scooter(p, iTime, col);\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_Rider(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 col = vec3(-1.0);\n    \n    layer_Rider(p, iTime, col);\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n","IMG_1126":"void layer_Background(inout vec3 col) {\n    col = vec3(0.05, 0.05, 0.05); \n}\n\nvoid layer_ChromeExhaust(in vec2 p, in float iTime, inout vec3 col) {\n    if (p.x > 0.1 && p.x < 0.8 && p.y > -0.8 && p.y < 0.5) {\n        float exhaustDist = abs((p.x - 0.4) - (p.y + 0.1) * 0.5);\n        if (exhaustDist < 0.2) {\n            col = vec3(0.7, 0.75, 0.8); \n            \n            float reflection = sin(p.x * 20.0 + p.y * 10.0) * 0.2 + 0.8;\n            col *= reflection;\n            \n            float shine = smoothstep(0.4, 0.6, sin(p.y * 5.0 - iTime * 4.0));\n            col += shine * 0.3;\n            \n            if (abs(p.y - 0.2) < 0.05 || abs(p.y + 0.3) < 0.05) {\n                col = vec3(0.1);\n            }\n        }\n    }\n}\n\nvoid layer_SuspensionSpring(in vec2 p, inout vec3 col) {\n    if (p.x > -0.4 && p.x < 0.1 && p.y > -0.2 && p.y < 0.4) {\n        float springDist = abs(p.x + 0.15);\n        if (springDist < 0.1) {\n            col = vec3(0.1, 0.1, 0.1); \n            \n            float coil = fract(p.y * 12.0) - 0.5;\n            if (abs(coil) < 0.15) {\n                col = vec3(0.3, 0.3, 0.35); \n                float highlight = smoothstep(0.0, 0.1, abs(coil));\n                col += highlight * 0.2;\n            }\n        }\n    }\n}\n\nvoid layer_CarbonFiberPlates(in vec2 p, in float iTime, inout vec3 col) {\n    if (p.x > -0.2 && p.x < 0.2 && p.y > 0.2 && p.y < 0.6) {\n        col = vec3(0.6, 0.6, 0.65); \n        \n        float cfPattern = step(0.5, fract(p.x * 40.0 + p.y * 40.0)) * step(0.5, fract(p.x * 40.0 - p.y * 40.0));\n        cfPattern += step(0.5, fract(p.x * 40.0 + p.y * 40.0 + 0.5)) * step(0.5, fract(p.x * 40.0 - p.y * 40.0 + 0.5));\n        \n        col *= mix(0.7, 1.0, cfPattern);\n        \n        float plateAnim = sin(p.x * 10.0 + p.y * 10.0 + iTime) * 0.05;\n        col += plateAnim;\n    }\n}\n\nvec4 layer_Background(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 col = vec3(-1.0);\n    \n    layer_Background(col);\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_ChromeExhaust(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 col = vec3(-1.0);\n    \n    layer_ChromeExhaust(p, iTime, col);\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_SuspensionSpring(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 col = vec3(-1.0);\n    \n    layer_SuspensionSpring(p, col);\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_CarbonFiberPlates(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 col = vec3(-1.0);\n    \n    layer_CarbonFiberPlates(p, iTime, col);\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n","IMG_1151":"void layer_Road(in vec2 p, inout vec3 col) {\n    col = vec3(0.45, 0.45, 0.45); \n    float roadNoise = fract(sin(dot(p, vec2(12.9898, 78.233))) * 43758.5453);\n    col -= roadNoise * 0.1;\n}\n\nvoid layer_MotorcycleParts(in vec2 p, in float iTime, inout vec3 col) {\n    if (p.x > 0.3 && p.y > 0.2) {\n        col = vec3(0.1); \n        \n        if (p.x > 0.4 && p.x < 0.6 && p.y > 0.3 && p.y < 0.8) {\n            col = vec3(0.7, 0.1, 0.1); \n            float reflector = step(0.5, fract(p.x * 15.0)) * step(0.5, fract(p.y * 15.0));\n            col *= mix(0.7, 1.2, reflector);\n            \n            float pulse = sin(iTime * 2.0) * 0.1 + 0.9;\n            col *= pulse;\n        }\n        \n        if (p.x > 0.6 && p.x < 0.9 && p.y > 0.3 && p.y < 0.9) {\n            col = vec3(0.9, 0.9, 0.95); \n            \n            float textMask = step(0.8, fract(p.x * 8.0)) * step(0.2, fract(p.y * 5.0));\n            if (textMask > 0.5 && p.x > 0.65 && p.x < 0.85 && p.y > 0.4 && p.y < 0.8) {\n                col = vec3(0.1);\n            }\n        }\n    }\n}\n\nvoid layer_TrousersLeg(in vec2 p, in float iTime, inout vec3 col) {\n    if (p.x > -0.8 && p.x < 0.4 && p.y > -0.8 && p.y < 0.9) {\n        float legDist = abs(p.x + 0.2 + sin(p.y * 2.0) * 0.2);\n        if (legDist < 0.3) {\n            col = vec3(0.4, 0.25, 0.15); \n            \n            float leafX = fract(p.x * 8.0 + sin(p.y * 5.0));\n            float leafY = fract(p.y * 10.0 + cos(p.x * 5.0));\n            \n            if (leafX < 0.3 && leafY < 0.3) {\n                col = vec3(0.8, 0.8, 0.85); \n            } else if (leafX > 0.7 && leafY > 0.7) {\n                col = vec3(0.8, 0.5, 0.2); \n            }\n            \n            float foldAnim = sin(p.x * 10.0 + p.y * 5.0 + iTime) * 0.1;\n            col += foldAnim;\n            \n            col *= smoothstep(0.3, 0.1, legDist);\n        }\n    }\n}\n\nvoid layer_FootAndShoe(in vec2 p, inout vec3 col) {\n    if (p.x > 0.1 && p.x < 0.6 && p.y > -0.9 && p.y < -0.4) {\n        col = vec3(0.6, 0.4, 0.2); \n        float shoeShine = smoothstep(0.4, 0.5, sin(p.x * 5.0 + p.y * 5.0));\n        col += shoeShine * 0.1;\n    }\n}\n\nvec4 layer_Road(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 col = vec3(-1.0);\n    \n    layer_Road(p, col);\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_MotorcycleParts(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 col = vec3(-1.0);\n    \n    layer_MotorcycleParts(p, iTime, col);\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_TrousersLeg(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 col = vec3(-1.0);\n    \n    layer_TrousersLeg(p, iTime, col);\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_FootAndShoe(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 col = vec3(-1.0);\n    \n    layer_FootAndShoe(p, col);\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n","IMG_1183":"void layer_RollerDoor(in vec2 p, inout vec3 col) {\n    col = vec3(0.85, 0.85, 0.8); \n    \n    float grooves = fract(p.x * 20.0);\n    if (grooves < 0.2) col *= 0.7; \n    if (grooves > 0.8) col *= 1.1; \n    \n    float dirt = fract(sin(p.x * 50.0 + p.y * 30.0) * 43758.5);\n    col *= mix(0.9, 1.0, dirt);\n}\n\nvoid layer_Posters(in vec2 p, in float iTime, inout vec3 col) {\n    if (p.x > -0.6 && p.x < -0.2 && p.y > 0.0 && p.y < 0.6) {\n        col = vec3(0.95); \n        float textY = fract(p.y * 10.0);\n        if (textY > 0.2 && textY < 0.8 && p.x > -0.55 && p.x < -0.25) {\n            float textX = fract(p.x * 20.0);\n            if (textX > 0.2 && textX < 0.8) col = vec3(0.1); \n        }\n        float tear = sin(p.y * 50.0) * 0.02;\n        if (p.x < -0.6 + tear || p.x > -0.2 - tear) col = vec3(0.85, 0.85, 0.8); \n    }\n    \n    if (p.x > -0.1 && p.x < 0.3 && p.y > -0.3 && p.y < 0.2) {\n        col = vec3(0.95);\n        float textY = fract(p.y * 8.0);\n        if (textY > 0.2 && textY < 0.8 && p.x > -0.05 && p.x < 0.25) {\n            float textX = fract(p.x * 15.0);\n            if (textX > 0.2 && textX < 0.8) col = vec3(0.1);\n        }\n    }\n    \n    if (p.x > -0.7 && p.x < -0.3 && p.y > -0.8 && p.y < -0.2) {\n        col = vec3(0.92);\n        float textY = fract(p.y * 12.0);\n        if (textY > 0.2 && textY < 0.8 && p.x > -0.65 && p.x < -0.35) {\n            float textX = fract(p.x * 25.0);\n            if (textX > 0.2 && textX < 0.8) col = vec3(0.15);\n        }\n        float flutter = sin(p.y * 10.0 + iTime * 2.0) * 0.05 + 0.95;\n        col *= flutter;\n    }\n}\n\nvoid layer_Scooter(in vec2 p, inout vec3 col) {\n    if (p.x > 0.5 && p.y < -0.2) {\n        float bodyDist = length(p - vec2(0.8, -0.6));\n        if (bodyDist < 0.4) {\n            col = vec3(0.15); \n            float shine = smoothstep(0.3, 0.4, sin(p.x * 10.0 + p.y * 10.0));\n            col += shine * 0.2;\n        }\n        \n        float handleDist = abs(p.y - (-p.x * 0.5 + 0.1));\n        if (handleDist < 0.05 && p.x > 0.6 && p.x < 0.9 && p.y > -0.4 && p.y < -0.1) {\n            col = vec3(0.1); \n            float grip = fract(p.x * 40.0);\n            if (grip < 0.5) col *= 0.8;\n        }\n    }\n}\n\nvec4 layer_RollerDoor(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 col = vec3(-1.0);\n    \n    layer_RollerDoor(p, col);\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_Posters(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 col = vec3(-1.0);\n    \n    layer_Posters(p, iTime, col);\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_Scooter(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 col = vec3(-1.0);\n    \n    layer_Scooter(p, col);\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n","IMG_1185":"void layer_WoodenFloor(in vec2 p, inout vec3 col) {\n    col = vec3(0.6, 0.45, 0.3); \n    \n    float planks = fract(p.x * 5.0 + p.y * 2.0);\n    if (planks < 0.05) col *= 0.6; \n    \n    float grain = sin(p.x * 50.0 + sin(p.y * 20.0) * 10.0);\n    col *= mix(0.9, 1.1, grain * 0.1 + 0.5);\n}\n\nvoid layer_MainBodyWrapped(in vec2 p, in float iTime, inout vec3 col) {\n    if (p.x > -0.4 && p.x < 0.6 && p.y > -0.5 && p.y < 0.4) {\n        col = vec3(0.75, 0.6, 0.45); \n        \n        float corrugation = abs(fract((p.x + p.y) * 20.0) - 0.5);\n        if (corrugation < 0.1) col *= 0.9;\n        \n        float tape = smoothstep(0.4, 0.5, sin(p.x * 30.0 - p.y * 20.0 + iTime));\n        col += tape * 0.15; \n        \n        if (p.x > -0.1 && p.x < 0.2 && p.y > -0.1 && p.y < 0.1) {\n            float text = step(0.5, fract(p.x * 15.0)) * step(0.5, fract(p.y * 10.0));\n            col = mix(col, vec3(0.8, 0.2, 0.2), text * 0.8); \n        }\n        if (p.x > 0.2 && p.x < 0.5 && p.y > 0.1 && p.y < 0.3) {\n            float text = step(0.5, fract(p.x * 20.0)) * step(0.5, fract(p.y * 15.0));\n            col = mix(col, vec3(0.1, 0.4, 0.8), text * 0.8); \n        }\n    }\n}\n\nvoid layer_FrontWheel(in vec2 p, inout vec3 col) {\n    float wheelDist = length(p - vec2(0.6, -0.4));\n    if (wheelDist < 0.2) {\n        col = vec3(0.1); \n        float rimDist = length(p - vec2(0.6, -0.4));\n        if (rimDist < 0.12) {\n            col = vec3(0.5); \n            float angle = atan(p.y + 0.4, p.x - 0.6);\n            float spokes = sin(angle * 5.0);\n            if (spokes > 0.8) col = vec3(0.3);\n        }\n    }\n}\n\nvoid layer_BackPartWrapping(in vec2 p, in float iTime, inout vec3 col) {\n    if (p.x > -0.8 && p.x < -0.3 && p.y > 0.0 && p.y < 0.5) {\n        col = vec3(0.9, 0.9, 0.9); \n        float pattern = step(0.5, fract(p.x * 10.0 + p.y * 10.0));\n        col *= mix(vec3(1.0), vec3(0.2, 0.4, 0.8), pattern * 0.5); \n        \n        float tape = smoothstep(0.4, 0.5, sin(p.x * 20.0 - p.y * 10.0 + iTime * 1.5));\n        col += tape * 0.2;\n    }\n}\n\nvec4 layer_WoodenFloor(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 col = vec3(-1.0);\n    \n    layer_WoodenFloor(p, col);\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_MainBodyWrapped(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 col = vec3(-1.0);\n    \n    layer_MainBodyWrapped(p, iTime, col);\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_FrontWheel(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 col = vec3(-1.0);\n    \n    layer_FrontWheel(p, col);\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_BackPartWrapping(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 col = vec3(-1.0);\n    \n    layer_BackPartWrapping(p, iTime, col);\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n","IMG_1213":"#define segment(p, a, b) length(p - a - (b - a) * clamp(dot(p - a, b - a) / dot(b - a, b - a), 0.0, 1.0))\n\nvoid layer_Background(inout vec3 col) {\n    col = vec3(0.88, 0.88, 0.9);\n}\n\nvoid layer_GoldenFrame(in vec2 p, in vec2 fp, in float frameBox, in float innerBox, inout vec3 col) {\n    if (innerBox > 0.0) {\n        vec3 gold = vec3(0.8, 0.65, 0.2);\n        vec3 darkGold = vec3(0.4, 0.3, 0.1);\n        float latX = abs(fract(fp.x * 20.0 + fp.y * 20.0) - 0.5);\n        float latY = abs(fract(fp.x * 20.0 - fp.y * 20.0) - 0.5);\n        float lattice = min(latX, latY);\n        float rims = min(abs(frameBox), abs(innerBox));\n        if (rims < 0.01) col = vec3(0.9, 0.8, 0.4);\n        else {\n            col = mix(gold, darkGold, smoothstep(0.0, 0.2, lattice));\n            col *= 0.8 + 0.2 * sin(fp.x * 50.0);\n        }\n    }\n}\n\nvoid layer_SceneSky(in vec2 sp, in float iTime, inout vec3 col) {\n    col = mix(vec3(0.4, 0.7, 0.9), vec3(0.8, 0.9, 0.95), -sp.y * 0.5 + 0.5);\n    float cloudNoise = sin(sp.x * 5.0 + iTime * 0.5) * sin(sp.x * 10.0 + sp.y * 5.0 + iTime * 0.3);\n    if (sp.y > 0.2) col = mix(col, vec3(1.0), smoothstep(0.5, 0.9, cloudNoise));\n}\n\nvoid layer_SceneMountain(in vec2 sp, inout vec3 col) {\n    float mnt = -0.1 + sin(sp.x * 5.0) * 0.1 + cos(sp.x * 12.0) * 0.05;\n    if (sp.y < mnt) col = vec3(0.4, 0.45, 0.5);\n}\n\nvoid layer_SceneOcean(in vec2 sp, in float iTime, inout vec3 col) {\n    if (sp.y < -0.1) {\n        col = mix(vec3(0.1, 0.3, 0.6), vec3(0.4, 0.8, 0.9), (sp.y + 1.0)*0.5);\n        float wave = sin(sp.x * 40.0 - iTime * 4.0) * sin(sp.y * 60.0 + iTime * 2.0);\n        col += wave * 0.05;\n        if (wave > 0.8) col += 0.2; \n    }\n}\n\nvoid layer_SceneBoat(in vec2 sp, in float iTime, inout vec3 col) {\n    float sailProgress = mod(iTime * 0.2, 2.0) - 1.0; \n    vec2 bp = sp - vec2(sailProgress, -0.2);\n    bp.y += sin(iTime * 4.0) * 0.02;\n    float hull = max(abs(bp.x) - 0.1 + bp.y * 0.5, abs(bp.y + 0.02) - 0.02);\n    if (hull < 0.0) col = vec3(0.2, 0.15, 0.1);\n    \n    float mast = segment(bp, vec2(0.0, 0.0), vec2(0.0, 0.2));\n    if (mast < 0.005) col = vec3(0.1);\n    float billow = sin(bp.y * 10.0 - iTime * 5.0) * 0.02;\n    float sail = max(bp.x - billow, -bp.x - 0.1 + bp.y * 0.5);\n    sail = max(sail, max(bp.y - 0.18, -bp.y));\n    if (sail < 0.0) {\n        col = vec3(0.9, 0.2, 0.2);\n        col *= 0.8 + 0.2 * bp.x * 10.0; \n    }\n}\n\nvoid layer_SceneBalcony(in vec2 sp, in float iTime, inout vec3 col) {\n    if (sp.x > 0.4 && sp.y > -0.7) {\n        float rails = min(abs(fract(sp.x * 8.0) - 0.5), abs(sp.y + 0.4));\n        if (rails < 0.02) col = vec3(0.9);\n        \n        vec2 flp = sp * vec2(10.0, 15.0);\n        flp.x += sin(flp.y * 5.0 + iTime * 4.0) * 0.2;\n        \n        vec2 flpFrac = fract(flp) - 0.5;\n        float flower = length(flpFrac) - 0.2 - sin(atan(flpFrac.y, flpFrac.x) * 5.0) * 0.1;\n        \n        if (flower < 0.0 && sp.y > -0.4) {\n            float colorSeed = fract(floor(flp.x) + floor(flp.y));\n            vec3 fCol = colorSeed > 0.5 ? vec3(0.9, 0.2, 0.5) : vec3(0.8, 0.1, 0.2);\n            if (length(flpFrac) < 0.05) fCol = vec3(0.9, 0.9, 0.1);\n            col = mix(col, fCol, 0.95);\n        }\n    }\n}\n\nvec4 layer_Background(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n    \n    vec3 col = vec3(-1.0);\n    layer_Background(col);\n    \n    float frameW = 0.75, frameH = 0.65;\n    vec2 fp = vec2(p.x - 0.1, p.y + 0.05);\n    float frameBox = max(abs(fp.x) - frameW, abs(fp.y) - frameH);\n    \n    if (frameBox < 0.0) {\n        float innerBox = max(abs(fp.x) - frameW + 0.08, abs(fp.y) - frameH + 0.08);\n        if (innerBox > 0.0) {\n        } else {\n            vec2 sp = fp / vec2(frameW - 0.08, frameH - 0.08);\n        }\n    } else {\n        col = mix(vec3(0.6), col, smoothstep(0.0, 0.02, frameBox - 0.02)); \n    }\n    \n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_GoldenFrame(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n    \n    vec3 col = vec3(-1.0);\n    \n    float frameW = 0.75, frameH = 0.65;\n    vec2 fp = vec2(p.x - 0.1, p.y + 0.05);\n    float frameBox = max(abs(fp.x) - frameW, abs(fp.y) - frameH);\n    \n    if (frameBox < 0.0) {\n        float innerBox = max(abs(fp.x) - frameW + 0.08, abs(fp.y) - frameH + 0.08);\n        if (innerBox > 0.0) {\n            layer_GoldenFrame(p, fp, frameBox, innerBox, col);\n        } else {\n            vec2 sp = fp / vec2(frameW - 0.08, frameH - 0.08);\n        }\n    } else {\n        col = mix(vec3(0.6), col, smoothstep(0.0, 0.02, frameBox - 0.02)); \n    }\n    \n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_SceneSky(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n    \n    vec3 col = vec3(-1.0);\n    \n    float frameW = 0.75, frameH = 0.65;\n    vec2 fp = vec2(p.x - 0.1, p.y + 0.05);\n    float frameBox = max(abs(fp.x) - frameW, abs(fp.y) - frameH);\n    \n    if (frameBox < 0.0) {\n        float innerBox = max(abs(fp.x) - frameW + 0.08, abs(fp.y) - frameH + 0.08);\n        if (innerBox > 0.0) {\n        } else {\n            vec2 sp = fp / vec2(frameW - 0.08, frameH - 0.08);\n            layer_SceneSky(sp, iTime, col);\n        }\n    } else {\n        col = mix(vec3(0.6), col, smoothstep(0.0, 0.02, frameBox - 0.02)); \n    }\n    \n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_SceneMountain(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n    \n    vec3 col = vec3(-1.0);\n    \n    float frameW = 0.75, frameH = 0.65;\n    vec2 fp = vec2(p.x - 0.1, p.y + 0.05);\n    float frameBox = max(abs(fp.x) - frameW, abs(fp.y) - frameH);\n    \n    if (frameBox < 0.0) {\n        float innerBox = max(abs(fp.x) - frameW + 0.08, abs(fp.y) - frameH + 0.08);\n        if (innerBox > 0.0) {\n        } else {\n            vec2 sp = fp / vec2(frameW - 0.08, frameH - 0.08);\n            layer_SceneMountain(sp, col);\n        }\n    } else {\n        col = mix(vec3(0.6), col, smoothstep(0.0, 0.02, frameBox - 0.02)); \n    }\n    \n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_SceneOcean(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n    \n    vec3 col = vec3(-1.0);\n    \n    float frameW = 0.75, frameH = 0.65;\n    vec2 fp = vec2(p.x - 0.1, p.y + 0.05);\n    float frameBox = max(abs(fp.x) - frameW, abs(fp.y) - frameH);\n    \n    if (frameBox < 0.0) {\n        float innerBox = max(abs(fp.x) - frameW + 0.08, abs(fp.y) - frameH + 0.08);\n        if (innerBox > 0.0) {\n        } else {\n            vec2 sp = fp / vec2(frameW - 0.08, frameH - 0.08);\n            layer_SceneOcean(sp, iTime, col);\n        }\n    } else {\n        col = mix(vec3(0.6), col, smoothstep(0.0, 0.02, frameBox - 0.02)); \n    }\n    \n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_SceneBoat(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n    \n    vec3 col = vec3(-1.0);\n    \n    float frameW = 0.75, frameH = 0.65;\n    vec2 fp = vec2(p.x - 0.1, p.y + 0.05);\n    float frameBox = max(abs(fp.x) - frameW, abs(fp.y) - frameH);\n    \n    if (frameBox < 0.0) {\n        float innerBox = max(abs(fp.x) - frameW + 0.08, abs(fp.y) - frameH + 0.08);\n        if (innerBox > 0.0) {\n        } else {\n            vec2 sp = fp / vec2(frameW - 0.08, frameH - 0.08);\n            layer_SceneBoat(sp, iTime, col);\n        }\n    } else {\n        col = mix(vec3(0.6), col, smoothstep(0.0, 0.02, frameBox - 0.02)); \n    }\n    \n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_SceneBalcony(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n    \n    vec3 col = vec3(-1.0);\n    \n    float frameW = 0.75, frameH = 0.65;\n    vec2 fp = vec2(p.x - 0.1, p.y + 0.05);\n    float frameBox = max(abs(fp.x) - frameW, abs(fp.y) - frameH);\n    \n    if (frameBox < 0.0) {\n        float innerBox = max(abs(fp.x) - frameW + 0.08, abs(fp.y) - frameH + 0.08);\n        if (innerBox > 0.0) {\n        } else {\n            vec2 sp = fp / vec2(frameW - 0.08, frameH - 0.08);\n            layer_SceneBalcony(sp, iTime, col);\n        }\n    } else {\n        col = mix(vec3(0.6), col, smoothstep(0.0, 0.02, frameBox - 0.02)); \n    }\n    \n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n","IMG_1240":"#define segment(p, a, b) length(p - a - (b - a) * clamp(dot(p - a, b - a) / dot(b - a, b - a), 0.0, 1.0))\n\nvoid layer_Signboard(in vec2 p, inout vec3 col, out bool insideSign) {\n    float signW = 1.1, signH = 0.85;\n    insideSign = false;\n    if (abs(p.x) < signW && abs(p.y) < signH) {\n        col = vec3(0.05); \n        insideSign = true;\n    } else {\n        col = vec3(0.2); \n    }\n}\n\nvoid layer_TextGiai(in vec2 p, inout vec3 col) {\n    vec2 t1 = p - vec2(-0.6, 0.5);\n    float d1 = 1.0;\n    \n    d1 = min(d1, segment(t1, vec2(0.0, 0.08), vec2(-0.05, 0.08)));\n    d1 = min(d1, segment(t1, vec2(-0.05, 0.08), vec2(-0.05, -0.08)));\n    d1 = min(d1, segment(t1, vec2(-0.05, -0.08), vec2(0.0, -0.08)));\n    d1 = min(d1, segment(t1, vec2(0.0, -0.08), vec2(0.0, 0.0)));\n    d1 = min(d1, segment(t1, vec2(0.0, 0.0), vec2(-0.02, 0.0)));\n    \n    d1 = min(d1, segment(t1, vec2(0.08, 0.08), vec2(0.08, -0.08)));\n    \n    d1 = min(d1, segment(t1, vec2(0.16, -0.08), vec2(0.2, 0.08)));\n    d1 = min(d1, segment(t1, vec2(0.2, 0.08), vec2(0.24, -0.08)));\n    d1 = min(d1, segment(t1, vec2(0.18, 0.0), vec2(0.22, 0.0)));\n    if (d1 < 0.015) col = vec3(0.15, 0.8, 0.2);\n}\n\nvoid layer_TextNuocMia(in vec2 p, inout vec3 col) {\n    vec2 t2 = p - vec2(-0.3, 0.1);\n    float d2 = 1.0;\n    \n    d2 = min(d2, segment(t2, vec2(-0.2, -0.15), vec2(-0.2, 0.15)));\n    d2 = min(d2, segment(t2, vec2(-0.2, 0.15), vec2(-0.05, -0.15)));\n    d2 = min(d2, segment(t2, vec2(-0.05, -0.15), vec2(-0.05, 0.15)));\n    \n    d2 = min(d2, segment(t2, vec2(0.15, -0.15), vec2(0.15, 0.15)));\n    d2 = min(d2, segment(t2, vec2(0.15, 0.15), vec2(0.25, 0.0)));\n    d2 = min(d2, segment(t2, vec2(0.25, 0.0), vec2(0.35, 0.15)));\n    d2 = min(d2, segment(t2, vec2(0.35, 0.15), vec2(0.35, -0.15)));\n    \n    d2 = min(d2, segment(t2, vec2(0.45, 0.15), vec2(0.45, -0.15)));\n    \n    d2 = min(d2, segment(t2, vec2(0.55, -0.15), vec2(0.65, 0.15)));\n    d2 = min(d2, segment(t2, vec2(0.65, 0.15), vec2(0.75, -0.15)));\n    if (d2 < 0.02) {\n        col = vec3(0.9, 0.1, 0.15); \n        if (d2 > 0.01) col = vec3(1.0, 0.8, 0.8); \n    }\n}\n\nvoid layer_SugarcaneStalks(in vec2 p, inout vec3 col) {\n    for (float i = 0.0; i < 4.0; i++) {\n        float cx = 0.6 + i * 0.1;\n        float sway = sin(p.y * 2.0 + i) * 0.05;\n        float stalk = abs(p.x - cx - sway);\n        \n        if (stalk < 0.025) {\n            vec3 stalkCol = mix(vec3(0.5, 0.7, 0.2), vec3(0.7, 0.8, 0.3), stalk * 40.0);\n            float seg = fract(p.y * 6.0 + i * 0.4);\n            if (seg > 0.9) stalkCol = vec3(0.3, 0.4, 0.1); \n            \n            col = stalkCol;\n        }\n    }\n}\n\nvoid layer_SugarcaneLeaves(in vec2 p, inout vec3 col) {\n    vec2 lp = p - vec2(-0.8, -0.6);\n    float leaf = max(lp.y - lp.x * lp.x * 2.0, -lp.y - 0.05 + lp.x * lp.x * 1.5);\n    if (leaf < 0.0 && lp.x > 0.0 && lp.x < 1.0) {\n        col = vec3(0.2, 0.8, 0.2); \n        if (abs(lp.y - lp.x * lp.x * 1.7) < 0.005) col = vec3(0.1, 0.5, 0.1);\n    }\n}\n\nvec4 layer_Signboard(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n    \n    vec3 col = vec3(-1.0);\n    bool insideSign;\n    \n    layer_Signboard(p, col, insideSign);\n    \n    if (insideSign) {\n    }\n    \n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_TextGIAI(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n    \n    vec3 col = vec3(-1.0);\n    bool insideSign;\n    \n    \n    if (insideSign) {\n        layer_TextGiai(p, col);\n    }\n    \n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_TextNUOCMIA(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n    \n    vec3 col = vec3(-1.0);\n    bool insideSign;\n    \n    \n    if (insideSign) {\n        layer_TextNuocMia(p, col);\n    }\n    \n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_SugarcaneStalks(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n    \n    vec3 col = vec3(-1.0);\n    bool insideSign;\n    \n    \n    if (insideSign) {\n        layer_SugarcaneStalks(p, col);\n    }\n    \n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_SugarcaneLeaves(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n    \n    vec3 col = vec3(-1.0);\n    bool insideSign;\n    \n    \n    if (insideSign) {\n        layer_SugarcaneLeaves(p, col);\n    }\n    \n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n","IMG_1241":"void layer_Ground(in vec2 p, inout vec3 col) {\n    col = vec3(0.55, 0.5, 0.45); \n    float dirt = fract(sin(dot(p, vec2(12.9898, 78.233))) * 43758.5453);\n    col *= 0.8 + 0.2 * dirt;\n}\n\nvoid layer_Chair(in vec2 p, inout vec3 col) {\n    float seat = length(max(abs(p - vec2(0.0, -0.1)) - vec2(0.4, 0.2), 0.0)) - 0.05;\n    float backrest = length(max(abs(p - vec2(0.0, 0.4)) - vec2(0.35, 0.3), 0.0)) - 0.05;\n    float legs = length(max(abs(vec2(abs(p.x) - 0.35, p.y + 0.6)) - vec2(0.05, 0.3), 0.0)) - 0.02;\n\n    if (seat < 0.0) {\n        col = vec3(0.7, 0.7, 0.7); \n        col *= 0.9 + 0.1 * fract(sin(p.x * 50.0) * 43758.5);\n    }\n    \n    if (backrest < 0.0) {\n        col = vec3(0.7, 0.7, 0.7);\n        if (abs(fract(p.y * 5.0) - 0.5) > 0.4 && abs(p.x) < 0.25) col = vec3(0.55, 0.5, 0.45);\n    }\n\n    if (legs < 0.0) {\n        col = vec3(0.65, 0.65, 0.65);\n    }\n    \n    if (seat < 0.0 || backrest < 0.0) {\n         float wear = smoothstep(0.4, 0.6, fract(sin(p.x * 12.0 + p.y * 34.0) * 43758.5));\n         col *= 1.0 - 0.2 * wear;\n    }\n}\n\nvoid layer_LicensePlate(in vec2 p, in float iTime, inout vec3 col) {\n    float a = -0.2;\n    vec2 rp = vec2(p.x * cos(a) - p.y * sin(a), p.x * sin(a) + p.y * cos(a));\n    \n    float plate = length(max(abs(rp - vec2(-0.2, 0.1)) - vec2(0.3, 0.15), 0.0)) - 0.02;\n    if (plate < 0.0) {\n        col = vec3(0.1, 0.2, 0.7); \n        \n        float border = length(max(abs(rp - vec2(-0.2, 0.1)) - vec2(0.28, 0.13), 0.0)) - 0.01;\n        if (border > 0.0) col = vec3(0.9);\n        \n        float textLine1 = length(max(abs(rp - vec2(-0.2, 0.15)) - vec2(0.2, 0.03), 0.0));\n        float textLine2 = length(max(abs(rp - vec2(-0.2, 0.05)) - vec2(0.2, 0.03), 0.0));\n        \n        if (textLine1 < 0.01 || textLine2 < 0.01) {\n            col = vec3(0.9); \n            \n            float shimmer = sin(rp.x * 20.0 - iTime * 3.0) * 0.5 + 0.5;\n            col += vec3(0.2) * shimmer;\n        }\n        \n        float string1 = length(max(abs(p - vec2(-0.4, 0.2)) - vec2(0.01, 0.1), 0.0));\n        if(string1 < 0.005) col = vec3(0.2, 0.5, 0.8);\n    }\n}\n\nvoid layer_Vignette(in vec2 p, inout vec3 col) {\n    col *= 1.0 - 0.3 * length(p);\n}\n\nvec4 layer_Ground(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 col = vec3(-1.0);\n    \n    layer_Ground(p, col);\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_Chair(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 col = vec3(-1.0);\n    \n    layer_Chair(p, col);\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_LicensePlate(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 col = vec3(-1.0);\n    \n    layer_LicensePlate(p, iTime, col);\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_Vignette(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 col = vec3(-1.0);\n    \n    layer_Vignette(p, col);\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n","IMG_1276":"#define segment(p, a, b) length(p - a - (b - a) * clamp(dot(p - a, b - a) / dot(b - a, b - a), 0.0, 1.0))\n\nvoid layer_Walls(inout vec3 col) {\n    col = vec3(0.8, 0.8, 0.8); \n}\n\nvoid layer_TerracottaRoof(in vec2 p, inout vec3 col) {\n    if (p.y > 0.4 && p.y < 0.7) {\n        vec2 tileUV = p * vec2(15.0, 10.0);\n        float tX = fract(tileUV.x) - 0.5;\n        float tY = fract(tileUV.y);\n        \n        col = mix(vec3(0.7, 0.3, 0.2), vec3(0.5, 0.2, 0.1), tY); \n        col += 0.2 * smoothstep(0.3, 0.0, abs(tX));\n        if (abs(tX) > 0.45) col = vec3(0.3, 0.1, 0.05);\n    }\n}\n\nvoid layer_CourtyardTarp(in vec2 p, in float iTime, inout vec3 col) {\n    float tarpFlap = sin(p.x * 10.0 + iTime * 8.0) * 0.03;\n    float tarp = length(max(abs(p - vec2(0.0, -0.5 + tarpFlap)) - vec2(0.8, 0.3), 0.0));\n    if (tarp < 0.01) {\n        col = vec3(0.65, 0.65, 0.65);\n        float fold = sin(p.x * 15.0 + iTime * 5.0) * 0.1;\n        col += vec3(fold);\n    }\n}\n\nvoid layer_Railings(in vec2 p, inout vec3 col) {\n    float railV = abs(fract(p.x * 20.0) - 0.5);\n    float railH = abs(p.y - (-0.2));\n    if ((railV < 0.1 && p.y > -0.8 && p.y < -0.2) || railH < 0.02) {\n        col = vec3(0.4, 0.3, 0.25); \n        col += 0.2 * smoothstep(0.1, 0.0, railV);\n    }\n}\n\nvoid layer_Tree(in vec2 p, in float iTime, inout vec3 col) {\n    vec2 trunkP = p - vec2(0.2, -0.2);\n    float tree = segment(trunkP, vec2(0.0, -0.4), vec2(0.0, 0.4)) - 0.04;\n    tree = min(tree, segment(trunkP, vec2(0.0, 0.1), vec2(-0.2, 0.3)) - 0.02);\n    tree = min(tree, segment(trunkP, vec2(0.0, 0.2), vec2(0.25, 0.4)) - 0.02);\n    \n    if (tree < 0.0) {\n        col = vec3(0.3, 0.2, 0.1); \n        float bark = sin(trunkP.x * 100.0) * sin(trunkP.y * 50.0);\n        col *= 0.8 + 0.2 * bark;\n    }\n    \n    for(int i=0; i<30; i++) {\n        float fi = float(i);\n        vec2 cp = vec2(0.2 + sin(fi*2.1)*0.4, 0.2 + cos(fi*1.7)*0.3);\n        cp.x += sin(iTime * 5.0 + fi) * 0.05;\n        cp.y += cos(iTime * 4.0 + fi) * 0.03;\n        \n        float leaf = length(p - cp) - (0.05 + fract(fi*0.3)*0.05);\n        if (leaf < 0.0) {\n            vec3 leafCol = mix(vec3(0.4, 0.8, 0.2), vec3(0.2, 0.6, 0.1), fract(fi*0.7));\n            col = mix(col, leafCol, 0.95);\n            if (fract(p.x * 50.0 + p.y * 30.0) < 0.1) col *= 0.8; \n        }\n    }\n}\n\nvoid layer_FallingLeaves(in vec2 p, in float iTime, inout vec3 col) {\n    for(float i=0.0; i<15.0; i++) {\n        float startX = -1.0 + i * 0.2 + sin(i * 10.0) * 0.1;\n        float fallY = 1.0 - fract(iTime * 0.2 + i * 0.1) * 2.5;\n        float swayX = startX + sin(fallY * 10.0 + i) * 0.1;\n        \n        vec2 fallP = p - vec2(swayX, fallY);\n        float spinAngle = iTime * 5.0 + i;\n        mat2 sRot = mat2(cos(spinAngle), -sin(spinAngle), sin(spinAngle), cos(spinAngle));\n        vec2 rFallP = sRot * fallP;\n        \n        float fallingLeaf = length(max(abs(rFallP) - vec2(0.015, 0.03), 0.0)) - 0.01;\n        if (fallingLeaf < 0.0 && fallY > -1.0) {\n            col = vec3(0.5, 0.7, 0.2); \n            if (abs(rFallP.x) < 0.005) col = vec3(0.2, 0.4, 0.1); \n        }\n    }\n}\n\nvoid layer_Shadows(in vec2 p, inout vec3 col) {\n    col *= 1.0 - 0.3 * exp(-10.0 * abs(p.y - (-0.2))); \n    col *= 1.0 - 0.4 * exp(-5.0 * length(p - vec2(0.2, -0.2))); \n}\n\nvec4 layer_Walls(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 col = vec3(-1.0);\n    \n    layer_Walls(col);\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_TerracottaRoof(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 col = vec3(-1.0);\n    \n    layer_TerracottaRoof(p, col);\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_CourtyardTarp(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 col = vec3(-1.0);\n    \n    layer_CourtyardTarp(p, iTime, col);\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_Railings(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 col = vec3(-1.0);\n    \n    layer_Railings(p, col);\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_Tree(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 col = vec3(-1.0);\n    \n    layer_Tree(p, iTime, col);\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_FallingLeaves(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 col = vec3(-1.0);\n    \n    layer_FallingLeaves(p, iTime, col);\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_Shadows(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 col = vec3(-1.0);\n    \n    layer_Shadows(p, col);\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n","IMG_1300":"void layer_Background(inout vec3 col) {\n    col = vec3(0.9, 0.9, 0.9); \n}\n\nvoid layer_VestBody(in vec2 p, in float vestBase, in float neck, in float iTime, inout vec3 col) {\n    if (vestBase < 0.0 && neck > 0.0) {\n        col = vec3(0.1, 0.1, 0.12); \n        \n        float fabric = fract(sin(p.x * 200.0) * cos(p.y * 200.0) * 10.0);\n        col *= 0.9 + 0.1 * fabric;\n        \n        float molle = fract(p.y * 15.0);\n        if (molle < 0.2 && p.y < 0.3 && p.y > -0.5 && abs(p.x) < 0.35) {\n            col = vec3(0.05); \n            float shine = sin(p.x * 10.0 + iTime * 2.0) * 0.5 + 0.5;\n            col += vec3(0.05) * shine;\n        }\n        \n        float padL = length(max(abs(p - vec2(-0.35, 0.5)) - vec2(0.1, 0.05), 0.0)) - 0.02;\n        float padR = length(max(abs(p - vec2(0.35, 0.5)) - vec2(0.1, 0.05), 0.0)) - 0.02;\n        if (padL < 0.0 || padR < 0.0) col = vec3(0.15);\n        \n        float lowerFlap = length(max(abs(p - vec2(0.0, -0.8)) - vec2(0.2, 0.1), 0.0)) - 0.05;\n        if (lowerFlap < 0.0 && p.y < -0.7) col = vec3(0.1);\n    }\n}\n\nvoid layer_Pouches(in vec2 p, in float vestBase, in float neck, inout vec3 col) {\n    if (vestBase < 0.0 && neck > 0.0) {\n        float pouch1 = length(max(abs(p - vec2(-0.25, -0.2)) - vec2(0.08, 0.15), 0.0)) - 0.02;\n        if (pouch1 < 0.0) {\n            col = vec3(0.12);\n            if (p.y > -0.1) col = vec3(0.08);\n        }\n        float pouch2 = length(max(abs(p - vec2(-0.1, -0.2)) - vec2(0.08, 0.15), 0.0)) - 0.02;\n        if (pouch2 < 0.0) col = vec3(0.12);\n        \n        float pouch3 = length(max(abs(p - vec2(0.25, -0.6)) - vec2(0.15, 0.15), 0.0)) - 0.03;\n        if (pouch3 < 0.0) {\n            col = vec3(0.1);\n            if (abs(p.x - 0.25) < 0.01) col = vec3(0.05);\n        }\n        \n        float pouch4 = length(max(abs(p - vec2(0.3, 0.1)) - vec2(0.08, 0.06), 0.0)) - 0.02;\n        if (pouch4 < 0.0) col = vec3(0.13);\n    }\n}\n\nvoid layer_ArmAttachments(in vec2 p, inout vec3 col) {\n    float armL = length(max(abs(p - vec2(-0.6, 0.2)) - vec2(0.1, 0.2), 0.0)) - 0.05;\n    if (armL < 0.0) {\n        col = vec3(0.1);\n        if (abs(p.y - 0.2) < 0.02) col = vec3(0.05); \n    }\n    \n    float armR = length(max(abs(p - vec2(0.6, 0.2)) - vec2(0.1, 0.2), 0.0)) - 0.05;\n    if (armR < 0.0) {\n        col = vec3(0.1);\n        if (abs(p.y - 0.2) < 0.02) col = vec3(0.05); \n    }\n}\n\nvoid layer_Collar(in vec2 p, inout vec3 col) {\n    float collar = length(max(abs(p - vec2(0.0, 0.65)) - vec2(0.2, 0.15), 0.0)) - 0.05;\n    if (collar < 0.0 - p.y * 0.1) {\n        col = vec3(0.08);\n        float fold = sin(p.x * 20.0) * 0.02;\n        col += vec3(fold);\n    }\n}\n\nvoid layer_Shadows(in vec2 p, inout vec3 col) {\n    col *= 1.0 - 0.3 * length(p - vec2(0.0, -0.2));\n}\n\nvec4 layer_Background(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 col = vec3(-1.0);\n    \n    layer_Background(col);\n    \n    float vestBase = length(max(abs(p - vec2(0.0, -0.1)) - vec2(0.4, 0.6), 0.0)) - 0.1;\n    float neck = length(p - vec2(0.0, 0.6)) - 0.25;\n    \n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_VestBody(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 col = vec3(-1.0);\n    \n    \n    float vestBase = length(max(abs(p - vec2(0.0, -0.1)) - vec2(0.4, 0.6), 0.0)) - 0.1;\n    float neck = length(p - vec2(0.0, 0.6)) - 0.25;\n    \n    layer_VestBody(p, vestBase, neck, iTime, col);\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_Pouches(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 col = vec3(-1.0);\n    \n    \n    float vestBase = length(max(abs(p - vec2(0.0, -0.1)) - vec2(0.4, 0.6), 0.0)) - 0.1;\n    float neck = length(p - vec2(0.0, 0.6)) - 0.25;\n    \n    layer_Pouches(p, vestBase, neck, col);\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_ArmAttachments(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 col = vec3(-1.0);\n    \n    \n    float vestBase = length(max(abs(p - vec2(0.0, -0.1)) - vec2(0.4, 0.6), 0.0)) - 0.1;\n    float neck = length(p - vec2(0.0, 0.6)) - 0.25;\n    \n    layer_ArmAttachments(p, col);\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_Collar(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 col = vec3(-1.0);\n    \n    \n    float vestBase = length(max(abs(p - vec2(0.0, -0.1)) - vec2(0.4, 0.6), 0.0)) - 0.1;\n    float neck = length(p - vec2(0.0, 0.6)) - 0.25;\n    \n    layer_Collar(p, col);\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_Shadows(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 col = vec3(-1.0);\n    \n    \n    float vestBase = length(max(abs(p - vec2(0.0, -0.1)) - vec2(0.4, 0.6), 0.0)) - 0.1;\n    float neck = length(p - vec2(0.0, 0.6)) - 0.25;\n    \n    layer_Shadows(p, col);\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n","IMG_1301":"void layer_Sky(inout vec3 col) {\n    col = vec3(0.4, 0.9, 0.95); \n}\n\nvoid layer_Flag(in vec2 p, in float iTime, inout vec3 col) {\n    float spikes = 0.0;\n    if (p.x > 0.4) {\n        float f = p.y * 10.0;\n        spikes = sin(f) * 0.15 * smoothstep(0.4, 0.7, p.x);\n        spikes += sin(f * 2.0 + iTime * 5.0) * 0.05 * smoothstep(0.4, 0.7, p.x);\n    }\n    \n    float flagFull = length(max(abs(p - vec2(spikes, 0.0)) - vec2(0.6, 0.5), 0.0)) - 0.05;\n\n    if (flagFull < 0.0) {\n        col = vec3(0.5, 0.2, 0.8);\n        \n        float wave = sin(p.x * 5.0 + p.y * 2.0 - iTime * 3.0) * 0.1;\n        float fold = abs(sin(p.x * 10.0 + iTime)) * 0.2;\n        col *= 0.8 + wave + fold;\n        \n        float border1 = length(max(abs(p) - vec2(0.5, 0.4), 0.0)) - 0.02;\n        float border2 = length(max(abs(p) - vec2(0.45, 0.35), 0.0)) - 0.02;\n        if (border1 < 0.0 && border2 > 0.0) {\n            col = vec3(0.95);\n            col *= 0.9 + 0.1 * wave; \n        }\n        \n        float border3 = length(max(abs(p) - vec2(0.35, 0.25), 0.0)) - 0.02;\n        float border4 = length(max(abs(p) - vec2(0.3, 0.2), 0.0)) - 0.02;\n        if (border3 < 0.0 && border4 > 0.0) {\n            col = vec3(0.95);\n            col *= 0.9 + 0.1 * wave;\n        }\n        \n        float crossV = length(max(abs(p - vec2(0.0, 0.0)) - vec2(0.02, 0.15), 0.0)) - 0.01;\n        float crossH = length(max(abs(p - vec2(0.0, 0.0)) - vec2(0.1, 0.02), 0.0)) - 0.01;\n        \n        float cEnd1 = length(p - vec2(0.0, 0.17)) - 0.03; \n        float cEnd2 = length(p - vec2(0.0, -0.17)) - 0.03; \n        float cEnd3 = length(p - vec2(0.12, 0.0)) - 0.03; \n        float cEnd4 = length(p - vec2(-0.12, 0.0)) - 0.03; \n        \n        float fullCross = min(min(crossV, crossH), min(min(cEnd1, cEnd2), min(cEnd3, cEnd4)));\n        \n        if (fullCross < 0.0) {\n            col = vec3(0.95);\n            if (p.x - p.y > 0.0) col = vec3(0.85);\n            col *= 0.9 + 0.1 * wave;\n        }\n        \n        float sleeve = length(max(abs(p - vec2(0.0, 0.55)) - vec2(0.4, 0.05), 0.0)) - 0.02;\n        if(sleeve < 0.0) col = vec3(0.45, 0.15, 0.75);\n    }\n}\n\nvec4 layer_Sky(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 col = vec3(-1.0);\n    \n    layer_Sky(col);\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_Flag(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 col = vec3(-1.0);\n    \n    layer_Flag(p, iTime, col);\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n","IMG_1306":"void layer_BackgroundRoad(in vec2 p, in float iTime, inout vec3 col) {\n    col = vec3(0.5, 0.5, 0.52); \n    \n    float roadNoise = fract(sin(dot(p, vec2(12.9898, 78.233))) * 43758.5453);\n    col *= 0.9 + 0.1 * roadNoise;\n    \n    if (p.y > 0.2) {\n        float bldg = sin(p.x * 10.0) * 0.1 + sin(p.x * 3.0) * 0.2;\n        if (p.y < 0.5 + bldg) col = mix(col, vec3(0.3, 0.3, 0.35), 0.5); \n        \n        float trees = sin(p.x * 20.0) * 0.05 + sin(p.x * 5.0) * 0.1;\n        if (p.y < 0.4 + trees) col = mix(col, vec3(0.2, 0.3, 0.2), 0.6); \n        \n        if (abs(p.y - 0.25) < 0.05 && fract(p.x * 5.0 + iTime) < 0.1) col += vec3(0.8, 0.1, 0.1);\n        if (abs(p.y - 0.25) < 0.05 && fract(p.x * 5.0 - iTime + 0.5) < 0.1) col += vec3(0.9, 0.9, 0.7);\n    }\n}\n\nvoid layer_MetalFrame(in vec2 p, inout vec3 col) {\n    float barH1 = length(max(abs(p - vec2(0.0, 0.3)) - vec2(0.6, 0.02), 0.0)) - 0.01;\n    float barH2 = length(max(abs(p - vec2(0.0, -0.4)) - vec2(0.6, 0.02), 0.0)) - 0.01;\n    float barV1 = length(max(abs(p - vec2(-0.6, -0.05)) - vec2(0.02, 0.35), 0.0)) - 0.01;\n    float barV2 = length(max(abs(p - vec2(0.6, -0.05)) - vec2(0.02, 0.35), 0.0)) - 0.01;\n    float barV3 = length(max(abs(p - vec2(-0.4, -0.05)) - vec2(0.02, 0.35), 0.0)) - 0.01;\n    float barV4 = length(max(abs(p - vec2(0.4, -0.05)) - vec2(0.02, 0.35), 0.0)) - 0.01;\n    \n    float foot1 = length(max(abs(p - vec2(-0.4, -0.5)) - vec2(0.05, 0.1), 0.0)) - 0.01;\n    float foot2 = length(max(abs(p - vec2(0.4, -0.5)) - vec2(0.05, 0.1), 0.0)) - 0.01;\n    \n    float frame = min(min(barH1, barH2), min(min(barV1, barV2), min(barV3, barV4)));\n    frame = min(frame, min(foot1, foot2));\n\n    if (frame < 0.0) {\n        col = vec3(0.6); \n        float rust = fract(sin(p.x * 50.0 + p.y * 50.0) * 123.45);\n        if (rust > 0.7) col = vec3(0.4, 0.3, 0.2);\n    }\n}\n\nvoid layer_BlueCloth(in vec2 p, in float iTime, inout vec3 col) {\n    float clothBase = length(max(abs(p - vec2(0.0, 0.0)) - vec2(0.5, 0.45), 0.0)) - 0.02;\n    float edgeWave = sin(p.y * 20.0) * 0.01;\n    \n    if (clothBase + edgeWave < 0.0) {\n        col = vec3(0.1, 0.4, 0.8); \n        \n        float windX = sin(p.x * 5.0 + p.y * 2.0 + iTime * 2.0) * 0.1;\n        float windY = cos(p.x * 3.0 + p.y * 4.0 - iTime * 1.5) * 0.1;\n        \n        float seams = abs(sin(p.x * 10.0 + windX));\n        col *= 0.8 + 0.2 * smoothstep(0.0, 0.2, seams);\n        \n        col += vec3(0.1) * windX;\n        col -= vec3(0.1) * windY;\n        \n        float knot = length(max(abs(p - vec2(0.0, 0.2)) - vec2(0.05, 0.15), 0.0)) - 0.03;\n        if (knot < 0.0) {\n            col = vec3(0.05, 0.3, 0.6); \n            float kFolds = sin((p.x + p.y) * 40.0);\n            col *= 0.8 + 0.2 * kFolds;\n        }\n    }\n}\n\nvoid layer_PlasticBags(in vec2 p, in float iTime, inout vec3 col) {\n    float bagL = length(max(abs(p - vec2(-0.65, 0.2)) - vec2(0.08, 0.12), 0.0)) - 0.03;\n    float bagLWind = sin(p.y * 10.0 + iTime * 4.0) * 0.02;\n    if (bagL + bagLWind < 0.0) {\n        col = vec3(0.85, 0.85, 0.85); \n        col *= 0.9 + 0.1 * fract(sin(p.x * 100.0) * 43758.5);\n    }\n    \n    float bagR = length(max(abs(p - vec2(0.65, 0.2)) - vec2(0.05, 0.1), 0.0)) - 0.02;\n    float bagRWind = cos(p.y * 15.0 + iTime * 5.0) * 0.02;\n    if (bagR + bagRWind < 0.0) {\n        col = vec3(0.85, 0.85, 0.85);\n        col *= 0.9 + 0.1 * fract(sin(p.x * 100.0 + 10.0) * 43758.5);\n    }\n}\n\nvoid layer_Shadows(in vec2 p, inout vec3 col) {\n    float shadow = exp(-10.0 * length(max(abs(p - vec2(0.0, -0.6)) - vec2(0.6, 0.1), 0.0)));\n    col *= 1.0 - 0.4 * shadow;\n}\n\nvec4 layer_BackgroundRoad(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 col = vec3(-1.0);\n    \n    layer_BackgroundRoad(p, iTime, col);\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_MetalFrame(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 col = vec3(-1.0);\n    \n    layer_MetalFrame(p, col);\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_BlueCloth(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 col = vec3(-1.0);\n    \n    layer_BlueCloth(p, iTime, col);\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_PlasticBags(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 col = vec3(-1.0);\n    \n    layer_PlasticBags(p, iTime, col);\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_Shadows(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 col = vec3(-1.0);\n    \n    layer_Shadows(p, col);\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n","IMG_1308":"#define segment(p, a, b) length(p - a - (b - a) * clamp(dot(p - a, b - a) / dot(b - a, b - a), 0.0, 1.0))\n\nvoid layer_Background(in vec2 p, inout vec3 col) {\n    col = vec3(0.98, 0.98, 0.98); \n    float paper = fract(sin(dot(p, vec2(12.9898, 78.233))) * 43758.5453);\n    col *= 0.98 + 0.02 * paper;\n}\n\nvoid layer_Head(in vec2 p, in float lineThickness, in vec3 ink, inout vec3 col) {\n    float head = abs(length(p - vec2(0.0, 0.7)) - 0.15);\n    if (head < lineThickness) col = ink;\n    \n    if (length(p - vec2(0.0, 0.7)) < 0.14) {\n        float hair = sin(p.x * 100.0 + p.y * 100.0);\n        if (hair > 0.8 && p.y > 0.7) col = ink;\n    }\n}\n\nvoid layer_KiteGarment(in vec2 p, in float lineThickness, in vec3 ink, inout vec3 col) {\n    float lines = 1.0;\n    lines = min(lines, segment(p, vec2(0.0, 0.55), vec2(0.0, -0.1))); \n    lines = min(lines, segment(p, vec2(0.0, 0.4), vec2(-0.8, 0.2))); \n    lines = min(lines, segment(p, vec2(0.0, 0.4), vec2(0.8, 0.2))); \n    lines = min(lines, segment(p, vec2(0.0, -0.1), vec2(-0.8, 0.2))); \n    lines = min(lines, segment(p, vec2(0.0, -0.1), vec2(0.8, 0.2))); \n    \n    float arc = abs(length(p - vec2(0.0, -0.4)) - 0.7);\n    if (arc < lineThickness && p.y > 0.1 && abs(p.x) < 0.6) col = ink;\n\n    lines = min(lines, segment(p, vec2(-0.8, 0.2), vec2(-0.8, 0.0)));\n    lines = min(lines, segment(p, vec2(0.8, 0.2), vec2(0.8, 0.0)));\n\n    lines = min(lines, segment(p, vec2(-0.2, -0.1), vec2(-0.3, -0.9)));\n    lines = min(lines, segment(p, vec2(0.2, -0.1), vec2(0.3, -0.9)));\n    lines = min(lines, segment(p, vec2(-0.05, -0.3), vec2(-0.1, -0.9)));\n    lines = min(lines, segment(p, vec2(0.05, -0.3), vec2(0.1, -0.9)));\n    lines = min(lines, segment(p, vec2(-0.2, -0.1), vec2(0.2, -0.1))); \n\n    if (lines < lineThickness) col = ink;\n}\n\nvoid layer_Shading(in vec2 p, in float iTime, in vec3 ink, inout vec3 col) {\n    if (p.y > 0.0 && p.y < 0.3 && abs(p.x) > 0.4 && abs(p.x) < 0.8) {\n        float hatch = sin((p.x + p.y) * 150.0);\n        float drawAnim = smoothstep(0.0, 1.0, sin(iTime * 2.0));\n        if (hatch > 0.9 && drawAnim > 0.5) col = mix(col, ink, 0.4);\n    }\n}\n\nvoid layer_TextForbes(in vec2 p, in vec3 ink, inout vec3 col) {\n    vec2 tp = p - vec2(-0.3, 0.1);\n    float text = 1.0;\n    \n    text = min(text, segment(tp, vec2(0.0, 0.0), vec2(0.0, 0.1)));\n    text = min(text, segment(tp, vec2(0.0, 0.1), vec2(0.05, 0.1)));\n    text = min(text, segment(tp, vec2(0.0, 0.05), vec2(0.04, 0.05)));\n    \n    text = min(text, abs(length(tp - vec2(0.1, 0.03)) - 0.03));\n    \n    text = min(text, segment(tp, vec2(0.16, 0.0), vec2(0.16, 0.06)));\n    text = min(text, segment(tp, vec2(0.16, 0.05), vec2(0.2, 0.06)));\n    \n    text = min(text, segment(tp, vec2(0.24, -0.02), vec2(0.24, 0.1)));\n    text = min(text, abs(length(tp - vec2(0.27, 0.02)) - 0.03));\n    \n    text = min(text, abs(length(tp - vec2(0.35, 0.03)) - 0.03));\n    text = min(text, segment(tp, vec2(0.32, 0.03), vec2(0.38, 0.03)));\n    \n    text = min(text, segment(tp, vec2(0.44, 0.06), vec2(0.4, 0.06)));\n    text = min(text, segment(tp, vec2(0.4, 0.06), vec2(0.4, 0.03)));\n    text = min(text, segment(tp, vec2(0.4, 0.03), vec2(0.44, 0.03)));\n    text = min(text, segment(tp, vec2(0.44, 0.03), vec2(0.44, 0.0)));\n    text = min(text, segment(tp, vec2(0.44, 0.0), vec2(0.4, 0.0)));\n\n    if (text < 0.005) col = ink;\n}\n\nvec4 layer_Background(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 col = vec3(-1.0);\n    vec3 ink = vec3(0.1, 0.1, 0.1); \n    float lineThickness = 0.008; \n    \n    layer_Background(p, col);\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_Head(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 col = vec3(-1.0);\n    vec3 ink = vec3(0.1, 0.1, 0.1); \n    float lineThickness = 0.008; \n    \n    layer_Head(p, lineThickness, ink, col);\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_KiteGarment(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 col = vec3(-1.0);\n    vec3 ink = vec3(0.1, 0.1, 0.1); \n    float lineThickness = 0.008; \n    \n    layer_KiteGarment(p, lineThickness, ink, col);\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_Shading(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 col = vec3(-1.0);\n    vec3 ink = vec3(0.1, 0.1, 0.1); \n    float lineThickness = 0.008; \n    \n    layer_Shading(p, iTime, ink, col);\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_TextForbes(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 col = vec3(-1.0);\n    vec3 ink = vec3(0.1, 0.1, 0.1); \n    float lineThickness = 0.008; \n    \n    layer_TextForbes(p, ink, col);\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n","IMG_1309":"void layer_Background(inout vec3 col) {\n    col = vec3(1.0); \n}\n\nvoid layer_Kite(in vec2 p, in vec2 rp, in float diamond, in float iTime, inout vec3 col) {\n    if (diamond < 0.01) {\n        col = vec3(0.3, 0.7, 0.5); \n        \n        if (p.x < 0.0 && p.y > 0.2) col = vec3(0.9, 0.5, 0.1);\n        if (p.x > 0.0 && p.y > 0.2) col = vec3(0.95, 0.85, 0.1);\n        \n        if (p.y > abs(p.x) * 0.8 + 0.2 && p.y > 0.4) {\n             col = vec3(0.9, 0.8, 0.1); \n             if(p.y > 0.6) col = vec3(0.9, 0.5, 0.1); \n        }\n        \n        float paper1 = sin(rp.x * 50.0 + rp.y * 30.0) * 0.02;\n        float paper2 = cos(rp.x * 20.0 - rp.y * 40.0) * 0.03;\n        float crinkle = abs(paper1 * paper2) * 50.0;\n        \n        float wind = sin(p.x * 10.0 + p.y * 5.0 + iTime * 4.0) * 0.05;\n        col *= 0.9 + crinkle + wind;\n        \n        float stickV = abs(p.x);\n        if (stickV < 0.01 && p.y > -0.6 && p.y < 0.8) {\n            col = vec3(0.8, 0.7, 0.5); \n            col *= 0.8 + 0.2 * sin(p.y * 100.0); \n        }\n        \n        float arcY = -0.8 * (p.x * p.x) + 0.6;\n        float stickH = abs(p.y - arcY);\n        if (stickH < 0.01 && abs(p.x) < 0.6) {\n            col = vec3(0.8, 0.7, 0.5);\n            col *= 0.8 + 0.2 * sin(p.x * 100.0);\n        }\n    }\n}\n\nvoid layer_KiteTail(in vec2 p, in float iTime, inout vec3 col) {\n    float tailV1 = abs(p.x) * 1.5 - 0.2; \n    float tailY = p.y + 0.8; \n    float tailTop = p.y + 0.6; \n    \n    if (tailV1 < tailY && tailY > 0.0 && tailTop < 0.0) {\n        col = vec3(0.35, 0.25, 0.25); \n        \n        float tailFolds = sin(p.x * 40.0) * 0.1;\n        float flap = sin(p.x * 20.0 + iTime * 10.0) * 0.1;\n        col *= 0.8 + tailFolds + flap;\n    }\n}\n\nvoid layer_Shadows(in vec2 p, in vec2 rp, in float diamond, inout vec3 col) {\n    float shadow = length(max(abs(rp - vec2(-0.02, 0.18)) - vec2(0.6, 0.6), 0.0));\n    if (shadow < 0.05 && diamond > 0.01) {\n        col = mix(col, vec3(0.8), 0.5 * (1.0 - shadow / 0.05));\n    }\n}\n\nvec4 layer_Background(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 col = vec3(-1.0);\n    \n    layer_Background(col);\n    \n    float a = 3.14159 / 4.0;\n    vec2 rp = vec2(p.x * cos(a) - p.y * sin(a), p.x * sin(a) + p.y * cos(a));\n    float diamond = length(max(abs(rp - vec2(0.0, 0.2)) - vec2(0.6, 0.6), 0.0));\n    \n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_Kite(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 col = vec3(-1.0);\n    \n    \n    float a = 3.14159 / 4.0;\n    vec2 rp = vec2(p.x * cos(a) - p.y * sin(a), p.x * sin(a) + p.y * cos(a));\n    float diamond = length(max(abs(rp - vec2(0.0, 0.2)) - vec2(0.6, 0.6), 0.0));\n    \n    layer_Kite(p, rp, diamond, iTime, col);\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_KiteTail(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 col = vec3(-1.0);\n    \n    \n    float a = 3.14159 / 4.0;\n    vec2 rp = vec2(p.x * cos(a) - p.y * sin(a), p.x * sin(a) + p.y * cos(a));\n    float diamond = length(max(abs(rp - vec2(0.0, 0.2)) - vec2(0.6, 0.6), 0.0));\n    \n    layer_KiteTail(p, iTime, col);\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_Shadows(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 col = vec3(-1.0);\n    \n    \n    float a = 3.14159 / 4.0;\n    vec2 rp = vec2(p.x * cos(a) - p.y * sin(a), p.x * sin(a) + p.y * cos(a));\n    float diamond = length(max(abs(rp - vec2(0.0, 0.2)) - vec2(0.6, 0.6), 0.0));\n    \n    layer_Shadows(p, rp, diamond, col);\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n","IMG_1323":"#define segment(p, a, b) length(p - a - (b - a) * clamp(dot(p - a, b - a) / dot(b - a, b - a), 0.0, 1.0))\n\nvoid layer_SignBox(in vec2 tp, in float signBoxTilted, inout vec3 col) {\n    if (signBoxTilted < 0.0) {\n        col = mix(vec3(0.9, 0.8, 0.1), vec3(1.0, 0.9, 0.3), tp.y + 0.4); \n        if (abs(signBoxTilted) < 0.01) col = vec3(0.8, 0.2, 0.2);\n    }\n}\n\nvoid layer_TextALoc(in vec2 tp, in float signBoxTilted, inout vec3 col) {\n    if (signBoxTilted < 0.0) {\n        vec2 rText = tp - vec2(-0.4, 0.2);\n        float dR = 1.0;\n        \n        dR = min(dR, segment(rText, vec2(-0.1, -0.08), vec2(-0.05, 0.08)));\n        dR = min(dR, segment(rText, vec2(-0.05, 0.08), vec2(0.0, -0.08)));\n        dR = min(dR, segment(rText, vec2(-0.07, 0.0), vec2(-0.03, 0.0)));\n        \n        dR = min(dR, segment(rText, vec2(0.1, 0.08), vec2(0.1, -0.08)));\n        dR = min(dR, segment(rText, vec2(0.1, -0.08), vec2(0.18, -0.08)));\n        \n        dR = min(dR, abs(length(rText - vec2(0.3, 0.0)) - 0.06));\n        \n        float cDist = length(rText - vec2(0.5, 0.0));\n        if (cDist < 0.06 && rText.x < 0.52) dR = min(dR, abs(cDist - 0.06));\n        \n        if (dR < 0.015) {\n            col = vec3(0.9, 0.1, 0.1);\n            if (dR < 0.005) col = vec3(1.0, 0.5, 0.5);\n        }\n    }\n}\n\nvoid layer_TextChaoLong(in vec2 tp, in float signBoxTilted, inout vec3 col) {\n    if (signBoxTilted < 0.0) {\n        vec2 bText = tp - vec2(-0.4, -0.1);\n        float dB = 1.0;\n        \n        float c2Dist = length(bText - vec2(-0.1, 0.0));\n        if (c2Dist < 0.06 && bText.x < -0.08) dB = min(dB, abs(c2Dist - 0.06));\n        \n        dB = min(dB, segment(bText, vec2(0.05, 0.08), vec2(0.05, -0.08)));\n        dB = min(dB, segment(bText, vec2(0.15, 0.08), vec2(0.15, -0.08)));\n        dB = min(dB, segment(bText, vec2(0.05, 0.0), vec2(0.15, 0.0)));\n        \n        dB = min(dB, segment(bText, vec2(0.2, -0.08), vec2(0.25, 0.08)));\n        dB = min(dB, segment(bText, vec2(0.25, 0.08), vec2(0.3, -0.08)));\n        \n        dB = min(dB, abs(length(bText - vec2(0.45, 0.0)) - 0.06));\n\n        if (dB < 0.015) {\n            col = vec3(0.1, 0.2, 0.8);\n            if (dB < 0.005) col = vec3(0.4, 0.6, 1.0);\n        }\n    }\n}\n\nvoid layer_TreeBark(in vec2 p, in float signBoxTilted, inout vec3 col) {\n    if (signBoxTilted > 0.0) {\n        if (p.x < -0.75) {\n            col = vec3(0.15, 0.1, 0.05);\n            float bark = sin(p.y * 60.0 + sin(p.x * 30.0)*15.0);\n            col *= 0.7 + 0.3 * bark;\n        }\n    }\n}\n\nvoid layer_NeonReflections(in vec2 p, in float signBoxTilted, in float iTime, inout vec3 col) {\n    if (signBoxTilted > 0.0) {\n        float neonGlow = exp(-length(p - vec2(0.5, -0.7)) * 2.0);\n        col += vec3(0.0, 0.5, 0.2) * neonGlow * (sin(iTime * 10.0) * 0.2 + 0.8);\n        \n        float neonGlow2 = exp(-length(p - vec2(-0.2, -0.8)) * 3.0);\n        col += vec3(0.8, 0.2, 0.2) * neonGlow2;\n    }\n}\n\nvec4 layer_SignBox(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 col = vec3(-1.0);\n    \n    float tilt = p.x * 0.1; \n    vec2 tp = vec2(p.x, p.y + tilt);\n    float signBoxTilted = length(max(abs(tp) - vec2(0.7, 0.4), 0.0)) - 0.02;\n    \n    layer_SignBox(tp, signBoxTilted, col);\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_TextALOC(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 col = vec3(-1.0);\n    \n    float tilt = p.x * 0.1; \n    vec2 tp = vec2(p.x, p.y + tilt);\n    float signBoxTilted = length(max(abs(tp) - vec2(0.7, 0.4), 0.0)) - 0.02;\n    \n    layer_TextALoc(tp, signBoxTilted, col);\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_TextCHAOLONG(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 col = vec3(-1.0);\n    \n    float tilt = p.x * 0.1; \n    vec2 tp = vec2(p.x, p.y + tilt);\n    float signBoxTilted = length(max(abs(tp) - vec2(0.7, 0.4), 0.0)) - 0.02;\n    \n    layer_TextChaoLong(tp, signBoxTilted, col);\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_TreeBark(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 col = vec3(-1.0);\n    \n    float tilt = p.x * 0.1; \n    vec2 tp = vec2(p.x, p.y + tilt);\n    float signBoxTilted = length(max(abs(tp) - vec2(0.7, 0.4), 0.0)) - 0.02;\n    \n    layer_TreeBark(p, signBoxTilted, col);\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_NeonReflections(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 col = vec3(-1.0);\n    \n    float tilt = p.x * 0.1; \n    vec2 tp = vec2(p.x, p.y + tilt);\n    float signBoxTilted = length(max(abs(tp) - vec2(0.7, 0.4), 0.0)) - 0.02;\n    \n    layer_NeonReflections(p, signBoxTilted, iTime, col);\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n","IMG_1333":"void layer_Background(in vec2 p, inout vec3 col) {\n    col = vec3(0.95, 0.95, 0.95); \n    float vignette = length(p);\n    col *= 1.0 - 0.1 * vignette;\n}\n\nvoid layer_Hood(in vec2 p, in float iTime, inout vec3 col) {\n    vec2 headP = p - vec2(0.0, 0.65);\n    float hoodOut = length(max(abs(headP) - vec2(0.12, 0.18), 0.0)) - 0.05; \n    float faceCutout = length(max(abs(headP - vec2(0.0, -0.05)) - vec2(0.08, 0.1), 0.0)) - 0.02; \n    \n    if (hoodOut < 0.0) {\n        if (faceCutout < 0.0) {\n            col = vec3(0.9, 0.7, 0.6); \n            float hat = length(max(abs(headP - vec2(0.0, 0.1)) - vec2(0.08, 0.02), 0.0));\n            if (hat < 0.01) col = vec3(0.6, 0.2, 0.2); \n            float hatBrim = length(max(abs(headP - vec2(0.0, 0.05)) - vec2(0.09, 0.01), 0.0));\n            if (hatBrim < 0.01) col = vec3(0.2); \n        } else {\n            col = vec3(0.2, 0.5, 0.25); \n            float hFolds = sin(headP.x * 30.0 + headP.y * 20.0);\n            col *= 0.8 + 0.2 * hFolds;\n            float wind = sin(iTime * 3.0 + headP.y * 10.0) * 0.05;\n            col *= 1.0 + wind;\n        }\n    }\n}\n\nvoid layer_Raincoat(in vec2 p, in float iTime, inout vec3 col) {\n    vec2 bodyP = p - vec2(0.0, 0.05);\n    float jacket = length(max(abs(bodyP) - vec2(0.25, 0.4), 0.0)) - 0.05;\n    \n    float epauletL = length(max(abs(bodyP - vec2(-0.25, 0.38)) - vec2(0.08, 0.02), 0.0)) - 0.01;\n    float epauletR = length(max(abs(bodyP - vec2(0.25, 0.38)) - vec2(0.08, 0.02), 0.0)) - 0.01;\n    \n    float collarL = length(max(abs(bodyP - vec2(-0.1, 0.35)) - vec2(0.03, 0.04), 0.0));\n    float collarR = length(max(abs(bodyP - vec2(0.1, 0.35)) - vec2(0.03, 0.04), 0.0));\n\n    float armL = length(max(abs(bodyP - vec2(-0.35, 0.0)) - vec2(0.06, 0.35), 0.0)) - 0.02;\n    float armR = length(max(abs(bodyP - vec2(0.35, 0.0)) - vec2(0.06, 0.35), 0.0)) - 0.02;\n    \n    float belt = length(max(abs(bodyP - vec2(0.0, -0.1)) - vec2(0.28, 0.03), 0.0));\n    float buckle = length(max(abs(bodyP - vec2(0.0, -0.1)) - vec2(0.04, 0.05), 0.0));\n\n    vec2 pantsP = p - vec2(0.0, -0.65);\n    float legL = length(max(abs(pantsP - vec2(-0.12, 0.0)) - vec2(0.08, 0.25), 0.0)) - 0.02;\n    float legR = length(max(abs(pantsP - vec2(0.12, 0.0)) - vec2(0.08, 0.25), 0.0)) - 0.02;\n\n    float raincoat = min(min(jacket, min(armL, armR)), min(legL, legR));\n\n    if (raincoat < 0.0) {\n        col = vec3(0.2, 0.5, 0.25); \n        \n        float wrinkleX = sin(p.x * 20.0 + p.y * 10.0 + sin(p.y * 5.0) * 2.0);\n        float wrinkleY = cos(p.y * 30.0 - p.x * 15.0);\n        \n        float shine = smoothstep(0.8, 1.0, wrinkleX * wrinkleY);\n        \n        col *= 0.7 + 0.3 * (wrinkleX + wrinkleY) * 0.5;\n        col += vec3(0.2) * shine;\n        \n        float flutter = sin(p.x * 5.0 + p.y * 10.0 + iTime * 4.0) * 0.05;\n        col *= 1.0 + flutter;\n\n        if (abs(bodyP.x) < 0.01 && bodyP.y > -0.4 && bodyP.y < 0.4) {\n            col *= 0.5; \n        }\n        if (abs(bodyP.x) < 0.02) {\n            float b1 = length(bodyP - vec2(0.0, 0.2)) - 0.015;\n            float b2 = length(bodyP - vec2(0.0, 0.05)) - 0.015;\n            float b3 = length(bodyP - vec2(0.0, -0.25)) - 0.015;\n            if (min(min(b1, b2), b3) < 0.0) col = vec3(0.8, 0.7, 0.2); \n        }\n\n        if (belt < 0.01) {\n            col = vec3(0.15, 0.4, 0.2); \n            if (buckle < 0.01) {\n                col = vec3(0.8, 0.7, 0.2); \n                if (length(max(abs(bodyP - vec2(0.0, -0.1)) - vec2(0.02, 0.03), 0.0)) < 0.005) {\n                    col = vec3(0.2, 0.5, 0.25);\n                }\n            }\n        }\n        \n        if (epauletL < 0.0 || epauletR < 0.0) {\n            col = vec3(0.9, 0.2, 0.2); \n            if (fract(p.x * 20.0) < 0.2) col = vec3(0.9); \n        }\n        \n        if (collarL < 0.01 || collarR < 0.01) {\n             col = vec3(0.9, 0.1, 0.1); \n             if (length(bodyP - vec2(-0.1, 0.35)) < 0.01 || length(bodyP - vec2(0.1, 0.35)) < 0.01) {\n                 col = vec3(0.9, 0.8, 0.2);\n             }\n        }\n    }\n}\n\nvoid layer_Hands(in vec2 p, inout vec3 col) {\n    vec2 bodyP = p - vec2(0.0, 0.05);\n    float handL = length(max(abs(bodyP - vec2(-0.35, -0.4)) - vec2(0.03, 0.05), 0.0)) - 0.02;\n    float handR = length(max(abs(bodyP - vec2(0.35, -0.4)) - vec2(0.03, 0.05), 0.0)) - 0.02;\n    if (handL < 0.0 || handR < 0.0) col = vec3(0.9, 0.7, 0.6);\n}\n\nvoid layer_Shoes(in vec2 p, inout vec3 col) {\n    vec2 pantsP = p - vec2(0.0, -0.65);\n    float shoeL = length(max(abs(pantsP - vec2(-0.15, -0.3)) - vec2(0.06, 0.04), 0.0)) - 0.02;\n    float shoeR = length(max(abs(pantsP - vec2(0.15, -0.3)) - vec2(0.06, 0.04), 0.0)) - 0.02;\n    if (shoeL < 0.0 || shoeR < 0.0) {\n        col = vec3(0.1); \n        if (pantsP.y > -0.28 && abs(pantsP.x) > 0.12 && abs(pantsP.x) < 0.16) col += vec3(0.2);\n    }\n}\n\nvoid layer_Floor(in vec2 p, inout vec3 col) {\n    if (p.y < -0.9) {\n        col = mix(col, vec3(0.8, 0.8, 0.85), 0.5); \n        if (fract(p.x * 5.0) < 0.02) col = vec3(0.6);\n    }\n    float shadow = exp(-15.0 * length(max(abs(p - vec2(0.0, -0.95)) - vec2(0.3, 0.05), 0.0)));\n    col *= 1.0 - 0.4 * shadow;\n}\n\nvec4 layer_Background(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 col = vec3(-1.0);\n    \n    layer_Background(p, col);\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_Hood(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 col = vec3(-1.0);\n    \n    layer_Hood(p, iTime, col);\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_Raincoat(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 col = vec3(-1.0);\n    \n    layer_Raincoat(p, iTime, col);\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_Hands(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 col = vec3(-1.0);\n    \n    layer_Hands(p, col);\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_Shoes(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 col = vec3(-1.0);\n    \n    layer_Shoes(p, col);\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_Floor(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 col = vec3(-1.0);\n    \n    layer_Floor(p, col);\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n","IMG_1348":"void layer_BackgroundWalls(inout vec3 col) {\n    col = vec3(0.9, 0.9, 0.92);\n}\n\nvoid layer_GodRays(in vec2 p, in float iTime, inout vec3 col) {\n    if (p.x < 0.5) {\n        float rayAngle = atan(p.y - 0.6, p.x - (-0.8));\n        float rays = sin(rayAngle * 20.0 + iTime * 0.5) * sin(rayAngle * 10.0 - iTime * 0.2);\n        rays = max(0.0, rays);\n        float mask = smoothstep(1.0, 0.0, length(p - vec2(-0.8, 0.6))); \n        col += vec3(0.2, 0.25, 0.3) * rays * mask;\n    }\n}\n\nvoid layer_Pillar(in vec2 p, inout vec3 col) {\n    float column = abs(p.x - 0.7);\n    if (column < 0.2) {\n        col = vec3(0.25, 0.5, 0.7); \n        col *= 0.5 + 0.5 * cos((p.x - 0.7) * 7.85); \n        \n        float rY = fract(p.y * 5.0 + sin(p.x * 10.0)*0.2);\n        float rX = (p.x - 0.7) * 5.0;\n        float cloud = sin(rY * 10.0 + rX * 10.0) * cos(rY * 10.0 - rX * 10.0);\n        \n        if (cloud > 0.3) {\n            col = mix(col, vec3(0.8, 0.2, 0.2), 0.7); \n            if (cloud > 0.7) col = mix(col, vec3(0.9, 0.8, 0.2), 0.8); \n            col *= 1.0 - 0.3 * smoothstep(0.3, 0.4, cloud); \n        }\n    }\n}\n\nvoid layer_Headpiece(in vec2 p, inout vec3 col) {\n    vec2 hpP = p - vec2(0.0, 0.65);\n    float hpCurve = length(max(abs(hpP - vec2(0.0, 0.1)) - vec2(0.15, 0.05), 0.0)) - 0.04;\n    hpCurve = max(hpCurve, -(length(hpP - vec2(0.0, 0.2)) - 0.15));\n    \n    if (hpCurve < 0.0) {\n        col = vec3(0.4, 0.2, 0.1); \n        float grain = sin(hpP.x * 80.0 + hpP.y * 30.0 + sin(hpP.x * 20.0)*5.0);\n        col *= 0.7 + 0.3 * grain;\n        col += vec3(0.1) * exp(-50.0 * pow(hpP.y - 0.12, 2.0)); \n    }\n}\n\nvoid layer_VeilAndEmblem(in vec2 p, in float iTime, inout vec3 col) {\n    float veilTop = p.y - (0.8 - abs(p.x) * 1.5);\n    float drapeL = -0.3 - (0.1 * (0.5 - p.y));\n    float drapeR = 0.3 + (0.1 * (0.5 - p.y));\n    float faceMask = length(max(abs(p - vec2(0.0, 0.4)) - vec2(0.08, 0.12), 0.0)) - 0.05;\n\n    if (veilTop < 0.0 && p.y > -1.0 && p.x > drapeL && p.x < drapeR) {\n        if (faceMask < 0.0) {\n            col = vec3(0.8, 0.6, 0.45); \n            float wrinkles = sin(p.y * 150.0) * 0.03 + sin(p.x * 100.0) * 0.03;\n            col *= 0.9 + wrinkles;\n            \n            float eyeL = length(p - vec2(-0.05, 0.45));\n            float eyeR = length(p - vec2(0.05, 0.45));\n            \n            float blink = smoothstep(0.95, 1.0, sin(iTime * 2.0)); \n            if (min(eyeL, eyeR) < 0.015 && abs(p.y - 0.45) > blink * 0.015) {\n                col = vec3(0.1); \n            }\n            if (length(max(abs(p - vec2(0.0, 0.32)) - vec2(0.03, 0.005), 0.0)) < 0.01) col = vec3(0.3, 0.1, 0.1);\n            \n        } else {\n            col = vec3(0.95, 0.95, 1.0); \n            float sway = sin(p.y * 5.0 - iTime * 2.0) * 0.05; \n            float folds = sin((p.x + sway) * 20.0);\n            col *= 0.85 + 0.15 * smoothstep(-1.0, 1.0, folds);\n            \n            if (abs(p.x + sway*0.5) < 0.005 && p.y < 0.2) col *= 0.8;\n            \n            vec2 embP = p - vec2(0.0, -0.3);\n            \n            embP.y += sin(iTime * 1.5) * 0.02;\n            \n            float emblemBase = length(embP) - 0.15;\n            float emblemTop = length(max(abs(embP - vec2(0.0, 0.18)) - vec2(0.05, 0.05), 0.0)) - 0.02; \n            float fCut1 = length(embP - vec2(-0.08, 0.2)) - 0.06;\n            float fCut2 = length(embP - vec2(0.08, 0.2)) - 0.06;\n            \n            if (emblemBase < 0.0 || (emblemTop < 0.0 && fCut1 > 0.0 && fCut2 > 0.0)) {\n                col = mix(vec3(0.2, 0.6, 0.8), vec3(0.8, 0.7, 0.2), smoothstep(0.12, 0.15, length(embP)));\n                \n                float eyeShape = length(vec2(embP.x, embP.y * 1.5)) - 0.1;\n                if (eyeShape < 0.0) {\n                    col = vec3(1.0); \n                    \n                    vec2 pupilP = embP - vec2(sin(iTime * 0.8) * 0.04, 0.0);\n                    float iris = length(pupilP) - 0.05;\n                    \n                    if (iris < 0.0) {\n                        col = vec3(0.5, 0.2, 0.1); \n                        col += sin(atan(pupilP.y, pupilP.x)*30.0)*0.1;\n                        if (length(pupilP) < 0.02) col = vec3(0.0);\n                        if (length(pupilP - vec2(-0.01, 0.01)) < 0.01) col = vec3(1.0);\n                    }\n                }\n            }\n            \n            float gripMove = sin(iTime * 1.5) * 0.01;\n            float handL = length(max(abs(p - vec2(-0.12 + gripMove, -0.4)) - vec2(0.04, 0.06), 0.0)) - 0.02;\n            float handR = length(max(abs(p - vec2(0.12 - gripMove, -0.4)) - vec2(0.04, 0.06), 0.0)) - 0.02;\n            if (handL < 0.0 || handR < 0.0) {\n                col = vec3(0.8, 0.6, 0.45); \n                if (fract(p.x * 40.0) < 0.1) col *= 0.8; \n            }\n        }\n    }\n}\n\nvec4 layer_BackgroundWalls(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 col = vec3(-1.0);\n    \n    layer_BackgroundWalls(col);\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_GodRays(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 col = vec3(-1.0);\n    \n    layer_GodRays(p, iTime, col);\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_Pillar(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 col = vec3(-1.0);\n    \n    layer_Pillar(p, col);\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_Headpiece(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 col = vec3(-1.0);\n    \n    layer_Headpiece(p, col);\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_VeilAndEmblem(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 col = vec3(-1.0);\n    \n    layer_VeilAndEmblem(p, iTime, col);\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n","IMG_1399":"void layer_StoreInterior(in vec2 p, inout vec3 col) {\n    if (p.y > 0.6) {\n        col = vec3(0.9, 0.85, 0.8); \n        float brickW = fract(p.x * 10.0);\n        float brickH = fract(p.y * 15.0);\n        if (fract(p.y * 15.0 * 0.5) > 0.5) brickW = fract(p.x * 10.0 + 0.5);\n        \n        float mortar = step(0.95, brickW) + step(0.9, brickH);\n        if (mortar > 0.0) col = vec3(0.7, 0.65, 0.6); \n    }\n    \n    if (p.y > 0.0 && p.y < 0.6) {\n        col = vec3(0.15); \n        \n        float racks = step(0.98, fract(p.x * 5.0)) + step(abs(p.y - 0.4), 0.01) + step(abs(p.y - 0.1), 0.01);\n        if (racks > 0.0) col = vec3(0.05);\n        \n        vec2 cp = fract(p * vec2(15.0, 5.0)) - 0.5;\n        if (length(cp * vec2(0.5, 1.0)) < 0.4) {\n            float rnd = fract(floor(p.x * 15.0) * 12.3 + floor(p.y * 5.0) * 45.6);\n            if (rnd < 0.2) col = vec3(0.8, 0.2, 0.2); \n            else if (rnd < 0.4) col = vec3(0.2, 0.3, 0.8); \n            else if (rnd < 0.6) col = vec3(0.9, 0.9, 0.2); \n            else col = vec3(0.9); \n            \n            col *= 0.8 + 0.2 * sin(cp.x * 20.0);\n        }\n        \n        if (p.x < -0.4 && p.y < 0.2) {\n             col = vec3(0.4, 0.4, 0.45); \n             float folds = fract(p.y * 40.0);\n             if (folds < 0.1) col = vec3(0.2); \n        }\n    }\n}\n\nvoid layer_CheckeredFloor(in vec2 p, inout vec3 col) {\n    if (p.y <= 0.0) {\n        vec2 floorUV = vec2(p.x / (p.y + 0.5), 1.0 / (p.y + 0.5));\n        \n        float checkers = step(0.5, fract(floorUV.x * 3.0)) == step(0.5, fract(floorUV.y * 3.0)) ? 1.0 : 0.0;\n        col = mix(vec3(0.1), vec3(0.9), checkers);\n        \n        col *= smoothstep(-0.2, 0.2, -p.y);\n    }\n}\n\nvoid layer_MainSubjectTorso(in vec2 p, in float iTime, inout vec3 col) {\n    vec2 torsoP = p - vec2(0.0, 0.0);\n    float torso = length(max(abs(torsoP) - vec2(0.25, 0.35), 0.0)) - 0.05;\n    \n    float armL = length(max(abs(torsoP - vec2(-0.3, -0.1)) - vec2(0.06, 0.2), 0.0)) - 0.02;\n    float armR1 = length(max(abs(torsoP - vec2(0.35, 0.1)) - vec2(0.05, 0.15), 0.0)) - 0.02; \n    float armR2 = length(max(abs(torsoP - vec2(0.2, 0.3)) - vec2(0.15, 0.04), 0.0)) - 0.02; \n    \n    if (torso < 0.0 || armL < 0.0 || armR1 < 0.0 || armR2 < 0.0) {\n        col = vec3(0.9, 0.9, 0.95);\n        \n        vec2 clothUV = torsoP;\n        if (armL < 0.0 && torso > 0.0) clothUV.x -= 0.1; \n        \n        float stripeAnim = sin(clothUV.y * 5.0 + iTime * 2.0) * 0.02;\n        float stripes = step(0.5, fract((clothUV.x + stripeAnim) * 12.0));\n        \n        vec3 stripeCol = vec3(0.3, 0.35, 0.4);\n        col = mix(col, stripeCol, stripes);\n        \n        float collarL = length(max(abs(torsoP - vec2(-0.15, 0.35)) - vec2(0.05, 0.08), 0.0));\n        float collarR = length(max(abs(torsoP - vec2(0.15, 0.35)) - vec2(0.05, 0.08), 0.0));\n        if (collarL < 0.01 || collarR < 0.01) {\n            col = stripeCol;\n        }\n        \n        float pocket = length(max(abs(torsoP - vec2(-0.12, 0.15)) - vec2(0.06, 0.07), 0.0));\n        if (pocket < 0.01) {\n            if (pocket > 0.005) col = vec3(0.2);\n            else {\n                col = mix(vec3(0.9,0.9,0.95), stripeCol, step(0.5, fract((clothUV.x + stripeAnim)*12.0)));\n                \n                float snoopyHead = length(max(abs(torsoP - vec2(-0.12, 0.15)) - vec2(0.03, 0.02), 0.0)) - 0.01;\n                float snoopyEar = length(max(abs(torsoP - vec2(-0.08, 0.13)) - vec2(0.01, 0.03), 0.0)) - 0.005;\n                float snoopyNose = length(torsoP - vec2(-0.16, 0.15)) - 0.01;\n                \n                if (snoopyHead < 0.0) col = vec3(0.95); \n                if (snoopyEar < 0.0 || snoopyNose < 0.0) col = vec3(0.1); \n            }\n        }\n        \n        float seam = abs(torsoP.x - 0.02);\n        if (seam < 0.01 && torsoP.y < 0.35) {\n            col = mix(col, vec3(0.2), 0.3); \n            if (fract(torsoP.y * 5.0) < 0.1) col = vec3(0.1); \n        }\n        \n        float folds = sin(torsoP.x * 15.0 + torsoP.y * 10.0);\n        col *= 0.8 + 0.2 * smoothstep(-1.0, 1.0, folds);\n    }\n}\n\nvoid layer_SkinArmsNeck(in vec2 p, inout vec3 col) {\n    vec2 torsoP = p - vec2(0.0, 0.0);\n    float neck = length(max(abs(torsoP - vec2(0.0, 0.4)) - vec2(0.05, 0.05), 0.0));\n    float bareArmL = length(max(abs(torsoP - vec2(-0.35, -0.35)) - vec2(0.03, 0.15), 0.0)) - 0.02; \n    float handL = length(max(abs(torsoP - vec2(-0.2, -0.4)) - vec2(0.05, 0.04), 0.0)) - 0.01; \n    float handR = length(max(abs(torsoP - vec2(0.05, 0.35)) - vec2(0.04, 0.05), 0.0)) - 0.02; \n    \n    if (neck < 0.02 || bareArmL < 0.0 || handL < 0.0 || handR < 0.0) {\n        col = vec3(0.9, 0.7, 0.6); \n        if (bareArmL < 0.0) col *= 0.8 + 0.2 * sin(torsoP.x * 40.0);\n    }\n}\n\nvoid layer_Shorts(in vec2 p, in float iTime, inout vec3 col) {\n    vec2 shortsP = p - vec2(0.0, -0.6);\n    float shortsL = length(max(abs(shortsP - vec2(-0.15, 0.0)) - vec2(0.1, 0.2), 0.0)) - 0.02;\n    float shortsR = length(max(abs(shortsP - vec2(0.15, 0.0)) - vec2(0.1, 0.2), 0.0)) - 0.02;\n    \n    if (shortsL < 0.0 || shortsR < 0.0) {\n        vec3 stripeCol = vec3(0.3, 0.35, 0.4);\n        \n        float stripeAnim = sin(shortsP.y * 5.0 - iTime * 2.0) * 0.02;\n        float stripes = step(0.5, fract((shortsP.x + stripeAnim) * 12.0));\n        \n        col = mix(vec3(0.9, 0.9, 0.95), stripeCol, stripes);\n        \n        if (abs(shortsP.x) < 0.05 && shortsP.y > -0.1) col *= 0.5;\n        col *= 0.8 + 0.2 * sin(shortsP.x * 20.0);\n    }\n}\n\nvoid layer_BareLegs(in vec2 p, inout vec3 col) {\n    vec2 shortsP = p - vec2(0.0, -0.6);\n    float legL = length(max(abs(shortsP - vec2(-0.15, -0.3)) - vec2(0.04, 0.1), 0.0)) - 0.02;\n    float legR = length(max(abs(shortsP - vec2(0.15, -0.3)) - vec2(0.04, 0.1), 0.0)) - 0.02;\n    if (legL < 0.0 || legR < 0.0) {\n        col = vec3(0.9, 0.7, 0.6); \n        col *= 0.7 + 0.3 * sin(shortsP.x * 40.0); \n    }\n}\n\nvoid layer_Head(in vec2 p, inout vec3 col) {\n    float head = length(p - vec2(0.0, 0.55)) - 0.12;\n    if (head < 0.0) {\n        col = vec3(0.9, 0.7, 0.6); \n        \n        float hair = length(max(abs(p - vec2(0.0, 0.62)) - vec2(0.1, 0.05), 0.0)) - 0.03;\n        if (hair < 0.0) col = vec3(0.1, 0.1, 0.1);\n        \n        if (abs(p.x) > 0.1 && head < -0.05) col = vec3(0.8, 0.6, 0.5);\n    }\n}\n\nvoid layer_Phone(in vec2 p, inout vec3 col) {\n    float phone = length(max(abs(p - vec2(0.05, 0.45)) - vec2(0.06, 0.12), 0.0)) - 0.02;\n    if (phone < 0.0) {\n        col = vec3(0.15); \n        float lens = length(p - vec2(0.02, 0.52)) - 0.02;\n        if (lens < 0.0) col = vec3(0.05);\n        float flash = exp(-50.0 * pow(p.x - 0.05 + p.y - 0.45, 2.0));\n        col += vec3(0.5) * flash;\n    }\n}\n\nvec4 layer_StoreInterior(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 col = vec3(-1.0);\n    \n    layer_StoreInterior(p, col);\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_CheckeredFloor(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 col = vec3(-1.0);\n    \n    layer_CheckeredFloor(p, col);\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_MainSubjectTorso(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 col = vec3(-1.0);\n    \n    layer_MainSubjectTorso(p, iTime, col);\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_SkinArmsNeck(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 col = vec3(-1.0);\n    \n    layer_SkinArmsNeck(p, col);\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_Shorts(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 col = vec3(-1.0);\n    \n    layer_Shorts(p, iTime, col);\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_BareLegs(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 col = vec3(-1.0);\n    \n    layer_BareLegs(p, col);\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_Head(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 col = vec3(-1.0);\n    \n    layer_Head(p, col);\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_Phone(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 col = vec3(-1.0);\n    \n    layer_Phone(p, col);\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n","IMG_1415":"void layer_RunwayBackground(in vec2 p, inout vec3 col) {\n    col = mix(vec3(0.95, 0.95, 0.95), vec3(0.7, 0.7, 0.75), smoothstep(0.0, -1.0, p.y));\n    float panel = fract(p.x * 1.5);\n    if (abs(panel) < 0.02) col = vec3(0.8);\n}\n\nvoid layer_ModelSharedFeatures(in vec2 localP, in int modelId, inout vec3 col) {\n    float head = length(localP - vec2(0.0, 0.75)) - 0.1;\n    float neck = length(max(abs(localP - vec2(0.0, 0.6)) - vec2(0.03, 0.05), 0.0));\n    \n    if (head < 0.0 || neck < 0.01) {\n        col = vec3(0.9, 0.75, 0.65); \n        if (modelId == 2) col = vec3(0.7, 0.5, 0.4); \n        \n        if (localP.y > 0.8) {\n             if (modelId == 0) col = vec3(0.4, 0.2, 0.1); \n             if (modelId == 1) col = vec3(0.9, 0.7, 0.2); \n             if (modelId == 2) col = vec3(0.2, 0.1, 0.1); \n             \n             float hTex = sin(localP.x * 100.0);\n             col *= 0.8 + 0.2 * hTex;\n        }\n        \n        float glasses = length(max(abs(localP - vec2(0.0, 0.75)) - vec2(0.07, 0.02), 0.0));\n        if (glasses < 0.01) {\n            if (modelId == 0 || modelId == 1) {\n                col = vec3(0.8, 0.1, 0.1); \n                if (localP.y > 0.75 && localP.x > -0.05) col += vec3(0.3);\n            }\n            if (modelId == 2) {\n                col = vec3(0.1); \n            }\n        }\n        \n        float lips = length(max(abs(localP - vec2(0.0, 0.68)) - vec2(0.02, 0.005), 0.0));\n        if (lips < 0.005) col = vec3(0.8, 0.4, 0.4);\n    }\n    \n    float armL = length(max(abs(localP - vec2(-0.3, 0.0)) - vec2(0.03, 0.3), 0.0)) - 0.02;\n    float armR = length(max(abs(localP - vec2(0.3, 0.0)) - vec2(0.03, 0.3), 0.0)) - 0.02;\n    if ((armL < 0.0 || armR < 0.0) && localP.y < 0.4 && modelId != 2) {\n        col = vec3(0.9, 0.75, 0.65); \n        col *= 0.8 + 0.2 * sin(localP.x * 40.0); \n    }\n}\n\nvoid layer_ModelLeft(in vec2 localP, in int modelId, in float iTime, inout vec3 col) {\n    if (modelId == 0) {\n        float top = length(max(abs(localP - vec2(0.0, 0.2)) - vec2(0.2, 0.25), 0.0)) - 0.1;\n        float sleeveL = length(max(abs(localP - vec2(-0.35, 0.25)) - vec2(0.05, 0.15), 0.0)) - 0.08;\n        float sleeveR = length(max(abs(localP - vec2(0.35, 0.25)) - vec2(0.05, 0.15), 0.0)) - 0.08;\n        \n        if (top < 0.0 || sleeveL < 0.0 || sleeveR < 0.0) {\n            col = vec3(0.95); \n            \n            float folds = sin(localP.x * 20.0 + localP.y * 10.0) * cos(localP.x * 15.0);\n            float puffAnim = sin(iTime * 3.0 + localP.y * 5.0) * 0.05;\n            \n            col *= 0.8 + 0.2 * smoothstep(-1.0, 1.0, folds + puffAnim);\n            \n            float neckHole = length(localP - vec2(0.0, 0.55)) - 0.1;\n            if (neckHole < 0.0) col = vec3(0.9, 0.75, 0.65); \n        }\n        \n        float pants = length(max(abs(localP - vec2(0.0, -0.6)) - vec2(0.3, 0.5), 0.0)) - 0.05;\n        if (pants < 0.0 && localP.y < -0.1) {\n             col = vec3(0.1, 0.15, 0.3); \n             \n             float pleats = abs(sin(localP.x * 15.0));\n             float sway = sin(localP.y * 5.0 - iTime * 4.0) * 0.1;\n             float pAnim = abs(sin((localP.x + sway) * 15.0));\n             \n             col *= 0.6 + 0.4 * pAnim;\n             if (abs(localP.x) < 0.05 && localP.y > -0.3) col *= 0.5;\n        }\n        \n        float belt = length(max(abs(localP - vec2(0.0, -0.05)) - vec2(0.25, 0.08), 0.0)) - 0.02;\n        if (belt < 0.0) {\n            col = vec3(0.15); \n            float bag = length(max(abs(localP - vec2(0.0, -0.05)) - vec2(0.15, 0.06), 0.0)) - 0.01;\n            if (bag < 0.0) {\n                col = vec3(0.2);\n                col += vec3(0.1) * sin(localP.x * 30.0); \n                if (abs(localP.y - 0.0) < 0.005 && abs(localP.x) < 0.15) col = vec3(0.8);\n            }\n        }\n    }\n}\n\nvoid layer_ModelCenter(in vec2 localP, in int modelId, in float iTime, inout vec3 col) {\n    if (modelId == 1) {\n        float dress = length(max(abs(localP - vec2(0.0, 0.0)) - vec2(0.2, 0.5), 0.0)) - 0.15;\n        float billowingL = length(localP - vec2(-0.25, 0.0)) - 0.25;\n        float billowingR = length(localP - vec2(0.25, 0.0)) - 0.25;\n        \n        if (dress < 0.0 || billowingL < 0.0 || billowingR < 0.0) {\n            col = vec3(0.92, 0.92, 0.95); \n            \n            float folds1 = sin(localP.x * 25.0 + localP.y * 10.0);\n            float folds2 = cos(localP.x * 15.0 - localP.y * 20.0);\n            \n            float windX = sin(localP.x * 5.0 + iTime * 3.0) * 0.05;\n            float windY = cos(localP.y * 5.0 - iTime * 2.0) * 0.05;\n            float flow = sin((localP.x + windX) * 30.0 + (localP.y + windY) * 20.0);\n            \n            float shadow = smoothstep(-1.0, 1.0, folds1) * smoothstep(-1.0, 1.0, flow);\n            float shine = smoothstep(0.8, 1.0, flow * folds2);\n            \n            col *= 0.6 + 0.4 * shadow;\n            col += vec3(0.1) * shine;\n            \n            float centerFold = abs(localP.x - sin(localP.y * 2.0) * 0.05);\n            if (centerFold < 0.02) col *= 0.5; \n            \n            float scoop = length(localP - vec2(0.0, 0.6)) - 0.08;\n            if (scoop < 0.0) col = vec3(0.9, 0.75, 0.65); \n        }\n    }\n}\n\nvoid layer_ModelRight(in vec2 localP, in int modelId, in float iTime, inout vec3 col) {\n    if (modelId == 2) {\n        float cape = length(max(abs(localP - vec2(0.0, 0.1)) - vec2(0.35, 0.4), 0.0)) - 0.05;\n        cape = max(cape, localP.y - 0.55 + abs(localP.x) * 0.5);\n        \n        float skirt = length(max(abs(localP - vec2(0.0, -0.6)) - vec2(0.2, 0.4), 0.0)) - 0.05;\n\n        if (cape < 0.0 || skirt < 0.0) {\n            col = vec3(0.75, 0.7, 0.6); \n            \n            float tex = sin(localP.x * 20.0) * cos(localP.y * 30.0);\n            float sway = sin(localP.y * 3.0 - iTime * 5.0) * 0.03;\n            float vFolds = abs(sin((localP.x + sway) * 15.0));\n            \n            col *= 0.8 + 0.2 * tex + 0.1 * vFolds;\n            \n            float armDrape = abs(abs(localP.x) - 0.25);\n            if (armDrape < 0.05 && localP.y > -0.3) col *= 0.7;\n            \n            if (localP.y < -0.25 && localP.y > -0.35 && skirt < 0.0) {\n                float dropShad = exp(-20.0 * (0.35 + localP.y));\n                col *= 1.0 - 0.4 * dropShad;\n            }\n            \n            float collar = length(max(abs(localP - vec2(0.0, 0.55)) - vec2(0.08, 0.04), 0.0));\n            if (collar < 0.01) col = vec3(0.65, 0.6, 0.5); \n            \n            if (abs(localP.y - (-0.3)) < 0.02 && cape < 0.0) {\n                if (fract(localP.x * 40.0) < 0.5) col = vec3(0.5, 0.45, 0.4);\n            }\n        }\n        \n        float bag = length(max(abs(localP - vec2(0.0, -0.15)) - vec2(0.18, 0.08), 0.0)) - 0.02;\n        if (bag < 0.0) {\n            col = vec3(0.9, 0.8, 0.7); \n            \n            float edgeDist = length(max(abs(localP - vec2(0.0, -0.15)) - vec2(0.15, 0.05), 0.0));\n            col *= 0.8 + 0.2 * smoothstep(0.05, 0.0, edgeDist);\n            \n            float clasp = length(localP - vec2(0.1, -0.15)) - 0.02;\n            if (clasp < 0.0) {\n                col = vec3(0.2); \n                if (clasp > -0.005) col = vec3(0.5); \n            }\n            \n            float flap = abs(localP.y - (-0.1));\n            if (flap < 0.005 && abs(localP.x) < 0.16) col *= 0.6;\n        }\n    }\n}\n\nvec4 layer_RunwayBackground(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 col = vec3(-1.0);\n    \n    layer_RunwayBackground(p, col);\n    \n    vec2 localP = vec2(fract(p.x * 1.5 + 0.5) * 2.0 - 1.0, p.y);\n    int modelId = int(floor(p.x * 1.5 + 1.5)); \n    \n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_ModelSharedFeatures(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 col = vec3(-1.0);\n    \n    \n    vec2 localP = vec2(fract(p.x * 1.5 + 0.5) * 2.0 - 1.0, p.y);\n    int modelId = int(floor(p.x * 1.5 + 1.5)); \n    \n    layer_ModelSharedFeatures(localP, modelId, col);\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_ModelLeft(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 col = vec3(-1.0);\n    \n    \n    vec2 localP = vec2(fract(p.x * 1.5 + 0.5) * 2.0 - 1.0, p.y);\n    int modelId = int(floor(p.x * 1.5 + 1.5)); \n    \n    layer_ModelLeft(localP, modelId, iTime, col);\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_ModelCenter(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 col = vec3(-1.0);\n    \n    \n    vec2 localP = vec2(fract(p.x * 1.5 + 0.5) * 2.0 - 1.0, p.y);\n    int modelId = int(floor(p.x * 1.5 + 1.5)); \n    \n    layer_ModelCenter(localP, modelId, iTime, col);\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_ModelRight(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 col = vec3(-1.0);\n    \n    \n    vec2 localP = vec2(fract(p.x * 1.5 + 0.5) * 2.0 - 1.0, p.y);\n    int modelId = int(floor(p.x * 1.5 + 1.5)); \n    \n    layer_ModelRight(localP, modelId, iTime, col);\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n","IMG_1426":"float hash(vec2 p, float iTime) { return fract(sin(dot(p + iTime*0.05, vec2(12.9898, 78.233))) * 43758.5453); }\nfloat noise(vec2 p, float iTime) {\n    vec2 i = floor(p), f = fract(p);\n    vec2 u = f*f*(3.0-2.0*f);\n    return mix(mix(hash(i + vec2(0.0,0.0), iTime), hash(i + vec2(1.0,0.0), iTime), u.x),\n               mix(hash(i + vec2(0.0,1.0), iTime), hash(i + vec2(1.0,1.0), iTime), u.x), u.y);\n}\n\nvoid layer_Sky(in vec2 uv, inout vec3 col) {\n    col = mix(vec3(0.3, 0.6, 0.85), vec3(0.2, 0.5, 0.8), uv.y);\n}\n\nvoid layer_Clouds(in vec2 p, in float iTime, inout vec3 col) {\n    float cloudNoise1 = noise(p * vec2(2.0, 15.0), iTime);\n    float cloudNoise2 = noise(p * vec2(4.0, 20.0) + vec2(10.0, 0.0), iTime);\n    \n    float drift = iTime * 0.05;\n    \n    float cloudShape = smoothstep(0.4, 0.6, cloudNoise1 * sin(p.y * 20.0 + drift * 10.0));\n    cloudShape += smoothstep(0.5, 0.7, cloudNoise2 * cos(p.y * 30.0 - drift * 5.0)) * 0.5;\n    \n    if (p.y > 0.0) {\n        col = mix(col, vec3(0.95, 0.95, 0.98), cloudShape * 0.8);\n        col *= 1.0 - 0.05 * noise(p * vec2(50.0, 200.0), iTime);\n    }\n}\n\nvoid layer_Birds(in vec2 p, in float iTime, inout vec3 col) {\n    vec2 bp = p - vec2(0.5, 0.6); \n    float a = -0.5; \n    bp = vec2(bp.x * cos(a) - bp.y * sin(a), bp.x * sin(a) + bp.y * cos(a));\n    \n    vec2 birdGrid = fract(bp * vec2(6.0, 6.0)) - 0.5;\n    vec2 birdID = floor(bp * vec2(6.0, 6.0));\n    \n    float vMask = abs(birdID.y) - birdID.x * 0.5;\n    \n    if (abs(vMask) < 1.0 && birdID.x > -2.0 && birdID.x < 4.0) {\n        float flap = sin(iTime * 10.0 + birdID.x * 2.0 + birdID.y * 3.0);\n        \n        float wingY = birdGrid.y + abs(birdGrid.x) * (0.5 + flap * 0.2);\n        float bird = length(max(abs(vec2(birdGrid.x, wingY)) - vec2(0.06, 0.01), 0.0));\n        \n        bird = min(bird, length(birdGrid - vec2(0.0, 0.02)) - 0.02);\n        \n        if (bird < 0.01) {\n            col = vec3(0.1); \n            if (noise(p * 200.0, iTime) > 0.6) col = mix(col, vec3(0.3, 0.6, 0.85), 0.5);\n        }\n    }\n}\n\nvoid layer_Rocks(in vec2 p, out float rockHeight, in float iTime, inout vec3 col) {\n    rockHeight = -0.5;\n    rockHeight += 0.9 * exp(-15.0 * pow(p.x + 0.9, 2.0)); \n    rockHeight += 1.3 * exp(-20.0 * pow(p.x + 0.4, 2.0));       \n    rockHeight += 0.8 * exp(-12.0 * pow(p.x - 0.1, 2.0)); \n    rockHeight += 0.5 * exp(-15.0 * pow(p.x - 0.6, 2.0));\n    \n    rockHeight += 0.15 * sin(p.x * 20.0) + 0.1 * cos(p.x * 45.0);\n    rockHeight += 0.1 * noise(p * 15.0, iTime);\n    \n    if (p.y < rockHeight) {\n        col = vec3(0.7, 0.7, 0.75);\n        \n        float rTex1 = noise(p * vec2(10.0, 5.0), iTime);\n        float rTex2 = noise(p * vec2(30.0, 10.0), iTime);\n        \n        col = mix(col, vec3(0.85, 0.85, 0.9), rTex1); \n        col = mix(col, vec3(0.4, 0.4, 0.4), rTex2 * 0.5); \n        \n        float crevice = abs(sin(p.x * 30.0 + noise(p*10.0, iTime)*8.0));\n        if (crevice < 0.15) {\n            col *= 0.4 + crevice * 3.0; \n        }\n        \n        float crack = abs(cos(p.y * 25.0 + noise(p*5.0, iTime)*5.0));\n        if (crack < 0.05) col *= 0.6;\n        \n        float ao = smoothstep(0.0, 0.5, rockHeight - p.y);\n        col *= 1.0 - 0.3 * ao;\n        \n        if (p.y < -0.7 + noise(p*5.0, iTime)*0.2) {\n            col = mix(col, vec3(0.3, 0.2, 0.1), 0.6);\n            \n            float roots = abs(sin(p.x * 50.0 + p.y * 10.0));\n            if (roots < 0.1 && noise(p*20.0, iTime) > 0.5) col = vec3(0.15, 0.1, 0.05);\n        }\n    }\n}\n\nvoid layer_Plants(in vec2 p, in float rockHeight, in float iTime, inout vec3 col) {\n    float plantZone = noise(p * vec2(8.0, 8.0), iTime);\n    if (plantZone > 0.7 && p.y < rockHeight - 0.1) {\n        \n        vec2 leafP = fract(p * vec2(15.0, 15.0)) - 0.5;\n        float leafID = floor(p.x * 15.0) + floor(p.y * 15.0);\n        \n        float angle = leafID * 2.0;\n        leafP = vec2(leafP.x * cos(angle) - leafP.y * sin(angle), leafP.x * sin(angle) + leafP.y * cos(angle));\n        \n        float rustle = sin(iTime * 4.0 + leafID) * 0.1;\n        leafP.x += rustle * leafP.y; \n        \n        float leaf = length(max(abs(leafP) - vec2(0.05, 0.3), 0.0)); \n        leaf += 0.05 * sin(leafP.y * 100.0);\n        \n        if (leaf < 0.02) {\n            if (mod(leafID, 3.0) == 0.0) {\n                col = vec3(0.5, 0.2, 0.2); \n                if (abs(leafP.x) < 0.01) col = vec3(0.2, 0.4, 0.2); \n            } else if (mod(leafID, 4.0) == 0.0) {\n                 col = vec3(0.2, 0.4, 0.2); \n                 if (abs(leafP.x) > 0.03) col = vec3(0.7, 0.7, 0.2);\n            } else {\n                col = vec3(0.2, 0.5, 0.2); \n                if (leafP.x > 0.0) col = vec3(0.4, 0.7, 0.3);\n            }\n            \n            col *= 0.5 + 0.5 * smoothstep(-0.3, 0.0, leafP.y);\n        }\n    }\n}\n\nvoid layer_Vignette(in vec2 p, inout vec3 col) {\n    col *= 1.0 - 0.3 * length(p);\n}\n\nvec4 layer_Sky(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 col = vec3(-1.0);\n    float rockHeight = 0.0;\n    \n    layer_Sky(uv, col);\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_Clouds(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 col = vec3(-1.0);\n    float rockHeight = 0.0;\n    \n    layer_Clouds(p, iTime, col);\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_Birds(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 col = vec3(-1.0);\n    float rockHeight = 0.0;\n    \n    layer_Birds(p, iTime, col);\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_Rocks(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 col = vec3(-1.0);\n    float rockHeight = 0.0;\n    \n    layer_Rocks(p, rockHeight, iTime, col);\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_Plants(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 col = vec3(-1.0);\n    float rockHeight = 0.0;\n    \n    layer_Plants(p, rockHeight, iTime, col);\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_Vignette(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 col = vec3(-1.0);\n    float rockHeight = 0.0;\n    \n    layer_Vignette(p, col);\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n","IMG_1449":"void layer_MosaicWall(in vec2 p, in float iTime, in float frameOuter, inout vec3 col) {\n    if (frameOuter >= 0.0) {\n        vec2 tileUV = fract(p * 20.0);\n        float tileEdge = smoothstep(0.9, 1.0, tileUV.x) + smoothstep(0.9, 1.0, tileUV.y) + smoothstep(0.1, 0.0, tileUV.x) + smoothstep(0.1, 0.0, tileUV.y);\n        float sparkle = fract(sin(dot(floor(p * 20.0) + floor(iTime*2.0), vec2(12.9898, 78.233))) * 43758.5453);\n        vec3 tileCol = vec3(0.6 + 0.3*sparkle, 0.65 + 0.25*sparkle, 0.7 + 0.2*sparkle); \n        if (tileEdge > 0.0) tileCol *= 0.5;\n        col = tileCol;\n    }\n}\n\nvoid layer_GoldFrame(in vec2 p, in float iTime, in float frameOuter, in float frameInner, inout vec3 col) {\n    if (frameOuter < 0.0 && frameInner > 0.0) {\n        float goldTex = sin(p.x*100.0 + iTime)*sin(p.y*100.0 + iTime);\n        col = vec3(0.8, 0.6, 0.1) * (0.8 + 0.2*goldTex);\n        if (abs(frameOuter) < 0.02 || abs(frameInner) < 0.02) col *= 0.6;\n    }\n}\n\nvoid layer_Padding(in vec2 p, in float iTime, in float frameInner, in float centerHoleOuter, inout vec3 col) {\n    if (frameInner < 0.0 && centerHoleOuter >= 0.0) {\n        vec2 diaP = p;\n        float diaSize = 0.35;\n        \n        diaP.x += (mod(floor(diaP.y / diaSize), 2.0) * diaSize * 0.5);\n        \n        vec2 id = floor(diaP / diaSize);\n        vec2 fDia = fract(diaP / diaSize) - 0.5;\n        \n        float d1 = abs(fDia.x + fDia.y);\n        float d2 = abs(fDia.x - fDia.y);\n        \n        float pillow = 1.0 - max(d1, d2); \n        \n        col = vec3(0.85, 0.85, 0.85) * (0.5 + 0.5 * pillow);\n        \n        float leatherTex = fract(sin(dot(p*50.0, vec2(12.9898, 78.233))) * 43758.5453);\n        col -= 0.05 * leatherTex;\n        \n        vec2 gridP = fract((p + vec2(diaSize*0.5, 0.0)) / diaSize) - 0.5;\n        if (length(fDia) < 0.04) {\n            float btnLight = 0.5 + 0.5*sin(iTime*3.0 + id.x*10.0 + id.y*10.0);\n            col = vec3(0.8) + 0.2*btnLight;\n            if (length(fDia) > 0.03) col *= 0.5;\n        }\n    }\n}\n\nvoid layer_WoodFrame(in vec2 p, in float frameInner, in float centerHoleOuter, in float centerHoleInner, inout vec3 col) {\n    if (frameInner < 0.0 && centerHoleOuter < 0.0 && centerHoleInner > 0.0) {\n        col = vec3(0.15); \n        if (fract(p.x*20.0 + sin(p.y*10.0)) < 0.2) col *= 0.8;\n    }\n}\n\nvoid layer_InsideWall(in vec2 p, in float iTime, in float frameInner, in float centerHoleOuter, in float centerHoleInner, inout vec3 col) {\n    if (frameInner < 0.0 && centerHoleOuter < 0.0 && centerHoleInner <= 0.0) {\n        col = vec3(0.6, 0.55, 0.5); \n        float wallTex = fract(sin(dot(p, vec2(12.9898, 78.233))) * 43758.5453);\n        col -= 0.1 * wallTex;\n        \n        vec2 wireP1 = p - vec2(0.0, -0.2);\n        float w1 = length(vec2(abs(wireP1.x) - 0.1, wireP1.y + 0.1*sin(wireP1.x*10.0 + iTime*2.0))) - 0.01;\n        if (w1 < 0.0 && p.y < -0.1) col = vec3(0.1, 0.3, 0.8);\n        \n        vec2 wireP2 = p - vec2(0.1, -0.1);\n        float w2 = length(vec2(abs(wireP2.x) - 0.15, wireP2.y + 0.15*cos(wireP2.x*8.0 - iTime*1.5))) - 0.01;\n        if (w2 < 0.0 && p.y < 0.0) col = vec3(0.15);\n    }\n}\n\nvec4 layer_MosaicWall(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 col = vec3(-1.0);\n    \n    float frameOuter = max(abs(p.x) - 1.2, abs(p.y) - 0.9);\n    float frameInner = max(abs(p.x) - 1.1, abs(p.y) - 0.8);\n    float centerHoleOuter = max(abs(p.x) - 0.4, abs(p.y) - 0.5);\n    float centerHoleInner = max(abs(p.x) - 0.35, abs(p.y) - 0.45);\n    \n    layer_MosaicWall(p, iTime, frameOuter, col);\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_GoldFrame(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 col = vec3(-1.0);\n    \n    float frameOuter = max(abs(p.x) - 1.2, abs(p.y) - 0.9);\n    float frameInner = max(abs(p.x) - 1.1, abs(p.y) - 0.8);\n    float centerHoleOuter = max(abs(p.x) - 0.4, abs(p.y) - 0.5);\n    float centerHoleInner = max(abs(p.x) - 0.35, abs(p.y) - 0.45);\n    \n    layer_GoldFrame(p, iTime, frameOuter, frameInner, col);\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_Padding(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 col = vec3(-1.0);\n    \n    float frameOuter = max(abs(p.x) - 1.2, abs(p.y) - 0.9);\n    float frameInner = max(abs(p.x) - 1.1, abs(p.y) - 0.8);\n    float centerHoleOuter = max(abs(p.x) - 0.4, abs(p.y) - 0.5);\n    float centerHoleInner = max(abs(p.x) - 0.35, abs(p.y) - 0.45);\n    \n    layer_Padding(p, iTime, frameInner, centerHoleOuter, col);\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_WoodFrame(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 col = vec3(-1.0);\n    \n    float frameOuter = max(abs(p.x) - 1.2, abs(p.y) - 0.9);\n    float frameInner = max(abs(p.x) - 1.1, abs(p.y) - 0.8);\n    float centerHoleOuter = max(abs(p.x) - 0.4, abs(p.y) - 0.5);\n    float centerHoleInner = max(abs(p.x) - 0.35, abs(p.y) - 0.45);\n    \n    layer_WoodFrame(p, frameInner, centerHoleOuter, centerHoleInner, col);\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_InsideWall(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 col = vec3(-1.0);\n    \n    float frameOuter = max(abs(p.x) - 1.2, abs(p.y) - 0.9);\n    float frameInner = max(abs(p.x) - 1.1, abs(p.y) - 0.8);\n    float centerHoleOuter = max(abs(p.x) - 0.4, abs(p.y) - 0.5);\n    float centerHoleInner = max(abs(p.x) - 0.35, abs(p.y) - 0.45);\n    \n    layer_InsideWall(p, iTime, frameInner, centerHoleOuter, centerHoleInner, col);\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n","IMG_1621":"void layer_Background(in vec2 p, in float iTime, inout vec3 col) {\n    col = vec3(0.85, 0.85, 0.85); \n    \n    if (p.x < -0.2 && p.y > 0.0) {\n        float leafDensity = fract(sin(dot(floor(p*20.0), vec2(12.9898, 78.233))) * 43758.5453);\n        if (leafDensity > 0.4) {\n            float anim = sin(iTime*2.0 + p.y*10.0)*0.02;\n            vec2 leafP = fract(p*20.0 + vec2(anim, 0.0)) - 0.5;\n            if (length(leafP) < 0.3) {\n                col = vec3(0.2, 0.4 + 0.1*leafDensity, 0.2);\n            }\n        }\n    }\n}\n\nvoid layer_Floor(in vec2 p, in float iTime, inout vec3 col) {\n    if (p.y < 0.0) {\n        vec2 floorUV = vec2(p.x / (abs(p.y) + 0.5), 1.0 / (abs(p.y) + 0.5));\n        floorUV.y -= iTime * 0.1; \n        float tileX = fract(floorUV.x * 5.0);\n        float tileY = fract(floorUV.y * 5.0);\n        float lines = step(0.9, tileX) + step(0.9, tileY);\n        col = vec3(0.3, 0.3, 0.35) - 0.1*lines;\n    }\n}\n\nvoid layer_WoodRack(in vec2 p, in float iTime, inout vec3 col) {\n    if (p.x > 0.1 && p.x < 0.8 && p.y > -0.2 && p.y < 0.9) {\n        col = vec3(0.5, 0.35, 0.2); \n        \n        vec2 wP = fract(p * vec2(6.0, 6.0)) - 0.5;\n        float dist = length(wP);\n        float angle = atan(wP.y, wP.x);\n        float star = cos(angle*4.0 + iTime)*0.1 + 0.2;\n        \n        if (dist < star) {\n            col = vec3(0.6, 0.45, 0.25); \n        } else if (dist > 0.4) {\n            col = vec3(0.4, 0.25, 0.1); \n        }\n        \n        if (abs(p.x - 0.15) < 0.02 || abs(p.x - 0.75) < 0.02) col = vec3(0.3, 0.2, 0.1);\n    }\n}\n\nvoid layer_ChairTable(in vec2 p, inout vec3 col) {\n    if (p.x > -0.4 && p.x < -0.1 && p.y > -0.1 && p.y < 0.2) {\n        float chairBody = length(max(abs(p - vec2(-0.25, 0.05)) - vec2(0.1, 0.1), 0.0));\n        if (chairBody < 0.02) col = vec3(0.4, 0.6, 0.4); \n    }\n}\n\nvoid layer_Motorcycle(in vec2 p, in float iTime, inout vec3 col) {\n    vec2 mcP = p - vec2(0.2, -0.4);\n    \n    float wheelL = length(p - vec2(-0.6, -0.5));\n    float wheelR = length(p - vec2(0.8, -0.5));\n    \n    float spokeAngle = iTime * 2.0;\n    \n    if (wheelL < 0.3) {\n        if (wheelL > 0.25) col = vec3(0.15); \n        else {\n            col = vec3(0.7); \n            if (fract((atan(p.y + 0.5, p.x + 0.6) + spokeAngle)*8.0 / 6.28) < 0.1) col = vec3(0.4); \n        }\n    }\n    if (wheelR < 0.3) {\n        if (wheelR > 0.25) col = vec3(0.15); \n        else {\n            col = vec3(0.7); \n            if (fract((atan(p.y + 0.5, p.x - 0.8) + spokeAngle)*8.0 / 6.28) < 0.1) col = vec3(0.4); \n        }\n    }\n    \n    float body1 = length(max(abs(mcP - vec2(-0.1, 0.0)) - vec2(0.3, 0.15), 0.0)) - 0.05;\n    if (body1 < 0.0) {\n        col = vec3(0.8, 0.15, 0.15);\n        if (mcP.y > 0.1) col += 0.1; \n    }\n    \n    float sidePanel = length(max(abs(mcP - vec2(0.4, 0.05)) - vec2(0.15, 0.08), 0.0)) - 0.02;\n    if (sidePanel < 0.0) {\n        col = vec3(0.8);\n        if (fract(mcP.x*30.0) < 0.5 && mcP.y > 0.0) col *= 0.5;\n    }\n    \n    float seat = length(max(abs(mcP - vec2(-0.2, 0.25)) - vec2(0.3, 0.05), 0.0)) - 0.03;\n    if (seat < 0.0) {\n        col = vec3(0.1);\n        col += 0.05 * fract(sin(dot(mcP*50.0, vec2(12.9898, 78.233))) * 43758.5453);\n    }\n    \n    float pipe = length(max(abs(p - vec2(0.0, -0.4)) - vec2(0.6, 0.03), 0.0)) - 0.03;\n    if (pipe < 0.0) {\n        col = vec3(0.7, 0.7, 0.7); \n        if (fract(p.x * 15.0) < 0.4 && p.x > -0.2 && p.x < 0.4) col = vec3(0.3);\n    }\n    \n    float engine = length(max(abs(p - vec2(0.2, -0.55)) - vec2(0.15, 0.1), 0.0)) - 0.02;\n    if (engine < 0.0 && pipe > 0.0) {\n        col = vec3(0.6);\n        if (fract(p.y * 30.0) < 0.3) col *= 0.5;\n    }\n}\n\nvec4 layer_Background(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 col = vec3(-1.0);\n    \n    layer_Background(p, iTime, col);\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_Floor(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 col = vec3(-1.0);\n    \n    layer_Floor(p, iTime, col);\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_WoodRack(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 col = vec3(-1.0);\n    \n    layer_WoodRack(p, iTime, col);\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_ChairTable(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 col = vec3(-1.0);\n    \n    layer_ChairTable(p, col);\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_Motorcycle(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 col = vec3(-1.0);\n    \n    layer_Motorcycle(p, iTime, col);\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n","IMG_1644":"void layer_Background(inout vec3 col) {\n    col = vec3(0.95, 0.96, 0.98); \n}\n\nvoid layer_TextElements(in vec2 p, in float iTime, inout vec3 col) {\n    if (p.x > 0.8 && p.x < 1.1) {\n        float textY = p.y + iTime*0.2;\n        if (fract(textY * 10.0) < 0.6 && abs(p.x - 0.9) < 0.05) col = vec3(0.8, 0.2, 0.2); \n        if (fract(textY * 15.0) < 0.5 && abs(p.x - 1.0) < 0.04) col = vec3(0.2, 0.6, 0.2); \n    }\n}\n\nvoid layer_TopImpact(in vec2 p, in float iTime, inout vec3 col) {\n    vec2 impP = p - vec2(-0.8, 0.3);\n    float ang = atan(impP.y, impP.x);\n    float burstAnim1 = iTime * 5.0;\n    float rad = 0.4 + 0.1 * sin(ang * 8.0 - burstAnim1) + 0.15 * cos(ang * 5.0 + burstAnim1);\n    if (length(impP) < rad) {\n        col = vec3(0.8, 0.25, 0.25); \n        if (length(impP) > rad - 0.02) col = vec3(0.1);\n    }\n    \n    if (length(max(abs(p - vec2(-0.8, 0.4)) - vec2(0.2, 0.05), 0.0)) - 0.02 < 0.0) {\n        col = vec3(0.9, 0.8, 0.4); \n    }\n    if (length(max(abs(p - vec2(-0.6, 0.4)) - vec2(0.05, 0.15), 0.0)) - 0.02 < 0.0) {\n        col = vec3(0.2); \n    }\n    \n    if (p.x < -0.9 && p.y > 0.3 && p.y < 0.5) {\n        if (fract(p.y*20.0 - iTime*10.0) < 0.2) col = vec3(0.2);\n    }\n}\n\nvoid layer_BottomImpact(in vec2 p, in float iTime, inout vec3 col) {\n    vec2 imp2 = p - vec2(-0.6, -0.4);\n    float ang2 = atan(imp2.y, imp2.x);\n    float burstAnim2 = iTime * 4.0;\n    float rad2 = 0.35 + 0.1 * sin(ang2 * 6.0 + burstAnim2);\n    if (length(imp2) < rad2) {\n        col = vec3(0.8, 0.25, 0.25); \n        if (length(imp2) > rad2 - 0.02) col = vec3(0.1);\n    }\n    \n    if (length(max(abs(p - vec2(-0.6, -0.4)) - vec2(0.15, 0.05), 0.0)) - 0.02 < 0.0) {\n        col = vec3(0.9, 0.9, 0.5); \n    }\n    \n    if (p.x < -0.7 && p.y > -0.5 && p.y < -0.3) {\n        if (fract(p.y*20.0 - iTime*12.0) < 0.2) col = vec3(0.2);\n    }\n}\n\nvoid layer_Hardhat(in vec2 p, inout vec3 col) {\n    float hatRadius = 0.6;\n    vec2 hatP = p - vec2(-0.2, -0.5);\n    if (length(hatP) < hatRadius && hatP.y > 0.0) {\n        col = vec3(0.9, 0.85, 0.2); \n        if (hatP.y < 0.05 && abs(hatP.x) < 0.65) col = vec3(0.9, 0.85, 0.2);\n        if (hatP.y > 0.4 && abs(hatP.x) < 0.3 && fract(hatP.x*10.0) < 0.2) col = vec3(0.1);\n    }\n    if (abs(length(hatP) - hatRadius) < 0.01 && hatP.y > 0.0) col = vec3(0.1);\n}\n\nvoid layer_FamilyGroup(in vec2 p, inout vec3 col) {\n    vec2 famP = p - vec2(0.3, -0.2);\n    \n    if (length(famP - vec2(-0.15, -0.2)) < 0.2) {\n        col = vec3(0.2, 0.4, 0.8); \n    }\n    if (length(famP - vec2(-0.15, -0.1)) < 0.15) {\n        col = vec3(0.9, 0.8, 0.2); \n        if (length(max(abs(famP - vec2(-0.15, -0.1)) - vec2(0.04, 0.01), 0.0)) < 0.01) col = vec3(0.2, 0.6, 0.2); \n        if (length(max(abs(famP - vec2(-0.15, -0.1)) - vec2(0.01, 0.04), 0.0)) < 0.01) col = vec3(0.2, 0.6, 0.2); \n    }\n    if (length(famP - vec2(-0.05, -0.25)) < 0.1) col = vec3(1.0); \n    \n    if (length(famP - vec2(0.2, 0.0)) < 0.2) col = vec3(0.8, 0.2, 0.2); \n    if (length(famP - vec2(0.1, 0.2)) < 0.12) col = vec3(1.0); \n    if (length(famP - vec2(0.1, 0.3)) < 0.14) {\n        col = vec3(0.1); \n        if (length(famP - vec2(0.05, 0.35)) < 0.03) col = vec3(1.0);\n    }\n    \n    if (length(famP - vec2(0.0, 0.0)) < 0.15) col = vec3(0.9, 0.9, 0.2); \n    if (length(famP - vec2(-0.05, 0.1)) < 0.1) col = vec3(1.0); \n    if (length(famP - vec2(-0.05, 0.18)) < 0.08) col = vec3(0.1); \n}\n\nvec4 layer_Background(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 col = vec3(-1.0);\n    \n    layer_Background(col);\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_TextElements(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 col = vec3(-1.0);\n    \n    layer_TextElements(p, iTime, col);\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_TopImpact(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 col = vec3(-1.0);\n    \n    layer_TopImpact(p, iTime, col);\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_BottomImpact(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 col = vec3(-1.0);\n    \n    layer_BottomImpact(p, iTime, col);\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_Hardhat(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 col = vec3(-1.0);\n    \n    layer_Hardhat(p, col);\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_FamilyGroup(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 col = vec3(-1.0);\n    \n    layer_FamilyGroup(p, col);\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n","IMG_1663":"void layer_SignArea(in vec2 p, in float iTime, inout vec3 col) {\n    if (p.y > 0.8) {\n        col = vec3(0.2, 0.5, 0.75); \n        float signText = sin(p.x*20.0 + iTime*2.0)*sin(p.y*20.0);\n        if (signText > 0.5) col *= 0.8; \n    }\n}\n\nvoid layer_Pavement(in vec2 p, inout vec3 col) {\n    if (p.y < -0.85) {\n        col = vec3(0.35, 0.35, 0.35); \n        float pebble = fract(sin(dot(floor(p*50.0), vec2(12.9898, 78.233))) * 43758.5453);\n        col -= 0.1 * pebble;\n    }\n}\n\nvoid layer_DecorativeIronGate(in vec2 p, in float iTime, inout vec3 col) {\n    if (p.y <= 0.8 && p.y >= -0.85) {\n        vec2 gridP = p * vec2(3.0, 4.0);\n        vec2 fGrid = fract(gridP) - 0.5;\n        vec2 iGrid = floor(gridP);\n        \n        float iron = 0.0;\n        vec3 ironCol = vec3(0.3, 0.4, 0.4); \n        \n        if (abs(fGrid.x) > 0.46 || abs(fGrid.y) > 0.46) iron = 1.0;\n        \n        float cellType = fract(sin(dot(iGrid, vec2(12.9898, 78.233))) * 43758.5453);\n        \n        if (cellType < 0.3) {\n            float r = length(fGrid);\n            if (abs(r - 0.3) < 0.03) iron = 1.0;\n            if (abs(fGrid.x) < 0.02) iron = 1.0;\n            \n            float angle = atan(fGrid.y, fGrid.x);\n            float petals = cos(angle * 8.0 + iTime);\n            if (r < 0.3 && r > 0.25 && petals > 0.5) ironCol = vec3(0.7, 0.8, 0.5); \n        } \n        else if (cellType < 0.6) {\n            float d = abs(fGrid.x) + abs(fGrid.y);\n            if (abs(d - 0.35) < 0.03) iron = 1.0;\n            \n            if (abs(d - 0.15 - 0.05*sin(iTime*2.0 + iGrid.x)) < 0.02) ironCol = vec3(0.6, 0.7, 0.8); \n        } \n        else {\n            if (abs(fGrid.x) < 0.02 || abs(fGrid.y) < 0.02) iron = 1.0;\n            if (abs(fGrid.y - 0.25) < 0.02 || abs(fGrid.y + 0.25) < 0.02) iron = 1.0;\n            if (fGrid.y > 0.0 && abs(fGrid.x - 0.25) < 0.02) iron = 1.0;\n            if (fGrid.y < 0.0 && abs(fGrid.x + 0.25) < 0.02) iron = 1.0;\n        }\n        \n        if (iron > 0.0) {\n            col = ironCol; \n            if (fGrid.x > 0.46 || fGrid.y > 0.46) col *= 0.6;\n            if (fGrid.x < -0.46 || fGrid.y < -0.46) col *= 1.4;\n        } else {\n            float mesh = step(0.8, fract(p.x * 100.0)) + step(0.8, fract(p.y * 100.0));\n            col = vec3(0.1) + 0.05 * mesh;\n        }\n    }\n}\n\nvoid layer_RedWarningSign(in vec2 p, inout vec3 col) {\n    if (p.y <= 0.8 && p.y >= -0.85) {\n        if (abs(p.x) < 0.35 && abs(p.y + 0.1) < 0.18) {\n            col = vec3(0.85, 0.15, 0.15); \n            \n            if (abs(p.y + 0.05) < 0.04 && fract(p.x*25.0) < 0.7 && abs(p.x) < 0.3) {\n                col = vec3(0.95, 0.95, 0.2); \n            }\n            if (abs(p.y + 0.13) < 0.015 && fract(p.x*30.0) < 0.6 && abs(p.x) < 0.25) col = vec3(1.0); \n            if (abs(p.y + 0.18) < 0.015 && fract(p.x*30.0) < 0.6 && abs(p.x) < 0.28) col = vec3(1.0); \n            \n            if (max(abs(p.x) - 0.33, abs(p.y + 0.1) - 0.16) > 0.0) {\n                col = vec3(0.95, 0.95, 0.2); \n            }\n        }\n    }\n}\n\nvec4 layer_SignArea(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 col = vec3(-1.0);\n    \n    layer_SignArea(p, iTime, col);\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_Pavement(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 col = vec3(-1.0);\n    \n    layer_Pavement(p, col);\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_DecorativeIronGate(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 col = vec3(-1.0);\n    \n    layer_DecorativeIronGate(p, iTime, col);\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_RedWarningSign(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 col = vec3(-1.0);\n    \n    layer_RedWarningSign(p, col);\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n","IMG_1714":"void layer_StorefrontAlley(in vec2 p, inout vec3 col) {\n    if (p.x < 0.0) {\n        col = vec3(0.6, 0.6, 0.6); \n        \n        vec2 mcP = p - vec2(-0.5, -0.4);\n        \n        if (length(max(abs(mcP) - vec2(0.15, 0.2), 0.0)) < 0.05) col = vec3(0.6, 0.4, 0.2); \n        if (length(mcP - vec2(0.0, 0.3)) < 0.1) {\n            col = vec3(0.9, 0.8, 0.7); \n            if (mcP.y < 0.28 && mcP.x > 0.0) col = vec3(0.9, 0.9, 0.95);\n        }\n        \n        vec2 bikeP = p - vec2(-0.2, -0.6);\n        if (length(max(abs(bikeP) - vec2(0.2, 0.15), 0.0)) < 0.05) col = vec3(0.85, 0.85, 0.2);\n        if (length(bikeP - vec2(-0.15, 0.25)) < 0.1) {\n            col = vec3(0.1, 0.3, 0.8);\n            if (bikeP.y > 0.28 && abs(bikeP.x + 0.15) < 0.02) col = vec3(1.0); \n        }\n        if (length(bikeP - vec2(-0.2, -0.15)) < 0.12) col = vec3(0.1);\n        if (length(bikeP - vec2(0.3, -0.15)) < 0.12) col = vec3(0.1);\n        \n        if (p.y > 0.6) col = mix(col, vec3(0.2, 0.2, 0.25), 0.6); \n    }\n}\n\nvoid layer_WhiteWall(in vec2 p, inout vec3 col) {\n    if (p.x >= 0.0) {\n        col = vec3(0.9, 0.9, 0.92); \n        \n        float grunge = fract(sin(p.x*100.0)*cos(p.y*100.0)*437.5);\n        if (grunge > 0.8) col *= 0.95;\n        \n        float crackX = 0.8 + sin(p.y*10.0)*0.02 + cos(p.y*30.0)*0.01;\n        if (abs(p.x - crackX) < 0.01) col = vec3(0.2, 0.2, 0.2); \n    }\n}\n\nvoid layer_DecorativeVentBlock(in vec2 p, in float iTime, inout vec3 col) {\n    if (p.x >= 0.0) {\n        if (abs(p.x - 0.4) < 0.35 && abs(p.y) < 0.35) {\n            col = vec3(0.1, 0.1, 0.15); \n            \n            vec2 vP = fract((p - vec2(0.05, -0.35)) * vec2(1.428, 1.428)) - 0.5;\n            \n            float h1 = length(vP - vec2(-0.5, -0.5));\n            float h2 = length(vP - vec2(0.5, -0.5));\n            float h3 = length(vP - vec2(-0.5, 0.5));\n            float h4 = length(vP - vec2(0.5, 0.5));\n            \n            float holeSize = 0.4 + 0.05 * sin(iTime * 2.0);\n            \n            if (h1 > holeSize && h2 > holeSize && h3 > holeSize && h4 > holeSize) {\n                col = vec3(0.9, 0.9, 0.9); \n                \n                float d1 = min(min(h1, h2), min(h3, h4));\n                if (d1 < holeSize + 0.03) col *= 0.7; \n                \n                if (abs(vP.x) < 0.02 || abs(vP.y) < 0.02) col *= 0.85;\n            }\n            \n            if (max(abs(p.x - 0.4), abs(p.y)) > 0.33) {\n                col = vec3(0.9, 0.9, 0.9);\n                if (max(abs(p.x - 0.4), abs(p.y)) > 0.34) col *= 0.8; \n            }\n        }\n    }\n}\n\nvec4 layer_StorefrontAlley(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 col = vec3(-1.0);\n    \n    layer_StorefrontAlley(p, col);\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_WhiteWall(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 col = vec3(-1.0);\n    \n    layer_WhiteWall(p, col);\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_DecorativeVentBlock(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 col = vec3(-1.0);\n    \n    layer_DecorativeVentBlock(p, iTime, col);\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n","IMG_1718":"void layer_Background(in vec2 p, inout vec3 col) {\n    col = vec3(0.9, 0.9, 0.9); \n    \n    float concreteTex = fract(sin(dot(p*100.0, vec2(12.9898, 78.233))) * 43758.5453);\n    col -= 0.1 * concreteTex;\n    \n    if (p.y < -0.8) {\n        col = vec3(0.4, 0.4, 0.45); \n        float groundTex = fract(sin(dot(p*50.0, vec2(12.9898, 78.233))) * 43758.5453);\n        col -= 0.15 * groundTex;\n    }\n}\n\nvoid layer_IronGate(in vec2 p, in float iTime, inout vec3 col) {\n    vec2 gateP = p * vec2(4.0, 4.0);\n    vec2 fGate = fract(gateP) - 0.5;\n    vec2 iGate = floor(gateP);\n    \n    float iron = 0.0;\n    \n    if (p.x > -1.2 && p.x < 1.2 && p.y < 0.6 && p.y > -0.8) {\n        if (abs(fGate.x) > 0.46 || abs(fGate.y) > 0.46) iron = 1.0;\n        \n        float d1 = abs(fGate.x + fGate.y);\n        float d2 = abs(fGate.x - fGate.y);\n        \n        if (abs(d1) < 0.04 || abs(d2) < 0.04) iron = 1.0;\n        \n        float centerDist = length(fGate);\n        if (centerDist < 0.25 && centerDist > 0.2) iron = 1.0;\n        \n        float cellAnim = sin(iTime + iGate.x*10.0 + iGate.y*10.0);\n        if (cellAnim > 0.5 && centerDist < 0.15) {\n            if (abs(fGate.x) < 0.02 || abs(fGate.y) < 0.02) iron = 1.0;\n        }\n        \n        if (p.y > 0.55 || p.y < -0.75 || p.x > 1.15 || p.x < -1.15) iron = 1.0;\n    }\n    \n    if (iron > 0.0) {\n        col = vec3(0.2, 0.6, 0.6);\n        if (fGate.x > 0.0 && fGate.y > 0.0) col += 0.1;\n        if (fGate.x < 0.0 && fGate.y < 0.0) col -= 0.1;\n    }\n}\n\nvoid layer_HouseSign(in vec2 p, in float iTime, inout vec3 col) {\n    if (p.x < -0.9 && p.x > -1.4 && p.y > 0.0 && p.y < 0.2) {\n        col = vec3(0.7, 0.6, 0.3); \n        if (fract(p.x*20.0 + iTime*0.5) < 0.1) col = vec3(0.9, 0.8, 0.4); \n        \n        if (abs(p.y - 0.1) < 0.05 && p.x > -1.3 && p.x < -1.0) {\n            if (fract(p.x * 10.0) < 0.4) col = vec3(0.2, 0.2, 0.1);\n        }\n    }\n}\n\nvoid layer_Plant(in vec2 p, in float iTime, inout vec3 col) {\n    vec2 plantP = p - vec2(-1.2, -0.6);\n    \n    if (length(max(abs(plantP - vec2(0.0, 0.1)) - vec2(0.2, 0.2), 0.0)) - 0.05 < 0.0 && plantP.y < 0.3) {\n        col = vec3(0.1, 0.2, 0.6); \n        if (fract(plantP.y*20.0) < 0.2) col *= 0.8;\n    }\n    \n    if (abs(plantP.x) < 0.04 && plantP.y > 0.3 && plantP.y < 1.0) {\n        col = vec3(0.5, 0.6, 0.3); \n        if (fract(plantP.y*15.0 - iTime) < 0.2) col = vec3(0.4, 0.5, 0.2);\n    }\n    \n    if (plantP.y > 0.8) {\n        vec2 leafOrig = plantP - vec2(0.0, 0.8);\n        float r = length(leafOrig);\n        float a = atan(leafOrig.y, leafOrig.x);\n        \n        float sway = sin(iTime + a*2.0)*0.1;\n        \n        float fronds = sin(a*12.0 + sway*10.0);\n        if (r < 0.8 && r > 0.1 && fronds > 0.6) {\n            col = vec3(0.3, 0.6, 0.2);\n            if (fronds > 0.9) col += 0.1; \n        }\n    }\n}\n\nvoid layer_VinesLeaves(in vec2 p, in float iTime, inout vec3 col) {\n    if (p.y > 0.6) {\n        float vineTex = sin(p.x * 20.0 + iTime*0.5)*cos(p.y * 30.0) + sin(p.x * 50.0)*0.5;\n        if (vineTex > 0.2) col = vec3(0.2, 0.4, 0.2); \n        else if (vineTex > 0.0) col = vec3(0.4, 0.6, 0.3); \n        else {\n             col = mix(col, vec3(0.6, 0.8, 0.9), 0.5); \n        }\n    }\n}\n\nvec4 layer_Background(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 col = vec3(-1.0);\n    \n    layer_Background(p, col);\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_IronGate(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 col = vec3(-1.0);\n    \n    layer_IronGate(p, iTime, col);\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_HouseSign(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 col = vec3(-1.0);\n    \n    layer_HouseSign(p, iTime, col);\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_Plant(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 col = vec3(-1.0);\n    \n    layer_Plant(p, iTime, col);\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_VinesLeaves(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 col = vec3(-1.0);\n    \n    layer_VinesLeaves(p, iTime, col);\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n","IMG_1720":"void layer_Background(in vec2 p, inout vec3 col) {\n    col = vec3(0.85, 0.85, 0.85);\n}\n\nvoid layer_GroundPath(in vec2 p, in float iTime, inout vec3 col) {\n    if (p.y < -0.6) {\n        col = vec3(0.5, 0.35, 0.3);\n        \n        vec2 floorUV = vec2(p.x / (abs(p.y) + 0.1), 1.0 / (abs(p.y) + 0.1));\n        float brickX = fract(floorUV.x * 10.0);\n        float brickY = fract(floorUV.y * 10.0 + iTime*0.5); \n        if (brickX > 0.9 || brickY > 0.9) col = vec3(0.3, 0.2, 0.15); \n    }\n}\n\nvoid layer_RightWallColumn(in vec2 p, inout vec3 col) {\n    if (p.x > 1.2 && p.y >= -0.6) {\n        col = vec3(0.6, 0.6, 0.55);\n        vec2 stoneP = p * vec2(2.0, 5.0);\n        stoneP.x += mod(floor(stoneP.y), 2.0) * 0.5;\n        if (fract(stoneP.x) > 0.9 || fract(stoneP.y) > 0.9) col = vec3(0.4, 0.4, 0.35);\n        \n        col -= 0.1 * fract(sin(dot(p*100.0, vec2(12.9898, 78.233))) * 43758.5453);\n    }\n}\n\nvoid layer_WhiteGateStructure(in vec2 p, in float iTime, inout vec3 col) {\n    if (p.y < 0.6 && p.x <= 1.2 && p.y >= -0.6) {\n        col = vec3(0.9, 0.9, 0.9); \n        \n        vec2 gP = p;\n        gP.x = abs(gP.x) - 0.6;\n        \n        float iron = 0.0;\n        \n        if (abs(p.x) < 1.15 && abs(p.y + 0.1) < 0.6) {\n             if (abs(abs(p.x) - 1.1) < 0.03 || abs(abs(p.y + 0.1) - 0.55) < 0.03) iron = 1.0;\n             if (abs(p.x) < 0.02) iron = 1.0;\n             \n             float angle = atan(gP.y + 0.1, gP.x);\n             float dist = length(gP + vec2(0.0, 0.1));\n             \n             if (abs(dist - 0.4) < 0.02 && gP.y > -0.1) iron = 1.0;\n             if (abs(dist - 0.5) < 0.02 && gP.y > -0.1) iron = 1.0;\n             \n             float rayAnim = fract(dist - iTime*0.5);\n             if (dist < 0.5 && gP.y > -0.1 && fract(angle * 4.0) < 0.1) {\n                 iron = 1.0;\n                 if (rayAnim < 0.2) col = vec3(1.0); \n             }\n             \n             if (p.y < -0.1 && p.y > -0.5) {\n                 vec2 swirlP = fract(p * 4.0 + vec2(iTime*0.2, 0.0)) - 0.5;\n                 if (abs(length(swirlP) - 0.3) < 0.04) iron = 1.0;\n                 if (length(swirlP) < 0.08) iron = 1.0;\n             }\n        }\n        \n        if (iron > 0.0) {\n            col = vec3(0.85, 0.85, 0.85); \n            float rust = fract(sin(dot(p*80.0, vec2(12.9898, 78.233))) * 43758.5453);\n            if (rust > 0.9) col = vec3(0.6, 0.4, 0.2);\n            \n            col *= 0.9;\n        } else if (abs(p.x) < 1.15 && abs(p.y + 0.1) < 0.6) {\n            col = vec3(0.8, 0.8, 0.8);\n        }\n        \n        if (p.x > 0.1 && p.x < 0.4 && p.y > 0.45 && p.y < 0.55) {\n            col = vec3(0.9, 0.9, 0.9); \n            if (abs(p.y - 0.5) < 0.02 && p.x > 0.15 && p.x < 0.35) col = vec3(0.8, 0.1, 0.1);\n        }\n    }\n}\n\nvoid layer_TopFoliage(in vec2 p, in float iTime, inout vec3 col) {\n    if (p.y > 0.3) {\n        float leaves = sin(p.x * 15.0 + iTime)*cos(p.y * 15.0) + sin(p.x * 30.0 + p.y * 10.0 - iTime)*0.5;\n        float density = p.y + (1.0 - p.x)*0.5; \n        \n        if (leaves > -0.2 && density > 1.0) col = vec3(0.2, 0.4, 0.1); \n        if (leaves > 0.3 && density > 0.8) col = vec3(0.4, 0.6, 0.2); \n        if (leaves > 0.6 && density > 0.6) col = vec3(0.6, 0.8, 0.3); \n    }\n}\n\nvoid layer_Motorcycle(in vec2 p, inout vec3 col) {\n    vec2 scP = p - vec2(0.9, -0.4);\n    if (length(max(abs(scP) - vec2(0.35, 0.3), 0.0)) < 0.15) {\n        float mcBody = 0.0;\n        \n        if (length(scP - vec2(-0.2, 0.1)) < 0.25) mcBody = 1.0;\n        if (length(scP - vec2(-0.3, 0.4)) < 0.15) {\n            mcBody = 1.0;\n            if (length(scP - vec2(-0.4, 0.6)) < 0.05) col = vec3(0.1, 0.3, 0.2); \n        }\n        if (length(scP - vec2(0.2, 0.2)) < 0.2) mcBody = 2.0; \n        \n        if (mcBody == 1.0) {\n            col = vec3(0.6, 0.3, 0.1); \n            if (scP.y > 0.0 && scP.x < 0.0) col += 0.2;\n        } else if (mcBody == 2.0) {\n            col = vec3(0.15); \n        }\n        \n        if (length(scP - vec2(-0.1, -0.3)) < 0.15) col = vec3(0.1);\n        if (length(scP - vec2(0.3, -0.3)) < 0.15) col = vec3(0.1);\n    }\n}\n\nvec4 layer_Background(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 col = vec3(-1.0);\n    \n    layer_Background(p, col);\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_GroundPath(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 col = vec3(-1.0);\n    \n    layer_GroundPath(p, iTime, col);\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_RightWallColumn(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 col = vec3(-1.0);\n    \n    layer_RightWallColumn(p, col);\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_WhiteGateStructure(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 col = vec3(-1.0);\n    \n    layer_WhiteGateStructure(p, iTime, col);\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_TopFoliage(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 col = vec3(-1.0);\n    \n    layer_TopFoliage(p, iTime, col);\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_Motorcycle(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 col = vec3(-1.0);\n    \n    layer_Motorcycle(p, col);\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n","IMG_1721":"#define segment(p, a, b) length(p - a - (b - a) * clamp(dot(p - a, b - a) / dot(b - a, b - a), 0.0, 1.0))\n\nvoid layer_BackgroundWall(in vec2 p, inout vec3 col) {\n    col = vec3(0.9, 0.88, 0.85);\n    float siding = fract(p.y * 30.0);\n    if (siding > 0.9) col *= 0.9;\n}\n\nvoid layer_BottomStone(in vec2 p, inout vec3 col) {\n    if (p.y < -0.6) {\n        col = vec3(0.2, 0.18, 0.15); \n        vec2 stoneP = fract(p * vec2(10.0, 5.0) + vec2(sin(p.y*10.0), 0.0)) - 0.5;\n        if (length(stoneP) < 0.4) col = vec3(0.3, 0.25, 0.2);\n    }\n}\n\nvoid layer_TopTrim(in vec2 p, in float iTime, inout vec3 col) {\n    if (p.y > 0.8) {\n        col = vec3(0.1, 0.08, 0.05); \n        if (fract(p.x * 20.0 + iTime) < 0.3) col = vec3(0.05); \n    }\n}\n\nvoid layer_StrepText(in vec2 p, inout vec3 col) {\n    if (p.y > 0.5 && p.x > 0.3) {\n        vec2 tp = p - vec2(0.5, 0.55);\n        float dText = 1.0;\n        \n        dText = min(dText, segment(tp, vec2(0.05, 0.1), vec2(0.0, 0.1)));\n        dText = min(dText, segment(tp, vec2(0.0, 0.1), vec2(0.0, 0.05)));\n        dText = min(dText, segment(tp, vec2(0.0, 0.05), vec2(0.05, 0.05)));\n        dText = min(dText, segment(tp, vec2(0.05, 0.05), vec2(0.05, 0.0)));\n        dText = min(dText, segment(tp, vec2(0.05, 0.0), vec2(0.0, 0.0)));\n        \n        dText = min(dText, segment(tp, vec2(0.1, 0.15), vec2(0.1, 0.0)));\n        dText = min(dText, segment(tp, vec2(0.08, 0.08), vec2(0.12, 0.08)));\n        \n        dText = min(dText, segment(tp, vec2(0.18, 0.0), vec2(0.18, 0.1)));\n        dText = min(dText, segment(tp, vec2(0.18, 0.08), vec2(0.22, 0.1)));\n        \n        dText = min(dText, abs(length(tp - vec2(0.3, 0.05)) - 0.04));\n        dText = min(dText, segment(tp, vec2(0.26, 0.05), vec2(0.34, 0.05)));\n        \n        dText = min(dText, segment(tp, vec2(0.4, -0.1), vec2(0.4, 0.1)));\n        dText = min(dText, abs(length(tp - vec2(0.45, 0.05)) - 0.04));\n        \n        if (dText < 0.01) col = vec3(0.1);\n    }\n}\n\nvoid layer_CartoonCharacter(in vec2 p, in float iTime, inout vec3 col) {\n    vec2 cP = p - vec2(-0.3, 0.1);\n    \n    float body = segment(cP, vec2(-0.05, 0.2), vec2(-0.05, -0.3));\n    body = min(body, segment(cP, vec2(-0.05, 0.2), vec2(0.15, 0.2)));\n    body = min(body, segment(cP, vec2(0.15, 0.2), vec2(0.15, -0.3)));\n    body = min(body, segment(cP, vec2(-0.05, -0.3), vec2(0.15, -0.3)));\n    \n    if (cP.x > -0.05 && cP.x < 0.15 && cP.y > -0.3 && cP.y < 0.2) col = vec3(0.9, 0.4, 0.4); \n    if (body < 0.01) col = vec3(0.1); \n    \n    float headMask = length(max(abs(cP - vec2(0.05, 0.35)) - vec2(0.15, 0.12), 0.0));\n    if (headMask < 0.05) col = vec3(1.0, 0.8, 0.7); \n    if (abs(headMask - 0.05) < 0.01) col = vec3(0.1);\n    \n    float e1 = length(cP - vec2(0.0, 0.4));\n    if (e1 < 0.04) col = vec3(1.0);\n    if (abs(e1 - 0.04) < 0.01) col = vec3(0.1);\n    if (e1 < 0.015) col = vec3(0.1);\n    \n    vec2 eye2P = cP - vec2(0.12, 0.4);\n    eye2P.y += sin(iTime*3.0)*0.02; \n    float e2 = length(eye2P);\n    if (e2 < 0.04) col = vec3(1.0);\n    if (abs(e2 - 0.04) < 0.01) col = vec3(0.1);\n    if (e2 < 0.015) col = vec3(0.1);\n\n    if (length(cP - vec2(0.05, 0.28)) < 0.05 && cP.y < 0.28) col = vec3(0.1);\n\n    float armL = segment(cP, vec2(-0.05, 0.1), vec2(-0.25, 0.1 + sin(iTime*4.0)*0.1));\n    if (armL < 0.02) col = vec3(0.9, 0.4, 0.4);\n    if (abs(armL - 0.02) < 0.01) col = vec3(0.1);\n\n    float armR = segment(cP, vec2(0.15, 0.1), vec2(0.3, 0.15));\n    if (armR < 0.02) col = vec3(0.9, 0.4, 0.4);\n    if (abs(armR - 0.02) < 0.01) col = vec3(0.1);\n}\n\nvec4 layer_BackgroundWall(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 col = vec3(-1.0);\n    \n    layer_BackgroundWall(p, col);\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_BottomStone(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 col = vec3(-1.0);\n    \n    layer_BottomStone(p, col);\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_TopTrim(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 col = vec3(-1.0);\n    \n    layer_TopTrim(p, iTime, col);\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_StrepText(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 col = vec3(-1.0);\n    \n    layer_StrepText(p, col);\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_CartoonCharacter(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 col = vec3(-1.0);\n    \n    layer_CartoonCharacter(p, iTime, col);\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n","IMG_1725":"void layer_StorefrontStructure(inout vec3 col) {\n    col = vec3(0.2, 0.15, 0.1); \n}\n\nvoid layer_LeftDisplayPanel(in vec2 p, in float iTime, inout vec3 col) {\n    if (p.x < -0.7 && p.y < 0.8 && p.y > -0.5) {\n        col = vec3(0.85, 0.88, 0.88); \n        \n        float textAnim = sin(iTime + p.y*10.0);\n        if (abs(p.x + 0.9) < 0.05 && fract(p.y*4.0) < 0.5) {\n            col = vec3(0.7, 0.2, 0.2); \n            if (textAnim > 0.5) col *= 0.8; \n        }\n        if (abs(p.x + 0.8) < 0.05 && fract(p.y*6.0 + 0.5) < 0.5) {\n            col = vec3(0.2, 0.5, 0.3);\n            if (textAnim < -0.5) col *= 0.8;\n        }\n        if (abs(p.x + 0.85) < 0.15 && p.y > -0.4 && p.y < -0.2 && fract(p.y*20.0) < 0.4) {\n            col = vec3(0.2, 0.4, 0.6);\n        }\n        \n        if (p.x > -0.72 || p.y < -0.48 || p.y > 0.78) col = vec3(0.3, 0.2, 0.1);\n    }\n}\n\nvoid layer_UpperDisplayCase(in vec2 p, in float iTime, inout vec3 col) {\n    if (p.x > -0.5 && p.x < 1.0 && p.y > 0.05 && p.y < 0.8) {\n        col = vec3(0.5, 0.7, 0.8); \n        \n        if (p.y < 0.3) {\n            col = vec3(0.2, 0.4, 0.7);\n            float wave = sin(p.x*20.0 - iTime*3.0)*sin(p.y*50.0);\n            if (wave > 0.5) col = vec3(0.8, 0.9, 1.0); \n        }\n        \n        if (p.y > 0.3 && p.y < 0.4) {\n            float mt = sin(p.x*10.0)*0.05 + 0.35;\n            if (p.y < mt) col = vec3(0.4, 0.5, 0.4);\n        }\n        \n        vec2 hP = p - vec2(0.2, 0.4);\n        \n        float horse = length(max(abs(hP) - vec2(0.12, 0.08), 0.0));\n        if (horse < 0.05) col = vec3(0.3, 0.2, 0.15); \n        if (length(hP - vec2(0.15, 0.15)) < 0.06) col = vec3(0.3, 0.2, 0.15);\n        float legAnim = sin(iTime*8.0)*0.05;\n        if (length(hP - vec2(-0.1 + legAnim, -0.15)) < 0.03) col = vec3(0.2, 0.1, 0.05);\n        if (length(hP - vec2(0.1 - legAnim, -0.15)) < 0.03) col = vec3(0.2, 0.1, 0.05);\n        \n        if (length(hP - vec2(-0.05, 0.15)) < 0.06) col = vec3(0.7, 0.3, 0.3); \n        if (length(hP - vec2(-0.05, 0.25)) < 0.04) col = vec3(0.9, 0.8, 0.7); \n        \n        if (length(p - vec2(0.7, 0.4)) < 0.15) {\n            col = vec3(0.2, 0.5, 0.3); \n            if (abs(p.x - 0.7) < 0.02 && p.y < 0.4 && p.y > 0.3) col = vec3(0.3, 0.2, 0.1);\n        }\n        \n        if (p.x < -0.48 || p.x > 0.98 || p.y < 0.07 || p.y > 0.78) col = vec3(0.3, 0.2, 0.1);\n    }\n}\n\nvoid layer_LowerDisplayCase(in vec2 p, in float iTime, inout vec3 col) {\n    if (p.x > -0.5 && p.x < 1.0 && p.y < -0.05 && p.y > -0.7) {\n        col = vec3(0.6, 0.55, 0.5); \n        \n        if (p.y < -0.5) col = vec3(0.4, 0.35, 0.3);\n        \n        vec2 fP = p - vec2(0.2, -0.3);\n        \n        if (length(max(abs(fP - vec2(-0.1, 0.05)) - vec2(0.05, 0.1), 0.0)) < 0.02) col = vec3(0.8, 0.2, 0.3); \n        if (length(fP - vec2(-0.1, 0.2)) < 0.03) col = vec3(0.9, 0.8, 0.7); \n        \n        if (length(max(abs(fP - vec2(0.1, -0.1)) - vec2(0.08, 0.05), 0.0)) < 0.02) col = vec3(0.2, 0.5, 0.8); \n        if (length(fP - vec2(0.1, 0.0)) < 0.03) col = vec3(0.9, 0.8, 0.7); \n        \n        if (length(p - vec2(0.75, -0.45)) < 0.15) {\n            col = vec3(0.1, 0.3, 0.2); \n            if (fract(p.x*20.0 + p.y*15.0 - iTime) < 0.3) col = vec3(0.8, 0.8, 0.4); \n        }\n        \n        if (p.x < -0.48 || p.x > 0.98 || p.y < -0.68 || p.y > -0.07) col = vec3(0.3, 0.2, 0.1);\n    }\n}\n\nvoid layer_CurvyWoodenDividers(in vec2 p, inout vec3 col) {\n    if (abs(p.y - 0.0) < 0.05 && p.x > -0.5) {\n        col = vec3(0.4, 0.25, 0.15); \n        float wave = sin(p.x * 10.0)*0.02;\n        if (abs(p.y - wave) < 0.01) col = vec3(0.2, 0.1, 0.05); \n    }\n}\n\nvoid layer_LowerItems(in vec2 p, inout vec3 col) {\n    if (p.y < -0.7) {\n        if (length(max(abs(p - vec2(0.1, -0.9)) - vec2(0.15, 0.1), 0.0)) < 0.05 && p.y < -0.8) {\n            col = vec3(0.2, 0.4, 0.8);\n            if (fract((p.x + p.y)*20.0) < 0.2 || fract((p.x - p.y)*20.0) < 0.2) col *= 0.8;\n        }\n        \n        if (length(max(abs(p - vec2(0.6, -0.9)) - vec2(0.1, 0.05), 0.0)) < 0.05 && p.y < -0.85) {\n            col = vec3(0.8, 0.8, 0.8);\n            if (p.y > -0.88 && abs(p.x - 0.6) < 0.12) col = vec3(0.4);\n        }\n    }\n}\n\nvec4 layer_StorefrontStructure(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 col = vec3(-1.0);\n    \n    layer_StorefrontStructure(col);\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_LeftDisplayPanel(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 col = vec3(-1.0);\n    \n    layer_LeftDisplayPanel(p, iTime, col);\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_UpperDisplayCase(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 col = vec3(-1.0);\n    \n    layer_UpperDisplayCase(p, iTime, col);\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_LowerDisplayCase(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 col = vec3(-1.0);\n    \n    layer_LowerDisplayCase(p, iTime, col);\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_CurvyWoodenDividers(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 col = vec3(-1.0);\n    \n    layer_CurvyWoodenDividers(p, col);\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_LowerItems(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 col = vec3(-1.0);\n    \n    layer_LowerItems(p, col);\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n","IMG_1847":"void layer_Background(in vec2 p, in float iTime, inout vec3 col, out float noise) {\n    col = vec3(0.75, 0.73, 0.7);\n    noise = fract(sin(dot(p + iTime*0.01, vec2(12.9898, 78.233))) * 43758.5453);\n    col -= noise * 0.1;\n}\n\nvoid layer_CarvedSymbol(in vec2 p, in float iTime, in float noise, inout vec3 col) {\n    if (abs(p.x) < 0.7 && p.y < 0.8 && p.y > -0.7) {\n        if (abs(abs(p.x) - 0.65) < 0.05 || abs(p.y - 0.75) < 0.05 || abs(p.y + 0.65) < 0.05) {\n            col = vec3(0.3, 0.3, 0.35); \n            col -= noise * 0.15;\n            \n            if (abs(p.x) - 0.65 < 0.0 && p.x > 0.0) col += 0.05;\n            if (p.y - 0.75 < 0.0 && p.y > 0.0) col -= 0.05;\n        } \n        else {\n            col = vec3(0.8, 0.78, 0.75);\n            col -= noise * 0.05;\n            \n            vec2 sP = p; \n            float sym = 0.0;\n            \n            if (abs(sP.x) < 0.04 && sP.y > -0.45 && sP.y < 0.4) sym = 1.0;\n            \n            if (abs(length(vec2(sP.x, sP.y - 0.55)) - 0.1) < 0.03) sym = 1.0;\n            \n            if (sP.y > 0.2 && sP.y < 0.45) {\n                float bell = 0.4 - sP.y;\n                if (abs(abs(sP.x) - bell) < 0.03 && sP.y > 0.25) sym = 1.0;\n                if (abs(length(vec2(abs(sP.x) - 0.15, sP.y - 0.3)) - 0.05) < 0.02) sym = 1.0;\n            }\n            \n            if (abs(sP.y - 0.1) < 0.03 && abs(sP.x) < 0.25) sym = 1.0;\n            if (abs(abs(sP.x) - 0.15) < 0.02 && sP.y > 0.1 && sP.y < 0.2) sym = 1.0;\n            \n            if (abs(sP.y + 0.1) < 0.03 && abs(sP.x) < 0.35) sym = 1.0;\n            if (abs(abs(sP.x) - 0.25) < 0.02 && abs(sP.y + 0.1) < 0.08) sym = 1.0;\n            \n            if (sP.y < -0.2 && sP.y > -0.6) {\n                float swirlR = length(vec2(abs(sP.x) - 0.2, sP.y + 0.35));\n                if (abs(swirlR - 0.15) < 0.03 && sP.y < -0.3) sym = 1.0;\n                \n                if (abs(length(vec2(sP.x, sP.y + 0.5)) - 0.1) < 0.03 && sP.y < -0.5) sym = 1.0;\n                \n                if (abs(abs(sP.x) - 0.1) < 0.02 && sP.y < -0.25 && sP.y > -0.35) sym = 1.0;\n            }\n            \n            if (sym > 0.0) {\n                col = vec3(0.3, 0.3, 0.35); \n                \n                float highlight = sin(p.x * 5.0 + p.y * 5.0 + iTime * 2.0);\n                if (highlight > 0.8) col += 0.2 * (highlight - 0.8) * 5.0;\n                \n                col -= noise * 0.2;\n            } else {\n                vec2 shadP = p + vec2(-0.01, 0.01);\n                float shadow = 0.0;\n                if (abs(shadP.x) < 0.04 && shadP.y > -0.45 && shadP.y < 0.4) shadow = 1.0;\n                if (abs(length(vec2(shadP.x, shadP.y - 0.55)) - 0.1) < 0.03) shadow = 1.0;\n                if (abs(shadP.y - 0.1) < 0.03 && abs(shadP.x) < 0.25) shadow = 1.0;\n                if (abs(shadP.y + 0.1) < 0.03 && abs(shadP.x) < 0.35) shadow = 1.0;\n                \n                if (shadow > 0.0) col *= 0.8;\n            }\n        }\n    }\n}\n\nvoid layer_TopLouvers(in vec2 p, inout vec3 col) {\n    if (p.y > 0.85) {\n        col = vec3(0.9, 0.9, 0.9); \n        if (fract(p.x * 5.0) < 0.8 && p.y > 0.9) col = vec3(0.1, 0.1, 0.15); \n    }\n}\n\nvoid layer_BottomSurface(in vec2 p, inout vec3 col) {\n    if (p.y < -0.7) {\n       if (p.y < -0.8) {\n           col = vec3(0.3, 0.15, 0.1);\n           float speckle = fract(sin(dot(p*200.0, vec2(12.9898, 78.233))) * 43758.5453);\n           col += 0.1 * speckle;\n       } else {\n           col = vec3(0.4, 0.2, 0.15); \n       }\n    }\n}\n\nvec4 layer_Background(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 col = vec3(-1.0);\n    float noise = 0.0;\n    \n    layer_Background(p, iTime, col, noise);\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_CarvedSymbol(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 col = vec3(-1.0);\n    float noise = 0.0;\n    \n    layer_CarvedSymbol(p, iTime, noise, col);\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_TopLouvers(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 col = vec3(-1.0);\n    float noise = 0.0;\n    \n    layer_TopLouvers(p, col);\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_BottomSurface(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 col = vec3(-1.0);\n    float noise = 0.0;\n    \n    layer_BottomSurface(p, col);\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n","IMG_2148":"#define segment(p, a, b) length(p - a - (b - a) * clamp(dot(p - a, b - a) / dot(b - a, b - a), 0.0, 1.0))\n\nvoid layer_Sky(inout vec3 col) {\n    col = vec3(0.7, 0.8, 0.9); \n}\n\nvoid layer_Road(in vec2 p, in float iTime, inout vec3 col) {\n    if (p.y < -0.4) {\n        col = vec3(0.3, 0.3, 0.32); \n        float speedNoise = fract(sin(dot(floor(p * vec2(1.0, 20.0)), vec2(12.9898, 78.233))) * 43758.5453);\n        col -= speedNoise * 0.05;\n        \n        vec2 roadP = p;\n        roadP.x /= (roadP.y + 0.41); \n        float dashes = fract(roadP.x * 2.0 - iTime * 15.0);\n        if (dashes < 0.4 && abs(p.y - (-0.6)) < 0.02) col = vec3(0.9, 0.8, 0.2); \n    }\n}\n\nvoid layer_Buildings(in vec2 p, in float iTime, inout vec3 col) {\n    if (p.y > -0.4 && p.y < 0.4) {\n        float bgX = p.x + iTime * 2.0; \n        if (fract(bgX) < 0.5) {\n            col = vec3(0.8, 0.75, 0.6);\n            if (fract(bgX * 5.0) < 0.2 && fract(p.y * 5.0) < 0.3) col = vec3(0.2, 0.3, 0.4); \n        } else {\n            col = vec3(0.7, 0.6, 0.5);\n        }\n        col *= smoothstep(-0.4, -0.3, p.y);\n    }\n}\n\nvoid layer_PowerLines(in vec2 p, in float iTime, inout vec3 col) {\n    float sag1 = -0.1 + sin(p.x * 2.0 + iTime * 5.0) * 0.05;\n    float sag2 = 0.0 + cos(p.x * 3.0 + iTime * 6.0) * 0.08;\n    if (abs(p.y - sag1) < 0.003 || abs(p.y - sag2) < 0.003) col = vec3(0.1);\n}\n\nvoid layer_ScooterRider(in vec2 p, in float iTime, inout vec3 col, out float suspension, out vec2 rP) {\n    suspension = sin(iTime * 20.0) * 0.01; \n    rP = p - vec2(0.0, -0.25 + suspension);\n    \n    if (rP.x > 0.0 && rP.x < 1.0) {\n        float beamCurve = rP.x * 0.2;\n        if (abs(rP.y - 0.05) < beamCurve) {\n            col += vec3(0.9, 0.9, 0.6) * 0.3 * (1.0 - rP.x);\n        }\n    }\n    \n    for(float i=0.0; i<2.0; i++) {\n        vec2 wP = rP - vec2(-0.2 + i*0.4, -0.1);\n        float wheel = length(wP);\n        if (wheel < 0.08) {\n            col = vec3(0.1); \n            if (wheel < 0.05) {\n                col = vec3(0.6); \n                float angle = atan(wP.y, wP.x) + iTime * 30.0;\n                if (abs(sin(angle * 5.0)) < 0.2) col = vec3(0.3);\n            }\n        }\n    }\n    \n    float body = segment(rP, vec2(-0.2, 0.0), vec2(0.2, 0.0)) - 0.05;\n    body = min(body, segment(rP, vec2(0.15, 0.0), vec2(0.2, 0.1)) - 0.03); \n    if (body < 0.0) col = vec3(0.7, 0.2, 0.2); \n    \n    float torso = segment(rP, vec2(0.0, 0.0), vec2(-0.05, 0.15)) - 0.06;\n    if (torso < 0.0) col = vec3(0.1, 0.1, 0.3); \n    \n    float jacketTrail = segment(rP, vec2(-0.05, 0.05), vec2(-0.15, 0.08 + sin(iTime*30.0)*0.02)) - 0.02;\n    if (jacketTrail < 0.0) col = vec3(0.1, 0.1, 0.3);\n    \n    float helmet = length(max(abs(rP - vec2(-0.05, 0.2)) - vec2(0.04, 0.04), 0.0)) - 0.02;\n    if (helmet < 0.0) {\n        col = vec3(0.8, 0.8, 0.8);\n        if (rP.x > -0.05 && rP.y > 0.18 && rP.y < 0.22) col = vec3(0.1); \n    }\n}\n\nvoid wovenBag(vec2 bp, float rad, vec3 col1, vec3 col2, inout vec3 col) {\n    if (length(bp) < rad) {\n        float str = bp.x * 25.0 + sin(bp.y * 15.0) * 2.0;\n        vec3 bCol = mix(col1, col2, step(0.5, fract(str)));\n        bCol *= 0.7 + 0.3 * smoothstep(rad, 0.0, length(bp));\n        col = bCol;\n    }\n}\n\nvoid layer_Bundles(in vec2 p, in float iTime, in float suspension, in vec2 rP, inout vec3 col) {\n    float bundleLag = sin(iTime * 20.0 - 1.0) * 0.02; \n    vec2 bP = p - vec2(-0.2, 0.1 + suspension + bundleLag);\n    \n    wovenBag((bP - vec2(0.0, 0.0)), 0.2, vec3(0.8, 0.3, 0.3), vec3(0.2, 0.4, 0.8), col); \n    wovenBag((bP - vec2(-0.05, 0.3)), 0.18, vec3(0.3, 0.6, 0.3), vec3(0.8, 0.8, 0.2), col); \n    wovenBag((bP - vec2(0.05, 0.5)), 0.15, vec3(0.8), vec3(0.2, 0.2, 0.8), col); \n    \n    float ropes = min(abs(bP.x), abs(bP.x - 0.05));\n    if (ropes < 0.005 && bP.y > -0.2 && bP.y < 0.6) col = vec3(0.9, 0.8, 0.6); \n    \n    float exX = rP.x + 0.25;\n    float exY = rP.y + 0.05;\n    float smokeId = floor((exX - iTime*2.0) * 10.0);\n    float smokeYOffset = sin(smokeId * 45.2) * 0.1;\n    vec2 smP = vec2(fract((exX - iTime*2.0) * 10.0) - 0.5, exY + smokeYOffset + exX*0.1); \n    if (exX < 0.0 && exX > -0.5 && length(smP) < 0.2 * abs(exX)) {\n        col += vec3(0.5) * exp(-length(smP)*20.0);\n    }\n}\n\nvec4 layer_Sky(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 col = vec3(-1.0);\n    float suspension = 0.0;\n    vec2 rP = vec2(0.0);\n    \n    layer_Sky(col);\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_Road(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 col = vec3(-1.0);\n    float suspension = 0.0;\n    vec2 rP = vec2(0.0);\n    \n    layer_Road(p, iTime, col);\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_Buildings(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 col = vec3(-1.0);\n    float suspension = 0.0;\n    vec2 rP = vec2(0.0);\n    \n    layer_Buildings(p, iTime, col);\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_PowerLines(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 col = vec3(-1.0);\n    float suspension = 0.0;\n    vec2 rP = vec2(0.0);\n    \n    layer_PowerLines(p, iTime, col);\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_ScooterRider(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 col = vec3(-1.0);\n    float suspension = 0.0;\n    vec2 rP = vec2(0.0);\n    \n    layer_ScooterRider(p, iTime, col, suspension, rP);\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_Bundles(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 col = vec3(-1.0);\n    float suspension = 0.0;\n    vec2 rP = vec2(0.0);\n    \n    layer_Bundles(p, iTime, suspension, rP, col);\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n","IMG_2249":"void layer_Ceiling(in vec2 p, inout vec3 col) {\n    if (p.y > 0.6 - p.x*p.x*0.1) {\n        col = vec3(0.8, 0.8, 0.8); \n        float noise = fract(sin(dot(p, vec2(12.9898, 78.233))) * 43758.5453);\n        col -= noise * 0.1;\n    } \n}\n\nvoid layer_LatticeStructure(in vec2 p, in float iTime, inout vec3 col) {\n    if (!(p.y > 0.6 - p.x*p.x*0.1) && !(p.x < -0.8 || p.x > 0.8)) {\n        vec2 gridP = p;\n        \n        float warp = 1.0 - (gridP.y * 0.2); \n        gridP.x /= warp;\n        gridP.y = gridP.y * 1.2 - iTime*0.1; \n        \n        col = vec3(0.5, 0.6, 0.7); \n        \n        float numCols = 15.0;\n        float numRows = 20.0;\n        \n        vec2 cell = fract(gridP * vec2(numCols, numRows));\n        vec2 cellId = floor(gridP * vec2(numCols, numRows));\n        \n        vec3 brickCol = vec3(0.7, 0.3, 0.2);\n        brickCol *= 0.8 + 0.2 * sin(cellId.x * 12.3 + cellId.y * 45.6); \n        \n        float isBrick = 0.0;\n        \n        if (cell.x < 0.08 || cell.x > 0.92 || cell.y < 0.08 || cell.y > 0.92) {\n            col = vec3(0.8); \n        } else {\n            isBrick = 1.0;\n            vec2 holeP = cell - vec2(0.5);\n            float holeDist = max(abs(holeP.x) - 0.3, abs(holeP.y) - 0.2);\n            holeDist = max(holeDist, abs(holeP.x) + abs(holeP.y) - 0.45); \n            \n            if (holeDist < 0.0) {\n                isBrick = 0.0; \n                col = vec3(0.5, 0.6, 0.7);\n                if (sin(gridP.x*5.0 + gridP.y*5.0) > 0.5) col += 0.2;\n            }\n        }\n        \n        if (isBrick > 0.0) {\n            col = brickCol;\n            if (cell.y > 0.8) col *= 1.2; \n            if (cell.y < 0.2) col *= 0.7; \n            if (cell.x > 0.8) col *= 0.8; \n            \n            float light = sin(p.x * 3.0 + p.y * 3.0 - iTime * 2.0);\n            if (light > 0.8) col += 0.2;\n        }\n    }\n}\n\nvoid layer_SideWalls(in vec2 p, inout vec3 col) {\n    if (!(p.y > 0.6 - p.x*p.x*0.1) && (p.x < -0.8 || p.x > 0.8)) {\n        col = vec3(0.7, 0.7, 0.7); \n        col -= fract(sin(dot(p, vec2(12.9898, 78.233))) * 43758.5453) * 0.1;\n    }\n}\n\nvec4 layer_Ceiling(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 col = vec3(-1.0);\n    \n    layer_Ceiling(p, col);\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_LatticeStructure(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 col = vec3(-1.0);\n    \n    layer_LatticeStructure(p, iTime, col);\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_SideWalls(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 col = vec3(-1.0);\n    \n    layer_SideWalls(p, col);\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n","IMG_2250":"void layer_Ceiling(in vec2 p, inout vec3 col) {\n    if (p.y > 0.7) {\n        col = vec3(0.8, 0.8, 0.8); \n    }\n}\n\nvoid layer_StoneBase(in vec2 p, inout vec3 col) {\n    if (p.y < -0.4 && !(p.y > 0.7)) {\n        col = vec3(0.6, 0.6, 0.55); \n        \n        vec2 grid = floor(p * vec2(8.0, 10.0));\n        vec2 f = fract(p * vec2(8.0, 10.0));\n        if (f.x < 0.05 || f.y < 0.05) col = vec3(0.3); \n        \n        float blockVal = fract(sin(dot(grid, vec2(12.9898, 78.233))) * 43758.5453);\n        col += (blockVal - 0.5) * 0.1; \n        \n        if (p.y > -0.43 && p.y < -0.4) col = vec3(0.9);\n        \n        if (p.y < -0.8) {\n            col = vec3(0.7); \n            if (abs(p.x) < 0.02) col *= 0.8; \n        }\n    }\n}\n\nvoid layer_LatticeWall(in vec2 p, in float iTime, inout vec3 col) {\n    if (!(p.y < -0.4) && !(p.y > 0.7)) {\n        vec2 gridP = p;\n        float curve = cos(p.x * 1.5) * 0.1;\n        gridP.y += curve; \n        \n        col = vec3(0.6, 0.7, 0.8);\n        \n        float numCols = 12.0;\n        float numRows = 12.0;\n        \n        float row = floor((gridP.y + 0.4) * numRows);\n        if (mod(row, 2.0) == 0.0) gridP.x += 0.5 / numCols;\n        \n        vec2 cell = fract(vec2(gridP.x, gridP.y + 0.4) * vec2(numCols, numRows));\n        vec2 cellId = floor(vec2(gridP.x, gridP.y + 0.4) * vec2(numCols, numRows));\n        \n        vec3 brickCol = vec3(0.75, 0.35, 0.25); \n        brickCol *= 0.8 + 0.2 * fract(sin(dot(cellId, vec2(12.9898, 78.233))) * 43758.5453);\n        \n        float isBrick = 0.0;\n        \n        if (cell.x < 0.05 || cell.x > 0.95 || cell.y < 0.05 || cell.y > 0.95) {\n            if (cell.y < 0.05 || cell.y > 0.95) {\n                col = vec3(0.85); \n            } else {\n                col = vec3(0.7); \n            }\n        } \n        else {\n            isBrick = 1.0;\n            vec2 holeP = cell - vec2(0.5);\n            float holeDist = max(abs(holeP.x) - 0.35, abs(holeP.y) - 0.25);\n            holeDist = max(holeDist, abs(holeP.x) + abs(holeP.y) - 0.5); \n            \n            if (holeDist < 0.0) {\n                isBrick = 0.0; \n                col = vec3(0.6, 0.7, 0.8);\n                float bgAnim = sin(p.x*10.0 + iTime) * cos(p.y*10.0);\n                if (bgAnim > 0.5) col = vec3(0.9); \n                if (holeP.y > 0.15 || holeP.x > 0.25) col *= 0.5; \n            }\n        }\n        \n        if (isBrick > 0.0) {\n            col = brickCol;\n            if (cell.y > 0.8) col *= 1.2; \n            if (cell.y < 0.2) col *= 0.7; \n            if (cell.x > 0.8) col *= 0.8;\n            \n            col *= 1.0 - abs(p.x) * 0.3;\n        }\n    }\n}\n\nvoid layer_SidePillars(in vec2 p, inout vec3 col) {\n    if (abs(p.x) > 0.9) {\n        col = vec3(0.65, 0.65, 0.65);\n        if (p.x > 0.9 && p.y < -0.4) {\n            if (abs(fract((p.x - 0.9) * 15.0) - 0.5) < 0.2) col = vec3(0.2, 0.4, 0.7); \n            if (abs(p.y - (-0.45)) < 0.02) col = vec3(0.2, 0.4, 0.7); \n        }\n    }\n}\n\nvec4 layer_Ceiling(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 col = vec3(-1.0);\n    \n    layer_Ceiling(p, col);\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_StoneBase(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 col = vec3(-1.0);\n    \n    layer_StoneBase(p, col);\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_LatticeWall(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 col = vec3(-1.0);\n    \n    layer_LatticeWall(p, iTime, col);\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_SidePillars(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 col = vec3(-1.0);\n    \n    layer_SidePillars(p, col);\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n","IMG_2263":"void layer_Background(in vec2 p, in float iTime, inout vec3 col) {\n    if (p.y > 0.5) {\n        col = vec3(0.6, 0.7, 0.8); \n        float tree = sin(p.x*10.0)*0.1 + cos(p.x*20.0)*0.05 + 0.6;\n        if (p.y < tree) col = vec3(0.2, 0.4, 0.2); \n    } else {\n        col = vec3(0.2, 0.4, 0.2); \n        float leafAnim = sin(p.x * 20.0 + iTime*2.0) * cos(p.y * 15.0 + iTime);\n        if (leafAnim > 0.5) col = vec3(0.3, 0.5, 0.2);\n    }\n}\n\nvoid layer_Ground(in vec2 p, inout vec3 col) {\n    if (p.y < -0.6) {\n        col = vec3(0.4, 0.4, 0.4); \n    }\n}\n\nvoid layer_BluePillar(in vec2 p, inout vec3 col) {\n    if (p.x > -0.8 && p.x < -0.4 && p.y > -0.8) {\n        col = vec3(0.3, 0.65, 0.85); \n        \n        vec2 tile = fract(p * vec2(8.0, 20.0));\n        if (tile.x < 0.05 || tile.y < 0.05) col *= 0.8; \n        \n        if (p.y > 0.6) {\n            col = vec3(0.2, 0.7, 0.5); \n            if (fract((p.y - 0.6) * 15.0) < 0.4) col = vec3(0.1); \n        }\n        \n        if (p.x < -0.45 && p.y > -0.2 && p.y < 0.3) {\n            col = vec3(0.95); \n            if (fract(p.y * 25.0) < 0.3 && abs(p.x + 0.62) < 0.14) col = vec3(0.1); \n        }\n    }\n}\n\nvoid layer_BlueGateStructure(in vec2 p, in float iTime, inout vec3 col) {\n    vec3 gateCol = vec3(0.3, 0.7, 0.85); \n    if (p.x > -0.4 && p.y > -0.8) {\n        if (p.y < -0.45) {\n            col = gateCol;\n            if (abs(p.y + 0.45) < 0.02 || abs(p.y + 0.75) < 0.02) col *= 0.8;\n            if (length(vec2(p.x, p.y + 0.78)) < 0.04) col = vec3(0.4); \n        } else {\n            if (abs(p.y - 0.8 + (p.x+0.4)*0.2) < 0.02) col = gateCol; \n            if (abs(p.x + 0.38) < 0.02) col = gateCol; \n            if (abs(p.y + 0.45) < 0.02) col = gateCol; \n            \n            vec2 gP = vec2(p.x + 0.4, p.y + 0.45); \n            \n            if (gP.x > 0.0 && gP.y > 0.0 && gP.y < 1.25 - gP.x*0.2) {\n                vec2 grid = fract(gP * 10.0);\n                vec2 cell = floor(gP * 10.0);\n                \n                vec2 center = vec2(0.5);\n                float dist = length(grid - center);\n                \n                if (abs(dist - 0.4) < 0.05) {\n                    col = gateCol;\n                    float shine = sin(cell.x*0.5 + cell.y*0.5 - iTime*3.0);\n                    if (shine > 0.8) col = vec3(0.8, 0.9, 1.0);\n                }\n                \n                if ((abs(grid.x - 0.5) < 0.02 && (grid.y < 0.1 || grid.y > 0.9)) ||\n                    (abs(grid.y - 0.5) < 0.02 && (grid.x < 0.1 || grid.x > 0.9))) {\n                    col = gateCol;\n                }\n            }\n        }\n    }\n}\n\nvoid layer_Fence(in vec2 p, inout vec3 col) {\n    if (p.x < -0.8 && p.y > -0.5) {\n        if (fract(p.x * 10.0) < 0.1) col = vec3(0.5, 0.5, 0.4);\n        if (fract(p.x * 20.0 + p.y * 20.0) < 0.05 || fract(p.x * 20.0 - p.y * 20.0) < 0.05) col = vec3(0.4);\n    }\n}\n\nvec4 layer_Background(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 col = vec3(-1.0);\n    \n    layer_Background(p, iTime, col);\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_Ground(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 col = vec3(-1.0);\n    \n    layer_Ground(p, col);\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_BluePillar(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 col = vec3(-1.0);\n    \n    layer_BluePillar(p, col);\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_BlueGateStructure(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 col = vec3(-1.0);\n    \n    layer_BlueGateStructure(p, iTime, col);\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_Fence(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 col = vec3(-1.0);\n    \n    layer_Fence(p, col);\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n","IMG_2450":"void layer_Background(in vec2 p, inout vec3 col) {\n    col = vec3(0.92, 0.9, 0.88); \n    float paper = fract(sin(dot(p, vec2(12.9898, 78.233))) * 43758.5453);\n    col -= paper * 0.03;\n}\n\nvoid layer_HeadStructure(in vec2 p, in float iTime, inout vec3 col, out float headMask) {\n    vec2 fP = p;\n    headMask = length(vec2(fP.x * 0.9, fP.y * 1.0)) - 0.65;\n    \n    if (headMask < 0.0) {\n        col = vec3(0.94, 0.92, 0.9); \n        \n        if (fP.y > 0.5 && fract(fP.x*10.0 + sin(fP.y*20.0)) < 0.1) col = vec3(0.2);\n        \n        if (fP.x < -0.1 && fP.y > 0.0) {\n            float veinDist = sin(fP.x * 30.0 + sin(fP.y * 40.0)) * cos(fP.y * 30.0);\n            float pulse = sin(iTime*4.0 - length(fP)*10.0);\n            if (abs(veinDist) < 0.15) {\n                col = vec3(0.8, 0.3, 0.3); \n                if (pulse > 0.5) col = vec3(0.9, 0.4, 0.4); \n            }\n        }\n        \n        float h1 = length(vec2(fP.x + 0.1, fP.y - 0.2)) - 0.18; \n        float h2 = length(vec2(fP.x - 0.1, fP.y + 0.3)) - 0.2; \n        float h3 = length(vec2(fP.x + 0.3, fP.y + 0.15)) - 0.2; \n        float h4 = length(vec2(fP.x + 0.4, fP.y - 0.1)) - 0.15; \n        \n        float t1 = length(vec2(fP.x, fP.y)) - 0.12; \n        \n        float heart = min(min(min(h1, h2), min(h3, h4)), t1);\n        \n        if (heart < 0.0 && fP.y < 0.5 && fP.x > -0.3) {\n            col = vec3(0.8, 0.45, 0.25); \n            \n            float beat = sin(iTime*5.0);\n            if (beat > 0.8) col = vec3(0.85, 0.5, 0.3);\n            \n            if (h2 < 0.0 && fract(fP.x * 20.0 + fP.y * 20.0) < 0.2) col = vec3(0.9);\n            if (h3 < 0.0 && fract(fP.x * 20.0 - fP.y * 20.0) < 0.2) col = vec3(0.9);\n            \n            if (abs(heart) < 0.015) col = vec3(0.1);\n        }\n        \n        if (abs(length(vec2(fP.x - 0.5, fP.y - 0.3)) - 0.15) < 0.01) col = vec3(0.2);\n    }\n    \n    if (abs(headMask) < 0.01) col = vec3(0.2);\n}\n\nvoid layer_LabelTexts(in vec2 p, inout vec3 col) {\n    vec2 fP = p;\n    \n    if (abs(length(vec2(fP.x - 0.2, fP.y - 0.6)) - 0.2) < 0.01 && fP.x > 0.0 && fP.y > 0.5) col = vec3(0.1, 0.2, 0.6); \n    if (abs(length(vec2(fP.x - 0.4, fP.y - 0.5)) - 0.1) < 0.01 && fP.x > 0.3 && fP.y > 0.4) col = vec3(0.1, 0.2, 0.6);\n    \n    if (fP.y > 0.4 && fP.x < -0.3) { \n        if (sin(fP.x * 40.0 + fP.y * 10.0) * cos(fP.y * 40.0) > 0.7) col = vec3(0.1, 0.2, 0.6);\n    }\n    if (fP.y < -0.5 && fP.x > 0.1 && fP.x < 0.4) { \n        if (sin(fP.x * 50.0) * cos(fP.y * 50.0) > 0.7) col = vec3(0.1, 0.2, 0.6);\n    }\n    \n    if (fP.x > 0.6) {\n        if (abs(fP.x - 0.7) < 0.05 && fP.y > -0.4 && fP.y < 0.4) {\n            if (fract(fP.y * 20.0) < 0.3) col = vec3(0.2); \n        }\n        if (abs(fP.x - 0.8) < 0.02 && fP.y > 0.0 && fP.y < 0.4) {\n            if (fract(fP.y * 15.0) < 0.3) col = vec3(0.2);\n        }\n    }\n}\n\nvec4 layer_Background(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 col = vec3(-1.0);\n    float headMask = 0.0;\n    \n    layer_Background(p, col);\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_HeadStructure(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 col = vec3(-1.0);\n    float headMask = 0.0;\n    \n    layer_HeadStructure(p, iTime, col, headMask);\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_LabelTexts(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 col = vec3(-1.0);\n    float headMask = 0.0;\n    \n    layer_LabelTexts(p, col);\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n","IMG_2477":"void layer_Background(in vec2 p, inout vec3 col) {\n    col = vec3(0.98, 0.96, 0.9); \n    col -= fract(sin(dot(p, vec2(12.9898, 78.233))) * 43758.5453) * 0.05;\n}\n\nvoid layer_TopCertificate(in vec2 p, in float iTime, inout vec3 col) {\n    if (p.y > 0.1 && p.y < 0.9 && abs(p.x) < 0.8) {\n        float borderDist = max(abs(p.x) - 0.75, abs(p.y - 0.5) - 0.35);\n        if (borderDist > 0.0 && borderDist < 0.05) {\n            col = vec3(0.8, 0.2, 0.15); \n            if (fract(p.x * 30.0 + p.y * 30.0 + iTime) < 0.3) col *= 0.8;\n            if (fract(p.x * 40.0 - p.y * 40.0 - iTime) < 0.3) col *= 0.9;\n        } else if (borderDist <= 0.0) {\n            if (abs(p.y - 0.7) < 0.04 && abs(p.x) < 0.3) {\n                if (fract(p.x * 10.0) < 0.7) col = vec3(0.8, 0.2, 0.15); \n            }\n            \n            if (p.y > 0.35 && p.y < 0.45 && abs(p.x) < 0.4) {\n                float stampX = fract((p.x + 0.4 + iTime*0.05) * 1.25 * 3.0); \n                if (stampX < 0.5 && p.y > 0.35 && p.y < 0.43) {\n                    float pulse = 1.0 + sin(iTime*10.0 + p.x*10.0)*0.1;\n                    vec2 sP = vec2((stampX - 0.25)/pulse, (p.y - 0.39)/pulse);\n                    \n                    if (abs(sP.x) < 0.2 && abs(sP.y) < 0.03) {\n                        col = vec3(0.8, 0.2, 0.15);\n                        if (fract(sP.x * 40.0 + sP.y * 40.0) < 0.2) col = vec3(0.98, 0.96, 0.9);\n                    }\n                }\n            }\n            \n            if (abs(p.y - 0.25) < 0.02 && abs(p.x) < 0.4) {\n                float draw = smoothstep(-0.4, 0.4, sin(iTime*2.0 + p.x));\n                if (draw > 0.5) col = vec3(0.2, 0.2, 0.4); \n            }\n        }\n    }\n}\n\nvoid layer_BottomCertificate(in vec2 p, in float iTime, inout vec3 col) {\n    if (p.y < -0.1 && p.y > -0.9 && abs(p.x) < 0.8) {\n        float borderDist = max(abs(p.x) - 0.75, abs(p.y + 0.5) - 0.35);\n        if (borderDist > 0.0 && borderDist < 0.05) {\n            col = vec3(0.2, 0.6, 0.3); \n            if (sin(p.x * 40.0 + iTime*2.0) * cos(p.y * 40.0 + iTime) > 0.0) col = vec3(0.1, 0.5, 0.2);\n            \n        } else if (borderDist <= 0.0) {\n            if (abs(p.x - 0.5) < 0.08 && abs(p.y + 0.4) < 0.1) {\n                col = vec3(0.8, 0.6, 0.5); \n                if (p.y > -0.35) col = vec3(0.2); \n                if (p.y < -0.45) col = vec3(0.4, 0.5, 0.8); \n            }\n            \n            if (abs(p.y + 0.3) < 0.02 && abs(p.x) < 0.4) {\n                if (fract(p.x * 12.0) < 0.5) col = vec3(0.1); \n            }\n            \n            if (p.y > -0.8 && p.y < -0.7 && abs(p.x) < 0.6) {\n                float sigNoise = sin(p.x * 40.0 + iTime*5.0) * 0.02;\n                if (abs(p.y + 0.75 - sigNoise) < 0.005) col = vec3(0.1); \n            }\n        }\n    }\n}\n\nvec4 layer_Background(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 col = vec3(-1.0);\n    \n    layer_Background(p, col);\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_TopCertificate(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 col = vec3(-1.0);\n    \n    layer_TopCertificate(p, iTime, col);\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_BottomCertificate(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 col = vec3(-1.0);\n    \n    layer_BottomCertificate(p, iTime, col);\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n","IMG_2586":"void layer_Background(in vec2 p, in float iTime, inout vec3 col) {\n    if (p.y > 0.2) {\n        if (p.x > -0.5 && p.x < 0.6) {\n            col = vec3(0.25, 0.25, 0.3); \n            if (fract((p.x + 0.5) * 15.0) < 0.2) col = vec3(0.15); \n            if (fract(p.x * 10.0 + p.y * 10.0) < 0.1 || fract(p.x * 10.0 - p.y * 10.0) < 0.1) col = vec3(0.2); \n        } else {\n            col = vec3(0.8, 0.7, 0.4); \n            if (abs(p.x + 0.55) < 0.05 || abs(p.x - 0.65) < 0.05) col = vec3(0.85); \n        }\n    } else {\n        col = vec3(0.6, 0.3, 0.2); \n        \n        float floorDepth = 0.1 - p.y;\n        vec2 groundP = vec2(p.x / floorDepth, 1.0 / floorDepth);\n        if (p.y < 0.1) {\n            groundP.x += iTime * 0.2;\n            groundP.y -= iTime * 0.5;\n            \n            vec2 brickGrid = fract(groundP * vec2(2.0, 5.0));\n            if (fract(groundP.y * 5.0) > 0.5) brickGrid.x = fract(groundP.x * 2.0 + 0.5); \n            \n            if (brickGrid.x < 0.05 || brickGrid.y < 0.05) col = vec3(0.4, 0.2, 0.15); \n            \n            float noise = fract(sin(dot(floor(groundP * vec2(2.0, 5.0)), vec2(12.9898, 78.233))) * 43758.5453);\n            col += (noise - 0.5) * 0.1;\n        }\n    }\n}\n\nvoid layer_TableBenches(in vec2 p, in float iTime, inout vec3 col) {\n    vec2 tP = p - vec2(0.0, 0.0);\n    vec2 tableUV = vec2(tP.x + tP.y * 2.0, tP.x - tP.y * 2.0) * 1.5;\n    \n    vec2 lbP = p - vec2(-0.8, -0.2);\n    vec2 lbUV = vec2(lbP.x + lbP.y * 2.0, lbP.x - lbP.y * 2.0) * 1.8;\n    \n    vec2 rbP = p - vec2(0.8, -0.4);\n    vec2 rbUV = vec2(rbP.x + rbP.y * 2.0, rbP.x - rbP.y * 2.0) * 1.8;\n    \n    vec2 fbP = p - vec2(0.2, -0.5);\n    vec2 fbUV = vec2(fbP.x + fbP.y * 2.0, fbP.x - fbP.y * 2.0) * 1.5;\n\n    float isTable = 0.0;\n    \n    if (tableUV.x > -1.0 && tableUV.x < 1.0 && tableUV.y > -1.0 && tableUV.y < 1.0) {\n        if (abs(tableUV.x) < 0.3 && abs(tableUV.y) < 0.3) {\n            vec2 cbGrid = fract(tableUV * 15.0);\n            if ((cbGrid.x > 0.5 && cbGrid.y > 0.5) || (cbGrid.x < 0.5 && cbGrid.y < 0.5)) {\n                col = vec3(0.1, 0.3, 0.15); \n            } else {\n                col = vec3(0.9, 0.9, 0.9); \n            }\n        } else {\n            vec2 tGrid = fract(tableUV * 4.0);\n            col = vec3(0.6, 0.75, 0.85); \n            if (abs(tGrid.x - 0.5) + abs(tGrid.y - 0.5) < 0.4) col = vec3(0.85, 0.9, 0.95); \n            \n            if (tGrid.x < 0.02 || tGrid.y < 0.02) col = vec3(0.3);\n            \n            float shine = sin(tableUV.x*5.0 + tableUV.y*5.0 - iTime*3.0);\n            if (shine > 0.9) col += vec3(0.2);\n        }\n        isTable = 1.0;\n    }\n    \n    if (isTable == 0.0 && tableUV.x < 1.0 && tableUV.y > -1.0 && tP.y < 0.0 && tP.y > -0.05) {\n        if (tP.x + tP.y * 2.0 > -1.0 && tP.x - tP.y * 2.0 < 1.0) {\n            col = vec3(0.5); \n        }\n    }\n    \n    if (p.x > -0.15 && p.x < 0.15 && p.y < 0.0 && p.y > -0.4) {\n        col = vec3(0.6); \n        if (fract(p.x * 15.0) < 0.2) col *= 0.8; \n        col *= 1.0 - (0.0 - p.y) * 0.5; \n    }\n    \n    if (fbUV.x > -0.8 && fbUV.x < 0.8 && fbUV.y > -0.3 && fbUV.y < 0.3) {\n        vec2 tGrid = fract(fbUV * 4.0);\n        col = vec3(0.6, 0.75, 0.85);\n        if (abs(tGrid.x - 0.5) + abs(tGrid.y - 0.5) < 0.4) col = vec3(0.85, 0.9, 0.95);\n        if (tGrid.x < 0.02 || tGrid.y < 0.02) col = vec3(0.3);\n        \n        float shine = sin(fbUV.x*5.0 + fbUV.y*5.0 - iTime*3.0);\n        if (shine > 0.9) col += vec3(0.2);\n    }\n    if (abs(fbP.x - 0.3) < 0.08 && fbP.y < -0.05 && fbP.y > -0.3) col = vec3(0.55); \n    if (abs(fbP.x + 0.3) < 0.08 && fbP.y < -0.05 && fbP.y > -0.3) col = vec3(0.55); \n}\n\nvec4 layer_Background(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 col = vec3(-1.0);\n    \n    layer_Background(p, iTime, col);\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_TableBenches(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 col = vec3(-1.0);\n    \n    layer_TableBenches(p, iTime, col);\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n","IMG_2700":"void layer_Background(in vec2 p, in float iTime, inout vec3 col) {\n    col = vec3(0.05); \n    \n    if (p.y > 0.5 && p.x > -0.2) {\n        float treeDist = abs(p.y - 0.8 + (p.x - 0.2)*0.5);\n        if (treeDist < 0.15) {\n            col = vec3(0.2, 0.15, 0.1); \n            col *= 1.0 + fract(sin(p.x * 20.0 + p.y * 30.0) * 10.0) * 0.2;\n        }\n    }\n    \n    if (p.x < -0.5) {\n        float light1 = length(vec2(p.x + 0.8, p.y + 0.4));\n        float flicker1 = 0.5 + 0.5 * sin(iTime * 10.0);\n        if (light1 < 0.2) col += vec3(0.8, 0.8, 0.9) * (0.2 - light1) * 5.0 * flicker1; \n        \n        float light2 = length(vec2(p.x + 0.9, p.y + 0.2));\n        float flicker2 = 0.5 + 0.5 * sin(iTime * 8.0 + 1.0);\n        if (light2 < 0.1) col += vec3(0.9, 0.9, 0.8) * (0.1 - light2) * 10.0 * flicker2;\n    }\n}\n\nvoid layer_TrashBin(in vec2 p, inout vec3 col) {\n    if (p.x > 0.2 && p.x < 0.8 && p.y > 0.5 && p.y < 0.9) {\n        col = vec3(0.1, 0.4, 0.2); \n        if (p.y > 0.85) col = vec3(0.15, 0.5, 0.25); \n        if (fract(p.y * 15.0) < 0.1) col *= 0.8; \n    }\n}\n\nvoid layer_MainSignboard(in vec2 p, in float iTime, inout vec3 col) {\n    if (p.x > -0.6 && p.x < 0.5 && p.y > -0.6 && p.y < 0.5) {\n        col = vec3(0.9, 0.88, 0.75); \n        \n        float edgeDist = max(abs(p.x + 0.05) * 1.8, abs(p.y) * 2.0);\n        col *= 1.0 - smoothstep(0.8, 1.0, edgeDist) * 0.3;\n        \n        vec3 textColor = vec3(0.1, 0.5, 0.3); \n        \n        if (p.x > -0.5 && p.x < -0.2 && p.y > -0.4 && p.y < 0.4) {\n            vec2 letGrid = fract(vec2(p.x * 5.0, p.y * 5.0));\n            if (letGrid.x > 0.2 && letGrid.x < 0.8 && letGrid.y > 0.2 && letGrid.y < 0.8) {\n                if (sin(p.y * 20.0) * cos(p.x * 10.0) > 0.0) col = textColor;\n            }\n            if (abs(p.x + 0.35) < 0.1 && fract(p.y * 6.0) < 0.6) col = textColor;\n            \n            float pulse = 0.5 + 0.5 * sin(iTime*4.0 - p.y*10.0);\n            if (col == textColor) col += vec3(0.1, 0.3, 0.1) * pulse;\n        }\n        \n        vec2 ovalCenter = vec2(-0.05, 0.0);\n        float ovalDist = length(vec2((p.x - ovalCenter.x) * 1.5, (p.y - ovalCenter.y) * 1.0)) - 0.35;\n        \n        if (abs(ovalDist) < 0.01) col = textColor;\n        if (abs(ovalDist + 0.03) < 0.005) col = textColor;\n        \n        if (ovalDist < -0.05) {\n            float cursive1 = sin((p.y + p.x) * 30.0 + iTime*2.0) * 0.05; \n            if (abs(p.x - ovalCenter.x + 0.1 - cursive1) < 0.02 && p.y > -0.2 && p.y < 0.2) col = textColor;\n            \n            float cursive2 = sin((p.y - p.x) * 25.0 - iTime*1.5) * 0.05;\n            if (abs(p.x - ovalCenter.x - 0.1 - cursive2) < 0.02 && p.y > -0.15 && p.y < 0.25) col = textColor;\n        }\n        \n        if (p.x > 0.2 && p.x < 0.45 && p.y > -0.4 && p.y < 0.4) {\n            if (abs(p.x - 0.3) < 0.08 && fract(p.y * 5.0) < 0.7) col = textColor; \n            if (abs(p.x - 0.4) < 0.05 && fract(p.y * 15.0) < 0.5) col = textColor; \n            \n            float pulse = 0.5 + 0.5 * sin(iTime*4.0 - p.y*10.0 + 3.14);\n            if (col == textColor) col += vec3(0.1, 0.3, 0.1) * pulse;\n        }\n    }\n}\n\nvoid layer_SignFrameCart(in vec2 p, in float iTime, inout vec3 col) {\n    if (p.x > -0.65 && p.x <= -0.6 && p.y > -0.6 && p.y < 0.5) col = vec3(0.4); \n    if (p.x >= 0.5 && p.x < 0.55 && p.y > -0.6 && p.y < 0.5) col = vec3(0.4); \n    \n    if (p.x > 0.5 && p.y < 0.0) {\n        col = vec3(0.15); \n        float wheelRadius = 0.15;\n        vec2 wheelCenter = vec2(0.7, -0.4);\n        float wDist = length(vec2(p.x - wheelCenter.x, p.y - wheelCenter.y));\n        if (abs(wDist - wheelRadius) < 0.02) col = vec3(0.2); \n        if (wDist < wheelRadius) {\n            float angle = atan(p.y - wheelCenter.y, p.x - wheelCenter.x);\n            if (fract((angle + iTime) * 4.0) < 0.1) col = vec3(0.3);\n        }\n    }\n}\n\nvec4 layer_Background(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 col = vec3(-1.0);\n    \n    layer_Background(p, iTime, col);\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_TrashBin(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 col = vec3(-1.0);\n    \n    layer_TrashBin(p, col);\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_MainSignboard(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 col = vec3(-1.0);\n    \n    layer_MainSignboard(p, iTime, col);\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_SignFrameCart(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 col = vec3(-1.0);\n    \n    layer_SignFrameCart(p, iTime, col);\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n","IMG_2703":"void layer_Background(in vec2 uv, inout vec3 col) {\n    col = vec3(0.98); \n    if (fract(uv.x * 20.0) < 0.05 || fract(uv.y * 15.0) < 0.05) col = vec3(0.9);\n}\n\nvoid layer_FibonacciTrendLines(in vec2 p, in float iTime, inout vec3 col) {\n    for (int i = 0; i < 40; i++) {\n        float fi = float(i);\n        vec2 p1 = vec2(sin(fi * 1.3 + iTime*0.1) * 2.0, cos(fi * 1.7 - iTime*0.05) * 2.0);\n        vec2 p2 = vec2(sin(fi * 2.1 - iTime*0.15) * 2.0, cos(fi * 2.5 + iTime*0.2) * 2.0);\n        \n        vec2 pa = p - p1, ba = p2 - p1;\n        float h = clamp(dot(pa, ba) / dot(ba, ba), 0.0, 1.0);\n        float d = length(pa - ba * h);\n        \n        if (d < 0.003) {\n            float type = fract(sin(fi * 11.2) * 43758.5);\n            if (type < 0.3) col = vec3(0.8, 0.2, 0.2); \n            else if (type < 0.6) col = vec3(0.2, 0.6, 0.2); \n            else if (type < 0.85) col = vec3(0.2, 0.3, 0.8); \n            else col = vec3(0.5, 0.2, 0.5); \n        }\n    }\n}\n\nvoid layer_HorizontalPriceLevels(in vec2 p, in float iTime, inout vec3 col) {\n    for (int i = 0; i < 20; i++) {\n        float fi = float(i);\n        float ypos = sin(fi * 15.3 + iTime*0.1) * 0.8;\n        if (abs(p.y - ypos) < 0.003) {\n            col = vec3(0.3, 0.3, 0.4);\n            if (fi < 3.0 && abs(p.y - ypos) < 0.005) col = vec3(0.1); \n        }\n    }\n}\n\nvoid layer_Candlesticks(in vec2 p, in float iTime, inout vec3 col) {\n    float xScaled = p.x * 5.0 + iTime; \n    \n    float barX = floor(xScaled * 5.0) / 5.0;\n    float nextBarX = floor((xScaled + 0.2) * 5.0) / 5.0;\n    \n    float openPrice = barX * 0.2 + sin(barX * 2.0)*0.2 + sin(barX * 5.0)*0.1;\n    float closePrice = nextBarX * 0.2 + sin(nextBarX * 2.0)*0.2 + sin(nextBarX * 5.0)*0.1;\n    \n    float vol = 0.15 + 0.05 * sin(iTime*5.0 + barX);\n    float highPrice = max(openPrice, closePrice) + abs(sin(barX * 30.0)) * vol;\n    float lowPrice = min(openPrice, closePrice) - abs(sin(barX * 20.0)) * vol;\n    \n    if (abs(fract(xScaled * 5.0) - 0.5) < 0.05 && p.y > lowPrice && p.y < highPrice) {\n        if (closePrice > openPrice) col = vec3(0.1, 0.6, 0.1); \n        else col = vec3(0.8, 0.1, 0.1); \n    }\n    \n    if (abs(fract(xScaled * 5.0) - 0.5) < 0.25 && p.y > min(openPrice, closePrice) && p.y < max(openPrice, closePrice)) {\n        if (closePrice > openPrice) col = vec3(0.2, 0.8, 0.2); \n        else col = vec3(0.9, 0.2, 0.2); \n    }\n}\n\nvoid layer_RightSidebar(in vec2 p, in vec2 uv, in float iTime, inout vec3 col) {\n    if (uv.x > 0.9) {\n        col = vec3(0.95);\n        if (fract(p.y * 10.0) < 0.05) col = vec3(0.5); \n        \n        float curPrice = sin(iTime*0.5)*0.2 + 0.2; \n        if (abs(p.y - curPrice) < 0.05) {\n            col = vec3(0.1); \n            if (fract(p.x * 50.0) < 0.5) col = vec3(0.9); \n        }\n    }\n}\n\nvec4 layer_Background(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 col = vec3(-1.0);\n    \n    layer_Background(uv, col);\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_FibonacciTrendLines(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 col = vec3(-1.0);\n    \n    layer_FibonacciTrendLines(p, iTime, col);\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_HorizontalPriceLevels(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 col = vec3(-1.0);\n    \n    layer_HorizontalPriceLevels(p, iTime, col);\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_Candlesticks(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 col = vec3(-1.0);\n    \n    layer_Candlesticks(p, iTime, col);\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_RightSidebar(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 col = vec3(-1.0);\n    \n    layer_RightSidebar(p, uv, iTime, col);\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n","IMG_2745":"void layer_Background(in vec2 p, inout vec3 col) {\n    if (p.y > 0.6) {\n        col = vec3(0.85, 0.85, 0.8); \n        float noise = fract(sin(dot(p, vec2(12.9898, 78.233))) * 43758.5453);\n        col -= noise * 0.05;\n        if (fract(sin(p.x * 10.0 + cos(p.y * 15.0)) * 20.0) < 0.05) col *= 0.8;\n    }\n    else {\n        col = vec3(0.4, 0.1, 0.1); \n        vec2 tileP = vec2(p.x * 2.0, p.y * 3.0);\n        if (fract(tileP.y) > 0.5) tileP.x += 0.5; \n        \n        vec2 grid = fract(tileP);\n        if (grid.x < 0.02 || grid.y < 0.02) col = vec3(0.6, 0.5, 0.5); \n        \n        col *= 0.5 + 0.5 * smoothstep(1.5, 0.0, length(p));\n        if (p.x > 0.8) col *= 0.6; \n    }\n}\n\nvoid layer_LanternWire(in vec2 p, in vec2 lanternCenter, inout vec3 col) {\n    if (p.y > 0.55 && p.y < 0.65 && abs(p.x - lanternCenter.x * (0.65-p.y)*10.0) < 0.01) {\n        col = vec3(0.1);\n    }\n}\n\nvoid layer_Lantern(in vec2 p, in vec2 lanternCenter, in float iTime, inout vec3 col) {\n    float lanternRadius = 0.55;\n    vec2 lP = p - lanternCenter;\n    lP.x /= 1.1; \n    \n    float lDist = length(lP);\n    \n    if (lDist < lanternRadius) {\n        col = vec3(0.9, 0.8, 0.6);\n        \n        float z = sqrt(lanternRadius*lanternRadius - lP.x*lP.x - lP.y*lP.y);\n        \n        float flicker = 0.9 + 0.1 * sin(iTime * 15.0) * cos(iTime * 7.0);\n        float glow = (z / lanternRadius) * flicker;\n        col *= 0.7 + 0.4 * glow;\n        \n        float ribAng = atan(lP.x, z);\n        if (fract(ribAng * 8.0) < 0.05) col *= 0.85;\n        \n        float wrapX = atan(lP.x, z) * 2.0 + iTime*0.5; \n        \n        if (lP.y > 0.15 && lP.y < 0.4) {\n            if (sin(wrapX * 10.0) * cos(lP.y * 30.0) > 0.2) col = vec3(0.7, 0.2, 0.2);\n            if (abs(fract(wrapX) - 0.5) < 0.2 && abs(lP.y - 0.25) < 0.05) col = vec3(0.7, 0.2, 0.2);\n        }\n        if (lP.y > -0.1 && lP.y < 0.15) {\n            if (sin(wrapX * 8.0 + sin(lP.y * 20.0)) > 0.5) col = vec3(0.1); \n            if (abs(lP.y - 0.02) < 0.04 && fract(wrapX*2.0) < 0.5) col = vec3(0.1);\n        }\n        if (lP.y > -0.35 && lP.y < -0.1) {\n            if (cos(wrapX * 12.0) * sin(lP.y * 25.0) > 0.2) col = vec3(0.7, 0.2, 0.2);\n        }\n        \n        if (lP.y < -0.35) {\n            col = vec3(0.8, 0.7, 0.2); \n            float scallop = sin(wrapX * 20.0);\n            if (lP.y < -0.4 + scallop * 0.02) {\n                col = vec3(0.9, 0.3, 0.2); \n                if (fract(wrapX * 8.0) < 0.2) col = vec3(0.3, 0.6, 0.3); \n            }\n        }\n    }\n}\n\nvoid layer_Tassel(in vec2 p, in vec2 lanternCenter, in float iTime, inout vec3 col) {\n    vec2 tP = p - lanternCenter;\n    if (tP.x > -0.1 && tP.x < 0.1 && tP.y < -0.55 && tP.y > -0.85) {\n        col = vec3(0.6, 0.2, 0.15); \n        \n        float strandSway = sin(iTime * 3.0 + tP.y * 10.0) * 0.02;\n        if (fract((tP.x + strandSway) * 100.0) < 0.3) col *= 0.7; \n        \n        if (tP.y > -0.6) {\n            col = vec3(0.8, 0.7, 0.2);\n            if (fract(tP.x * 20.0) < 0.2) col *= 0.8;\n        }\n    }\n}\n\nvec4 layer_Background(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 col = vec3(-1.0);\n    \n    layer_Background(p, col);\n    \n    float swing = sin(iTime * 2.0) * 0.1;\n    vec2 lanternCenter = vec2(swing, 0.0);\n    \n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_LanternWire(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 col = vec3(-1.0);\n    \n    \n    float swing = sin(iTime * 2.0) * 0.1;\n    vec2 lanternCenter = vec2(swing, 0.0);\n    \n    layer_LanternWire(p, lanternCenter, col);\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_Lantern(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 col = vec3(-1.0);\n    \n    \n    float swing = sin(iTime * 2.0) * 0.1;\n    vec2 lanternCenter = vec2(swing, 0.0);\n    \n    layer_Lantern(p, lanternCenter, iTime, col);\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_Tassel(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 col = vec3(-1.0);\n    \n    \n    float swing = sin(iTime * 2.0) * 0.1;\n    vec2 lanternCenter = vec2(swing, 0.0);\n    \n    layer_Tassel(p, lanternCenter, iTime, col);\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n","IMG_2768":"void layer_AltarCloth(in vec2 p, in float iTime, inout vec3 col) {\n    if (p.y > 0.4) {\n        col = vec3(0.85, 0.75, 0.2); \n        \n        float shimmer = sin(p.x * 20.0 + p.y * 30.0 + iTime * 2.0) * 0.1;\n        col += shimmer;\n        \n        float folds = sin(p.x * 10.0) * 0.1;\n        col *= 1.0 + folds;\n        \n        if (p.y > 0.5) {\n            vec2 fP = vec2(fract(p.x * 5.0 + iTime*0.2), p.y);\n            if (length(fP - vec2(0.5, 0.7)) < 0.1) col = vec3(0.9, 0.2, 0.4); \n            \n            if (abs(p.y - 0.6) < 0.15 && fract(p.x * 3.0 - iTime*0.1) < 0.4) {\n                if (sin(p.x * 40.0) * cos(p.y * 50.0) > 0.3) col = vec3(0.8, 0.1, 0.1); \n            }\n            \n            if (abs(p.x) < 0.6 && p.y > 0.45 && p.y < 0.75) {\n                vec2 charGrid = fract(vec2(p.x * 4.0, p.y * 5.0));\n                if (charGrid.x > 0.2 && charGrid.x < 0.8 && charGrid.y > 0.2 && charGrid.y < 0.8) {\n                    if (fract(p.x * 20.0 + sin(p.y * 30.0)) < 0.3) col = vec3(0.8, 0.1, 0.1);\n                }\n            }\n        }\n        \n        if (p.y > 0.4 && p.y < 0.43) {\n            if (fract(p.x * 30.0) < 0.4) col = vec3(0.8, 0.2, 0.2); \n            else col = vec3(0.7, 0.6, 0.2); \n        }\n    }\n}\n\nvoid layer_Floor(in vec2 p, in float iTime, inout vec3 col) {\n    if (!(p.y > 0.4)) {\n        float floorDepth = 0.4 - p.y;\n        vec2 floorP = vec2(p.x / floorDepth, 1.0 / floorDepth);\n        \n        floorP.y -= iTime * 0.5;\n        \n        vec2 grid = floorP * 3.0; \n        vec2 f = fract(grid);\n        vec2 id = floor(grid);\n        \n        vec3 tileCol = vec3(0.5, 0.35, 0.25); \n        \n        vec2 center = vec2(0.5);\n        float dist = abs(f.x - center.x) + abs(f.y - center.y); \n        \n        if (dist < 0.3) {\n            tileCol = vec3(0.3, 0.45, 0.3); \n            float starDist = max(abs(f.x - center.x)*1.5, abs(f.y - center.y)*1.5);\n            if (starDist < 0.15) tileCol = vec3(0.2, 0.3, 0.2); \n        } else if (dist > 0.45) {\n            tileCol = vec3(0.4, 0.5, 0.35); \n            if (abs(f.x - 0.5) < 0.48 && abs(f.y - 0.5) < 0.48) {\n                if (abs(f.x - f.y) < 0.05) tileCol = vec3(0.8, 0.8, 0.7); \n            }\n        }\n        \n        if (f.x < 0.02 || f.y < 0.02) tileCol = vec3(0.2); \n        \n        col = tileCol;\n        \n        if (floorP.x > -1.5 && floorP.x < 1.5 && floorP.y > -1.0 && floorP.y < 1.0) {\n            vec2 matGrid = floorP * 5.0;\n            vec2 mf = fract(matGrid);\n            \n            col = vec3(0.6); \n            if (mf.x > 0.5) col = vec3(0.8); \n            if (mf.y > 0.5) col = vec3(0.4); \n            if (mf.x > 0.5 && mf.y > 0.5) col = vec3(0.7);\n            \n            float rnd = fract(sin(dot(floor(matGrid), vec2(12.9898, 78.233))) * 43758.5453);\n            if (rnd < 0.3) col *= 0.8; \n            if (rnd > 0.7) col *= 1.2; \n        }\n        \n        float flash = 1.0 / (length(vec2(p.x, p.y + 0.2)) * 5.0);\n        float pulse = 0.5 + 0.5 * sin(iTime * 3.0);\n        col += vec3(0.9, 0.9, 0.8) * clamp(flash * 0.3 * pulse, 0.0, 0.3);\n        \n        col *= clamp((0.4 - p.y) * 2.0, 0.0, 1.0);\n    }\n}\n\nvoid layer_Feet(in vec2 p, in float iTime, inout vec3 col) {\n    if (p.y < -0.8) {\n        float bob = sin(iTime * 2.0) * 0.02;\n        if (abs(p.x + 0.2) < 0.1 || abs(p.x - 0.2) < 0.1) {\n            if (p.y > -0.95 + bob) {\n                col = vec3(0.7, 0.5, 0.4); \n                if (fract(p.x * 40.0) < 0.2 && p.y > -0.85 + bob) col *= 0.8; \n            }\n        }\n        if (p.y < -0.95 + bob) {\n            col = vec3(0.1); \n            if (abs(p.x) < 0.1) col = vec3(0.6, 0.8, 0.2); \n        }\n    }\n}\n\nvec4 layer_AltarCloth(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 col = vec3(-1.0);\n    \n    layer_AltarCloth(p, iTime, col);\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_Floor(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 col = vec3(-1.0);\n    \n    layer_Floor(p, iTime, col);\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_Feet(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 col = vec3(-1.0);\n    \n    layer_Feet(p, iTime, col);\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n","IMG_2773":"void layer_WallBase(in vec2 p, in float iTime, inout vec3 col) {\n    col = vec3(0.5, 0.1, 0.15); \n    vec2 wallGrid = vec2(p.x * 5.0, p.y * 10.0);\n    if (fract(wallGrid.x) < 0.03 || fract(wallGrid.y) < 0.05) {\n        col = vec3(0.8); \n    }\n    \n    float wallNoise = fract(sin(dot(p + iTime*0.05, vec2(12.9898, 78.233))) * 43758.5453);\n    col -= wallNoise * 0.05;\n}\n\nvoid layer_Awning(in vec2 p, in float iTime, inout vec3 col) {\n    if (p.y < -0.4) {\n        float awX = p.x + p.y * 0.5; \n        \n        float stripe = fract(awX * 15.0 - iTime);\n        if (stripe < 0.3) col = vec3(0.8, 0.7, 0.2); \n        else if (stripe < 0.6) col = vec3(0.7, 0.2, 0.2); \n        else if (stripe < 0.8) col = vec3(0.3, 0.6, 0.3); \n        else col = vec3(0.8, 0.7, 0.2); \n        \n        col *= clamp((p.y + 1.0) * 1.5, 0.2, 1.0);\n    }\n}\n\nvoid layer_Symbol(in vec2 p, in float iTime, inout vec3 col) {\n    vec2 center = vec2(-0.2, 0.1); \n    vec2 symP = p - center;\n    float r = length(symP);\n    float angle = atan(symP.y, symP.x);\n    \n    if (r < 0.45) {\n        col = vec3(0.85, 0.7, 0.3); \n        \n        if (r < 0.1) {\n            col = vec3(0.9); \n            if (symP.x > 0.0) col = vec3(0.1); \n        }\n        \n        if (r > 0.12 && r < 0.25) {\n            float seg = fract(angle / 6.28318 * 8.0);\n            if (seg > 0.1 && seg < 0.9) {\n                float ringDist = (r - 0.12) / 0.13;\n                if (fract(ringDist * 4.0) < 0.6) col = vec3(0.6, 0.2, 0.2); \n            }\n        }\n        \n        if (r > 0.28 && r < 0.4) {\n            float aId = floor(angle / 6.28318 * 8.0 + 0.5);\n            vec2 dotPos = vec2(cos(aId * 6.28318 / 8.0), sin(aId * 6.28318 / 8.0)) * 0.35;\n            float d2 = length(symP - dotPos);\n            \n            float dotScale = 1.0 + 0.2 * sin(iTime * 3.0 + aId);\n            \n            if (d2 < 0.05 * dotScale) {\n                if (mod(aId, 2.0) < 1.0) col = vec3(0.1);\n                else col = vec3(0.8);\n            }\n            if (r > 0.33 && r < 0.37 && fract(angle * 4.0) < 0.1) col = vec3(0.1);\n        }\n        \n        if (r > 0.43) col *= 0.6;\n        col *= 0.8 + 0.2 * sin(r * 30.0 + iTime); \n    }\n}\n\nvoid layer_LeavesOverlay(in vec2 p, in float iTime, inout vec3 col) {\n    float leafDensity = 0.0;\n    \n    vec2 wind = vec2(sin(iTime*0.5)*0.1, cos(iTime*0.3)*0.05);\n    vec2 lP = p + wind;\n    \n    leafDensity += sin(lP.x * 20.0 + sin(lP.y * 15.0 + iTime)) * 0.5 + 0.5;\n    leafDensity += cos(lP.x * 35.0 - lP.y * 40.0 - iTime*1.5) * 0.5 + 0.5;\n    \n    float leafMask = smoothstep(-0.2, 1.0, p.y + p.x);\n    leafDensity *= leafMask;\n    \n    if (leafDensity > 1.0) {\n        vec3 leafCol = vec3(0.2, 0.4, 0.2); \n        if (fract(lP.x * 12.0) < 0.5) leafCol = vec3(0.3, 0.5, 0.25); \n        \n        vec2 grid = fract(lP * 15.0);\n        if (length(grid - vec2(0.5)) < 0.4) {\n            col = leafCol;\n            if (grid.x > 0.5 && grid.y > 0.5) col += vec3(0.1);\n        }\n    }\n    \n    if (leafDensity > 0.5 && leafDensity <= 1.0) {\n        col *= 0.7; \n    }\n}\n\nvec4 layer_WallBase(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 col = vec3(-1.0);\n    \n    layer_WallBase(p, iTime, col);\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_Awning(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 col = vec3(-1.0);\n    \n    layer_Awning(p, iTime, col);\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_Symbol(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 col = vec3(-1.0);\n    \n    layer_Symbol(p, iTime, col);\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_LeavesOverlay(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 col = vec3(-1.0);\n    \n    layer_LeavesOverlay(p, iTime, col);\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n","IMG_2982":"void layer_Background(in vec2 p, inout vec3 col) {\n    col = vec3(0.1, 0.15, 0.15) * (1.0 - length(p)*0.3); \n}\n\nvoid layer_ShrineStructure(in vec2 p, in vec3 lightPos, inout vec3 col) {\n    vec3 glazeColor = vec3(0.65, 0.75, 0.75); \n    vec3 edgeColor = vec3(0.6, 0.45, 0.35); \n    \n    if (abs(p.x) < 0.7 && p.y > -0.5 && p.y < 0.4) {\n        col = glazeColor;\n        \n        col *= 0.8 + 0.2 * sin(p.x * 5.0 + p.y * 10.0);\n        \n        float roofCurve = 0.4 - pow(abs(p.x), 2.0) * 0.2;\n        if (p.y > roofCurve) {\n            col = vec3(0.1, 0.15, 0.15) * (1.0 - length(p)*0.3);\n        } else if (p.y > roofCurve - 0.05) {\n            col = edgeColor;\n            if (fract(p.x * 20.0) < 0.2) col *= 0.8; \n        } else {\n            \n            if (abs(p.x) > 0.35 && abs(p.x) < 0.6) {\n                vec2 latP = p * 15.0;\n                vec2 grid = fract(latP);\n                vec2 id = floor(latP);\n                \n                float d1 = abs(grid.x + grid.y - 1.0);\n                float d2 = abs(grid.x - grid.y);\n                float d3 = abs(grid.x - 0.5);\n                float d4 = abs(grid.y - 0.5);\n                \n                if (min(min(d1, d2), min(d3, d4)) > 0.15) {\n                    float shadow = clamp(dot(normalize(vec3(p, 0.0) - lightPos), vec3(0.0, 0.0, -1.0)), 0.0, 1.0);\n                    col = vec3(0.05) + vec3(0.1, 0.15, 0.2) * shadow; \n                } else {\n                    col = glazeColor * 0.9;\n                    if (abs(p.x) > 0.58 || abs(p.x) < 0.37) col = glazeColor; \n                }\n            }\n            \n            if (abs(p.x) < 0.25 && p.y < 0.1 && p.y > -0.3) {\n                col = vec3(0.1, 0.1, 0.12);\n                \n                float innerLight = clamp(1.0 - length(p - lightPos.xy*0.2)*3.0, 0.0, 1.0);\n                col += vec3(0.2, 0.25, 0.3) * innerLight;\n                \n                if (abs(p.x + 0.08) < 0.03 && p.y > -0.25 && p.y < -0.05) {\n                    col = glazeColor * 0.6; \n                    if (p.y > -0.1) col *= 1.2; \n                }\n                if (abs(p.x - 0.1) < 0.035 && p.y > -0.25 && p.y < -0.02) {\n                    col = glazeColor * 0.65;\n                    if (p.y > -0.08) col *= 1.2; \n                }\n            }\n            \n            if (abs(p.x) < 0.35 && p.y > 0.05 && p.y < 0.2) {\n                float archDist = length(vec2(p.x, p.y - 0.1));\n                if (archDist < 0.3 && archDist > 0.2) {\n                    col = glazeColor;\n                    if (fract(atan(p.y - 0.1, p.x) * 5.0) < 0.3) col = edgeColor;\n                }\n            }\n            \n            if (p.y > -0.45 && p.y < -0.3) {\n                vec2 railP = p * 10.0;\n                if (fract(railP.x) > 0.8 || fract(railP.y) > 0.8) {\n                    col = glazeColor * 0.8;\n                } else {\n                    if (abs(fract(railP.x)-0.4) + abs(fract(railP.y)-0.4) < 0.2) col = vec3(0.05); \n                }\n            }\n            \n            float cracks = fract(sin(p.x * 50.0 + sin(p.y * 40.0)) * 43758.5);\n            if (cracks < 0.05) col *= 0.8;\n            \n            if (abs(p.x) > 0.68 || p.y < -0.48) col = edgeColor;\n            \n            vec3 normal = vec3(0.0, 0.0, 1.0); \n            vec3 lDir = normalize(lightPos - vec3(p, 0.0));\n            float diff = max(dot(normal, lDir), 0.0);\n            col *= 0.5 + 0.5 * diff;\n        }\n    }\n}\n\nvoid layer_FloorShadow(in vec2 p, in vec3 lightPos, inout vec3 col) {\n    if (p.y < -0.5) {\n        float shadow = 1.0 - smoothstep(0.0, 0.3, abs(p.x - lightPos.x * 0.2));\n        col *= clamp(shadow + (p.y + 0.8)*2.0, 0.2, 1.0);\n    }\n}\n\nvec4 layer_Background(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 col = vec3(-1.0);\n    vec3 lightPos = vec3(sin(iTime)*1.5, cos(iTime)*0.5 + 0.5, 1.0);\n    \n    layer_Background(p, col);\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_ShrineStructure(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 col = vec3(-1.0);\n    vec3 lightPos = vec3(sin(iTime)*1.5, cos(iTime)*0.5 + 0.5, 1.0);\n    \n    layer_ShrineStructure(p, lightPos, col);\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_FloorShadow(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 col = vec3(-1.0);\n    vec3 lightPos = vec3(sin(iTime)*1.5, cos(iTime)*0.5 + 0.5, 1.0);\n    \n    layer_FloorShadow(p, lightPos, col);\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n","IMG_3105":"void layer_Sky(in vec2 p, in float iTime, inout vec3 col) {\n    if (p.y > 0.4) {\n        col = mix(vec3(0.7, 0.75, 0.8), vec3(0.85, 0.85, 0.8), p.y);\n        \n        float cloudNoise = sin((p.x - iTime*0.05) * 5.0 + sin(p.y * 10.0)) * cos(p.y * 3.0);\n        col += cloudNoise * 0.1;\n        \n        if (p.x < -0.5 && p.y > 0.5) {\n            float treeProfile = 0.6 + sin(p.x * 15.0) * 0.05 + cos(p.x * 30.0) * 0.02;\n            if (p.y < treeProfile) col = vec3(0.2, 0.3, 0.2); \n            if (p.y < treeProfile + sin(iTime + p.x*10.0)*0.01) col = vec3(0.25, 0.35, 0.25);\n        }\n    }\n}\n\nvoid layer_RoofBehind(in vec2 p, inout vec3 col) {\n    if (p.y > 0.2 && p.y <= 0.4) {\n        float roofLine = 0.4 + p.x * -0.1;\n        if (p.y < roofLine) {\n            col = vec3(0.6, 0.6, 0.55); \n            if (fract(p.x * 20.0) < 0.05) col *= 0.9;\n        }\n    }\n}\n\nvoid layer_Cat(in vec2 p, in float iTime, inout vec3 col) {\n    vec2 catP = p - vec2(0.0, 0.4);\n    if (catP.x > -0.05 && catP.x < 0.05 && catP.y > 0.0 && catP.y < 0.12) {\n        col = vec3(0.1); \n        \n        if (catP.y > 0.08) {\n            if (abs(catP.x) > 0.02 && catP.y > 0.1) col = vec3(0.1); \n            \n            if (abs(catP.x) < 0.015 && catP.y < 0.1) col = vec3(0.9);\n            \n            float blink = smoothstep(0.95, 1.0, sin(iTime * 2.0));\n            if (abs(abs(catP.x) - 0.01) < 0.005 && abs(catP.y - 0.09) < 0.005 * (1.0 - blink)) {\n                col = vec3(0.8, 0.9, 0.2); \n            }\n        }\n        if (catP.y > 0.03 && catP.y < 0.08 && abs(catP.x) < 0.02) {\n            col = vec3(0.9);\n        }\n        \n        float tailAnim = sin(iTime * 3.0) * 0.02;\n        if (catP.y < 0.02 && catP.x > 0.04 && catP.x < 0.1 + tailAnim) {\n            float tailCurve = -(catP.x - 0.04)*0.5;\n            if (abs(catP.y - tailCurve) < 0.01) col = vec3(0.1);\n        }\n    }\n}\n\nvoid layer_MainWall(in vec2 p, in float iTime, inout vec3 col) {\n    if (p.y > -0.5 && p.y <= 0.3) {\n        col = vec3(0.6, 0.6, 0.6); \n        \n        float drips = sin(p.x * 50.0) * sin(p.x * 12.0) + cos(p.x * 30.0);\n        col -= drips * 0.05;\n        \n        if (p.y < 0.2 + p.x * 0.3 && p.y > -0.2 - p.x * 0.2) {\n            col = mix(col, vec3(0.85, 0.85, 0.85), 0.7); \n            if (fract(p.x * 10.0 + p.y * 10.0) < 0.1) col *= 0.9;\n        }\n        \n        float shadowSweep = sin(p.x + iTime*0.5) * 0.1;\n        if (p.y < -0.3 + shadowSweep) col *= 0.7;\n    }\n}\n\nvoid layer_ExposedBrick(in vec2 p, inout vec3 col) {\n    if (p.y <= -0.5) {\n        float edgeNoise = sin(p.x * 20.0) * 0.05 + cos(p.x * 45.0) * 0.02;\n        if (p.y < -0.5 + edgeNoise) {\n            \n            col = vec3(0.3, 0.3, 0.3); \n            \n            vec2 brickGrid = vec2(p.x * 4.0, p.y * 8.0);\n            vec2 bId = floor(brickGrid);\n            float holeChance = fract(sin(dot(bId, vec2(12.9898, 78.233))) * 43758.5453);\n            \n            if (holeChance > 0.5) {\n                vec2 bF = fract(brickGrid);\n                if (bF.x > 0.2 && bF.x < 0.8 && bF.y > 0.2 && bF.y < 0.8) {\n                    col = vec3(0.6, 0.3, 0.2); \n                    if (fract(bF.x * 5.0) < 0.1) col *= 0.8;\n                }\n            }\n            \n            float dirt = sin(p.x * 80.0) * 0.5 + 0.5;\n            col *= 0.8 + 0.2 * dirt;\n        }\n    }\n}\n\nvec4 layer_Sky(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 col = vec3(-1.0);\n    \n    layer_Sky(p, iTime, col);\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_RoofBehind(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 col = vec3(-1.0);\n    \n    layer_RoofBehind(p, col);\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_Cat(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 col = vec3(-1.0);\n    \n    layer_Cat(p, iTime, col);\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_MainWall(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 col = vec3(-1.0);\n    \n    layer_MainWall(p, iTime, col);\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_ExposedBrick(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 col = vec3(-1.0);\n    \n    layer_ExposedBrick(p, col);\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n","IMG_3714":"void layer_Asphalt(in vec2 scrollP, in float iTime, inout vec3 col) {\n    col = vec3(0.2, 0.2, 0.22); \n    \n    float noise = fract(sin(dot(scrollP * 150.0, vec2(12.9898, 78.233))) * 43758.5453);\n    col += (noise - 0.5) * 0.15;\n    \n    vec2 manholeP = scrollP;\n    manholeP.y = fract((manholeP.y + 1.0)/3.0)*3.0 - 1.0; \n    if (length(manholeP - vec2(0.0, 1.3)) < 0.35) {\n        col = vec3(0.15);\n        if (fract(manholeP.x * 20.0 + manholeP.y * 20.0) < 0.2) col *= 0.8;\n    }\n}\n\nvoid layer_ArrowShape(in vec2 scrollP, inout vec3 col) {\n    vec2 arrP = scrollP;\n    arrP.y = fract((arrP.y + 1.5)/3.0)*3.0 - 1.5;\n    \n    float arrowDist = max(abs(arrP.x + 0.5) - 0.15, abs(arrP.y - 0.3) - 0.6);\n    vec2 pRot = mat2(0.707, -0.707, 0.707, 0.707) * (arrP - vec2(-0.2, -0.1));\n    float box2 = max(abs(pRot.x) - 0.3, abs(pRot.y) - 0.2);\n    float tri1 = max(abs(arrP.x - 0.4) - 0.3, arrP.y + arrP.x*1.5 - 0.8);\n    tri1 = max(tri1, -arrP.y - 0.5);\n    \n    arrowDist = min(arrowDist, min(box2, tri1));\n    float mask = smoothstep(0.01, -0.01, arrowDist);\n    \n    vec2 vp = arrP * 10.0;\n    vec2 i = floor(vp);\n    vec2 f = fract(vp);\n    float minDist = 1.0;\n    for(int y=-1; y<=1; y++) {\n        for(int x=-1; x<=1; x++) {\n            vec2 neighbor = vec2(float(x), float(y));\n            vec2 point = fract(sin(vec2(dot(i + neighbor, vec2(127.1,311.7)), dot(i + neighbor, vec2(269.5,183.3)))) * 43758.5453);\n            vec2 diff = neighbor + point - f;\n            float dist = length(diff);\n            minDist = min(minDist, dist);\n        }\n    }\n    float cracks = smoothstep(0.05, 0.08, minDist);\n    \n    if (mask > 0.0) {\n        col = vec3(0.85, 0.85, 0.85) * cracks; \n    }\n}\n\nvoid layer_FeetClothes(in vec2 p, in float scrollSpeed, in float iTime, inout vec3 col) {\n    float walkCycle = sin(iTime * scrollSpeed * 5.0);\n    \n    if (p.x < -0.3 && p.y < -0.4 + walkCycle*0.05) {\n        col = vec3(0.05); \n        if (fract(p.y*10.0 + iTime*2.0) < 0.1) col *= 1.2; \n    }\n    \n    float leftStep = max(0.0, -walkCycle);\n    vec2 lShoeP = p - vec2(-0.4, -0.85 + leftStep * 0.1);\n    if (length(lShoeP) < 0.15) {\n        col = vec3(0.1);\n        if (length(lShoeP - vec2(0.05, 0.05)) < 0.05) col = vec3(0.8, 0.3, 0.1); \n    }\n    \n    float rightStep = max(0.0, walkCycle);\n    vec2 rShoeP = p - vec2(0.4, -0.85 + rightStep * 0.1);\n    if (length(rShoeP) < 0.15) {\n        col = vec3(0.1);\n        if (length(rShoeP - vec2(-0.05, 0.05)) < 0.05) col = vec3(0.8, 0.3, 0.1); \n    }\n}\n\nvec4 layer_Asphalt(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    float scrollSpeed = 1.0;\n    vec2 scrollP = p;\n    scrollP.y += iTime * scrollSpeed;\n    \n    vec3 col = vec3(-1.0);\n    \n    layer_Asphalt(scrollP, iTime, col);\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_ArrowShape(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    float scrollSpeed = 1.0;\n    vec2 scrollP = p;\n    scrollP.y += iTime * scrollSpeed;\n    \n    vec3 col = vec3(-1.0);\n    \n    layer_ArrowShape(scrollP, col);\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_FeetClothes(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    float scrollSpeed = 1.0;\n    vec2 scrollP = p;\n    scrollP.y += iTime * scrollSpeed;\n    \n    vec3 col = vec3(-1.0);\n    \n    layer_FeetClothes(p, scrollSpeed, iTime, col);\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n","IMG_3922":"void layer_Background(in vec2 p, in float iTime, inout vec3 col) {\n    col = vec3(0.8, 0.85, 0.2);\n    \n    float sweep = sin(p.x * 2.0 - p.y * 3.0 + iTime) * 0.5 + 0.5;\n    col += vec3(0.1, 0.1, 0.0) * sweep;\n    \n    if (p.x < -0.8) {\n        col = vec3(0.2, 0.2, 0.5); \n        if (fract(p.y * 10.0 + iTime*0.5) > 0.5 && p.x > -0.9) col = vec3(1.0);\n    }\n}\n\nvoid layer_TextLetters(in vec2 p, in float iTime, inout vec3 col) {\n    vec3 red = vec3(0.8, 0.1, 0.1);\n    float pulse = 1.0 + 0.1 * sin(iTime * 3.0);\n    \n    float dP = max(abs(p.x + 0.4) - 0.05*pulse, abs(p.y - 0.5) - 0.2*pulse);\n    dP = min(dP, max(abs(p.x + 0.25) - 0.15*pulse, abs(p.y - 0.6) - 0.1*pulse));\n    dP = max(dP, -max(abs(p.x + 0.25) - 0.05*pulse, abs(p.y - 0.6) - 0.05*pulse));\n    if (dP < 0.0) col = red;\n    \n    float dH = max(abs(abs(p.x + 0.4) - 0.1*pulse) - 0.03*pulse, abs(p.y - 0.0) - 0.15*pulse);\n    dH = min(dH, max(abs(p.x + 0.4) - 0.1*pulse, abs(p.y - 0.0) - 0.03*pulse));\n    if (dH < 0.0) col = red;\n\n    float dO = max(abs(p.x + 0.4) - 0.15*pulse, abs(p.y + 0.4) - 0.15*pulse);\n    dO = max(dO, -(max(abs(p.x + 0.4) - 0.05*pulse, abs(p.y + 0.4) - 0.05*pulse)));\n    if (dO < 0.0) col = red;\n    \n    float dC = max(abs(p.x - 0.2) - 0.15*pulse, abs(p.y - 0.6) - 0.15*pulse);\n    dC = max(dC, -(max(abs(p.x - 0.2) - 0.05*pulse, abs(p.y - 0.6) - 0.05*pulse)));\n    dC = max(dC, -(p.x - 0.2));\n    if (dC < 0.0) col = red;\n    \n    float dA = max(abs(p.x - 0.2) - 0.15*pulse, abs(p.y - 0.2) - 0.15*pulse);\n    dA = max(dA, -(max(abs(p.x - 0.2) - 0.05*pulse, abs(p.y - 0.2) - 0.05*pulse)));\n    if (dA < 0.0 && p.y < 0.3) col = red;\n\n    float dY = max(abs(abs(p.x - 0.2) - 0.1*pulse) - 0.03*pulse, abs(p.y + 0.2) - 0.15*pulse);\n    if (dY < 0.0) col = red;\n    \n    if (p.x > 0.4 && p.x < 0.7 && fract(p.y * 15.0) > 0.8) {\n        col = red;\n    }\n}\n\nvoid layer_FoodPicture(in vec2 p, in float iTime, inout vec3 col) {\n    if (p.x > -0.1 && p.x < 0.8 && p.y > 0.5 && p.y < 0.95) {\n        col = vec3(0.7, 0.6, 0.5); \n        \n        vec2 center = vec2(0.35, 0.72);\n        float dBowl = length(p - center);\n        if (dBowl < 0.2) {\n            col = vec3(0.9, 0.9, 0.9); \n            if (dBowl < 0.18) {\n                col = vec3(0.8, 0.7, 0.6); \n                float f = fract(sin(dot(p * 50.0 + iTime*0.05, vec2(12.9898, 78.233))) * 43758.5453);\n                if (f > 0.7) col = vec3(0.4, 0.2, 0.1); \n                if (f < 0.2) col = vec3(0.2, 0.5, 0.1); \n            }\n        }\n        \n        float steamDist = p.y - 0.72;\n        if (steamDist > 0.0 && steamDist < 0.2) {\n            float steamPhase = p.x * 20.0 - iTime * 3.0 + sin(p.y * 20.0 + iTime);\n            float steamDensity = sin(steamPhase) * 0.5 + 0.5;\n            steamDensity *= smoothstep(0.2, 0.0, steamDist) * smoothstep(0.2, 0.0, abs(p.x - 0.35));\n            col = mix(col, vec3(0.9, 0.95, 1.0), steamDensity * 0.6);\n        }\n    }\n}\n\nvec4 layer_Background(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 col = vec3(-1.0);\n    \n    layer_Background(p, iTime, col);\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_TextLetters(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 col = vec3(-1.0);\n    \n    layer_TextLetters(p, iTime, col);\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_FoodPicture(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 col = vec3(-1.0);\n    \n    layer_FoodPicture(p, iTime, col);\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n","IMG_4080":"void layer_Fabric(in vec2 p, in float iTime, inout vec3 col) {\n    col = vec3(0.8, 0.9, 0.2);\n    \n    float wrinkle = sin(p.x * 5.0 + p.y * 2.0 + iTime) * sin(p.y * 3.0 - iTime*0.5);\n    col += wrinkle * 0.1;\n}\n\nvoid layer_RedSymbols(in vec2 p, in float iTime, inout vec3 col) {\n    vec3 red = vec3(0.85, 0.1, 0.15);\n    \n    vec2 sCenter = vec2(0.0, 0.6);\n    vec2 p1 = p - sCenter;\n    float dSq = max(abs(p1.x), abs(p1.y)) - 0.2;\n    float dCirc = length(p1) - 0.18;\n    float a = iTime * 0.5;\n    mat2 rotStar = mat2(cos(a), -sin(a), sin(a), cos(a));\n    vec2 p1Star = p1 * rotStar;\n    float dStar = max(abs(p1Star.x) - 0.05, abs(p1Star.y) - 0.05);\n    dStar = min(dStar, max(abs(p1Star.x + p1Star.y) - 0.05, abs(p1Star.x - p1Star.y) - 0.05));\n    \n    if (dSq < 0.0 && dCirc > 0.0) col = red;\n    if (dStar < 0.0) col = red;\n    \n    vec2 iCenter = vec2(0.0, 0.1);\n    vec2 p2 = p - iCenter;\n    float body = length(p2 / vec2(0.05, 0.15)) - 1.0;\n    float wingFlap = abs(sin(iTime * 15.0)) * 0.05;\n    float wings = length(abs(p2) - vec2(0.1, 0.05 - wingFlap)) - 0.05;\n    if (min(body, wings) < 0.0) col = red;\n    \n    vec2 tCenter = vec2(0.0, -0.4);\n    vec2 p3 = p - tCenter;\n    float d3 = max(abs(p3.x + 0.15) - 0.1, abs(p3.y) - 0.15);\n    d3 = max(d3, -(max(abs(p3.x + 0.15) - 0.05, abs(p3.y) - 0.1)));\n    d3 = max(d3, -max(abs(p3.x + 0.2), abs(p3.y) - 0.02));\n    \n    float d6 = max(abs(p3.x) - 0.1, abs(p3.y) - 0.15);\n    d6 = max(d6, -(max(abs(p3.x) - 0.05, abs(p3.y) - 0.1)));\n    \n    float dx = max(abs(abs(p3.x - 0.2) - 0.05) - 0.02, abs(p3.y) - 0.1);\n    \n    float text = min(d3, min(d6, dx));\n    if (text < 0.0) {\n        col = vec3(1.0); \n        if (text < -0.02) col = red;\n    }\n}\n\nvoid layer_WoodSlats(in vec2 p, inout vec3 col) {\n    if (p.x > 0.4 && p.y > 0.5) {\n        float slat = fract(p.x * 4.0 - p.y * 2.0);\n        if (slat > 0.1) {\n            col = vec3(0.8, 0.5, 0.3) * (0.8 + 0.2*sin(p.x*50.0)); \n        }\n    }\n}\n\nvoid layer_Ground(in vec2 uv, in vec2 p, inout vec3 col) {\n    if (p.x > 0.6) {\n        vec2 gp = uv * 2.0 - 1.0; \n        gp.x *= iResolution.x / iResolution.y;\n        float gNoise = fract(sin(dot(gp * 200.0, vec2(12.9898, 78.233))) * 43758.5453);\n        col = vec3(0.2 + gNoise * 0.1);\n    }\n}\n\nvec4 layer_Fabric(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    \n    float waveX = sin(uv.y * 10.0 + iTime * 2.0) * 0.02;\n    float waveY = cos(uv.x * 5.0 + iTime * 1.5) * 0.02;\n    vec2 wuv = uv + vec2(waveX, waveY);\n    \n    vec2 p = wuv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 col = vec3(-1.0);\n    \n    layer_Fabric(p, iTime, col);\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_RedSymbols(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    \n    float waveX = sin(uv.y * 10.0 + iTime * 2.0) * 0.02;\n    float waveY = cos(uv.x * 5.0 + iTime * 1.5) * 0.02;\n    vec2 wuv = uv + vec2(waveX, waveY);\n    \n    vec2 p = wuv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 col = vec3(-1.0);\n    \n    layer_RedSymbols(p, iTime, col);\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_WoodSlats(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    \n    float waveX = sin(uv.y * 10.0 + iTime * 2.0) * 0.02;\n    float waveY = cos(uv.x * 5.0 + iTime * 1.5) * 0.02;\n    vec2 wuv = uv + vec2(waveX, waveY);\n    \n    vec2 p = wuv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 col = vec3(-1.0);\n    \n    layer_WoodSlats(p, col);\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_Ground(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    \n    float waveX = sin(uv.y * 10.0 + iTime * 2.0) * 0.02;\n    float waveY = cos(uv.x * 5.0 + iTime * 1.5) * 0.02;\n    vec2 wuv = uv + vec2(waveX, waveY);\n    \n    vec2 p = wuv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 col = vec3(-1.0);\n    \n    layer_Ground(uv, p, col);\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n","IMG_4742":"void layer_Street(in vec2 sp, inout vec3 col) {\n    col = vec3(0.3, 0.3, 0.32);\n    float noise = fract(sin(dot(sp * 100.0, vec2(12.9898, 78.233))) * 43758.5453);\n    col += (noise - 0.5) * 0.05;\n}\n\nvoid layer_Person(in vec2 p, in float iTime, in float bob, in float legSway, inout vec3 col) {\n    vec2 pCenter = vec2(-0.1, 0.0 + bob);\n    vec2 pp = p - pCenter;\n\n    float body = max(abs(pp.x) - 0.3, abs(pp.y) - 0.15);\n    \n    float leg1 = length(max(abs(p - vec2(0.2 - legSway, -0.2 + bob)) - vec2(0.05, 0.2), 0.0));\n    float leg2 = length(max(abs(p - vec2(0.4 + legSway, -0.25 + bob)) - vec2(0.05, 0.2), 0.0));\n    \n    vec3 clothesBase = vec3(0.05, 0.1, 0.15); \n    vec3 dotColor = vec3(0.2, 0.8, 0.3); \n    float clothPattern = fract(sin(pp.x * 20.0 + iTime) * cos(pp.y * 20.0) * 10.0);\n    vec3 clothesCol = mix(clothesBase, dotColor, step(0.7, clothPattern));\n    \n    vec2 hp = p - vec2(-0.5, 0.0 + bob);\n    float hA = sin(iTime*2.0)*0.1;\n    mat2 rotH = mat2(cos(hA), -sin(hA), sin(hA), cos(hA));\n    vec2 rotHp = hp * rotH;\n    \n    float hat = max(abs(rotHp.x) - 0.25, abs(rotHp.y) - 0.3);\n    hat = max(hat, -rotHp.x - rotHp.y*1.5 - 0.4);\n    hat = max(hat, rotHp.x - rotHp.y*1.5 - 0.4);\n    \n    float shoe1 = length(max(abs(p - vec2(0.25 - legSway, -0.45 + bob)) - vec2(0.06, 0.03), 0.0));\n    float shoe2 = length(max(abs(p - vec2(0.45 + legSway, -0.5 + bob)) - vec2(0.06, 0.03), 0.0));\n    \n    if (body < 0.0 || min(leg1, leg2) < 0.05) {\n        col = clothesCol;\n    }\n    \n    if (hat < 0.0) {\n        col = vec3(0.3, 0.9, 0.1); \n        vec2 hGrid = fract(rotHp * 8.0);\n        if (length(hGrid - 0.5) < 0.2) col = vec3(0.05);\n    }\n    \n    if (min(shoe1, shoe2) < 0.02) col = vec3(0.8, 0.2, 0.2);\n}\n\nvoid layer_Bags(in vec2 p, in float iTime, in float bob, inout vec3 col) {\n    vec2 bP = p - vec2(0.1, -0.3 + bob);\n    float bSway = sin(iTime*4.0 + 1.0)*0.05;\n    bP.x += bSway;\n    float basket = max(abs(bP.x) - 0.15, abs(bP.y) - 0.1);\n    \n    vec2 rp = p - vec2(-0.1, -0.25 + bob);\n    rp.x -= bSway*0.5;\n    float rBag = max(abs(rp.x) - 0.1, abs(rp.y) - 0.08);\n    \n    if (basket < 0.0) {\n        col = vec3(0.2, 0.6, 0.2);\n        if (fract(bP.x * 30.0) < 0.1 || fract(bP.y * 30.0) < 0.1) col *= 0.5;\n    }\n    \n    if (rBag < 0.0) {\n        col = vec3(0.7, 0.1, 0.1);\n        if (fract(rp.x * 20.0) < 0.2 || fract(rp.y * 20.0) < 0.2) col = vec3(0.1);\n    }\n}\n\nvoid layer_Motorcycle(in vec2 sp, inout vec3 col) {\n    vec2 mp = sp - vec2(-0.6, -0.8);\n    float moto = max(abs(mp.x) - 0.3, abs(mp.y) - 0.3);\n    if (moto < 0.0) {\n        col = vec3(0.1);\n        if (length(mp - vec2(0.2, 0.2)) < 0.05) col = vec3(0.9, 0.9, 0.8);\n    }\n}\n\nvec4 layer_Street(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec2 sp = p;\n    sp.y += iTime * 0.5;\n    \n    vec3 col = vec3(-1.0);\n    \n    layer_Street(sp, col);\n    \n    float bob = abs(sin(iTime * 4.0)) * 0.05;\n    float legSway = sin(iTime * 4.0) * 0.1;\n    \n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_Person(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec2 sp = p;\n    sp.y += iTime * 0.5;\n    \n    vec3 col = vec3(-1.0);\n    \n    \n    float bob = abs(sin(iTime * 4.0)) * 0.05;\n    float legSway = sin(iTime * 4.0) * 0.1;\n    \n    layer_Person(p, iTime, bob, legSway, col);\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_Bags(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec2 sp = p;\n    sp.y += iTime * 0.5;\n    \n    vec3 col = vec3(-1.0);\n    \n    \n    float bob = abs(sin(iTime * 4.0)) * 0.05;\n    float legSway = sin(iTime * 4.0) * 0.1;\n    \n    layer_Bags(p, iTime, bob, col);\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_Motorcycle(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec2 sp = p;\n    sp.y += iTime * 0.5;\n    \n    vec3 col = vec3(-1.0);\n    \n    \n    float bob = abs(sin(iTime * 4.0)) * 0.05;\n    float legSway = sin(iTime * 4.0) * 0.1;\n    \n    layer_Motorcycle(sp, col);\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n","IMG_4782":"void layer_Wall(in vec2 cell, in float iTime, inout vec3 col) {\n    col = vec3(0.9, 0.85, 0.7); \n    \n    float wallLight = sin(iTime + cell.x + cell.y) * 0.05;\n    col += wallLight;\n}\n\nvoid layer_Floor(in vec2 p, inout vec3 col) {\n    if (p.y < -0.3) {\n        col = vec3(0.6, 0.4, 0.2);\n        if (fract(p.x * 5.0 + p.y * 2.0) < 0.05) col *= 0.8;\n    }\n}\n\nvoid layer_Window(in vec2 p, in float iTime, inout vec3 col) {\n    float win = max(abs(p.x) - 0.3, abs(p.y - 0.4) - 0.3);\n    if (win < 0.0) {\n        col = vec3(0.3, 0.4, 0.5); \n        col += sin(p.x*10.0 + iTime + p.y*5.0) * 0.05;\n    }\n}\n\nvoid layer_Table(in vec2 p, inout vec3 col) {\n    float tableLeg1 = max(abs(p.x - 0.4) - 0.05, abs(p.y + 0.2) - 0.2);\n    float tableLeg2 = max(abs(p.x + 0.1) - 0.05, abs(p.y + 0.2) - 0.2);\n    if (min(tableLeg1, tableLeg2) < 0.0) col = vec3(0.1); \n    \n    float tableTop = max(abs(p.x - 0.15) - 0.4, abs(p.y - 0.0) - 0.05);\n    if (tableTop < 0.0) col = vec3(0.1);\n    \n    float cloth = max(abs(p.x - 0.15) - 0.35, abs(p.y + 0.05) - 0.15);\n    if (cloth < 0.0) {\n        col = vec3(0.9);\n        if (p.y < -0.05 && fract(p.x * 20.0) < 0.2) col = vec3(0.1); \n    }\n}\n\nvoid layer_Chair(in vec2 p, inout vec3 col) {\n    float chair = max(abs(p.x + 0.4) - 0.15, abs(p.y + 0.1) - 0.3);\n    if (chair < 0.0 && p.x < -0.3) col = vec3(0.15);\n}\n\nvoid layer_Figure(in vec2 p, in vec2 cell, in float iTime, inout vec3 col) {\n    vec3 figCol = vec3(0.1); \n    float hash = fract(sin(dot(cell, vec2(12.9898, 78.233))) * 43758.5453);\n    \n    float sparkle = pow(abs(sin(p.x*30.0 + p.y*40.0 + iTime*3.0)), 10.0);\n    vec3 gold = vec3(1.0, 0.8, 0.2) * sparkle;\n    \n    if (hash < 0.25) figCol = vec3(0.2, 0.3, 0.8); \n    else if (hash < 0.5) figCol = vec3(0.8, 0.2, 0.2); \n    else if (hash < 0.75) {\n        figCol = vec3(0.1);\n        if (fract(p.x*15.0)*fract(p.y*15.0) > 0.5) figCol = vec3(0.8, 0.6, 0.2);\n    }\n    \n    figCol += gold;\n    \n    if (length(p - vec2(-0.4, 0.25)) < 0.1) col = figCol;\n    float body = max(abs(p.x + 0.4) - 0.12, abs(p.y) - 0.2);\n    if (body < 0.0) col = figCol;\n}\n\nvoid layer_GridBorders(in vec2 gridUV, inout vec3 col) {\n    if (max(abs(fract(gridUV.x)-0.5), abs(fract(gridUV.y)-0.5)) > 0.49) col = vec3(1.0);\n}\n\nvec4 layer_Wall(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    \n    vec2 gridUV = uv * vec2(3.0, 2.0);\n    vec2 cell = floor(gridUV);\n    vec2 p = fract(gridUV) * 2.0 - 1.0;\n    p.x *= (iResolution.x/3.0) / (iResolution.y/2.0);\n    \n    vec3 col = vec3(-1.0);\n    \n    layer_Wall(cell, iTime, col);\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_Floor(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    \n    vec2 gridUV = uv * vec2(3.0, 2.0);\n    vec2 cell = floor(gridUV);\n    vec2 p = fract(gridUV) * 2.0 - 1.0;\n    p.x *= (iResolution.x/3.0) / (iResolution.y/2.0);\n    \n    vec3 col = vec3(-1.0);\n    \n    layer_Floor(p, col);\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_Window(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    \n    vec2 gridUV = uv * vec2(3.0, 2.0);\n    vec2 cell = floor(gridUV);\n    vec2 p = fract(gridUV) * 2.0 - 1.0;\n    p.x *= (iResolution.x/3.0) / (iResolution.y/2.0);\n    \n    vec3 col = vec3(-1.0);\n    \n    layer_Window(p, iTime, col);\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_Table(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    \n    vec2 gridUV = uv * vec2(3.0, 2.0);\n    vec2 cell = floor(gridUV);\n    vec2 p = fract(gridUV) * 2.0 - 1.0;\n    p.x *= (iResolution.x/3.0) / (iResolution.y/2.0);\n    \n    vec3 col = vec3(-1.0);\n    \n    layer_Table(p, col);\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_Chair(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    \n    vec2 gridUV = uv * vec2(3.0, 2.0);\n    vec2 cell = floor(gridUV);\n    vec2 p = fract(gridUV) * 2.0 - 1.0;\n    p.x *= (iResolution.x/3.0) / (iResolution.y/2.0);\n    \n    vec3 col = vec3(-1.0);\n    \n    layer_Chair(p, col);\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_Figure(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    \n    vec2 gridUV = uv * vec2(3.0, 2.0);\n    vec2 cell = floor(gridUV);\n    vec2 p = fract(gridUV) * 2.0 - 1.0;\n    p.x *= (iResolution.x/3.0) / (iResolution.y/2.0);\n    \n    vec3 col = vec3(-1.0);\n    \n    layer_Figure(p, cell, iTime, col);\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_GridBorders(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    \n    vec2 gridUV = uv * vec2(3.0, 2.0);\n    vec2 cell = floor(gridUV);\n    vec2 p = fract(gridUV) * 2.0 - 1.0;\n    p.x *= (iResolution.x/3.0) / (iResolution.y/2.0);\n    \n    vec3 col = vec3(-1.0);\n    \n    layer_GridBorders(gridUV, col);\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n","IMG_4783":"void layer_Wall(in vec2 p, inout vec3 col) {\n    col = vec3(0.95); \n    if (p.x > 0.6) col = vec3(0.85, 0.9, 0.95); \n}\n\nvoid layer_Floor(in vec2 p, in float iTime, inout vec3 col) {\n    if (p.y < -0.4) {\n        col = vec3(0.8, 0.85, 0.9); \n        if (fract(p.x * 4.0) < 0.05 || fract(p.y * 4.0) < 0.05) col = vec3(0.6);\n        col += vec3(0.1) * max(0.0, sin(p.x*5.0 - p.y*10.0 + iTime*2.0));\n    }\n}\n\nvoid layer_TV(in vec2 p, in float iTime, inout vec3 col) {\n    float tv = max(abs(p.x - 0.2) - 0.25, abs(p.y - 0.3) - 0.15);\n    if (tv < 0.0) {\n        col = vec3(0.2, 0.3, 0.5);\n        col += vec3(0.0, 0.1, 0.2) * sin(p.y * 50.0 - iTime * 10.0);\n    }\n}\n\nvoid layer_Plant(in vec2 p, in float iTime, inout vec3 col) {\n    if (p.x < -0.7 && p.y < -0.3) col = vec3(0.8); \n    if (p.x < -0.65 && p.y > -0.3 && p.y < 0.1) {\n        col = vec3(0.2, 0.6, 0.2); \n        if (fract(p.y*10.0+p.x*5.0 + sin(iTime)*0.5) > 0.5) col *= 0.8;\n    }\n}\n\nvoid layer_Furniture(in vec2 p, inout vec3 col) {\n    float f = 0.0;\n    f = max(f, 1.0 - smoothstep(0.0, 0.05, max(abs(p.x - 0.2) - 0.2, abs(p.y + 0.2) - 0.15)));\n    f = max(f, 1.0 - smoothstep(0.0, 0.05, max(abs(p.x - 0.05) - 0.05, abs(p.y + 0.3) - 0.15)));\n    f = max(f, 1.0 - smoothstep(0.0, 0.05, max(abs(p.x - 0.35) - 0.05, abs(p.y + 0.3) - 0.15)));\n    \n    if (f > 0.5) col = vec3(0.05); \n    \n    if (max(abs(p.x - 0.2) - 0.15, abs(p.y + 0.1) - 0.1) < 0.0) col = vec3(0.9);\n}\n\nvoid layer_Figure(in vec2 p, in float iTime, inout vec3 col) {\n    vec3 figCol = vec3(0.1);\n    float goldNoise = fract(sin(p.x*50.0 + p.y*30.0)*43758.5);\n    if (goldNoise > 0.7) {\n        figCol = vec3(0.8, 0.6, 0.1); \n        figCol += vec3(0.4) * pow(abs(sin(p.x*20.0 + p.y*20.0 - iTime*4.0)), 4.0); \n    }\n    \n    if (length(p - vec2(-0.3, 0.2)) < 0.1) col = figCol;\n    float body = max(abs(p.x + 0.3) - 0.12, abs(p.y + 0.05) - 0.25);\n    if (body < 0.0) col = figCol;\n}\n\nvoid layer_GridBorders(in vec2 gridUV, inout vec3 col) {\n    if (max(abs(fract(gridUV.x)-0.5), abs(fract(gridUV.y)-0.5)) > 0.49) col = vec3(1.0);\n}\n\nvec4 layer_Wall(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    \n    vec2 gridUV = uv * vec2(2.0, 2.0);\n    vec2 cell = floor(gridUV);\n    vec2 p = fract(gridUV) * 2.0 - 1.0;\n    p.x *= (iResolution.x/2.0) / (iResolution.y/2.0);\n    \n    vec3 col = vec3(-1.0);\n    \n    layer_Wall(p, col);\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_Floor(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    \n    vec2 gridUV = uv * vec2(2.0, 2.0);\n    vec2 cell = floor(gridUV);\n    vec2 p = fract(gridUV) * 2.0 - 1.0;\n    p.x *= (iResolution.x/2.0) / (iResolution.y/2.0);\n    \n    vec3 col = vec3(-1.0);\n    \n    layer_Floor(p, iTime, col);\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_TV(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    \n    vec2 gridUV = uv * vec2(2.0, 2.0);\n    vec2 cell = floor(gridUV);\n    vec2 p = fract(gridUV) * 2.0 - 1.0;\n    p.x *= (iResolution.x/2.0) / (iResolution.y/2.0);\n    \n    vec3 col = vec3(-1.0);\n    \n    layer_TV(p, iTime, col);\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_Plant(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    \n    vec2 gridUV = uv * vec2(2.0, 2.0);\n    vec2 cell = floor(gridUV);\n    vec2 p = fract(gridUV) * 2.0 - 1.0;\n    p.x *= (iResolution.x/2.0) / (iResolution.y/2.0);\n    \n    vec3 col = vec3(-1.0);\n    \n    layer_Plant(p, iTime, col);\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_Furniture(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    \n    vec2 gridUV = uv * vec2(2.0, 2.0);\n    vec2 cell = floor(gridUV);\n    vec2 p = fract(gridUV) * 2.0 - 1.0;\n    p.x *= (iResolution.x/2.0) / (iResolution.y/2.0);\n    \n    vec3 col = vec3(-1.0);\n    \n    layer_Furniture(p, col);\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_Figure(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    \n    vec2 gridUV = uv * vec2(2.0, 2.0);\n    vec2 cell = floor(gridUV);\n    vec2 p = fract(gridUV) * 2.0 - 1.0;\n    p.x *= (iResolution.x/2.0) / (iResolution.y/2.0);\n    \n    vec3 col = vec3(-1.0);\n    \n    layer_Figure(p, iTime, col);\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_GridBorders(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    \n    vec2 gridUV = uv * vec2(2.0, 2.0);\n    vec2 cell = floor(gridUV);\n    vec2 p = fract(gridUV) * 2.0 - 1.0;\n    p.x *= (iResolution.x/2.0) / (iResolution.y/2.0);\n    \n    vec3 col = vec3(-1.0);\n    \n    layer_GridBorders(gridUV, col);\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n","IMG_4784":"void layer_Background(inout vec3 col) {\n    col = vec3(1.0); \n}\n\nvoid layer_BluePatternedDress(in vec2 p, in float iTime, in float bob1, inout vec3 col) {\n    vec2 p1 = p - vec2(-0.7, 0.1 + bob1);\n    float dress1 = max(abs(p1.x) - 0.25, abs(p1.y) - 0.6);\n    if (dress1 < 0.0) {\n        col = vec3(0.1, 0.2, 0.6);\n        float pat1 = fract(sin(p1.x*50.0 + p1.y*20.0 + iTime)*43758.5);\n        if (pat1 > 0.8) col = vec3(0.3, 0.4, 0.8);\n        col += vec3(0.2) * pow(abs(sin(p1.x*10.0 + p1.y*10.0 - iTime*3.0)), 5.0); \n        if (length(p1 - vec2(0.0, 0.6)) < 0.1) col = vec3(1.0);\n    }\n}\n\nvoid layer_GreySuit(in vec2 p, in float bob2, inout vec3 col) {\n    vec2 p2 = p - vec2(-0.1, -0.2 + bob2);\n    float suit2 = max(abs(p2.x) - 0.2, abs(p2.y) - 0.3); \n    float leg2 = max(abs(p2.x + 0.1) - 0.1, abs(p2.y + 0.4) - 0.3); \n    if (min(suit2, leg2) < 0.0) {\n        col = vec3(0.4, 0.45, 0.5);\n        if (abs(p2.x) < 0.05 && p2.y > 0.0) col = vec3(1.0); \n        if (abs(p2.x) < 0.02 && p2.y > 0.0) col = vec3(0.8, 0.1, 0.1); \n        if (length(p2 - vec2(0.0, 0.35)) < 0.1) col = vec3(1.0);\n    }\n}\n\nvoid layer_MaroonDress(in vec2 p, in float iTime, in float bob3, inout vec3 col) {\n    vec2 p3 = p - vec2(0.3, -0.4 + bob3);\n    float dress3 = max(abs(p3.x) - 0.15, abs(p3.y) - 0.5);\n    if (dress3 < 0.0) {\n        col = vec3(0.5, 0.1, 0.2); \n        if (fract(p3.y * 10.0 + sin(p3.x*20.0 + iTime*2.0)) < 0.1) col *= 0.8;\n        if (length(p3 - vec2(0.0, 0.5)) < 0.08) col = vec3(1.0);\n    }\n}\n\nvoid layer_BlackFloralDress(in vec2 p, in float iTime, in float bob4, inout vec3 col) {\n    vec2 p4 = p - vec2(0.7, -0.1 + bob4);\n    float dress4 = max(abs(p4.x) - 0.2, abs(p4.y) - 0.6);\n    if (dress4 < 0.0) {\n        col = vec3(0.1);\n        float noise = fract(sin(p4.x*40.0 + iTime*0.5)*cos(p4.y*40.0 + iTime*0.5)*10.0);\n        if (noise > 0.7) col = vec3(0.8, 0.2, 0.3); \n        else if (noise > 0.6) col = vec3(0.5, 0.5, 0.5); \n        if (length(p4 - vec2(0.0, 0.6)) < 0.08) col = vec3(1.0);\n    }\n}\n\nvoid layer_BlackFormalAoDai(in vec2 p, in float iTime, in float bob5, inout vec3 col) {\n    vec2 p5 = p - vec2(0.4, 0.6 + bob5);\n    float dress5 = max(abs(p5.x) - 0.2, abs(p5.y) - 0.3);\n    if (dress5 < 0.0) {\n        col = vec3(0.05); \n        float pat5 = fract(p5.x*30.0 + sin(p5.y*30.0 + iTime));\n        if (pat5 < 0.1 && p5.y < 0.0) col = vec3(0.8 + 0.2*sin(iTime*5.0));\n        if (length(p5 - vec2(0.0, 0.35)) < 0.1) col = vec3(1.0);\n    }\n}\n\nvec4 layer_Background(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    float bob1 = sin(iTime * 1.5 + 0.0) * 0.05;\n    float bob2 = sin(iTime * 1.2 + 1.0) * 0.05;\n    float bob3 = sin(iTime * 1.8 + 2.0) * 0.05;\n    float bob4 = sin(iTime * 1.4 + 3.0) * 0.05;\n    float bob5 = sin(iTime * 1.6 + 4.0) * 0.05;\n    \n    vec3 col = vec3(-1.0);\n    \n    layer_Background(col);\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_BluePatternedDress(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    float bob1 = sin(iTime * 1.5 + 0.0) * 0.05;\n    float bob2 = sin(iTime * 1.2 + 1.0) * 0.05;\n    float bob3 = sin(iTime * 1.8 + 2.0) * 0.05;\n    float bob4 = sin(iTime * 1.4 + 3.0) * 0.05;\n    float bob5 = sin(iTime * 1.6 + 4.0) * 0.05;\n    \n    vec3 col = vec3(-1.0);\n    \n    layer_BluePatternedDress(p, iTime, bob1, col);\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_GreySuit(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    float bob1 = sin(iTime * 1.5 + 0.0) * 0.05;\n    float bob2 = sin(iTime * 1.2 + 1.0) * 0.05;\n    float bob3 = sin(iTime * 1.8 + 2.0) * 0.05;\n    float bob4 = sin(iTime * 1.4 + 3.0) * 0.05;\n    float bob5 = sin(iTime * 1.6 + 4.0) * 0.05;\n    \n    vec3 col = vec3(-1.0);\n    \n    layer_GreySuit(p, bob2, col);\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_MaroonDress(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    float bob1 = sin(iTime * 1.5 + 0.0) * 0.05;\n    float bob2 = sin(iTime * 1.2 + 1.0) * 0.05;\n    float bob3 = sin(iTime * 1.8 + 2.0) * 0.05;\n    float bob4 = sin(iTime * 1.4 + 3.0) * 0.05;\n    float bob5 = sin(iTime * 1.6 + 4.0) * 0.05;\n    \n    vec3 col = vec3(-1.0);\n    \n    layer_MaroonDress(p, iTime, bob3, col);\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_BlackFloralDress(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    float bob1 = sin(iTime * 1.5 + 0.0) * 0.05;\n    float bob2 = sin(iTime * 1.2 + 1.0) * 0.05;\n    float bob3 = sin(iTime * 1.8 + 2.0) * 0.05;\n    float bob4 = sin(iTime * 1.4 + 3.0) * 0.05;\n    float bob5 = sin(iTime * 1.6 + 4.0) * 0.05;\n    \n    vec3 col = vec3(-1.0);\n    \n    layer_BlackFloralDress(p, iTime, bob4, col);\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_BlackFormalAoDai(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    float bob1 = sin(iTime * 1.5 + 0.0) * 0.05;\n    float bob2 = sin(iTime * 1.2 + 1.0) * 0.05;\n    float bob3 = sin(iTime * 1.8 + 2.0) * 0.05;\n    float bob4 = sin(iTime * 1.4 + 3.0) * 0.05;\n    float bob5 = sin(iTime * 1.6 + 4.0) * 0.05;\n    \n    vec3 col = vec3(-1.0);\n    \n    layer_BlackFormalAoDai(p, iTime, bob5, col);\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n","IMG_5181":"void layer_ConcreteBackground(in vec2 p, in float iTime, inout vec3 col) {\n    col = vec3(0.5, 0.52, 0.5);\n    float noise = fract(sin(dot(p * 200.0 + iTime*0.05, vec2(12.9898, 78.233))) * 43758.5453);\n    col += (noise - 0.5) * 0.1;\n    col *= 0.8 + 0.2 * sin(p.x * 2.0 - iTime);\n}\n\nvoid layer_Card(in vec2 pr, in vec2 cardSize, in float iTime, inout vec3 col) {\n    float card = max(abs(pr.x) - cardSize.x, abs(pr.y) - cardSize.y);\n    \n    if (card < 0.0) {\n        col = vec3(0.95); \n        \n        if (max(abs(pr.x) - cardSize.x + 0.02, abs(pr.y) - cardSize.y + 0.02) > 0.0) {\n            float dotPattern = fract(pr.x * 30.0 - iTime) * fract(pr.y * 20.0 - iTime);\n            if (dotPattern < 0.25) {\n                col = vec3(0.9, 0.6, 0.2);\n            }\n        } else {\n            vec2 gridArea = pr;\n            vec2 gUv = (gridArea + cardSize - 0.05) / (cardSize * 2.0 - 0.1);\n            \n            if (gUv.x > 0.0 && gUv.x < 1.0 && gUv.y > 0.0 && gUv.y < 1.0) {\n                vec2 grid = gUv * vec2(9.0, 3.0);\n                vec2 cell = floor(grid);\n                vec2 cellP = fract(grid);\n                \n                if (cellP.x < 0.05 || cellP.y < 0.05) {\n                    col = vec3(0.0); \n                } else {\n                    float hash = fract(sin(dot(cell, vec2(12.9898, 78.233))) * 43758.5453);\n                    bool isOrange = hash > 0.3; \n                    \n                    float flash = 0.0;\n                    if (fract(hash * 123.456 + iTime * 0.5) > 0.95) flash = 0.5;\n\n                    if (isOrange) col = vec3(0.9, 0.6 + flash, 0.1 + flash);\n                    else col = vec3(0.95 + flash);\n                    \n                    if (isOrange && length(cellP - 0.5) < 0.2) {\n                        col = vec3(0.1); \n                    }\n                }\n            }\n        }\n        \n        float scanner = abs(pr.x - sin(iTime * 2.0) * 0.8);\n        if (scanner < 0.01) col = mix(col, vec3(1.0, 0.0, 0.0), 0.5); \n    }\n}\n\nvec4 layer_ConcreteBackground(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n    \n    float angle = 0.1 + sin(iTime * 0.5) * 0.02; \n    mat2 rot = mat2(cos(angle), -sin(angle), sin(angle), cos(angle));\n    vec2 pr = rot * p;\n    \n    vec3 col = vec3(-1.0);\n    vec2 cardSize = vec2(0.8, 0.5);\n    \n    layer_ConcreteBackground(p, iTime, col);\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_Card(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n    \n    float angle = 0.1 + sin(iTime * 0.5) * 0.02; \n    mat2 rot = mat2(cos(angle), -sin(angle), sin(angle), cos(angle));\n    vec2 pr = rot * p;\n    \n    vec3 col = vec3(-1.0);\n    vec2 cardSize = vec2(0.8, 0.5);\n    \n    layer_Card(pr, cardSize, iTime, col);\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n","IMG_5206":"void layer_Background(in vec2 p, in float iTime, inout vec3 col) {\n    col = vec3(0.85, 0.85, 0.88); \n    \n    if (p.x > 0.4) {\n        col = vec3(0.6, 0.4, 0.2); \n        float weave = abs(sin((p.x + p.y) * 50.0 + iTime)) * abs(sin((p.x - p.y) * 50.0 - iTime));\n        col *= 0.5 + 0.5 * weave;\n        if (p.x < 0.45) col = vec3(0.1); \n    }\n}\n\nvoid layer_LeftSign(in vec2 p, in float iTime, inout vec3 col) {\n    float signBox = max(abs(p.x + 0.5) - 0.15, abs(p.y) - 0.5);\n    if (signBox < 0.0) {\n        col = vec3(0.3); \n        \n        float signRefl = pow(max(0.0, sin(p.x*10.0 + p.y*10.0 + iTime*3.0)), 4.0);\n        col += signRefl * 0.2;\n        \n        if (abs(p.x + 0.5) < 0.1 && p.y > 0.0 && p.y < 0.4) {\n            if (fract(p.y * 20.0) < 0.3) col = vec3(0.8, 0.6, 0.3);\n        }\n        if (abs(p.x + 0.5) < 0.1 && p.y < 0.0 && p.y > -0.4) {\n            if (fract(p.y * 25.0) < 0.3) col = vec3(0.9);\n        }\n        \n        vec2 rp = abs(p - vec2(-0.5, 0.0));\n        if (length(rp - vec2(0.12, 0.45)) < 0.015) col = vec3(0.8); \n    }\n}\n\nvoid layer_GoldenBat(in vec2 p, in float iTime, inout vec3 col) {\n    vec2 bp = p - vec2(0.2, 0.0);\n    bp *= 1.0 + 0.02 * sin(iTime * 2.0);\n    \n    float body = max(abs(bp.x) - 0.2, abs(bp.y) - 0.2);\n    body = length(vec2(bp.x, bp.y*1.5)) - 0.2;\n    \n    float wing1 = max(length(bp - vec2(-0.25, 0.2)) - 0.3, -(length(bp - vec2(-0.1, 0.4)) - 0.25)); \n    float wing2 = max(length(bp - vec2(0.25, 0.2)) - 0.3, -(length(bp - vec2(0.1, 0.4)) - 0.25));  \n    \n    float wing3 = max(length(bp - vec2(-0.25, -0.2)) - 0.3, -(length(bp - vec2(-0.1, -0.4)) - 0.25)); \n    float wing4 = max(length(bp - vec2(0.25, -0.2)) - 0.3, -(length(bp - vec2(0.1, -0.4)) - 0.25));  \n    \n    float head = length(bp - vec2(0.0, 0.25)) - 0.1;\n    float ear1 = max(abs(bp.x + 0.08) - 0.03, abs(bp.y - 0.35) - 0.05); \n    float ear2 = max(abs(bp.x - 0.08) - 0.03, abs(bp.y - 0.35) - 0.05); \n    \n    float batShape = min(body, min(min(wing1, wing2), min(wing3, wing4)));\n    batShape = min(batShape, min(head, min(ear1, ear2)));\n    \n    if (batShape < 0.0) {\n        col = vec3(0.8, 0.6, 0.2); \n        col *= 0.6 + 0.4 * cos(bp.y * 10.0); \n        \n        float highlight = pow(max(0.0, sin(bp.x * 20.0 + bp.y * 20.0 - iTime * 4.0)), 4.0);\n        col += vec3(0.5, 0.4, 0.1) * highlight;\n        \n        float innerCirc = abs(length(bp) - 0.1) - 0.01;\n        if (innerCirc < 0.0) col = vec3(0.4, 0.3, 0.1); \n        \n        float eyeL = length(bp - vec2(-0.04, 0.25));\n        float eyeR = length(bp - vec2(0.04, 0.25));\n        if (min(eyeL, eyeR) < 0.02) {\n            col = vec3(1.0); \n            if (min(length(bp - vec2(-0.04, 0.25)), length(bp - vec2(0.04, 0.25))) < 0.01) col = vec3(0.0); \n            col += vec3(1.0, 0.0, 0.0) * (sin(iTime*5.0)*0.5 + 0.5); \n        }\n    }\n}\n\nvec4 layer_Background(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 col = vec3(-1.0);\n    \n    layer_Background(p, iTime, col);\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_LeftSign(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 col = vec3(-1.0);\n    \n    layer_LeftSign(p, iTime, col);\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_GoldenBat(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 col = vec3(-1.0);\n    \n    layer_GoldenBat(p, iTime, col);\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n","IMG_5211":"void layer_Background(in vec2 p, in float iTime, inout vec3 col) {\n    col = vec3(0.4, 0.45, 0.48);\n    col += vec3(0.1, 0.15, 0.2) * sin(p.x * 5.0 + p.y * 3.0 + iTime);\n    if (abs(p.y) < 0.01) col = vec3(0.3, 0.35, 0.4);\n}\n\nvoid layer_TopReflection(in vec2 p, in float iTime, inout vec3 col) {\n    if (p.y > 0.0 && p.x > -0.6 && p.x < 0.2 && p.y > 0.3 && p.y < 0.8) {\n        col = mix(col, vec3(0.9, 0.95, 1.0), 0.6); \n        float sway = sin(iTime)*0.02;\n        if (p.x + sway > -0.4 && p.x + sway < -0.2 && p.y > 0.4) col = vec3(0.8);\n        if (p.x + sway > 0.0 && p.x + sway < 0.1 && p.y > 0.5) col = vec3(0.7, 0.8, 0.7);\n    }\n}\n\nvoid layer_Leaf(in vec2 p, in float iTime, inout vec3 col) {\n    float y_offset = p.y > 0.0 ? 0.5 : -0.5;\n    vec2 lp = p - vec2(0.0, y_offset);\n    \n    float leafBase = length(lp * vec2(0.5, 2.5)) - 0.7;\n    float jagged = sin(lp.x * 40.0) * 0.03 + sin(lp.x * 80.0) * 0.015;\n    float leaf = leafBase + jagged;\n    \n    if (leaf < 0.0) {\n        col = vec3(0.1, 0.2, 0.15);\n        \n        float veins = abs(sin(lp.x * 100.0));\n        col *= 0.7 + 0.3 * veins;\n        \n        if (abs(lp.y) < 0.02) col = vec3(0.15, 0.25, 0.15);\n        \n        vec3 gold = vec3(0.8, 0.6, 0.2);\n        \n        for(float i=-0.8; i<=0.8; i+=0.35) {\n            vec2 cp = lp - vec2(i, 0.0);\n            \n            float charShape = length(cp) - 0.08;\n            charShape += sin(cp.x * 30.0 + i * 10.0) * 0.03;\n            charShape += cos(cp.y * 25.0 + i * 20.0) * 0.04;\n            \n            float stroke1 = max(abs(cp.x - cp.y) - 0.02, abs(cp.x + cp.y) - 0.06);\n            float stroke2 = max(abs(cp.x + cp.y*0.5) - 0.01, abs(cp.y) - 0.08);\n            \n            if (min(charShape, min(stroke1, stroke2)) < 0.0) {\n                float shine = pow(max(0.0, sin(cp.x * 50.0 + cp.y * 50.0 - iTime*5.0)), 4.0);\n                col = gold * (0.8 + 0.4 * cos(cp.x * 50.0 + cp.y * 50.0)) + vec3(0.4)*shine;\n            }\n        }\n        \n        vec2 vp = lp - vec2(-1.2, 0.0);\n        float grapeCluster = length(vp) - 0.15;\n        if (grapeCluster < 0.0) {\n            float grapes = 1.0;\n            for(float gx=-0.1; gx<=0.1; gx+=0.04) {\n                for(float gy=-0.1; gy<=0.1; gy+=0.04) {\n                    if(length(vec2(gx,gy)) < 0.12) {\n                        float g = length(vp - vec2(gx, gy)) - 0.02;\n                        grapes = min(grapes, g);\n                    }\n                }\n            }\n            if (grapes < 0.0) {\n                float gShine = pow(max(0.0, sin(vp.x*50.0 + vp.y*50.0 - iTime*3.0)), 4.0);\n                col = gold * (0.6 + 0.4*sin(vp.x*100.0)) + vec3(0.3)*gShine;\n            }\n        }\n        \n        if (leafBase + jagged > -0.02) {\n            float trimGleam = pow(max(0.0, sin(lp.x * 20.0 - iTime*4.0)), 8.0);\n            col = gold * 0.7 + vec3(0.5)*trimGleam;\n        }\n    }\n}\n\nvoid layer_SpecularHighlight(in vec2 p, in float iTime, inout vec3 col) {\n    col += vec3(0.1) * max(0.0, sin(p.x * 5.0 + p.y * 10.0 - iTime));\n}\n\nvec4 layer_Background(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 col = vec3(-1.0);\n    \n    layer_Background(p, iTime, col);\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_TopReflection(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 col = vec3(-1.0);\n    \n    layer_TopReflection(p, iTime, col);\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_Leaf(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 col = vec3(-1.0);\n    \n    layer_Leaf(p, iTime, col);\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_SpecularHighlight(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 col = vec3(-1.0);\n    \n    layer_SpecularHighlight(p, iTime, col);\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n","IMG_5212":"void layer_Sky(in vec2 p, in float iTime, inout vec3 col) {\n    col = vec3(0.3, 0.5, 0.7); \n    float clouds = sin(p.x * 3.0 + iTime*0.5 + sin(p.y*5.0)) * cos(p.y*4.0 - iTime*0.2);\n    if (p.y > 0.0) col += max(0.0, clouds)*0.2 * smoothstep(0.0, 1.0, p.y);\n}\n\nvoid layer_GreenBuilding(in vec2 p, inout vec3 col) {\n    float b1 = max(abs(p.x + 0.6) - 1.0, abs(p.y - 0.5) - 0.4);\n    if (b1 < 0.0) {\n        col = vec3(0.2, 0.5, 0.3); \n        if (fract(p.x * 50.0) < 0.1 || fract(p.y * 50.0) < 0.1) col *= 0.8;\n    }\n}\n\nvoid layer_Crane(in vec2 p, in float iTime, inout vec3 col) {\n    float craneSway = sin(iTime * 0.5) * 0.1;\n    if (p.x > -0.5 && p.x < -0.4 && p.y > 0.9 && p.y < 1.2) col = vec3(0.9, 0.7, 0.1);\n    \n    vec2 cp = p - vec2(-0.45, 0.95);\n    float a = craneSway;\n    mat2 rotC = mat2(cos(a), -sin(a), sin(a), cos(a));\n    vec2 cpr = rotC * cp;\n    if (cpr.x > -0.3 && cpr.x < 0.2 && abs(cpr.y) < 0.01) col = vec3(0.9, 0.7, 0.1);\n}\n\nvoid layer_WhiteBuilding(in vec2 p, in float iTime, inout vec3 col) {\n    float b2 = max(abs(p.x - 1.2) - 0.4, abs(p.y - 0.4) - 0.5);\n    if (b2 < 0.0) {\n        col = vec3(0.9); \n        if (fract(p.x * 10.0) < 0.3 && fract(p.y * 10.0) < 0.4 && p.y < 0.8) {\n            col = vec3(0.2, 0.3, 0.4); \n            col += max(0.0, sin(p.x*100.0 + iTime*2.0))*0.2;\n        }\n        if (p.y > 0.8 && p.y < 0.85) col = vec3(0.8, 0.2, 0.2);\n    }\n}\n\nvoid layer_Roof(in vec2 p, inout vec3 col) {\n    float roof = max(abs(p.x) - 2.0, abs(p.y - 0.05) - 0.05);\n    if (roof < 0.0) {\n        col = vec3(0.6, 0.3, 0.2); \n        if (fract(p.x * 30.0) < 0.1) col *= 0.8;\n    }\n}\n\nvoid layer_TerraceWalls(in vec2 p, inout vec3 col) {\n    float wall1 = max(abs(p.x) - 2.0, abs(p.y + 0.2) - 0.2);\n    if (wall1 < 0.0) {\n        col = vec3(0.9, 0.88, 0.8); \n        \n        if (p.x > -0.5 && p.x < -0.2 && p.y > -0.3 && p.y < -0.1) col = vec3(0.4, 0.5, 0.6);\n        \n        if (p.x > 0.5) {\n            if (fract(p.x * 5.0) > 0.8) col = vec3(0.2); \n        }\n    }\n}\n\nvoid layer_TerraceFloor(in vec2 p, in float iTime, inout vec3 col) {\n    if (p.y < -0.4 && p.y > -0.7) {\n        col = vec3(0.8, 0.8, 0.8); \n        float floorUvX = p.x / (1.0 + (p.y + 0.4) * 0.5);\n        if (fract(floorUvX * 5.0) < 0.02 || fract(p.y * 20.0) < 0.05) col *= 0.95;\n        \n        float shadow = sin(p.x * 5.0 + p.y * 10.0 + iTime);\n        if (shadow > 0.5) col *= 0.9;\n    }\n}\n\nvoid layer_Balustrade(in vec2 p, in float iTime, inout vec3 col) {\n    if (p.y < -0.7) {\n        col = vec3(0.85, 0.83, 0.75); \n        \n        if (p.y > -0.72) col *= 0.8;\n        \n        if (p.y < -0.75 && p.y > -0.9) {\n            float pillar = fract(p.x * 12.0);\n            if (pillar > 0.4) {\n                col = vec3(0.6, 0.6, 0.6);\n            } else {\n                col *= 0.8 + 0.4 * sin(pillar * 3.14 / 0.4 + sin(iTime)*0.1);\n            }\n        }\n        \n        if (p.y < -0.92 && p.y > -0.98) {\n            col = vec3(0.2, 0.25, 0.3); \n            float pattern = fract(p.x * 20.0 - iTime*0.5) + fract(p.x * 20.0 + p.y*100.0);\n            if (pattern > 0.5 && pattern < 1.5) col = vec3(0.8, 0.6, 0.3); \n        }\n    }\n}\n\nvec4 layer_Sky(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 col = vec3(-1.0);\n    \n    layer_Sky(p, iTime, col);\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_GreenBuilding(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 col = vec3(-1.0);\n    \n    layer_GreenBuilding(p, col);\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_Crane(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 col = vec3(-1.0);\n    \n    layer_Crane(p, iTime, col);\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_WhiteBuilding(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 col = vec3(-1.0);\n    \n    layer_WhiteBuilding(p, iTime, col);\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_Roof(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 col = vec3(-1.0);\n    \n    layer_Roof(p, col);\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_TerraceWalls(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 col = vec3(-1.0);\n    \n    layer_TerraceWalls(p, col);\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_TerraceFloor(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 col = vec3(-1.0);\n    \n    layer_TerraceFloor(p, iTime, col);\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_Balustrade(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 col = vec3(-1.0);\n    \n    layer_Balustrade(p, iTime, col);\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n","IMG_5214":"void layer_Background(in vec2 p, in vec3 lightDir, in vec2 vp, inout vec3 col) {\n    col = vec3(0.5, 0.52, 0.55);\n    col *= 0.8 + 0.2 * smoothstep(0.0, 1.0, dot(vec3(p, 0.0), lightDir));\n    \n    float shadow = length(vec2(vp.x - 0.7 - lightDir.x*0.2, vp.y * 1.5 - lightDir.y*0.1)) - 0.4;\n    if (shadow < 0.0) {\n        col = mix(col, vec3(0.1), 0.7 * smoothstep(0.0, -0.2, shadow));\n    }\n}\n\nvoid layer_Vase(in vec2 vp, in vec3 lightDir, inout vec3 col) {\n    float radius = 0.0;\n    if (vp.x > -0.8 && vp.x < 0.7) {\n        if (vp.x < -0.6) {\n            radius = mix(0.15, 0.08, (vp.x + 0.8) / 0.2);\n        } else if (vp.x < -0.1) {\n            radius = 0.08;\n        } else if (vp.x < 0.4) {\n            float t = (vp.x + 0.1) / 0.5 * 3.14159;\n            radius = 0.08 + 0.2 * sin(t);\n        } else {\n            radius = mix(0.15, 0.2, (vp.x - 0.4) / 0.3);\n            if (fract((vp.x - 0.4) * 20.0) < 0.2) radius += 0.02; \n        }\n    }\n    float vaseBody = abs(vp.y) - radius;\n    \n    float decor = 1.0;\n    for(float i=0.0; i<6.0; i++) {\n        vec2 gp = vec2(-0.2 + i*0.03, 0.0 + sin(i)*0.05);\n        decor = min(decor, length(vp - gp) - 0.03);\n        gp = vec2(-0.15 + i*0.02, 0.05 + cos(i)*0.06);\n        decor = min(decor, length(vp - gp) - 0.03);\n    }\n    \n    vec2 lp1 = vp - vec2(-0.05, 0.15);\n    float leaf1 = length(lp1) - 0.15 + sin(atan(lp1.y, lp1.x) * 5.0) * 0.03;\n    vec2 lp2 = vp - vec2(0.0, -0.15);\n    float leaf2 = length(lp2) - 0.18 + sin(atan(lp2.y, lp2.x) * 7.0) * 0.04;\n    decor = min(decor, min(leaf1, leaf2));\n    \n    float fullVase = vp.x > -0.8 && vp.x < 0.7 ? vaseBody : 1.0;\n    \n    if (min(fullVase, decor) < 0.0) {\n        col = vec3(0.2, 0.15, 0.12);\n        \n        float ny = vp.y / max(0.01, radius);\n        float nx = 0.05; \n        vec3 normal = normalize(vec3(nx, ny, 1.0 - ny*ny));\n        \n        float diff = max(0.0, dot(normal, lightDir));\n        float spec = pow(max(0.0, dot(reflect(-lightDir, normal), vec3(0.0, 0.0, 1.0))), 32.0);\n        \n        col *= 0.3 + 0.7 * diff;\n        col += vec3(0.8, 0.7, 0.5) * spec * 0.5;\n        \n        if (decor < 0.0 && fullVase > 0.0) {\n            col *= 1.2;\n            col -= vec3(0.1) * smoothstep(-0.02, 0.0, decor);\n        }\n        \n        if (vp.x > -0.8 && vp.x < -0.75 && fullVase < 0.0) {\n            float inner = length(vec2((vp.x + 0.8)*4.0, vp.y)) - 0.12;\n            if (inner < 0.0) col = vec3(0.02);\n        }\n    }\n}\n\nvec4 layer_Background(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 lightPos = vec3(sin(iTime)*2.0 - 1.0, 1.0, 1.0 + cos(iTime));\n    vec3 lightDir = normalize(lightPos);\n    \n    vec3 col = vec3(-1.0);\n    \n    float y_offset = p.y > 0.0 ? 0.4 : -0.4;\n    vec2 vp = p - vec2(0.0, y_offset);\n    \n    layer_Background(p, lightDir, vp, col);\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_Vase(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 lightPos = vec3(sin(iTime)*2.0 - 1.0, 1.0, 1.0 + cos(iTime));\n    vec3 lightDir = normalize(lightPos);\n    \n    vec3 col = vec3(-1.0);\n    \n    float y_offset = p.y > 0.0 ? 0.4 : -0.4;\n    vec2 vp = p - vec2(0.0, y_offset);\n    \n    layer_Vase(vp, lightDir, col);\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n","IMG_5215":"void layer_Background(in vec2 p, inout vec3 col) {\n    if (p.x < -0.3) {\n        col = vec3(0.5, 0.3, 0.4);\n        float textLines = fract(p.x * 20.0);\n        if (textLines < 0.2 && p.y > -0.8 && p.y < 0.8 && fract(p.y * 15.0) < 0.4) {\n            col = mix(col, vec3(0.9), 0.5);\n        }\n    } else {\n        col = vec3(0.4, 0.45, 0.48);\n    }\n}\n\nvoid layer_Smoke(in vec2 p, in float iTime, inout vec3 col) {\n    vec2 sp = p - vec2(-0.9, -0.4);\n    float smoke = sin(sp.x*10.0 + iTime*2.0 + sin(sp.y*5.0)) * cos(sp.y*8.0 - iTime);\n    if (sp.x < 0.0 && sp.y > -0.2) {\n        col += vec3(0.5) * max(0.0, smoke) * 0.3 * smoothstep(0.0, -1.0, sp.x);\n    }\n}\n\nvoid layer_Object(in vec2 p, in float iTime, inout vec3 col) {\n    float obj = 1.0;\n    vec3 objCol = vec3(0.25, 0.2, 0.15);\n    \n    if (p.x > 0.4 && p.x < 1.5) {\n        float h = 0.5;\n        if (p.x > 0.5) h = 0.6;\n        if (p.x > 0.7) h = 0.65;\n        if (p.x > 0.9) h = 0.7;\n        \n        float tier = abs(p.y) - h;\n        if (p.x > 0.6 && p.x < 0.7) tier += sin(p.y * 30.0 + iTime) * 0.02; \n        obj = min(obj, tier);\n    }\n    \n    if (p.x > -0.2 && p.x <= 0.4) {\n        float r = mix(0.4, 0.6, 1.0 - pow(abs((p.x - 0.1) / 0.3), 2.0));\n        float cageBase = abs(p.y) - r;\n        \n        float cx = p.x;\n        float cy = p.y;\n        float rotTime = iTime * 0.5;\n        float band1 = abs(sin(cx * 10.0 + cy * 15.0 + rotTime));\n        float band2 = abs(sin(cx * 10.0 - cy * 15.0 - rotTime));\n        float band3 = abs(sin(cy * 20.0 + rotTime*2.0));\n        \n        float cageCutout = min(band1, min(band2, band3));\n        \n        if (cageBase < 0.0) {\n            if (cageCutout > 0.2) {\n                float embers = max(0.0, sin(p.x*30.0 + iTime*5.0)*cos(p.y*30.0 + iTime*3.0));\n                col = mix(col, vec3(0.6, 0.2, 0.05)*embers, 0.8);\n            } else {\n                obj = min(obj, cageBase);\n            }\n        }\n    }\n    \n    if (p.x > -0.8 && p.x <= -0.2) {\n        float neckY = p.y - (p.x + 0.2) * 0.5;\n        float neck = abs(neckY) - 0.2;\n        obj = min(obj, neck);\n        \n        vec2 capP = p - vec2(-0.8, -0.3);\n        float cap = length(vec2(capP.x, capP.y * 0.8)) - 0.25;\n        if (cap < 0.0) {\n            obj = min(obj, cap);\n            float r = length(capP);\n            if (abs(r - 0.1 - sin(iTime)*0.01) < 0.02 || abs(r - 0.03 + cos(iTime)*0.01) < 0.01) {\n                objCol *= 0.5;\n            }\n        }\n        \n        float a = atan(capP.y, capP.x) + iTime; \n        float spikes = cap + 0.05 + sin(a * 8.0) * 0.08;\n        obj = min(obj, spikes);\n    }\n    \n    if (obj < 0.0) {\n        vec3 finalObjCol = objCol;\n        finalObjCol *= 0.6 + 0.4 * cos(p.y * 5.0);\n        \n        finalObjCol += vec3(0.2, 0.15, 0.1) * max(0.0, sin(p.x * 10.0 + p.y * 20.0 - iTime*3.0));\n        \n        if (obj > -0.01) finalObjCol = vec3(0.4, 0.35, 0.25);\n        col = finalObjCol;\n    }\n}\n\nvec4 layer_Background(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n    \n    vec3 col = vec3(-1.0);\n    \n    layer_Background(p, col);\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_Smoke(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n    \n    vec3 col = vec3(-1.0);\n    \n    layer_Smoke(p, iTime, col);\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_Object(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n    \n    vec3 col = vec3(-1.0);\n    \n    layer_Object(p, iTime, col);\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n","IMG_5225":"void layer_ImageCenter(in vec2 p, in float iTime, inout vec3 col) {\n    if (p.y > -0.5 && p.y < 0.4) {\n        vec2 imgP = (p - vec2(0.0, -0.05)) / vec2(1.0, 0.45);\n        col = vec3(0.6, 0.8, 0.9);\n        \n        if (imgP.y > 0.0) {\n            float perspective = 1.0 - imgP.y * 0.5;\n            float shift = sin(iTime * 0.5) * 0.1; \n            float gridX1 = sin((imgP.x * 30.0 + imgP.y * 20.0 + shift*20.0) / perspective);\n            float gridX2 = sin((imgP.x * 30.0 - imgP.y * 20.0 - shift*20.0) / perspective);\n            float gridY = sin((imgP.y * 40.0 + iTime) / perspective);\n            \n            if (abs(gridX1) < 0.1 || abs(gridX2) < 0.1 || abs(gridY) < 0.1) {\n                col = vec3(1.0);\n            }\n        }\n        \n        float foliageNoise = sin(imgP.x * 20.0 + iTime) * sin(imgP.y * 30.0 - iTime) + sin(imgP.x * 50.0 + imgP.y * 40.0) * 0.5;\n        if (imgP.y < 0.1 && (imgP.x > 0.0 || imgP.y < -0.3 + foliageNoise * 0.2)) {\n            col = vec3(0.1, 0.3 + foliageNoise * 0.1, 0.1);\n        }\n        \n        if (imgP.x > -1.0 && imgP.x < 0.1 && imgP.y < -0.4) {\n            float personIdx = floor((imgP.x + 1.0) * 7.0);\n            if (personIdx >= 0.0 && personIdx <= 7.0) {\n                vec2 pp = vec2(fract((imgP.x + 1.0) * 7.0) - 0.5, imgP.y + 0.6);\n                \n                float bounce = abs(sin(iTime * 3.0 + personIdx * 1.5)) * 0.05;\n                pp.y -= sin(personIdx * 12.3) * 0.1 + bounce;\n                \n                if (abs(pp.x) < 0.25 && pp.y < 0.2) {\n                    col = vec3(0.8, 0.1, 0.1);\n                    if (abs(pp.y) < 0.02) col = vec3(0.1);\n                }\n                if (length(pp - vec2(0.0, 0.3)) < 0.15) {\n                    col = vec3(0.9, 0.7, 0.6);\n                }\n            }\n        }\n    }\n}\n\nvoid layer_TopArea(in vec2 p, in float iTime, inout vec3 col) {\n    if (p.y > 0.4) {\n        col = vec3(0.55, 0.6, 0.65);\n        float textLines = abs(p.y - 0.7);\n        if (textLines < 0.15 && abs(p.x) < 0.9) {\n            float opacity = 0.7 + 0.3 * sin(iTime * 2.0);\n            if (fract(p.x * 20.0) < 0.7 && fract(p.y * 15.0) < 0.5) col = mix(col, vec3(1.0), opacity);\n        }\n        if (length(p - vec2(-0.8, 0.9)) < 0.05) col = vec3(0.1);\n    }\n}\n\nvoid layer_BottomArea(in vec2 p, in float iTime, inout vec3 col) {\n    if (p.y < -0.5) {\n        col = vec3(0.2, 0.25, 0.25);\n        \n        if (abs(p.x) < 0.4 && abs(p.y + 0.7) < 0.08) {\n            col = vec3(1.0);\n            if (abs(p.y + 0.7) < 0.01 && abs(p.x) < 0.3) col = vec3(0.8);\n            \n            float handleX = sin(iTime * 1.5) * 0.25;\n            if (abs(p.y + 0.7) < 0.01 && p.x > -0.3 && p.x < handleX) col = vec3(0.9, 0.2, 0.5);\n            \n            if (length(p - vec2(handleX, -0.7)) < 0.04) {\n                col = vec3(1.0, 0.6, 0.1);\n                if (length(p - vec2(handleX, -0.7)) < 0.02 + sin(iTime*10.0)*0.005) col = vec3(0.9, 0.1, 0.2);\n            }\n        }\n        \n        if (abs(p.x + 0.2) < 0.6 && abs(p.y + 0.9) < 0.05) {\n            float border = max(abs(p.x + 0.2) - 0.58, abs(p.y + 0.9) - 0.03);\n            if (border < 0.0 && border > -0.01) col = vec3(0.5);\n        }\n    }\n}\n\nvec4 layer_ImageCenter(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 col = vec3(-1.0);\n    \n    layer_ImageCenter(p, iTime, col);\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_TopArea(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 col = vec3(-1.0);\n    \n    layer_TopArea(p, iTime, col);\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_BottomArea(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 col = vec3(-1.0);\n    \n    layer_BottomArea(p, iTime, col);\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n","IMG_5251":"void layer_Fabric(in vec2 p, in float iTime, out vec2 dp, inout vec3 col) {\n    float wave = sin(p.x * 5.0 + iTime) * 0.05 + sin(p.y * 3.0 - iTime * 0.5) * 0.05;\n    dp = p + wave;\n    \n    col = vec3(0.1, 0.1, 0.2);\n    col *= 0.9 + 0.1 * sin(dp.x * 100.0) * sin(dp.y * 100.0);\n}\n\nvoid layer_Woman(in vec2 dp, in float iTime, inout vec3 col) {\n    vec2 faceP = dp - vec2(0.1, -0.1);\n    float face = length(vec2(faceP.x, faceP.y * 1.2)) - 0.4;\n    float hair = length(vec2(faceP.x + 0.2, faceP.y + 0.2)) - 0.5;\n    \n    if (hair < 0.0 && face > 0.0) {\n        col = vec3(0.15, 0.1, 0.1);\n        col *= 0.8 + 0.2 * sin(faceP.x * 20.0 + sin(faceP.y * 10.0 + iTime)); \n    }\n    \n    if (face < 0.0) {\n        col = vec3(0.9, 0.75, 0.65);\n        col *= 0.8 + 0.2 * smoothstep(-0.4, 0.0, faceP.x);\n        \n        if (length(faceP - vec2(-0.15, 0.1)) < 0.05) {\n            col = vec3(1.0);\n            if (length(faceP - vec2(-0.15, 0.1)) < 0.02) col = vec3(0.1);\n        }\n        \n        vec2 lipP = faceP - vec2(-0.05, -0.15);\n        float lip = length(vec2(lipP.x, lipP.y * 2.0)) - 0.04;\n        if (lip < 0.0) col = vec3(0.7, 0.1, 0.2);\n        \n        vec2 handP = faceP - vec2(0.2, -0.2);\n        float finger1 = max(abs(handP.x + handP.y - 0.1) - 0.03, abs(handP.x - handP.y + 0.1) - 0.15);\n        if (finger1 < 0.0) {\n            col = vec3(0.95, 0.8, 0.7);\n            if (handP.x < -0.05) col = vec3(0.6, 0.1, 0.2);\n        }\n    }\n}\n\nvoid layer_Flowers(in vec2 dp, in float iTime, inout vec3 col) {\n    vec2 f1P = dp - vec2(0.7, -0.1);\n    float a1 = atan(f1P.y, f1P.x);\n    float r1 = length(f1P);\n    float pPulse = sin(iTime * 2.0) * 0.02;\n    float petals1 = r1 - 0.3 - pPulse - 0.2 * sin(a1 * 5.0) * cos(a1 * 3.0);\n    if (petals1 < 0.0) {\n        col = mix(vec3(0.8, 0.1, 0.5), vec3(0.4, 0.1, 0.6), r1 * 3.0);\n        if (fract(a1 * 10.0 + iTime) < 0.1 && r1 > 0.1) col = vec3(0.9, 0.4, 0.6); \n    }\n    \n    vec2 f2P = dp - vec2(0.4, 0.5);\n    float a2 = atan(f2P.y, f2P.x);\n    float r2 = length(f2P);\n    float petals2 = r2 - 0.25 - pPulse - 0.15 * sin(a2 * 6.0);\n    if (petals2 < 0.0) {\n        col = mix(vec3(0.7, 0.1, 0.4), vec3(0.3, 0.1, 0.5), r2 * 4.0);\n    }\n    \n    vec2 leafP = dp - vec2(0.8, 0.3);\n    float leaf1 = length(vec2(leafP.x - leafP.y, leafP.x + leafP.y*2.0)) - 0.2;\n    if (leaf1 < 0.0 && petals1 > 0.0 && petals2 > 0.0) {\n        col = vec3(0.3, 0.5, 0.2);\n    }\n}\n\nvoid layer_Hat(in vec2 p, in float iTime, inout vec3 col) {\n    vec2 hatP = p - vec2(-1.2 + sin(iTime)*0.05, -0.2); \n    float hatRadius = length(hatP);\n    \n    if (hatRadius < 1.4) {\n        col = vec3(0.85, 0.75, 0.55);\n        vec3 lightDir = normalize(vec3(1.0, 1.0, 1.0));\n        vec3 normal = normalize(vec3(hatP.x, hatP.y, 1.4 - hatRadius));\n        float diff = max(0.0, dot(normal, lightDir));\n        col *= 0.6 + 0.5 * diff;\n        \n        float spec = pow(max(0.0, dot(reflect(-lightDir, normal), vec3(0.0, 0.0, 1.0))), 16.0);\n        col += vec3(0.9, 0.8, 0.6) * spec * (0.3 + 0.2*sin(iTime*3.0));\n        \n        float rings = fract(hatRadius * 30.0);\n        if (rings < 0.2) col *= 0.85;\n        if (rings > 0.8) col *= 1.1;\n        \n        float angle = atan(hatP.y, hatP.x);\n        float radials = fract(angle * 40.0 + sin(hatRadius * 10.0)*0.5);\n        if (radials < 0.1) col *= 0.9;\n    } else {\n        float shadow = smoothstep(1.4, 1.6, hatRadius);\n        col *= mix(0.3, 1.0, shadow);\n    }\n}\n\nvec4 layer_Fabric(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 col = vec3(-1.0);\n    vec2 dp;\n    \n    layer_Fabric(p, iTime, dp, col);\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_Woman(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 col = vec3(-1.0);\n    vec2 dp;\n    \n    layer_Woman(dp, iTime, col);\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_Flowers(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 col = vec3(-1.0);\n    vec2 dp;\n    \n    layer_Flowers(dp, iTime, col);\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_Hat(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 col = vec3(-1.0);\n    vec2 dp;\n    \n    layer_Hat(p, iTime, col);\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n","IMG_5320":"void layer_Background(in vec2 upP, in float iTime, inout vec3 col) {\n    if (upP.y < -0.3) {\n        col = vec3(0.55, 0.58, 0.6);\n        if (fract(upP.x * 2.0) < 0.05 || fract(upP.y * 2.0) < 0.05) col = vec3(0.3);\n    } else {\n        if (upP.x > 0.5) {\n            col = vec3(0.9, 0.9, 0.85);\n            if (upP.y > 0.2) col = vec3(0.6, 0.75, 0.65);\n        } else {\n            col = vec3(0.1, 0.15, 0.15);\n            if (length(upP - vec2(0.2, 0.2)) < 0.2) col = vec3(0.4, 0.2, 0.15);\n            if (fract((upP.y + iTime*0.2) * 10.0) < 0.1 && upP.x < 0.0) col = vec3(0.8, 0.3, 0.3);\n        }\n        \n        if (abs(upP.x - 0.6) < 0.08) {\n            col = vec3(0.6);\n            col *= 0.8 + 0.4 * cos((upP.x - 0.6) * 40.0);\n            if (abs(upP.y - (-0.2)) < 0.05) col *= 0.7;\n        }\n    }\n}\n\nvoid layer_Tripod(in vec2 upP, inout vec3 col) {\n    float objDist = 1.0;\n    \n    float pole = abs(upP.x - 0.1) - 0.02;\n    if (upP.y > -0.6 && upP.y < 0.0) objDist = min(objDist, pole);\n    \n    vec2 lp = upP - vec2(0.1, -0.2);\n    float l1 = max(abs(lp.x + lp.y) - 0.015, -lp.y);\n    float l2 = max(abs(lp.x - lp.y) - 0.015, -lp.y);\n    float l3 = max(abs(lp.x) - 0.015, lp.y);\n    if (length(lp) < 0.4) {\n        objDist = min(objDist, l1);\n        objDist = min(objDist, l2);\n        if (lp.y > 0.0 && lp.y < 0.3) objDist = min(objDist, l3);\n    }\n    \n    if (objDist < 0.0) {\n        col = vec3(0.15);\n    }\n}\n\nvoid layer_HangingPlant(in vec2 upP, in float iTime, inout vec3 col) {\n    vec2 plantCenter = vec2(0.05, 0.0);\n    if (length(upP - plantCenter) < 0.3) {\n        for(float i=0.0; i<30.0; i++) {\n            float a = i * 2.4 + sin(iTime * 2.0 + i)*0.1; \n            float r = fract(sin(i * 123.4) * 456.7) * 0.25;\n            vec2 pos = plantCenter + vec2(cos(a), sin(a)) * r;\n            pos.y -= r * 0.5;\n            \n            float leaf = length(upP - pos) - 0.03;\n            if (leaf < 0.0) {\n                vec3 objCol = vec3(0.4, 0.6, 0.3);\n                if (fract(upP.x * 30.0 + i) < 0.3) objCol = vec3(0.8, 0.9, 0.8);\n                col = objCol;\n            }\n        }\n    }\n}\n\nvoid layer_SwingingHead(in vec2 upP, in float iTime, in float swing, inout vec3 col) {\n    vec2 headP = upP - vec2(-0.35 + swing, 0.0);\n    \n    float hair = length(vec2(headP.x, headP.y * 1.2)) - 0.22;\n    float neck = max(abs(headP.x - 0.15) - 0.08, abs(headP.y) - 0.15);\n    float face = length(vec2(headP.x + 0.05, headP.y * 1.3)) - 0.18;\n    \n    if (hair < 0.0) {\n        col = vec3(0.9, 0.9, 0.95);\n        col *= 0.8 + 0.2 * sin(headP.y * 100.0);\n    }\n    \n    if (face < 0.0 || neck < 0.0) {\n        col = vec3(0.9, 0.75, 0.65);\n        col *= 0.8 + 0.2 * smoothstep(0.1, -0.1, headP.x);\n        \n        float blink = fract(iTime*0.3) < 0.1 ? 0.0 : 1.0;\n        \n        if (length(vec2((headP.x + 0.05)*2.0, headP.y + 0.08*blink)) < 0.03*blink) col = vec3(0.1);\n        if (length(vec2((headP.x + 0.05)*2.0, headP.y - 0.08*blink)) < 0.03*blink) col = vec3(0.1);\n        \n        if (length(vec2(headP.x - 0.1, headP.y)) < 0.02) col = vec3(0.7, 0.4, 0.4);\n    }\n}\n\nvoid layer_SwingingString(in vec2 upP, in float swing, inout vec3 col) {\n    vec2 p1 = vec2(0.1, 0.0); \n    vec2 p2 = vec2(-0.35 + swing, 0.0); \n    float dLine = length(upP - p1 - (p2 - p1) * clamp(dot(upP - p1, p2 - p1) / dot(p2 - p1, p2 - p1), 0.0, 1.0));\n    \n    if (dLine < 0.005) {\n        col = vec3(0.9);\n    }\n}\n\nvec4 layer_Background(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 col = vec3(-1.0);\n    vec2 upP = vec2(p.y, -p.x); \n    \n    layer_Background(upP, iTime, col);\n    \n    float swing = sin(iTime * 1.5) * 0.1;\n    \n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_Tripod(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 col = vec3(-1.0);\n    vec2 upP = vec2(p.y, -p.x); \n    \n    layer_Tripod(upP, col);\n    \n    float swing = sin(iTime * 1.5) * 0.1;\n    \n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_HangingPlant(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 col = vec3(-1.0);\n    vec2 upP = vec2(p.y, -p.x); \n    \n    layer_HangingPlant(upP, iTime, col);\n    \n    float swing = sin(iTime * 1.5) * 0.1;\n    \n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_SwingingHead(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 col = vec3(-1.0);\n    vec2 upP = vec2(p.y, -p.x); \n    \n    \n    float swing = sin(iTime * 1.5) * 0.1;\n    \n    layer_SwingingHead(upP, iTime, swing, col);\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_SwingingString(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 col = vec3(-1.0);\n    vec2 upP = vec2(p.y, -p.x); \n    \n    \n    float swing = sin(iTime * 1.5) * 0.1;\n    \n    layer_SwingingString(upP, swing, col);\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n","IMG_5326":"#define segment(p, a, b) length(p - a - (b - a) * clamp(dot(p - a, b - a) / dot(b - a, b - a), 0.0, 1.0))\n\nvoid layer_Background(in vec2 p, in float iTime, inout vec3 col) {\n    col = vec3(0.55, 0.55, 0.55);\n    float noise = fract(sin(dot(p + iTime*0.05, vec2(12.9898, 78.233))) * 43758.5453);\n    col *= 0.95 + 0.05 * noise;\n}\n\nvoid layer_Sign(in vec2 p, in float iTime, inout vec3 col) {\n    float angle = 0.05;\n    mat2 rot = mat2(cos(angle), -sin(angle), sin(angle), cos(angle));\n    vec2 sp = rot * p;\n    \n    vec2 signBox = vec2(0.4, 0.55);\n    vec2 d = abs(sp) - signBox + vec2(0.1);\n    float signDist = length(max(d, 0.0)) + min(max(d.x, d.y), 0.0) - 0.1;\n    \n    if (signDist < 0.0) {\n        col = vec3(0.7, 0.15, 0.15);\n        col *= 0.8 + 0.2 * smoothstep(-0.4, 0.4, sp.x + sp.y); \n        \n        if (abs(signDist + 0.03) < 0.01) col = vec3(0.9);\n        \n        float pLetter = segment(sp, vec2(-0.15, 0.2), vec2(-0.15, -0.2));\n        pLetter = min(pLetter, segment(sp, vec2(-0.15, 0.2), vec2(0.05, 0.2)));\n        pLetter = min(pLetter, segment(sp, vec2(-0.15, 0.0), vec2(0.05, 0.0)));\n        pLetter = min(pLetter, segment(sp, vec2(0.05, 0.2), vec2(0.15, 0.1)));\n        pLetter = min(pLetter, segment(sp, vec2(0.15, 0.1), vec2(0.05, 0.0)));\n        \n        if (pLetter < 0.06) col = vec3(1.0);\n        \n        float arrowAnim = fract(iTime * 0.5) * 0.1;\n        vec2 ap = sp - vec2(0.0, -0.35 + arrowAnim);\n        float arrow = segment(ap, vec2(0.0, 0.08), vec2(0.0, -0.08));\n        arrow = min(arrow, segment(ap, vec2(0.0, -0.08), vec2(-0.06, 0.0)));\n        arrow = min(arrow, segment(ap, vec2(0.0, -0.08), vec2(0.06, 0.0)));\n        \n        if (arrow < 0.02) col = vec3(1.0);\n    }\n}\n\nvoid layer_Leaves(in vec2 p, in float iTime, inout vec3 col) {\n    if (p.x < -0.4) {\n        float sway = sin(iTime * 2.0) * 0.1;\n        for(float i=0.0; i<15.0; i++) {\n            vec2 center = vec2(-1.2 + sin(i*1.2)*0.4 + sway * (1.0 + sin(i)), -0.8 + i*0.12);\n            vec2 lp = p - center;\n            float a = sin(i) * 0.5 + 0.5 + sway * 0.5 * cos(i);\n            mat2 rotL = mat2(cos(a), -sin(a), sin(a), cos(a));\n            lp = rotL * lp;\n            \n            float leaf = length(vec2(lp.x, lp.y * 2.5)) - 0.15 - sin(i)*0.05;\n            \n            if (leaf < 0.0) {\n                vec3 leafCol = vec3(0.2, 0.45, 0.15);\n                leafCol *= 0.7 + 0.3 * smoothstep(-0.1, 0.1, lp.x); \n                if (abs(lp.x) < 0.005) leafCol *= 0.6; \n                col = mix(col, leafCol, 0.95);\n            }\n        }\n    }\n}\n\nvec4 layer_Background(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 col = vec3(-1.0);\n    \n    layer_Background(p, iTime, col);\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_Sign(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 col = vec3(-1.0);\n    \n    layer_Sign(p, iTime, col);\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_Leaves(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 col = vec3(-1.0);\n    \n    layer_Leaves(p, iTime, col);\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n","IMG_5346":"void layer_Banner(in vec2 p, in float iTime, inout vec3 col) {\n    if (p.y > 0.2) {\n        col = vec3(0.8, 0.15, 0.15);\n        \n        vec3 yellow = vec3(0.95, 0.8, 0.2);\n        float highlight = 0.0;\n        \n        if (p.y > 0.6) {\n            float x1 = max(abs(p.x + p.y*0.5 + 0.4) - 0.05, abs(p.y - 0.8) - 0.15);\n            float x2 = max(abs(p.x - p.y*0.5 + 0.4) - 0.05, abs(p.y - 0.8) - 0.15);\n            float i1 = max(abs(p.x - 0.1) - 0.04, abs(p.y - 0.8) - 0.15);\n            float i2 = max(abs(p.x - 0.3) - 0.04, abs(p.y - 0.8) - 0.15);\n            float i3 = max(abs(p.x - 0.5) - 0.04, abs(p.y - 0.8) - 0.15);\n            \n            float numerals = min(min(x1, x2), min(min(i1, i2), i3));\n            if (numerals < 0.0) highlight = 1.0;\n        }\n        \n        if (p.y > 0.3 && p.y < 0.5) {\n            float letters = fract(p.x * 8.0);\n            if (letters < 0.6 && abs(p.x) < 1.2) {\n                if (fract(p.x * 24.0 + p.y*10.0) > 0.3) {\n                    highlight = 1.0;\n                }\n            }\n        }\n        \n        if (highlight > 0.0) {\n            col = yellow;\n            float glint = smoothstep(0.1, 0.0, abs(p.x + p.y - mod(iTime * 1.5, 4.0) + 1.0));\n            col += vec3(0.5) * glint;\n        }\n    }\n}\n\nvoid layer_Background(in vec2 p, inout vec3 col) {\n    if (p.y <= 0.2 && p.y > -0.25) {\n        col = vec3(0.5, 0.45, 0.4);\n        if (p.y > 0.18) col = vec3(0.6, 0.55, 0.5);\n    } else if (p.y <= -0.25) {\n        col = vec3(0.5, 0.35, 0.3);\n        vec2 floorP = vec2(p.x / (p.y - 0.1), p.y);\n        float pattern = sin(floorP.x * 20.0 + floorP.y * 30.0) * sin(floorP.x * 20.0 - floorP.y * 30.0);\n        if (pattern > 0.5) col = vec3(0.6, 0.45, 0.4);\n        if (abs(pattern) < 0.1) col = vec3(0.3, 0.25, 0.2);\n    }\n}\n\nvoid layer_Plants(in vec2 p, in float iTime, inout vec3 col) {\n    float cellWidth = 0.6;\n    float potIdx = floor((p.x + cellWidth*2.5) / cellWidth);\n    \n    if (potIdx >= 0.0 && potIdx <= 4.0) {\n        vec2 pp = vec2(mod(p.x + cellWidth*2.5, cellWidth) - cellWidth*0.5, p.y);\n        \n        float potBase = -0.25;\n        float potTop = 0.0;\n        float potW = 0.1 + (pp.y) * 0.12; \n        \n        if (pp.y > potBase && pp.y < potTop && abs(pp.x) < potW) {\n            col = vec3(0.9);\n            col *= 0.7 + 0.3 * smoothstep(0.0, potW, pp.x + 0.05);\n        }\n        \n        if (pp.y > -0.05 && pp.y < 0.4 && abs(pp.x) < 0.3) {\n            float sway = sin(iTime * 1.5 + potIdx) * (pp.y + 0.05) * 0.2;\n            vec2 ppp = pp - vec2(sway, 0.0);\n            \n            float dCenter = length(ppp - vec2(0.0, 0.15));\n            float leafShape = dCenter - 0.15 - sin(atan(ppp.y-0.15, ppp.x)*10.0 + potIdx)*0.08;\n            \n            if (leafShape < 0.0) {\n                col = vec3(0.1, 0.25, 0.1);\n                col *= 0.8 + 0.2 * sin(ppp.x*50.0 + potIdx);\n            }\n            \n            for(int i=0; i<3; i++) {\n                float fi = float(i) + potIdx;\n                vec2 fPos = vec2(sin(fi*7.3)*0.15, 0.2 + cos(fi*4.1)*0.1);\n                vec2 fp = ppp - fPos;\n                \n                float a = atan(fPos.x, fPos.y);\n                mat2 rot = mat2(cos(a), -sin(a), sin(a), cos(a));\n                fp = rot * fp;\n                \n                float flower = length(vec2(fp.x, max(0.0, fp.y) + min(0.0, fp.y)*2.0)) - 0.03;\n                \n                if (flower < 0.0) {\n                    col = vec3(0.95);\n                    if (abs(fp.x) < 0.005 && fp.y > -0.02 && fp.y < 0.02) {\n                        col = vec3(0.8, 0.8, 0.2);\n                    }\n                }\n            }\n        }\n    }\n}\n\nvec4 layer_Banner(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 col = vec3(-1.0);\n    \n    layer_Banner(p, iTime, col);\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_Background(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 col = vec3(-1.0);\n    \n    layer_Background(p, col);\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_Plants(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 col = vec3(-1.0);\n    \n    layer_Plants(p, iTime, col);\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n","IMG_5506":"void layer_TopSection(in vec2 p, in float iTime, inout vec3 col) {\n    if (p.y > 0.4) {\n        col = vec3(0.55, 0.82, 0.35);\n        \n        if (p.x > -1.2 && p.x < -0.4 && p.y > 0.6 && p.y < 0.75) {\n            float letters = fract(p.x * 5.0);\n            if (letters < 0.7 && fract(p.x * 20.0 + p.y*10.0 + iTime*2.0) > 0.2) { \n                col = vec3(0.1, 0.2, 0.7);\n            }\n        }\n        \n        if (abs(p.x) < 0.1 && p.y > 0.65 && p.y < 0.7) {\n            col = mix(vec3(0.2, 0.3, 0.5), vec3(0.1, 0.2, 0.8), sin(iTime*5.0)*0.5+0.5);\n        }\n        \n        vec2 logop = p - vec2(0.8, 0.65);\n        if (length(logop) < 0.3) {\n            float wave = sin(logop.x * 20.0 + logop.y * 10.0 + iTime*3.0) * cos(logop.y * 15.0 - iTime);\n            if (abs(wave) < 0.2 && logop.y > -0.1 && logop.y < 0.1) col = vec3(0.1, 0.1, 0.8);\n            if (length(logop - vec2(-0.2, sin(iTime*2.0)*0.02)) < 0.05) col = vec3(0.1, 0.1, 0.8);\n            if (length(logop - vec2(0.2, cos(iTime*2.0)*0.02)) < 0.05) col = vec3(0.1, 0.1, 0.8);\n        }\n    }\n}\n\nvoid layer_BottomPhoto(in vec2 p, inout vec3 col) {\n    if (p.y <= 0.4) {\n        col = vec3(0.08, 0.1, 0.12);\n        \n        float structNoise = fract(sin(dot(floor(p * 5.0), vec2(12.9898, 78.233))) * 43758.5453);\n        if (abs(fract(p.x * 3.0)) < 0.05) col += vec3(0.05);\n        if (abs(fract(p.y * 5.0)) < 0.02) col += vec3(0.05);\n        col *= 0.8 + 0.2 * structNoise;\n    }\n}\n\nvoid layer_Lasers(in vec2 p, in float iTime, inout vec3 col) {\n    if (p.y <= 0.4) {\n        vec3 laserCol = vec3(0.0);\n        \n        vec2 orig1 = vec2(-0.4, 0.0);\n        vec2 orig2 = vec2(0.4, 0.1);\n        vec2 orig3 = vec2(-0.1, 0.2);\n        \n        float sweep = sin(iTime * 1.5) * 0.5;\n        \n        for(float i=0.0; i<40.0; i++) {\n            vec2 o = (mod(i, 3.0) < 1.0) ? orig1 : ((mod(i, 3.0) < 2.0) ? orig2 : orig3);\n            \n            float angle = sin(i * 123.4) * 3.14 + p.y*0.1 + sweep * sin(i*0.5); \n            vec2 dir = vec2(cos(angle), sin(angle));\n            \n            vec2 pa = p - o;\n            float h = clamp(dot(pa, dir), 0.0, 2.0);\n            float d = length(pa - dir * h);\n            \n            float intensity = 0.001 / (d * d + 0.0001);\n            \n            vec3 lColor = vec3(1.0, 0.9, 0.6);\n            if (fract(i * 0.1) < 0.3) lColor = vec3(0.8, 0.9, 1.0);\n            \n            laserCol += lColor * intensity * (1.0 - h*0.4);\n        }\n        \n        col += laserCol * 0.5;\n    }\n}\n\nvoid layer_Foreground(in vec2 p, in float iTime, inout vec3 col) {\n    if (p.y <= 0.4) {\n        col += vec3(1.0, 0.9, 0.7) * (0.05 / (length(p - vec2(0.0, -0.1)) + 0.01)) * (0.8 + 0.2*sin(iTime*8.0));\n        \n        float djDist = 1.0;\n        \n        vec2 djP = p - vec2(0.0, -0.4 + sin(iTime*4.0)*0.02);\n        \n        float head = length(djP) - 0.15;\n        djDist = min(djDist, head);\n        float hp1 = length(djP - vec2(-0.16, 0.0)) - 0.05;\n        float hp2 = length(djP - vec2(0.16, 0.0)) - 0.05;\n        djDist = min(min(djDist, hp1), hp2);\n        \n        float body = max(abs(djP.x) - 0.3 - (djP.y+0.2)*0.2, djP.y + 0.1);\n        if (p.y < -0.5) djDist = min(djDist, body);\n        \n        if (djDist < 0.0) {\n            vec3 djCol = vec3(0.05, 0.08, 0.1);\n            djCol += vec3(0.1, 0.15, 0.1) * (0.5 + 0.5 * sin(p.y * 50.0)) * smoothstep(-0.5, -0.8, p.y);\n            col = djCol;\n        }\n        \n        vec2 screenL = p - vec2(-0.6, -0.6);\n        float sL = max(abs(screenL.x) - 0.15, abs(screenL.y) - 0.1);\n        if (sL < 0.0) {\n            vec3 sCol = vec3(0.2, 0.8, 0.4);\n            if (fract(screenL.x*10.0 - iTime) < 0.5 && screenL.y > 0.0) sCol = vec3(0.3, 0.9, 0.5);\n            if (abs(screenL.y) > 0.08 || abs(screenL.x) > 0.13) sCol = vec3(0.8);\n            col = sCol;\n        }\n        \n        vec2 screenR = p - vec2(0.6, -0.6);\n        float sR = max(abs(screenR.x) - 0.15, abs(screenR.y) - 0.08);\n        if (sR < 0.0) {\n            vec3 sCol2 = vec3(0.8, 0.8, 0.9);\n            if (fract(screenR.x * 20.0 + iTime*2.0) < 0.5 && screenR.y < 0.0) sCol2 = vec3(0.3, 0.3, 0.8);\n            col = sCol2;\n        }\n        \n        if (abs(p.x) > 0.7 && p.y > -0.3 && p.y < 0.1) {\n            if (abs(p.x) < 0.8) {\n                col = vec3(0.02);\n            }\n        }\n    }\n}\n\nvec4 layer_TopSection(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 col = vec3(-1.0);\n    \n    layer_TopSection(p, iTime, col);\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_BottomPhoto(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 col = vec3(-1.0);\n    \n    layer_BottomPhoto(p, col);\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_Lasers(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 col = vec3(-1.0);\n    \n    layer_Lasers(p, iTime, col);\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_Foreground(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 col = vec3(-1.0);\n    \n    layer_Foreground(p, iTime, col);\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n","IMG_5517":"void layer_Ground(in vec2 p, in float globalNoise, inout vec3 col) {\n    col = vec3(0.45, 0.42, 0.4);\n    vec2 gp = p * 4.0;\n    float r = length(gp - vec2(0.0, -4.0));\n    float arcLines = fract(r * 2.0);\n    float radialLines = fract(atan(gp.y + 4.0, gp.x) * 20.0);\n    \n    if (arcLines < 0.1 || radialLines < 0.1) {\n        col *= 0.8;\n    }\n    col += (globalNoise - 0.5) * 0.1;\n}\n\nvoid layer_PillarBase(in vec2 p, in float iTime, in float globalNoise, inout vec3 col) {\n    bool isLeftFace = (p.x < -0.55 && p.x > -1.2 && p.y < 0.7);\n    bool isFrontFace = (p.x >= -0.55 && p.x < 1.0 && p.y > -0.9 && p.y < 0.7 - p.x*0.05);\n    \n    if (isFrontFace || isLeftFace) {\n        vec3 tLight = vec3(0.65, 0.68, 0.7);\n        vec3 tMed = vec3(0.5, 0.55, 0.58);\n        vec3 tDark = vec3(0.35, 0.4, 0.45);\n        \n        vec2 tp = p;\n        if (isLeftFace) {\n            tp.x *= 1.5;\n            tp.y += tp.x * 0.1;\n        } else {\n            tp.y += tp.x * 0.05;\n        }\n        \n        float tileH = 0.15;\n        float tileW = 0.6;\n        \n        float row = floor(tp.y / tileH);\n        float offset = mod(row, 2.0) * (tileW * 0.5);\n        float colIdx = floor((tp.x + offset) / tileW);\n        \n        vec2 cellP = vec2(fract((tp.x + offset) / tileW), fract(tp.y / tileH));\n        \n        float rand = fract(sin(dot(vec2(colIdx, row), vec2(12.9898, 78.233))) * 43758.5453);\n        \n        vec3 tileCol = tMed;\n        if (rand < 0.3) tileCol = tLight;\n        else if (rand > 0.7) tileCol = tDark;\n        \n        float grain = fract(sin(tp.x * 100.0) * 123.4) * 0.1;\n        tileCol += (grain - 0.05);\n        tileCol += (globalNoise - 0.5) * 0.05;\n        \n        float groutThicknessX = 0.02;\n        float groutThicknessY = 0.08;\n        \n        if (cellP.x < groutThicknessX || cellP.x > 1.0 - groutThicknessX ||\n            cellP.y < groutThicknessY || cellP.y > 1.0 - groutThicknessY) {\n            tileCol = vec3(0.8, 0.8, 0.8);\n            tileCol *= 0.9 + 0.1 * fract(sin(tp.x*50.0 + tp.y*50.0)*432.1);\n        } else {\n            if (cellP.y < groutThicknessY + 0.1) tileCol *= 0.8;\n            if (cellP.x > 1.0 - groutThicknessX - 0.05) tileCol *= 0.9;\n            \n            float specRoll = fract(tp.x + tp.y + iTime*0.5);\n            if (specRoll > 0.9 && specRoll < 0.95) {\n                tileCol += vec3(0.15); \n            }\n        }\n        \n        col = tileCol;\n        \n        if (abs(p.x - -0.55) < 0.02) col *= 0.7;\n        \n        if (p.y > 0.55 - p.x*0.05 && p.y < 0.7 - p.x*0.05) {\n            col = vec3(0.7);\n            if (p.y < 0.6 - p.x*0.05 && p.y > 0.58 - p.x*0.05) col = vec3(0.9);\n        }\n    }\n}\n\nvoid layer_TopColumn(in vec2 p, in float globalNoise, inout vec3 col) {\n    if (p.x > -0.4 && p.x < 0.8 && p.y > 0.68 - p.x*0.05) {\n        col = vec3(0.65, 0.65, 0.68);\n        col *= 0.9 + 0.1 * cos((p.x - 0.2) * 2.0);\n        col += (globalNoise - 0.5) * 0.03;\n    }\n}\n\nvoid layer_DecorativeTile(in vec2 p, in float iTime, inout vec3 col) {\n    if (p.x > 1.1) {\n        col = vec3(0.8, 0.8, 0.75);\n        vec2 decP = p - vec2(1.4, 0.2);\n        float a = atan(decP.y, decP.x);\n        float rDec = length(decP);\n        \n        float petals = abs(sin((a + iTime*0.5) * 4.0)); \n        if (rDec < 0.3 + petals * 0.1) {\n            col = vec3(0.5, 0.5, 0.4);\n            if (fract(rDec * 20.0) < 0.2) col *= 0.8;\n        }\n        if (rDec < 0.1) col = vec3(0.8, 0.7, 0.5);\n    }\n}\n\nvec4 layer_Ground(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n    \n    vec3 col = vec3(-1.0);\n    \n    float globalNoise = fract(sin(dot(p + iTime*0.05, vec2(12.9898, 78.233))) * 43758.5453);\n    \n    layer_Ground(p, globalNoise, col);\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_PillarBase(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n    \n    vec3 col = vec3(-1.0);\n    \n    float globalNoise = fract(sin(dot(p + iTime*0.05, vec2(12.9898, 78.233))) * 43758.5453);\n    \n    layer_PillarBase(p, iTime, globalNoise, col);\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_TopColumn(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n    \n    vec3 col = vec3(-1.0);\n    \n    float globalNoise = fract(sin(dot(p + iTime*0.05, vec2(12.9898, 78.233))) * 43758.5453);\n    \n    layer_TopColumn(p, globalNoise, col);\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_DecorativeTile(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n    \n    vec3 col = vec3(-1.0);\n    \n    float globalNoise = fract(sin(dot(p + iTime*0.05, vec2(12.9898, 78.233))) * 43758.5453);\n    \n    layer_DecorativeTile(p, iTime, col);\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n","IMG_5538":"void layer_Background(in vec2 p, in float iTime, inout vec3 col, out float noise) {\n    col = vec3(0.2); \n    noise = fract(sin(dot(p + iTime*0.05, vec2(12.9898, 78.233))) * 43758.5453);\n    col *= 0.8 + 0.2 * noise;\n}\n\nvoid layer_MotorcycleWheel(in vec2 p, in float iTime, inout vec3 col) {\n    vec2 wheelCenter = vec2(1.5, -0.3);\n    float wheelR = length(p - wheelCenter);\n    \n    if (wheelR < 1.3) {\n        col = vec3(0.15);\n        \n        if (wheelR > 1.0) {\n            float angle = atan(p.y - wheelCenter.y, p.x - wheelCenter.x) + iTime * 2.0; \n            if (fract(angle * 20.0 + wheelR * 5.0) < 0.2) {\n                col *= 0.5;\n            }\n            if (abs(wheelR - 1.05) < 0.02) col = vec3(0.5); \n        } else {\n            col = vec3(0.3); \n            \n            float angle = atan(p.y - wheelCenter.y, p.x - wheelCenter.x) + iTime * 2.0; \n            float spokes1 = abs(sin(angle * 18.0));\n            float spokes2 = abs(cos((angle + 0.1) * 18.0));\n            \n            if (spokes1 < 0.02 || spokes2 < 0.02) {\n                col = vec3(0.8, 0.85, 0.9);\n                col *= 0.8 + 0.2 * sin(wheelR * 50.0);\n            }\n            \n            if (wheelR < 0.4) {\n                col = vec3(0.6); \n                if (fract(wheelR * 10.0) < 0.1) col *= 0.7; \n                if (max(abs(p.x - wheelCenter.x), abs(p.y - wheelCenter.y)) < 0.1) col = vec3(0.8);\n            }\n        }\n    }\n}\n\nvoid layer_RedPanel(in vec2 p, in float iTime, inout vec3 col) {\n    float curve1 = p.y - sin(p.x * 1.5 + 1.0) * 0.4;\n    float curve2 = p.y - (-p.x * 0.8 + 0.2); \n    \n    bool inRedPanel = p.x > -0.8 && p.x < 1.0 && p.y > -0.8 && curve1 > -0.8 && curve1 < 0.8;\n    if (p.x + p.y*0.5 > 0.8) inRedPanel = false;\n    \n    if (inRedPanel) {\n        col = vec3(0.85, 0.1, 0.15); \n        \n        col *= 0.8 + 0.3 * smoothstep(0.0, 0.5, curve1); \n        col *= 0.9 + 0.2 * cos((p.x - 0.2) * 5.0); \n        \n        float specMove = sin(iTime) * 0.2;\n        float hl1 = abs(p.y - sin(p.x * 1.5 + 0.8) * 0.4 - 0.4 - specMove);\n        if (hl1 < 0.05 && p.x < 0.2) {\n            col += vec3(0.8) * smoothstep(0.05, 0.0, hl1); \n        }\n        float hl2 = abs(p.x + p.y*0.2 + 0.2 + specMove*0.5);\n        if (hl2 < 0.1 && p.y > 0.2 && p.x < 0.0) {\n            col += vec3(0.5) * smoothstep(0.1, 0.0, hl2); \n        }\n        \n        float decalPath = p.y - sin(p.x * 1.2 + 0.5) * 0.5 + 0.4;\n        if (abs(decalPath) < 0.08 && p.x > 0.0 && p.x < 0.8) {\n            col = vec3(0.4, 0.1, 0.5);\n            float letters = fract(p.x * 10.0 - p.y*5.0); \n            if (letters < 0.6 && abs(decalPath) < 0.05) {\n                col = vec3(0.9, 0.8, 0.2); \n            }\n        }\n        \n        vec2 kp = p - vec2(0.2, -0.4);\n        float dKey = length(kp);\n        if (dKey < 0.08) {\n            col = vec3(0.8, 0.2, 0.2); \n            if (dKey < 0.06) {\n                col = vec3(0.6); \n                if (dKey < 0.04) col = vec3(0.7); \n                if (abs(kp.x) < 0.01 && abs(kp.y) < 0.03) col = vec3(0.1);\n            }\n        }\n        \n        if (p.x < -0.4 && p.y > 0.4) {\n            if (abs(p.x - -0.6) < 0.15 && abs(p.y - 0.7) < 0.15) {\n                col = vec3(0.95);\n                if (abs(p.x + p.y - 0.1) < 0.02) col = vec3(0.85, 0.1, 0.15); \n                if (abs(p.x - p.y + 1.3) < 0.02) col = vec3(0.85, 0.1, 0.15);\n            }\n        }\n    }\n}\n\nvoid layer_Seat(in vec2 p, in float noise, inout vec3 col) {\n    float seatEdge = p.y - sin(p.x * 2.0) * 0.2 + 0.2;\n    if (p.x < 0.0 && seatEdge < 0.0 && p.x + p.y < -0.2) {\n        col = vec3(0.15, 0.16, 0.17); \n        \n        col *= 0.95 + 0.05 * noise;\n        col *= 0.8 + 0.3 * smoothstep(0.0, -0.5, seatEdge);\n        \n        if (p.y > -0.6 && p.y < -0.4 && p.x < -0.3) {\n            float letters = fract(p.x * 8.0);\n            if (letters < 0.7 && fract(p.x*20.0 + p.y*15.0) > 0.3) {\n                col = vec3(0.9); \n                if (noise > 0.7) col *= 0.8;\n            }\n        }\n    }\n}\n\nvoid layer_GrabRail(in vec2 p, in float noise, inout vec3 col) {\n    vec2 rp = p;\n    rp.y -= sin(rp.x * 1.5) * 0.4 - 0.1;\n    \n    float railDist = abs(rp.y + rp.x*0.5); \n    if (railDist < 0.06 && p.x > -1.0 && p.x < 0.5 && p.y < 0.6) {\n        col = vec3(0.1); \n        float tubeShade = smoothstep(0.06, 0.0, railDist);\n        col *= 0.5 + 0.5 * tubeShade;\n        \n        if (abs(railDist - 0.04) < 0.005 && rp.y+rp.x*0.5 > 0.0) {\n            col += vec3(0.3); \n        }\n        \n        if (noise > 0.95) {\n            col = mix(col, vec3(0.4, 0.25, 0.15), 0.8); \n        }\n    }\n}\n\nvec4 layer_Background(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 col = vec3(-1.0);\n    float noise;\n    \n    layer_Background(p, iTime, col, noise);\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_MotorcycleWheel(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 col = vec3(-1.0);\n    float noise;\n    \n    layer_MotorcycleWheel(p, iTime, col);\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_RedPanel(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 col = vec3(-1.0);\n    float noise;\n    \n    layer_RedPanel(p, iTime, col);\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_Seat(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 col = vec3(-1.0);\n    float noise;\n    \n    layer_Seat(p, noise, col);\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_GrabRail(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 col = vec3(-1.0);\n    float noise;\n    \n    layer_GrabRail(p, noise, col);\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n","IMG_5569":"void layer_Background(in vec2 p, in float iTime, inout vec3 col) {\n    col = vec3(0.85, 0.85, 0.85);\n    float bgNoise = fract(sin(dot(p + iTime*0.05, vec2(12.9898, 78.233))) * 43758.5453);\n    if (p.x + p.y > 0.5) {\n        col *= 0.95 + 0.05 * bgNoise; \n    } else {\n        col = vec3(0.95); \n    }\n}\n\nvoid layer_RockSpeaker(in vec2 p, in float iTime, out float rLength, out vec2 rp, out float rockShape, inout vec3 col) {\n    float a = atan(p.y, p.x);\n    float r = length(p);\n    \n    rockShape = 0.65;\n    rockShape += sin(a * 3.0 + iTime*0.2) * 0.1; \n    rockShape += cos(a * 5.0 + 1.0) * 0.05;\n    rockShape += sin(a * 7.0) * 0.03;\n    \n    float edgeNoise = fract(sin(a * 50.0) * 4375.5453) * 0.02;\n    rockShape += edgeNoise;\n\n    rp = p - vec2(-0.1, -0.1);\n    rLength = length(rp);\n    \n    if (rLength < rockShape) {\n        col = vec3(0.22, 0.23, 0.25);\n        \n        float rockNoise = fract(sin(dot(rp * 10.0, vec2(12.9898, 78.233))) * 43758.5453);\n        float rockNoise2 = fract(sin(dot(rp * 30.0, vec2(39.346, 11.135))) * 43758.5453);\n        \n        col *= 0.8 + 0.4 * rockNoise;\n        \n        if (rockNoise2 > 0.8 && rLength > 0.4) {\n            col = mix(col, vec3(0.2, 0.4, 0.3), 0.5);\n        }\n        \n        vec3 normal = normalize(vec3(rp.x, rp.y, rockShape - rLength));\n        vec3 lightDir = normalize(vec3(-1.0, 1.0, 1.0));\n        float diff = max(dot(normal, lightDir), 0.0);\n        col *= 0.5 + 0.5 * diff;\n    }\n}\n\nvoid layer_SpeakerHoles(in vec2 rp, in float rLength, in float rockShape, in float iTime, inout vec3 col) {\n    if (rLength < rockShape) {\n        vec2 center = vec2(0.1, 0.0); \n        float dHoles = 1.0;\n        \n        float centerHole = length(rp - center) - 0.06;\n        dHoles = min(dHoles, centerHole);\n        \n        for(float i=0.0; i<8.0; i++) {\n            float ang = i * 3.14159 / 4.0;\n            vec2 pos = center + vec2(cos(ang), sin(ang)) * 0.18;\n            vec2 hp = rp - pos;\n            \n            float c = cos(-ang);\n            float s = sin(-ang);\n            mat2 rot = mat2(c, -s, s, c);\n            hp = rot * hp;\n            \n            float oval = max(abs(hp.x) - 0.06, abs(hp.y) - 0.02) - 0.02;\n            if (mod(i, 2.0) == 0.0) oval = length(rp - pos) - 0.05;\n            \n            dHoles = min(dHoles, oval);\n        }\n        \n         for(float i=0.0; i<4.0; i++) {\n            float ang = i * 3.14159 / 2.0 + 3.14159/4.0;\n            vec2 pos = center + vec2(cos(ang), sin(ang)) * 0.09;\n            float hole = length(rp - pos) - 0.03;\n            dHoles = min(dHoles, hole);\n        }\n\n        if (dHoles < 0.0) {\n            col = vec3(0.08, 0.09, 0.1);\n            float meshAnim = sin(iTime * 10.0 + length(rp) * 20.0) * 0.1;\n            if (fract(rp.x * 100.0 + meshAnim) < 0.2 || fract(rp.y * 100.0 + meshAnim) < 0.2) {\n                col *= 0.5;\n            }\n            float vibration = sin(iTime * 30.0) * 0.005;\n            col *= smoothstep(0.0, -0.01 + vibration, dHoles);\n        }\n    }\n}\n\nvoid layer_Logo(in vec2 rp, in float rLength, in float rockShape, inout vec3 col) {\n    if (rLength < rockShape) {\n        vec2 logoPos = vec2(0.5, 0.2);\n        if (length(rp - logoPos) < 0.05) {\n            col = vec3(0.15); \n            if (length(rp - logoPos) > 0.04) col = vec3(0.3); \n        }\n    }\n}\n\nvoid layer_Cable(in vec2 p, inout vec3 col) {\n    float dCable = abs(length(p - vec2(0.3, 1.2)) - 0.6);\n    if (dCable < 0.02 && p.x > 0.3 && p.y > 0.6) {\n        col = vec3(0.85); \n        col *= 0.7 + 0.3 * cos((dCable * 50.0) * 3.14159);\n        if (dCable < 0.005 && p.x > 0.8) col = vec3(0.2, 0.6, 0.3);\n    }\n}\n\nvec4 layer_Background(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 col = vec3(-1.0);\n    \n    layer_Background(p, iTime, col);\n    \n    float rLength, rockShape;\n    vec2 rp;\n    \n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_RockSpeaker(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 col = vec3(-1.0);\n    \n    \n    float rLength, rockShape;\n    vec2 rp;\n    layer_RockSpeaker(p, iTime, rLength, rp, rockShape, col);\n    \n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_SpeakerHoles(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 col = vec3(-1.0);\n    \n    \n    float rLength, rockShape;\n    vec2 rp;\n    layer_SpeakerHoles(rp, rLength, rockShape, iTime, col);\n    \n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_Logo(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 col = vec3(-1.0);\n    \n    \n    float rLength, rockShape;\n    vec2 rp;\n    layer_Logo(rp, rLength, rockShape, col);\n    \n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_Cable(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 col = vec3(-1.0);\n    \n    \n    float rLength, rockShape;\n    vec2 rp;\n    \n    layer_Cable(p, col);\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n","IMG_6550":"#define BOX(px, py, bw, bh) (abs(sp.x - (px)) < (bw) && abs(sp.y - (py)) < (bh))\n\nvoid layer_Background(in vec2 p, inout vec3 col) {\n    vec3 wallPink = vec3(0.88, 0.7, 0.72);\n    vec3 maroonTrim = vec3(0.45, 0.15, 0.2);\n    \n    col = wallPink;\n    \n    if (p.y < -0.8) col = maroonTrim;\n    if (p.x > -0.9 && p.x < -0.7 && p.y > -0.8) col = maroonTrim;\n    if (p.x > 0.3 && p.x < 0.5 && p.y > -0.8) col = maroonTrim;\n}\n\nvoid layer_Roof(in vec2 p, in float iTime, inout vec3 col) {\n    if (p.y > 0.5) {\n        col = vec3(0.8, 0.3, 0.2); \n        float shimmer = sin(iTime + p.x * 20.0) * 0.1;\n        if (fract(p.x * 10.0 + shimmer) < 0.1) col *= 0.8;\n        if (fract(p.y * 20.0) < 0.1) col *= 0.9;\n        \n        if (p.y < 0.55) col = vec3(0.2, 0.3, 0.2); \n    }\n}\n\nvoid layer_Symbols(in vec2 p, inout vec3 col) {\n    vec3 maroonTrim = vec3(0.45, 0.15, 0.2);\n    vec2 sp1 = p - vec2(-0.4, -0.3);\n    vec2 sp2 = p - vec2(0.1, -0.3);\n    \n    for (int i=0; i<2; i++) {\n        vec2 sp = (i == 0) ? sp1 : sp2;\n        bool inSymbol = false;\n        \n        inSymbol = inSymbol || BOX(0.0, 0.0, 0.03, 0.2);\n        inSymbol = inSymbol || BOX(0.0, 0.18, 0.15, 0.03);\n        inSymbol = inSymbol || BOX(0.0, 0.05, 0.1, 0.03);\n        inSymbol = inSymbol || BOX(0.0, -0.05, 0.1, 0.03);\n        inSymbol = inSymbol || BOX(0.0, -0.15, 0.18, 0.03);\n        inSymbol = inSymbol || BOX(-0.15, -0.22, 0.03, 0.08);\n        inSymbol = inSymbol || BOX(0.15, -0.22, 0.03, 0.08);\n        inSymbol = inSymbol || BOX(-0.08, -0.22, 0.03, 0.08);\n        inSymbol = inSymbol || BOX(0.08, -0.22, 0.03, 0.08);\n\n        if (inSymbol) col = maroonTrim;\n    }\n}\n\nvoid layer_Gate(in vec2 p, in float iTime, inout vec3 col) {\n    vec3 maroonTrim = vec3(0.45, 0.15, 0.2);\n    vec3 gateYellow = vec3(0.85, 0.7, 0.2);\n    \n    if (p.x > 0.5 && p.y > -0.8 && p.y < 0.3) {\n        col = maroonTrim; \n        \n        vec2 gp = p - vec2(0.5, -0.8); \n        bool isGate = false;\n        if (gp.x < 0.05 || gp.x > 0.95 || gp.y < 0.05 || gp.y > 1.05) isGate = true;\n        \n        vec2 innerP = gp - vec2(0.5, 0.55);\n        \n        float gA = iTime * 0.5;\n        mat2 gRot = mat2(cos(gA), -sin(gA), sin(gA), cos(gA));\n        vec2 rotP = gRot * innerP;\n        \n        float r = length(innerP);\n        if (abs(r - 0.2) < 0.03) isGate = true;\n        if (abs(r - 0.4) < 0.03) isGate = true;\n        \n        if (abs(rotP.x) < 0.03) isGate = true;\n        if (abs(rotP.y) < 0.03) isGate = true;\n        if (abs(rotP.x - rotP.y) < 0.04) isGate = true;\n        if (abs(rotP.x + rotP.y) < 0.04) isGate = true;\n        \n        if (isGate) {\n            col = gateYellow;\n            col *= 0.8 + 0.2 * sin(p.x * 50.0 + p.y * 30.0 + iTime * 5.0); \n        } else {\n            col = vec3(0.1, 0.15, 0.1); \n        }\n    }\n}\n\nvoid layer_Fence(in vec2 p, in float iTime, inout vec3 col) {\n    vec3 fenceGreen = vec3(0.1, 0.4, 0.3);\n    if (p.x < -0.9 && p.y > -0.8 && p.y < 0.3) {\n        col = vec3(0.1, 0.4, 0.4); \n        \n        if (p.x < -1.1 && p.y > -0.2 && p.y < 0.2) {\n            col = vec3(0.95); \n            if (fract(p.x * 20.0 + sin(p.y*10.0)) < 0.2) col *= 0.8;\n        }\n        \n        if (fract(p.x * 10.0 + iTime * 0.2) < 0.2) { \n            col = fenceGreen;\n            col *= 0.8 + 0.2 * sin(p.x * 100.0);\n        }\n        if (abs(p.y - 0.2) < 0.02) col = fenceGreen;\n    }\n}\n\nvoid layer_Noise(in vec2 p, in float iTime, inout vec3 col) {\n    col *= 0.95 + 0.05 * fract(sin(dot(p + iTime*0.05, vec2(12.9898, 78.233))) * 43758.5453);\n}\n\nvec4 layer_Background(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n    \n    vec3 col = vec3(-1.0);\n    \n    layer_Background(p, col);\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_Roof(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n    \n    vec3 col = vec3(-1.0);\n    \n    layer_Roof(p, iTime, col);\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_Symbols(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n    \n    vec3 col = vec3(-1.0);\n    \n    layer_Symbols(p, col);\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_Gate(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n    \n    vec3 col = vec3(-1.0);\n    \n    layer_Gate(p, iTime, col);\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_Fence(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n    \n    vec3 col = vec3(-1.0);\n    \n    layer_Fence(p, iTime, col);\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_Noise(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n    \n    vec3 col = vec3(-1.0);\n    \n    layer_Noise(p, iTime, col);\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n","IMG_6551":"void layer_Wall(in vec2 p, in float iTime, inout vec3 col) {\n    vec3 wallColor = vec3(0.85, 0.7, 0.65); \n    col = wallColor;\n    \n    float wallNoise = fract(sin(dot(p + iTime*0.05, vec2(12.9898, 78.233))) * 43758.5453);\n    col *= 0.95 + 0.05 * wallNoise;\n}\n\nvoid layer_Railing(in vec2 p, in float iTime, inout vec3 col) {\n    if (p.y > -0.6 && p.y < -0.1) {\n        vec3 balconyTan = vec3(0.85, 0.8, 0.6); \n        bool isRailing = false;\n        \n        if (abs(p.y - -0.1) < 0.015) isRailing = true;\n        if (abs(p.y - -0.6) < 0.015) isRailing = true;\n        if (fract(p.x * 4.0) < 0.05) isRailing = true;\n        \n        vec2 panelP = vec2(fract(p.x * 4.0) - 0.5, p.y - -0.35);\n        panelP.x *= 0.25; \n        \n        float bloom = sin(iTime * 2.0 + p.x * 5.0) * 0.01;\n        float rL = length(panelP);\n        \n        if (abs(rL - 0.1 - bloom) < 0.005 && panelP.y > -0.05) isRailing = true; \n        if (abs(length(panelP - vec2(0.0, 0.1)) - 0.08 + bloom) < 0.005) isRailing = true; \n        if (abs(panelP.x) < 0.005 && panelP.y < 0.0) isRailing = true;\n        \n        if (isRailing) {\n            col = balconyTan;\n            col *= 0.8 + 0.2 * cos(p.x * 20.0);\n        } else {\n            col *= 0.85; \n        }\n    }\n}\n\nvoid layer_Window(in vec2 p, in float iTime, inout vec3 col) {\n    vec2 winCenter = vec2(0.1, 0.4);\n    vec2 winSize = vec2(0.4, 0.45);\n    vec3 frameColor = vec3(0.8, 0.4, 0.4);  \n    vec3 grilleGreen = vec3(0.2, 0.6, 0.5); \n    \n    if (abs(p.x - winCenter.x) < winSize.x && abs(p.y - winCenter.y) < winSize.y) {\n        col = vec3(0.05, 0.08, 0.1); \n        \n        if (abs(p.x - winCenter.x) > winSize.x - 0.02 || abs(p.y - winCenter.y) > winSize.y - 0.02) {\n            col = frameColor;\n        } else {\n            vec2 gw = (p - winCenter + winSize) / (winSize * 2.0); \n            \n            vec2 cellCoords = vec2(gw.x * 2.0, gw.y * 4.0);\n            cellCoords.x += sin(iTime + cellCoords.y) * 0.1;\n            \n            vec2 localGrid = fract(cellCoords) - 0.5; \n            \n            bool isGrille = false;\n            \n            if (abs(localGrid.x) > 0.45 || abs(localGrid.y) > 0.45) isGrille = true;\n            \n            float rG = length(localGrid);\n            if (abs(rG - 0.15) < 0.04) isGrille = true;\n            \n            if (abs(length(localGrid - vec2(0.5, 0.5)) - 0.35) < 0.03) isGrille = true;\n            if (abs(length(localGrid - vec2(-0.5, -0.5)) - 0.35) < 0.03) isGrille = true;\n            if (abs(length(localGrid - vec2(-0.5, 0.5)) - 0.35) < 0.03) isGrille = true;\n            if (abs(length(localGrid - vec2(0.5, -0.5)) - 0.35) < 0.03) isGrille = true;\n            \n            if (isGrille) {\n                col = grilleGreen;\n                if (localGrid.x + localGrid.y > 0.0) col += vec3(0.1, 0.2, 0.1);\n            }\n        }\n    }\n}\n\nvoid layer_RoofAndDragon(in vec2 p, in float iTime, inout vec3 col) {\n    if (p.x > 0.8) {\n        float roofCurve = p.x - 0.8 + p.y * 0.2; \n        \n        if (p.y < -0.1 + roofCurve*2.0 && p.y > -0.5) {\n            col = vec3(0.9);\n            if (fract(p.x * 15.0) < 0.5) col = vec3(0.2, 0.5, 0.4); \n            \n            if (p.y > -0.2 + roofCurve*2.0) {\n                col = vec3(0.7, 0.3, 0.3); \n                float scallops = sin(p.x * 30.0) * sin(p.y * 20.0 + iTime*2.0); \n                col *= 0.8 + 0.2 * scallops;\n            }\n        }\n        \n        vec2 dragP = p - vec2(1.2, 0.2); \n        if (length(dragP) < 0.3) {\n            float aD = atan(dragP.y, dragP.x);\n            float dragShape = 0.15;\n            \n            dragShape += sin(aD * 8.0 + iTime*3.0) * 0.05;\n            dragShape += cos(aD * 15.0 - iTime*5.0) * 0.03;\n            dragShape += sin(dragP.x * 50.0) * 0.02; \n            \n            if (length(dragP) < dragShape) {\n                col = vec3(0.8, 0.7, 0.2); \n                float spec = pow(max(0.0, sin(dragP.x * 20.0 + dragP.y * 20.0 + iTime*4.0)), 4.0); \n                col += vec3(spec);\n                if (dragP.y < 0.0) col *= 0.6;\n            }\n        }\n    }\n}\n\nvec4 layer_Wall(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 col = vec3(-1.0);\n    \n    layer_Wall(p, iTime, col);\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_Railing(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 col = vec3(-1.0);\n    \n    layer_Railing(p, iTime, col);\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_Window(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 col = vec3(-1.0);\n    \n    layer_Window(p, iTime, col);\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_RoofandDragon(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 col = vec3(-1.0);\n    \n    layer_RoofAndDragon(p, iTime, col);\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n","IMG_6574":"void layer_Sky(in vec2 p, inout vec3 col) {\n    col = mix(vec3(0.8, 0.9, 0.95), vec3(0.5, 0.7, 0.9), p.y * 0.5 + 0.5);\n}\n\nvoid layer_Trees(in vec2 p, in float iTime, inout vec3 col) {\n    float treeNoise = fract(sin(dot(p * 5.0 + iTime*0.05, vec2(12.9898, 78.233))) * 43758.5453);\n    float treeNoise2 = fract(sin(dot(p * 15.0, vec2(39.346, 11.135))) * 43758.5453);\n    float treeSway = sin(iTime + p.x * 5.0) * 0.05;\n    float treeShape = sin(p.x * 3.0 + treeSway) * 0.2 + cos(p.x * 7.0 - treeSway) * 0.1 + 0.2;\n    if (p.y > treeShape - treeNoise * 0.3) {\n        vec3 treeCol = mix(vec3(0.1, 0.25, 0.15), vec3(0.15, 0.3, 0.2), treeNoise2);\n        col = mix(col, treeCol, 0.9); \n    }\n}\n\nvoid layer_Ground(in vec2 p, in float iTime, inout vec3 col) {\n    if (p.y < -0.7) {\n        col = vec3(0.15, 0.15, 0.18);\n        vec2 grid = fract(p * 20.0 - vec2(iTime*2.0, 0.0)) - 0.5;\n        if (length(grid) < 0.2) col *= 0.8;\n    }\n}\n\nvoid layer_Poles(in vec2 p, inout vec3 col) {\n    float dPole1 = abs(p.x + 0.9) - 0.05; \n    float dPole2 = abs(p.x - 0.7) - 0.05; \n    \n    if (dPole1 < 0.0 || dPole2 < 0.0) {\n        col = vec3(0.15, 0.1, 0.1);\n        float poleX = dPole1 < 0.0 ? (p.x + 0.9)/0.05 : (p.x - 0.7)/0.05;\n        col += vec3(0.1) * pow(max(0.0, sin(poleX * 3.14159 + 1.5)), 4.0);\n    }\n}\n\nvoid layer_Windows(in vec2 p, inout vec3 col) {\n    if (p.x < -0.95 && p.y > -0.6 && p.y < 0.2) {\n        col = vec3(0.1, 0.3, 0.7);\n        col *= 0.8 + 0.2 * cos(p.y * 20.0); \n    }\n    if (p.x > 0.65 && p.y > 0.6) {\n        col = vec3(0.1, 0.3, 0.7);\n    }\n}\n\nvoid layer_Moto(in vec2 p, in float iTime, inout vec3 col) {\n    vec3 motoOrange = vec3(0.9, 0.4, 0.1);\n    float dMoto = 1.0;\n    \n    vec2 mp = p - vec2(-0.1, -0.2) - vec2(0.0, sin(iTime * 10.0) * 0.005); \n    \n    float wheelFront = length(mp - vec2(0.5, -0.2)) - 0.25;\n    float wheelRear  = length(mp - vec2(-0.5, -0.2)) - 0.25;\n    dMoto = min(wheelFront, wheelRear);\n    \n    vec2 bp = mp - vec2(0.0, 0.0);\n    float body1 = max(abs(bp.x) - 0.4, abs(bp.y) - 0.15);\n    \n    vec2 fp = mp - vec2(0.4, 0.2);\n    float c = cos(0.5); float s = sin(0.5);\n    mat2 rot1 = mat2(c, -s, s, c);\n    fp = rot1 * fp;\n    float fairing = max(abs(fp.x) - 0.2, abs(fp.y) - 0.15);\n    \n    vec2 tp = mp - vec2(-0.4, 0.1);\n    float c2 = cos(-0.2); float s2 = sin(-0.2);\n    mat2 rot2 = mat2(c2, -s2, s2, c2);\n    tp = rot2 * tp;\n    float tail = max(abs(tp.x) - 0.25, abs(tp.y) - 0.1);\n    \n    float seatDip = length(mp - vec2(-0.1, 0.3)) - 0.2;\n    \n    float bodyShape = min(min(body1, fairing), tail);\n    bodyShape = max(bodyShape, -seatDip);\n    \n    dMoto = min(dMoto, bodyShape);\n    \n    float pipe = max(abs(mp.x) - 0.3, abs(mp.y + 0.4)) - 0.05;\n    dMoto = min(dMoto, pipe);\n\n    if (dMoto < 0.0) {\n        col = motoOrange;\n        \n        if (wheelFront < 0.0) {\n            float a = atan(mp.y + 0.2, mp.x - 0.5) + iTime * 5.0;\n            if (sin(a * 5.0) > 0.8) col *= 0.8; \n            if (length(mp - vec2(0.5, -0.2)) < 0.08) col *= 0.8; \n            if (length(mp - vec2(0.5, -0.2)) > 0.2) col *= 0.9; \n        }\n        if (wheelRear < 0.0) {\n            float a = atan(mp.y + 0.2, mp.x + 0.5) + iTime * 5.0;\n            if (sin(a * 5.0) > 0.8) col *= 0.8;\n            if (length(mp - vec2(-0.5, -0.2)) < 0.08) col *= 0.8;\n            if (length(mp - vec2(-0.5, -0.2)) > 0.2) col *= 0.9;\n        }\n        \n        if (length(mp - vec2(0.1, -0.1)) < 0.15) col *= 0.9; \n        if (abs(mp.x - 0.4) < 0.05 && mp.y > 0.1) col *= 0.85; \n        if (length(mp - vec2(0.4, 0.25)) < 0.03) col *= 0.7; \n        \n        col *= 0.8 + 0.3 * smoothstep(-0.02, 0.0, dMoto); \n        col *= 0.6 + 0.4 * smoothstep(-0.5, 0.5, mp.y + mp.x*0.2);\n    }\n}\n\nvoid layer_Noise(in vec2 p, in float iTime, inout vec3 col) {\n    col *= 0.95 + 0.05 * fract(sin(dot(p + iTime*0.05, vec2(12.9898, 78.233))) * 43758.5453);\n}\n\nvec4 layer_Sky(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n    \n    vec3 col = vec3(-1.0);\n    \n    layer_Sky(p, col);\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_Trees(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n    \n    vec3 col = vec3(-1.0);\n    \n    layer_Trees(p, iTime, col);\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_Ground(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n    \n    vec3 col = vec3(-1.0);\n    \n    layer_Ground(p, iTime, col);\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_Poles(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n    \n    vec3 col = vec3(-1.0);\n    \n    layer_Poles(p, col);\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_Windows(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n    \n    vec3 col = vec3(-1.0);\n    \n    layer_Windows(p, col);\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_Moto(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n    \n    vec3 col = vec3(-1.0);\n    \n    layer_Moto(p, iTime, col);\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_Noise(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n    \n    vec3 col = vec3(-1.0);\n    \n    layer_Noise(p, iTime, col);\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n","IMG_6609":"void layer_Background(in vec2 p, in float iTime, inout vec3 col) {\n    vec3 bgGreen = vec3(0.45, 0.6, 0.5);\n    col = bgGreen;\n    \n    float corrugation = sin(p.x * 60.0 + iTime * 2.0);\n    col *= 0.8 + 0.2 * corrugation;\n    col *= 0.6 + 0.4 * smoothstep(-1.0, 0.0, p.y);\n    \n    if (p.y < -0.8) {\n        col = vec3(0.3, 0.3, 0.28); \n        col *= 0.8 + 0.4 * fract(sin(dot(p * 10.0 + iTime*0.05, vec2(12.9898, 78.233))) * 43758.5453);\n    }\n}\n\nvoid layer_LatticePanels(in vec2 p, in float iTime, in int panel, inout vec3 col, in vec3 bgGreen, in vec3 framePurple) {\n    if (panel == 1 || panel == 3) {\n        vec2 lp = (panel == 1) ? (p - vec2(-0.55, 0.0)) : (p - vec2(0.55, 0.0));\n        vec2 grid = lp * vec2(4.0, 3.5); \n        \n        grid.y += sin(iTime + lp.x * 5.0) * 0.2;\n        \n        vec2 fGrid = fract(grid) - 0.5;\n        \n        float r1 = length(fGrid - vec2(0.15, 0.0));\n        float r2 = length(fGrid - vec2(-0.15, 0.0));\n        float r3 = length(fGrid - vec2(0.0, 0.15));\n        float r4 = length(fGrid - vec2(0.0, -0.15));\n        \n        float clover = min(min(r1, r2), min(r3, r4));\n        float diamond = abs(fGrid.x) + abs(fGrid.y);\n        \n        if (clover < 0.2 || diamond < 0.15) {\n            col = bgGreen * (0.6 + 0.2 * sin(p.x * 60.0 + iTime*2.0));\n            col *= 0.5; \n        } else {\n            col *= 0.9 + 0.1 * smoothstep(0.2, 0.22, clover);\n        }\n        \n        if (abs(lp.x) > 0.21 || abs(lp.y) > 0.65) {\n            col = framePurple; \n        }\n    }\n}\n\nvoid layer_CenterPanel(in vec2 p, in float iTime, in int panel, inout vec3 col, in vec3 framePurple) {\n    if (panel == 2) {\n        if (abs(p.x) < 0.27 && abs(p.y) < 0.65) {\n            vec3 paintCol = vec3(0.6, 0.6, 0.5);\n            \n            vec2 bp = p - vec2(-0.3, -0.7);\n            if (fract(bp.x * 8.0) < 0.05 || fract(bp.y * 10.0) < 0.05) paintCol *= 0.8;\n            \n            float archDist = length(p - vec2(-0.1, 0.2));\n            if (archDist < 0.2 && p.y < 0.2) paintCol = vec3(0.2); \n            if (abs(archDist - 0.2) < 0.03 && p.x < -0.1 && p.y < 0.2) paintCol = vec3(0.5, 0.5, 0.4); \n            \n            if (p.x > 0.15 && p.y > -0.1) paintCol = vec3(0.7, 0.7, 0.6); \n            \n            float horseBody = length(p - vec2(-0.05, -0.3));\n            if (horseBody < 0.25) paintCol = vec3(0.2, 0.1, 0.1); \n            \n            if (length(p - vec2(-0.15, -0.15)) < 0.1) paintCol = vec3(0.9);\n            \n            vec3 skinCol = vec3(0.9, 0.75, 0.65);\n            if (length(p - vec2(0.0, -0.15)) < 0.08) paintCol = skinCol; \n            if (length(p - vec2(0.05, -0.25)) < 0.06) paintCol = skinCol; \n            \n            float hairFlow = sin(p.y * 20.0 - iTime * 3.0) * 0.01;\n            if (length(p - vec2(0.05 + hairFlow, -0.1)) < 0.05) paintCol = vec3(0.1);\n            \n            float clothFlow = sin(p.y * 15.0 - iTime * 2.0) * 0.02;\n            if (abs(p.x - 0.1 - clothFlow) < 0.05 && p.y > -0.4 && p.y < -0.2) paintCol = vec3(0.85);\n\n            col = paintCol;\n        } else {\n            col = framePurple; \n        }\n    }\n}\n\nvec4 layer_Background(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 col = vec3(-1.0);\n    vec3 bgGreen = vec3(0.45, 0.6, 0.5);\n    vec3 framePurple = vec3(0.65, 0.55, 0.75); \n    \n    layer_Background(p, iTime, col);\n\n    bool inFrame = false;\n    int panel = 0; \n    \n    if (p.y > -0.7 && p.y < 0.7) {\n        if (p.x > -0.8 && p.x < -0.3) { inFrame = true; panel = 1; }\n        if (p.x >= -0.3 && p.x <= 0.3) { inFrame = true; panel = 2; }\n        if (p.x > 0.3 && p.x < 0.8) { inFrame = true; panel = 3; }\n    }\n    \n    if (inFrame) {\n        col = framePurple;\n        \n        \n        if (col == framePurple) {\n            col *= 0.8 + 0.2 * (p.x + p.y + 1.0);\n        }\n    }\n\n    col *= 1.0 - 0.3 * length(p);\n    \n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_LatticePanels(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 col = vec3(-1.0);\n    vec3 bgGreen = vec3(0.45, 0.6, 0.5);\n    vec3 framePurple = vec3(0.65, 0.55, 0.75); \n    \n\n    bool inFrame = false;\n    int panel = 0; \n    \n    if (p.y > -0.7 && p.y < 0.7) {\n        if (p.x > -0.8 && p.x < -0.3) { inFrame = true; panel = 1; }\n        if (p.x >= -0.3 && p.x <= 0.3) { inFrame = true; panel = 2; }\n        if (p.x > 0.3 && p.x < 0.8) { inFrame = true; panel = 3; }\n    }\n    \n    if (inFrame) {\n        col = framePurple;\n        \n        layer_LatticePanels(p, iTime, panel, col, bgGreen, framePurple);\n        \n        if (col == framePurple) {\n            col *= 0.8 + 0.2 * (p.x + p.y + 1.0);\n        }\n    }\n\n    col *= 1.0 - 0.3 * length(p);\n    \n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_CenterPanel(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 col = vec3(-1.0);\n    vec3 bgGreen = vec3(0.45, 0.6, 0.5);\n    vec3 framePurple = vec3(0.65, 0.55, 0.75); \n    \n\n    bool inFrame = false;\n    int panel = 0; \n    \n    if (p.y > -0.7 && p.y < 0.7) {\n        if (p.x > -0.8 && p.x < -0.3) { inFrame = true; panel = 1; }\n        if (p.x >= -0.3 && p.x <= 0.3) { inFrame = true; panel = 2; }\n        if (p.x > 0.3 && p.x < 0.8) { inFrame = true; panel = 3; }\n    }\n    \n    if (inFrame) {\n        col = framePurple;\n        \n        layer_CenterPanel(p, iTime, panel, col, framePurple);\n        \n        if (col == framePurple) {\n            col *= 0.8 + 0.2 * (p.x + p.y + 1.0);\n        }\n    }\n\n    col *= 1.0 - 0.3 * length(p);\n    \n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n","IMG_6693":"void layer_Shelves(in vec2 p, in float iTime, inout vec3 col) {\n    col = vec3(0.85, 0.85, 0.85); \n    \n    vec2 sp = p;\n    float shelfY = fract(sp.y * 3.0); \n    float shelfX = fract(sp.x * 2.0); \n    \n    if (shelfY < 0.05 || shelfX < 0.05) {\n        col = vec3(0.9); \n    } else {\n        vec2 cellId = floor(vec2(sp.x * 2.0, sp.y * 3.0));\n        float rand = fract(sin(dot(cellId, vec2(12.9898, 78.233))) * 43758.5453 + iTime * 0.2);\n        \n        if (rand < 0.3) col = vec3(0.9, 0.8, 0.2); \n        else if (rand < 0.5) col = vec3(0.8, 0.2, 0.2); \n        else if (rand < 0.7) col = vec3(0.2, 0.6, 0.3); \n        else if (rand < 0.8) col = vec3(0.3, 0.3, 0.3); \n        else col = vec3(0.7); \n        \n        col *= 0.8 + 0.2 * fract(sin(p.x * 50.0 + p.y * 50.0 + iTime)*100.0);\n        col *= 0.6 + 0.4 * shelfY; \n    }\n}\n\nvoid layer_SideObject(in vec2 p, inout vec3 col) {\n    if (p.x > 0.6 && p.y > -0.5 && p.y < 0.5) {\n        col = vec3(0.5, 0.5, 0.55); \n        if (abs(p.x - 0.8) < 0.02) col = vec3(0.9, 0.4, 0.1); \n    }\n}\n\nvoid layer_Figure(in vec2 p, in float iTime, inout vec3 col, out bool isFigure) {\n    float breath = sin(iTime * 2.0) * 0.01;\n    vec2 fp = p;\n    fp.y -= breath;\n\n    vec3 shirtBlue = vec3(0.15, 0.25, 0.7);\n    vec3 pantsBlue = vec3(0.2, 0.3, 0.4);\n    vec3 silhouetteBlack = vec3(0.0);\n    \n    isFigure = false;\n    \n    float dHead = length(fp - vec2(0.0, 0.65)) - 0.18;\n    if (dHead < 0.0) {\n        col = silhouetteBlack;\n        isFigure = true;\n    }\n    \n    if (abs(fp.x) < 0.1 && fp.y > 0.4 && fp.y < 0.5) {\n        col = shirtBlue * 0.8; \n        isFigure = true;\n    }\n    \n    float dTorso = max(abs(fp.x) - 0.3 - breath, abs(fp.y - 0.05) - 0.45);\n    float torsoWidth = 0.3 + breath - (fp.y - 0.05) * -0.05;\n    if (fp.y < 0.5 && fp.y > -0.4 && abs(fp.x) < torsoWidth) {\n        col = shirtBlue;\n        isFigure = true;\n        \n        if (abs(fp.x) < 0.01) col *= 0.8;\n        if (fract(fp.y * 5.0) < 0.1 && abs(fp.x) < 0.015) col = vec3(0.1);\n        \n        if (abs(abs(fp.x) - 0.15) < 0.08 && fp.y > 0.15 && fp.y < 0.3) {\n            col *= 0.9; \n            if (fp.y > 0.26) col *= 0.95;\n            if (abs(abs(fp.x) - 0.15) < 0.01 && abs(fp.y - 0.28) < 0.01) col = vec3(0.1);\n        }\n        \n        col *= 0.8 + 0.2 * cos(fp.x * 20.0 + fp.y * 10.0 + iTime*3.0);\n    }\n    \n    float dArmLeft = max(abs(fp.x + 0.35 + breath) - 0.08, abs(fp.y - 0.0) - 0.3);\n    if (dArmLeft < 0.0 && fp.y < 0.4) {\n        col = shirtBlue * 0.9; \n        isFigure = true;\n    }\n    float dArmRight = max(abs(fp.x - 0.35 - breath) - 0.08, abs(fp.y - 0.0) - 0.3);\n    if (dArmRight < 0.0 && fp.y < 0.4) {\n        col = shirtBlue * 0.9;\n        isFigure = true;\n    }\n    \n    if (length(fp - vec2(-0.35 - breath, -0.35)) < 0.06) {\n        col = silhouetteBlack;\n        isFigure = true;\n    }\n    if (length(fp - vec2(0.35 + breath, -0.35)) < 0.06) {\n        col = silhouetteBlack;\n        isFigure = true;\n    }\n    \n    if (p.y < -0.4 && abs(p.x) < 0.28) {\n        col = pantsBlue;\n        if (abs(p.x) < 0.02) col = vec3(0.1, 0.15, 0.2);\n        col *= 0.9 + 0.1 * fract(sin(p.x*100.0 + p.y*100.0)*10.0);\n        isFigure = true;\n    }\n}\n\nvoid layer_Shadow(in vec2 p, in bool isFigure, inout vec3 col) {\n    if (!isFigure) {\n        float shadow = length(p - vec2(0.0, 0.0)) - 0.5;\n        if (shadow < 0.0) col *= 0.8;\n    }\n}\n\nvec4 layer_Shelves(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 col = vec3(-1.0);\n    \n    layer_Shelves(p, iTime, col);\n    \n    bool isFigure;\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_SideObject(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 col = vec3(-1.0);\n    \n    layer_SideObject(p, col);\n    \n    bool isFigure;\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_Figure(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 col = vec3(-1.0);\n    \n    \n    bool isFigure;\n    layer_Figure(p, iTime, col, isFigure);\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_Shadow(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 col = vec3(-1.0);\n    \n    \n    bool isFigure;\n    layer_Shadow(p, isFigure, col);\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n","IMG_6694":"void layer_Shelves(in vec2 p, in float iTime, inout vec3 col) {\n    col = vec3(0.88, 0.88, 0.85); \n    \n    vec2 sp = p + vec2(0.1, 0.0); \n    float shelfY = fract(sp.y * 3.5); \n    float shelfX = fract(sp.x * 2.5); \n    \n    if (shelfY < 0.05 || shelfX < 0.05) {\n        col = vec3(0.95); \n    } else {\n        vec2 cellId = floor(vec2(sp.x * 2.5, sp.y * 3.5));\n        float rand = fract(sin(dot(cellId, vec2(39.346, 11.135))) * 43758.5453);\n        \n        if (rand < 0.2) col = vec3(0.8, 0.8, 0.2); \n        else if (rand < 0.4) col = vec3(0.1, 0.3, 0.7); \n        else if (rand < 0.6) col = vec3(0.8, 0.3, 0.2); \n        else if (rand < 0.8) col = vec3(0.2, 0.2, 0.2); \n        else col = vec3(0.6); \n        \n        col *= 0.8 + 0.2 * fract(sin(p.x * 40.0 + p.y * 60.0 + iTime*2.0)*100.0);\n        col *= 0.7 + 0.3 * shelfY; \n    }\n}\n\nvoid layer_SideObject(in vec2 p, inout vec3 col) {\n    if (p.x > 0.5 && p.y > -0.2 && p.y < 0.6) {\n        col = vec3(0.2, 0.2, 0.25); \n        if (abs(p.x - 0.7) < 0.02) col = vec3(0.8, 0.8, 0.1); \n    }\n}\n\nvoid layer_Figure(in vec2 p, in float iTime, inout vec3 col, out bool isFigure) {\n    vec3 shirtColor = vec3(0.85, 0.84, 0.82); \n    vec3 pantsBlue = vec3(0.2, 0.3, 0.45);\n    vec3 silhouetteBlack = vec3(0.0);\n    \n    isFigure = false;\n    \n    float breath = sin(iTime * 1.5) * 0.01;\n    vec2 fp = p;\n    fp.y -= breath;\n\n    float dHead = length(fp - vec2(0.0, 0.65)) - 0.18;\n    if (dHead < 0.0) {\n        col = silhouetteBlack;\n        isFigure = true;\n    }\n    \n    if (abs(fp.x) < 0.12 && fp.y > 0.4 && fp.y < 0.5) {\n        col = shirtColor;\n        if (abs(fp.x) < 0.02 && fp.y > 0.45) col *= 0.8;\n        isFigure = true;\n    }\n    \n    float torsoWidth = 0.32 + breath - (fp.y) * -0.05;\n    if (fp.y < 0.45 && fp.y > -0.45 && abs(fp.x) < torsoWidth) {\n        col = shirtColor;\n        isFigure = true;\n        \n        if (abs(fp.x) < 0.03) col *= 0.95;\n        if (abs(fp.x - 0.03) < 0.005) col *= 0.8;\n        if (fract(fp.y * 5.0) < 0.08 && abs(fp.x) < 0.01) col = vec3(0.7);\n        \n        if (abs(abs(fp.x) - 0.16) < 0.09 && fp.y > 0.15 && fp.y < 0.32) {\n            col *= 0.95; \n            if (fp.y > 0.27) {\n                col *= 0.9;\n                if (fp.y < 0.29 - abs(abs(fp.x)-0.16)*0.2) col *= 0.9;\n            }\n            if (abs(abs(fp.x) - 0.16) < 0.015 && abs(fp.y - 0.29) < 0.015) col = vec3(0.7);\n        }\n        \n        col *= 0.85 + 0.15 * cos(fp.x * 25.0 + fp.y * 15.0 + iTime*4.0);\n        if (fp.y < -0.4) col *= 0.8;\n    }\n    \n    float dArmLeft = max(abs(fp.x + 0.38 + breath) - 0.09, abs(fp.y - 0.0) - 0.35);\n    if (dArmLeft < 0.0 && fp.y < 0.4) {\n        col = shirtColor * 0.9; \n        isFigure = true;\n    }\n    float dArmRight = max(abs(fp.x - 0.38 - breath) - 0.09, abs(fp.y - 0.0) - 0.35);\n    if (dArmRight < 0.0 && fp.y < 0.4) {\n        col = shirtColor * 0.9;\n        isFigure = true;\n    }\n    \n    if (length(fp - vec2(-0.4 - breath, -0.4)) < 0.06) {\n        col = silhouetteBlack;\n        isFigure = true;\n    }\n    if (length(fp - vec2(0.4 + breath, -0.4)) < 0.06) {\n        col = silhouetteBlack;\n        isFigure = true;\n    }\n    \n    if (p.y < -0.45 && abs(p.x) < 0.28) {\n        col = pantsBlue;\n        if (abs(p.x) < 0.02) col = vec3(0.1, 0.15, 0.2);\n        col *= 0.9 + 0.1 * fract(sin(p.x*80.0 + p.y*80.0)*10.0);\n        isFigure = true;\n    }\n}\n\nvoid layer_Shadow(in vec2 p, in bool isFigure, inout vec3 col) {\n    if (!isFigure) {\n        float shadow = length(p - vec2(0.0, 0.0)) - 0.55;\n        if (shadow < 0.0) col *= 0.85;\n    }\n}\n\nvec4 layer_Shelves(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 col = vec3(-1.0);\n    \n    layer_Shelves(p, iTime, col);\n    \n    bool isFigure;\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_SideObject(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 col = vec3(-1.0);\n    \n    layer_SideObject(p, col);\n    \n    bool isFigure;\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_Figure(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 col = vec3(-1.0);\n    \n    \n    bool isFigure;\n    layer_Figure(p, iTime, col, isFigure);\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_Shadow(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 col = vec3(-1.0);\n    \n    \n    bool isFigure;\n    layer_Shadow(p, isFigure, col);\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n","IMG_6695":"void layer_Background(in vec2 p, in float iTime, inout vec3 col) {\n    if (p.y < -0.2) {\n        col = vec3(0.8, 0.75, 0.65); \n    } else {\n        col = mix(vec3(0.7, 0.7, 0.6), vec3(0.85, 0.85, 0.8), p.y);\n        if (p.x < -0.3 && p.y > 0.4) col = mix(col, vec3(0.8, 0.7, 0.3), 0.5);\n    }\n    \n    float bgNoise = sin(p.x * 5.0 + iTime) * cos(p.y * 4.0 + iTime*0.5);\n    col *= 0.95 + 0.05 * bgNoise;\n}\n\nvoid layer_Hat(in vec2 p, inout vec3 col, out bool isFigureHat) {\n    isFigureHat = false;\n    vec3 hatWhite = vec3(0.95);\n    float dHat = length(p - vec2(0.0, 0.7));\n    if (dHat < 0.18 && p.y > 0.65) {\n        col = hatWhite;\n        col *= 0.7 + 0.3 * smoothstep(0.18, 0.0, dHat);\n        if (abs(p.y - 0.65) < 0.02 && abs(p.x) < 0.2) col = vec3(0.9);\n        isFigureHat = true;\n    }\n}\n\nvoid layer_Face(in vec2 p, inout vec3 col, out bool isFigureFace) {\n    isFigureFace = false;\n    vec3 skinTone = vec3(0.85, 0.65, 0.5);\n    float dFace = length(vec2(p.x, (p.y - 0.55) * 1.2)); \n    if (dFace < 0.12 && p.y <= 0.65 && p.y > 0.4) {\n        col = skinTone;\n        col *= 0.8 + 0.2 * (p.y - 0.4)/0.25;\n        if (abs(p.y - 0.58) < 0.01 && abs(abs(p.x) - 0.05) < 0.02) col = vec3(0.1);\n        if (abs(p.y - 0.48) < 0.005 && abs(p.x) < 0.04) col = vec3(0.3, 0.2, 0.2);\n        if (abs(p.x) < 0.12 && abs(p.y - 0.42 - abs(p.x)*0.5) < 0.01) col = vec3(0.1); \n        if (abs(p.y - 0.4) < 0.01 && abs(p.x) < 0.05) col = vec3(0.1); \n        isFigureFace = true;\n    }\n}\n\nvoid layer_Torso(in vec2 p, in float iTime, inout vec3 col, out bool isFigureTorso) {\n    isFigureTorso = false;\n    vec3 uniformBlue = vec3(0.1, 0.15, 0.35); \n    vec3 accentOrange = vec3(0.95, 0.4, 0.1); \n    float torsoWidth = 0.35 - (p.y) * -0.02;\n    if (p.y < 0.4 && p.y > -0.2 && abs(p.x) < torsoWidth) {\n        col = uniformBlue;\n        isFigureTorso = true;\n        \n        if (p.y > 0.25 && p.y < 0.4) {\n            float collarLine = 0.4 - abs(p.x) * 1.5;\n            if (p.y < collarLine && abs(p.x) > 0.02) {\n                col = accentOrange;\n            }\n        }\n        \n        if (p.y > 0.15 && p.y < 0.22) {\n            col = vec3(0.7); \n            float sweep = smoothstep(0.05, 0.0, abs(fract(p.x * 2.0 - iTime * 1.5) - 0.5));\n            col += vec3(0.5) * sweep;\n            \n            if (p.y > 0.2) col = accentOrange; \n            if (p.y < 0.17) col = accentOrange; \n        }\n        \n        if (abs(p.x) < 0.005) col = accentOrange;\n        \n        if (abs(abs(p.x) - 0.18) < 0.08 && p.y > 0.0 && p.y < 0.12) {\n            col *= 0.9;\n            if (p.y > 0.1) col = accentOrange;\n        }\n        \n        col *= 0.8 + 0.2 * cos(p.x * 10.0);\n        if (p.y < -0.18) col *= 0.8;\n    }\n}\n\nvoid layer_Arms(in vec2 p, in bool isFigurePrev, inout vec3 col, out bool isFigureArms) {\n    isFigureArms = false;\n    vec3 uniformBlue = vec3(0.1, 0.15, 0.35); \n    vec3 accentOrange = vec3(0.95, 0.4, 0.1); \n    vec3 skinTone = vec3(0.85, 0.65, 0.5);\n    \n    float dArmLeft = max(abs(p.x + 0.4) - 0.08, abs(p.y - 0.0) - 0.3);\n    if (dArmLeft < 0.0 && p.y < 0.3 && !isFigurePrev) {\n        col = uniformBlue;\n        isFigureArms = true;\n        if (p.y < -0.22) col = accentOrange;\n        col *= 0.9;\n    }\n    float dArmRight = max(abs(p.x - 0.4) - 0.08, abs(p.y - 0.0) - 0.3);\n    if (dArmRight < 0.0 && p.y < 0.3 && !isFigurePrev) {\n        col = uniformBlue;\n        isFigureArms = true;\n        if (p.y < -0.22) col = accentOrange;\n        col *= 0.9;\n    }\n    \n    if (length(p - vec2(-0.4, -0.35)) < 0.05) { col = skinTone; isFigureArms = true; }\n    if (length(p - vec2(0.4, -0.35)) < 0.05) { col = skinTone; isFigureArms = true; }\n}\n\nvoid layer_Pants(in vec2 p, inout vec3 col, out bool isFigurePants) {\n    isFigurePants = false;\n    vec3 uniformBlue = vec3(0.1, 0.15, 0.35); \n    vec3 accentOrange = vec3(0.95, 0.4, 0.1); \n    \n    if (p.y < -0.2 && abs(p.x) < 0.25) {\n        col = uniformBlue;\n        if (abs(p.x) < 0.02 && p.y < -0.4) col = vec3(0.05, 0.08, 0.2); \n        if (p.y < -0.4 && p.y > -0.7) {\n            if (p.x < -0.2) col = accentOrange;\n            if (p.x > 0.2) col = accentOrange;\n            if (abs(abs(p.x) - 0.23) < 0.01) col = uniformBlue;\n        }\n        col *= 0.85 + 0.15 * cos(p.x * 20.0);\n        isFigurePants = true;\n    }\n}\n\nvoid layer_Shadow(in vec2 p, in bool isFigure, inout vec3 col) {\n    if (!isFigure) {\n        float shadow = max(abs(p.x) - 0.4, abs(p.y) - 0.8);\n        if (shadow < 0.0) col *= 0.9;\n    }\n}\n\nvec4 layer_Background(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 col = vec3(-1.0);\n    \n    layer_Background(p, iTime, col);\n    \n    bool isFigureHat, isFigureFace, isFigureTorso, isFigureArms, isFigurePants;\n    \n    \n    bool isFigurePrev = isFigureHat || isFigureFace || isFigureTorso;\n    \n    bool isFigure = isFigureHat || isFigureFace || isFigureTorso || isFigureArms || isFigurePants;\n    \n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_Hat(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 col = vec3(-1.0);\n    \n    \n    bool isFigureHat, isFigureFace, isFigureTorso, isFigureArms, isFigurePants;\n    \n    layer_Hat(p, col, isFigureHat);\n    \n    bool isFigurePrev = isFigureHat || isFigureFace || isFigureTorso;\n    \n    bool isFigure = isFigureHat || isFigureFace || isFigureTorso || isFigureArms || isFigurePants;\n    \n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_Face(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 col = vec3(-1.0);\n    \n    \n    bool isFigureHat, isFigureFace, isFigureTorso, isFigureArms, isFigurePants;\n    \n    layer_Face(p, col, isFigureFace);\n    \n    bool isFigurePrev = isFigureHat || isFigureFace || isFigureTorso;\n    \n    bool isFigure = isFigureHat || isFigureFace || isFigureTorso || isFigureArms || isFigurePants;\n    \n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_Torso(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 col = vec3(-1.0);\n    \n    \n    bool isFigureHat, isFigureFace, isFigureTorso, isFigureArms, isFigurePants;\n    \n    layer_Torso(p, iTime, col, isFigureTorso);\n    \n    bool isFigurePrev = isFigureHat || isFigureFace || isFigureTorso;\n    \n    bool isFigure = isFigureHat || isFigureFace || isFigureTorso || isFigureArms || isFigurePants;\n    \n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_Arms(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 col = vec3(-1.0);\n    \n    \n    bool isFigureHat, isFigureFace, isFigureTorso, isFigureArms, isFigurePants;\n    \n    \n    bool isFigurePrev = isFigureHat || isFigureFace || isFigureTorso;\n    layer_Arms(p, isFigurePrev, col, isFigureArms);\n    \n    bool isFigure = isFigureHat || isFigureFace || isFigureTorso || isFigureArms || isFigurePants;\n    \n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_Pants(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 col = vec3(-1.0);\n    \n    \n    bool isFigureHat, isFigureFace, isFigureTorso, isFigureArms, isFigurePants;\n    \n    \n    bool isFigurePrev = isFigureHat || isFigureFace || isFigureTorso;\n    layer_Pants(p, col, isFigurePants);\n    \n    bool isFigure = isFigureHat || isFigureFace || isFigureTorso || isFigureArms || isFigurePants;\n    \n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_Shadow(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 col = vec3(-1.0);\n    \n    \n    bool isFigureHat, isFigureFace, isFigureTorso, isFigureArms, isFigurePants;\n    \n    \n    bool isFigurePrev = isFigureHat || isFigureFace || isFigureTorso;\n    \n    bool isFigure = isFigureHat || isFigureFace || isFigureTorso || isFigureArms || isFigurePants;\n    \n    layer_Shadow(p, isFigure, col);\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n","IMG_6697":"void layer_Panel(in vec2 p, in float iTime, inout vec3 col) {\n    vec3 panelGrey = vec3(0.75, 0.76, 0.78);\n    col = panelGrey;\n    \n    float seam1 = abs(p.x + 0.35);\n    float seam2 = abs(p.x - 0.45);\n    \n    if (seam1 < 0.005 || seam2 < 0.005) {\n        col = vec3(0.3); \n    }\n    \n    float dirt = fract(sin(dot(p + vec2(iTime*0.05, 0.0), vec2(12.9898, 78.233))) * 43758.5453);\n    float dirt2 = fract(sin(dot(p * 5.0 + vec2(0.0, iTime*0.3), vec2(39.346, 11.135))) * 43758.5453);\n    col *= 0.9 + 0.1 * dirt;\n    \n    if (dirt2 > 0.8 && p.x < -0.3) {\n        col *= 0.85;\n    }\n}\n\nvoid layer_TornPoster(in vec2 p, in float iTime, inout vec3 col) {\n    if (p.x < -0.35) {\n        if (p.y < 0.8 && p.y > -0.4) {\n            float flutter = sin(iTime * 10.0 + p.y * 20.0) * 0.01;\n            float tearNoise = fract(sin(p.x * 50.0 + p.y * 30.0) * 100.0);\n            float tearShape = sin(p.y * 10.0) * 0.1 + cos(p.y * 25.0) * 0.05 + flutter;\n            \n            float innerTear = fract(sin(p.x * 10.0 + p.y * 15.0 + iTime*2.0) * 50.0);\n            \n            bool isPoster = true;\n            \n            if (p.x > -0.4 + tearShape + tearNoise*0.05) isPoster = false;\n            if (p.y < -0.3 + tearNoise * 0.1 + flutter) isPoster = false;\n            \n            if (innerTear > 0.85) isPoster = false;\n            if (length(p - vec2(-0.8, -0.2)) < 0.15 + flutter) isPoster = false; \n            if (length(p - vec2(-0.8, 0.6)) < 0.1 + flutter) isPoster = false; \n            \n            if (isPoster) {\n                col = vec3(0.1); \n                \n                vec2 tp = p - vec2(-0.9, 0.7); \n                tp.y = -tp.y; \n                \n                float row = floor(tp.y / 0.15);\n                float colIdx = floor((p.x + 0.9) / 0.3);\n                \n                if (row >= 0.0 && row < 8.0 && colIdx >= 0.0 && colIdx < 2.0) {\n                    vec2 cellP = vec2(fract((p.x + 0.9)/0.3), fract(tp.y / 0.15));\n                    \n                    if (cellP.y > 0.2 && cellP.y < 0.8) {\n                        if (mod(row, 2.0) == 0.0) {\n                            if (fract(cellP.x * 10.0) > 0.2 && cellP.y > 0.4) col = vec3(0.9);\n                        } else {\n                            if (fract(cellP.x * 15.0) > 0.15 && cellP.y > 0.1) col = vec3(0.95);\n                        }\n                    }\n                }\n            }\n        }\n    }\n}\n\nvoid layer_TopCenterPoster(in vec2 p, in float iTime, inout vec3 col) {\n    vec2 spTop = p - vec2(0.0, 0.8);\n    if (abs(spTop.x) < 0.25 && abs(spTop.y) < 0.15) {\n        col = vec3(0.95); \n        \n        if (spTop.x < -0.15 && spTop.x > -0.2) {\n            float zig = sin(spTop.y * 30.0 + iTime*10.0) * 0.02; \n            if (spTop.x < -0.18 + zig) col = vec3(0.8, 0.1, 0.2); \n        }\n        \n        if (spTop.x > -0.1) {\n            if (abs(spTop.y - 0.05) < 0.03 && fract(spTop.x * 20.0) > 0.2) col = vec3(0.1);\n            if (abs(spTop.y + 0.05) < 0.015 && fract(spTop.x * 30.0) > 0.3) col = vec3(0.1);\n            if (abs(spTop.y + 0.1) < 0.015 && fract(spTop.x * 30.0) > 0.3) col = vec3(0.1);\n        }\n        if (abs(abs(spTop.x) - 0.25) < 0.005 || abs(abs(spTop.y) - 0.15) < 0.005) col = vec3(0.8);\n    }\n}\n\n#define STICKER(cx, cy, w, h, isGreen) \\\nif (abs(p.x - (cx)) < (w) && abs(p.y - (cy)) < (h)) { \\\n    col = vec3(0.95); \\\n    vec2 lp = p - vec2(cx, cy); \\\n    if (lp.y > (h)*0.5 && fract(lp.x*50.0)>0.2) col = vec3(0.1); \\\n    if (lp.y > -(h)*0.6 && lp.y < (h)*0.4) { \\\n        if (fract(lp.x * (20.0/(w))) > 0.1) { \\\n            col = isGreen ? vec3(0.1, 0.5, 0.4) : vec3(0.1, 0.2, 0.6); \\\n        } \\\n    } \\\n    if (abs(abs(p.x-(cx))-(w))<0.002 || abs(abs(p.y-(cy))-(h))<0.002) col = vec3(0.8); \\\n}\n\nvoid layer_Stickers(in vec2 p, inout vec3 col) {\n    STICKER(-0.15, 0.2, 0.1, 0.08, false); \n    STICKER(-0.25, -0.05, 0.1, 0.08, false); \n    STICKER(-0.25, -0.25, 0.1, 0.08, true);  \n    STICKER(-0.25, -0.45, 0.1, 0.08, true);  \n    STICKER(0.0, -0.2, 0.1, 0.08, false);  \n    STICKER(-0.25, -0.85, 0.1, 0.08, false); \n\n    STICKER(0.65, 0.6, 0.1, 0.08, false); \n    STICKER(0.65, 0.4, 0.1, 0.08, true);  \n    STICKER(0.9, 0.3, 0.1, 0.08, false);  \n    STICKER(0.8, 0.0, 0.1, 0.08, true);   \n    STICKER(0.8, -0.15, 0.1, 0.08, false); \n    STICKER(0.85, -0.35, 0.1, 0.08, false); \n    STICKER(0.9, -0.55, 0.1, 0.08, true);  \n    STICKER(0.9, -0.7, 0.1, 0.08, false);  \n    STICKER(0.9, 0.85, 0.1, 0.08, true);  \n}\n\nvec4 layer_Panel(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 col = vec3(-1.0);\n    \n    layer_Panel(p, iTime, col);\n    \n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_TornPoster(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 col = vec3(-1.0);\n    \n    layer_TornPoster(p, iTime, col);\n    \n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_TopCenterPoster(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 col = vec3(-1.0);\n    \n    layer_TopCenterPoster(p, iTime, col);\n    \n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_Stickers(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 col = vec3(-1.0);\n    \n    layer_Stickers(p, col);\n    \n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n","IMG_6755":"void layer_Background(in vec2 up_p, in float iTime, inout vec3 col) {\n    if (up_p.y < 0.0) {\n        col = vec3(0.35, 0.36, 0.38);\n        col *= 0.9 + 0.1 * fract(sin(dot(up_p, vec2(12.9898, 78.233))) * 43758.5453);\n        float lightSpot = exp(-length(up_p - vec2(0.5 + sin(iTime)*0.2, -0.5)) * 2.0);\n        col += vec3(0.1, 0.1, 0.05) * lightSpot;\n    } else {\n        col = vec3(0.85, 0.85, 0.85);\n        col *= 0.8 + 0.2 * (1.0 - up_p.y);\n        \n        if (abs(up_p.y - 0.6) < 0.02) col = vec3(0.6); \n        if (abs(up_p.y - 0.5) < 0.015) col = vec3(0.7, 0.2, 0.2); \n    }\n}\n\nvoid layer_Shadow(in vec2 up_p, in float iTime, inout vec3 col) {\n    if (up_p.y < 0.0) {\n        float skShadow = max(abs(up_p.x - up_p.y*0.5 + 0.2) - 0.2, abs(up_p.y + 0.5) - 0.4);\n        if (skShadow < 0.0) col *= 0.7;\n    }\n}\n\nvoid layer_Cutout(in vec2 up_p, in float iTime, inout vec3 col) {\n    float pulse = sin(iTime * 5.0) * 0.02;\n    float dCutout = 1.0;\n    \n    float base = length((up_p - vec2(0.0, -0.8)) * vec2(1.0, 3.0)) - 0.25;\n    \n    vec2 gp = up_p - vec2(0.0, -0.1);\n    \n    float dLegs = max(abs(gp.x) - 0.15, abs(gp.y + 0.35) - 0.35);\n    float legGap = max(abs(gp.x) - 0.02, abs(gp.y + 0.5) - 0.2);\n    dLegs = max(dLegs, -legGap);\n    \n    float dTorso = max(abs(gp.x) - 0.18, abs(gp.y - 0.2) - 0.2);\n    \n    vec2 apL = gp - vec2(-0.35, 0.3);\n    float dArmL = max(abs(apL.x) - 0.2, abs(apL.y) - 0.06);\n    float dGlove = length(apL - vec2(-0.25, 0.0)) - 0.08;\n    \n    vec2 apR = gp - vec2(0.2, 0.1);\n    float dArmR = max(abs(apR.x) - 0.05, abs(apR.y) - 0.2);\n    \n    float dHead = length(gp - vec2(0.0, 0.55)) - 0.12;\n    float dHat = max(abs(gp.x) - 0.14, abs(gp.y - 0.65) - 0.06);\n    \n    float dFigure = min(dLegs, dTorso);\n    dFigure = min(dFigure, dArmL);\n    dFigure = min(dFigure, dGlove);\n    dFigure = min(dFigure, dArmR);\n    dFigure = min(dFigure, dHead);\n    dFigure = min(dFigure, dHat);\n    \n    vec2 arrP = up_p - vec2(-0.4 + pulse, 0.2); \n    float arrBody = max(abs(arrP.x + 0.15) - 0.15, abs(arrP.y) - 0.25);\n    float arrHeadX = arrP.x - (-0.3);\n    float arrHead = max(abs(arrP.y) - (arrHeadX + 0.3), -arrHeadX - 0.3);\n    if (arrP.x > -0.3) arrHead = 1.0; \n    \n    float dArrow = min(arrBody, arrHead);\n    dCutout = min(dFigure, dArrow);\n    \n    if (base < 0.0 && dCutout > 0.02) col = vec3(0.1);\n    \n    if (dCutout < 0.02 && dCutout > 0.0) {\n        col = vec3(1.0); \n    } \n    else if (dCutout <= 0.0) {\n        if (dArrow <= 0.0 && dFigure > 0.0) {\n            col = vec3(0.9, 0.45, 0.2);\n            if (max(abs(arrP.x + 0.1) - 0.12, abs(arrP.y) - 0.15) < 0.0) {\n                col = vec3(0.95);\n                if (abs(arrP.y - 0.05) < 0.02 && abs(arrP.x + 0.1) < 0.1) col = vec3(0.1);\n                if (abs(arrP.y + 0.05) < 0.02 && abs(arrP.x + 0.1) < 0.08) col = vec3(0.1);\n            }\n        } else {\n            if (dHat < 0.0 && gp.y > 0.6) {\n                col = vec3(0.2, 0.25, 0.3); \n                if (gp.y < 0.62) col = vec3(0.8, 0.2, 0.2); \n            } else if (dHead < 0.0) {\n                col = vec3(0.85, 0.65, 0.5); \n                if (gp.y > 0.55 && abs(gp.x) < 0.05) col = vec3(0.3); \n            } else if (dTorso < 0.0 || dArmL < 0.0 || dArmR < 0.0) {\n                col = vec3(0.8, 0.85, 0.9); \n                if (dGlove < 0.0 && apL.x < -0.15) col = vec3(0.95); \n                if (abs(gp.x) < 0.01 && gp.y > 0.0) col *= 0.9; \n                if (abs(gp.x - 0.08) < 0.05 && abs(gp.y - 0.25) < 0.05) col = vec3(0.9); \n                if (dArmL < 0.0 && apL.x > -0.05) col = vec3(0.8, 0.2, 0.2); \n            }\n            if (dLegs < 0.0 && gp.y < -0.0) {\n                col = vec3(0.2, 0.25, 0.3); \n                if (gp.y < -0.65) col = vec3(0.1);\n                if (gp.y > -0.05) col = vec3(0.1);\n            }\n            \n            col *= 0.9 + 0.1 * up_p.x;\n        }\n    }\n}\n\nvoid layer_Vignette(in vec2 p, inout vec3 col) {\n    col *= 1.0 - 0.2 * length(p);\n}\n\nvec4 layer_Background(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 col = vec3(-1.0);\n    vec2 up_p = vec2(p.y, -p.x); \n    \n    layer_Background(up_p, iTime, col);\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_Shadow(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 col = vec3(-1.0);\n    vec2 up_p = vec2(p.y, -p.x); \n    \n    layer_Shadow(up_p, iTime, col);\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_Cutout(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 col = vec3(-1.0);\n    vec2 up_p = vec2(p.y, -p.x); \n    \n    layer_Cutout(up_p, iTime, col);\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_Vignette(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 col = vec3(-1.0);\n    vec2 up_p = vec2(p.y, -p.x); \n    \n    layer_Vignette(p, col);\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n","IMG_6786":"void layer_Background(in vec2 p, in float iTime, inout vec3 col) {\n    vec3 woodDark = vec3(0.15, 0.1, 0.08);\n    vec3 woodLight = vec3(0.25, 0.15, 0.12);\n    \n    float grainTime = iTime * 0.1;\n    float grain = sin((p.x + grainTime) * 50.0 + iTime * 0.5) * cos((p.x + grainTime) * 20.0 + p.y * 5.0 - iTime * 0.2) + sin((p.x + grainTime) * 100.0) * 0.5;\n    col = mix(woodDark, woodLight, grain * 0.5 + 0.5);\n    col *= 0.8 + 0.2 * p.y; \n}\n\nvoid layer_Ticket(in vec2 p, in float iTime, inout vec3 col) {\n    float tW = 1.3;\n    float tH = 0.45;\n    \n    float floatT = sin(iTime * 1.5) * 0.02;\n    vec2 tp = p;\n    tp.y += sin(tp.x * 3.0) * 0.02 - floatT; \n    tp.x += cos(iTime * 1.2) * 0.01;\n    \n    float dTicket = max(abs(tp.x) - tW, abs(tp.y) - tH);\n    \n    if (dTicket < 0.0) {\n        vec3 tCol = vec3(0.9, 0.85, 0.65); \n        \n        tCol *= 0.95 - 0.05 * cos(tp.x * 10.0 + iTime);\n        \n        if (tp.x > -1.0 && tp.x < 1.25 && tp.y > -0.4 && tp.y < 0.4) {\n            vec2 gridP = tp - vec2(-1.0, 0.0);\n            float gx = fract(gridP.x * 3.5); \n            float gy = fract(gridP.y * 3.5); \n            \n            if (gx < 0.02 || gy < 0.03) {\n                tCol = vec3(0.1); \n            } else {\n                vec2 cellP = vec2(fract(gridP.x * 3.5), fract(gridP.y * 3.5));\n                \n                if (tp.x > -0.8) {\n                    vec2 cellId = floor(vec2(gridP.x * 3.5, gridP.y * 3.5));\n                    float rand = fract(sin(dot(cellId + floor(iTime*2.0), vec2(12.9898, 78.233))) * 43758.5453);\n                    \n                    if (rand > 0.4) {\n                        if (abs(cellP.x - 0.5) < 0.25 && abs(cellP.y - 0.5) < 0.25) {\n                            if (abs(abs(cellP.x - 0.5) - 0.15) > 0.05 && abs(abs(cellP.y - 0.5) - 0.15) > 0.05) {\n                                tCol = vec3(0.1);\n                            }\n                            if (abs(cellP.y - 0.5) < 0.05 && abs(cellP.x - 0.5) < 0.15) tCol = vec3(0.1);\n                        }\n                    }\n                }\n            }\n        }\n        \n        if (tp.x < -0.8) {\n            if (abs(tp.x + 1.1) < 0.15 && tp.y > 0.0) {\n                 if (fract(tp.y * 15.0 - iTime) > 0.3) tCol = vec3(0.1);\n            }\n            if (length(tp - vec2(-1.1, -0.2)) < 0.15) tCol = vec3(0.1);\n        }\n        \n        col = tCol;\n    }\n    \n    if (dTicket > 0.0 && dTicket < 0.05 && tp.y < 0.0) {\n        col *= 0.5; \n    }\n}\n\nvoid layer_Hand(in vec2 p, in float iTime, inout vec3 col) {\n    vec3 skin = vec3(0.85, 0.65, 0.55);\n    \n    float tap1 = abs(sin(iTime * 4.0)) * 0.05;\n    float tap2 = abs(cos(iTime * 3.5)) * 0.04;\n    float tap3 = abs(sin(iTime * 5.0 + 1.0)) * 0.03;\n    \n    vec2 f1P = p - vec2(-0.5, -0.8 + tap1);\n    float dF1 = length(f1P - vec2(0.0, clamp(f1P.y, -1.0, 0.4))) - 0.1;\n    vec2 f1Pr = vec2(f1P.x - f1P.y * 0.5, f1P.y);\n    float dF1r = length(f1Pr - vec2(0.0, clamp(f1Pr.y, -1.0, 0.5))) - 0.1;\n    \n    vec2 f2P = p - vec2(-0.2, -0.9 + tap2);\n    vec2 f2Pr = vec2(f2P.x - f2P.y * 0.8, f2P.y);\n    float dF2r = length(f2Pr - vec2(0.0, clamp(f2Pr.y, -1.0, 0.6))) - 0.12;\n\n    vec2 f3P = p - vec2(0.8, -0.8 + tap3);\n    vec2 f3Pr = vec2(f3P.x + f3P.y * 0.5, f3P.y);\n    float dF3r = length(f3Pr - vec2(0.0, clamp(f3Pr.y, -1.0, 0.4))) - 0.15;\n    \n    float dHand = min(min(dF1r, dF2r), dF3r);\n    \n    if (dHand < 0.0) {\n        col = skin;\n        col *= 0.8 + 0.2 * cos(dHand * 20.0);\n        if (dF1r < 0.0 && f1Pr.y > 0.35) col = vec3(0.9, 0.75, 0.65);\n        if (dF2r < 0.0 && f2Pr.y > 0.45) col = vec3(0.9, 0.75, 0.65);\n        if (dF3r < 0.0 && f3Pr.y > 0.25) col = vec3(0.9, 0.75, 0.65);\n        if (dHand > -0.02) col *= 0.8;\n    }\n}\n\nvec4 layer_Background(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 col = vec3(-1.0);\n    \n    layer_Background(p, iTime, col);\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_Ticket(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 col = vec3(-1.0);\n    \n    layer_Ticket(p, iTime, col);\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_Hand(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 col = vec3(-1.0);\n    \n    layer_Hand(p, iTime, col);\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n","IMG_6793":"void layer_Background(in vec2 p, inout vec3 col) {\n    col = vec3(0.9, 0.9, 0.9);\n}\n\nvoid layer_Casing(in vec2 p, inout vec2 bp, out float dCasing, out float boxW, out float boxH) {\n    boxW = 1.3;\n    boxH = 0.5;\n    \n    bp = p;\n    bp.y *= 1.0 + (bp.x + 1.0) * 0.05;\n    \n    dCasing = max(abs(bp.x) - boxW, abs(bp.y) - boxH);\n}\n\nvoid layer_ScrollingContent(in vec2 scrollP, in float iTime, inout vec3 ledCol) {\n    vec2 logoP = scrollP - vec2(-0.7, 0.0);\n    float dLogo = length(logoP) - 0.25;\n    if (dLogo < 0.0) {\n        float a = atan(logoP.y, logoP.x) + iTime * 2.0;\n        ledCol = vec3(1.0, 0.1, 0.1); \n        if (sin(a * 8.0) > 0.5 && length(logoP) > 0.15) ledCol = vec3(0.0);\n        if (length(logoP) < 0.1) ledCol = vec3(0.0); \n    }\n    \n    if (scrollP.x > -0.2 && scrollP.y > 0.15 && scrollP.y < 0.35) {\n        if (fract(scrollP.x * 15.0) > 0.2 && fract(scrollP.y * 10.0) > 0.2) {\n            ledCol = vec3(0.8, 1.0, 0.2); \n        }\n    }\n    \n    if (scrollP.x > -0.2 && scrollP.y > -0.1 && scrollP.y < 0.1) {\n        if (fract(scrollP.x * 12.0) > 0.2 && fract(scrollP.y * 8.0) > 0.2) {\n            ledCol = mix(vec3(1.0, 0.2, 0.8), vec3(0.2, 0.8, 1.0), sin(iTime*5.0)*0.5+0.5); \n        }\n    }\n    \n    if (scrollP.x > -0.8 && scrollP.y > -0.35 && scrollP.y < -0.15) {\n        if (fract(scrollP.x * 20.0) > 0.2 && fract(scrollP.y * 12.0) > 0.2) {\n            ledCol = vec3(0.1, 1.0, 0.3); \n        }\n    }\n}\n\nvoid layer_Screen(in vec2 bp, in float boxW, in float boxH, in float iTime, inout vec3 col) {\n    float sW = boxW - 0.12;\n    float sH = boxH - 0.08;\n    float dScreen = max(abs(bp.x + 0.05) - sW, abs(bp.y) - sH);\n    \n    if (dScreen < 0.0) {\n        vec3 screenCol = vec3(0.05, 0.0, 0.0);\n        \n        vec2 gridP = bp * vec2(60.0, 30.0);\n        vec2 fGrid = fract(gridP) - 0.5;\n        float isDot = smoothstep(0.45, 0.35, length(fGrid));\n        \n        if (isDot > 0.0) {\n            vec3 ledCol = vec3(0.0); \n            \n            float bx = abs(bp.x + 0.05);\n            float by = abs(bp.y);\n            if (bx > sW - 0.03 || by > sH - 0.04) {\n                float cId = mod(floor(gridP.x) + floor(gridP.y) - iTime*10.0, 3.0);\n                if (cId < 1.0) ledCol = vec3(1.0, 0.0, 0.0);\n                else if (cId < 2.0) ledCol = vec3(0.0, 1.0, 0.0);\n                else ledCol = vec3(0.0, 0.0, 1.0);\n            } else {\n                vec2 scrollP = bp;\n                scrollP.x += fract(iTime * 0.3) * 3.0 - 1.5; \n                scrollP.x = mod(scrollP.x + 1.5, 3.0) - 1.5;\n                \n                layer_ScrollingContent(scrollP, iTime, ledCol);\n            }\n            \n            screenCol = mix(screenCol, ledCol * 1.5, isDot * (ledCol.r + ledCol.g + ledCol.b > 0.0 ? 1.0 : 0.0));\n        }\n        \n        col = screenCol;\n        col += vec3(0.1, 0.2, 0.1) * (1.0 - length(bp));\n    }\n}\n\nvec4 layer_Background(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 col = vec3(-1.0);\n    \n    layer_Background(p, col);\n    \n    vec2 bp;\n    float dCasing, boxW, boxH;\n    \n    if (dCasing < 0.0) {\n        col *= 0.8 + 0.2 * bp.y;\n        \n        if (bp.x < -boxW + 0.1) {\n        } else {\n        }\n    }\n\n    col *= 1.0 - 0.1 * length(p);\n    \n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_Casing(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 col = vec3(-1.0);\n    \n    \n    vec2 bp;\n    float dCasing, boxW, boxH;\n    layer_Casing(p, bp, dCasing, boxW, boxH);\n    \n    if (dCasing < 0.0) {\n        col *= 0.8 + 0.2 * bp.y;\n        \n        if (bp.x < -boxW + 0.1) {\n        } else {\n        }\n    }\n\n    col *= 1.0 - 0.1 * length(p);\n    \n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_Screen(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 col = vec3(-1.0);\n    \n    \n    vec2 bp;\n    float dCasing, boxW, boxH;\n    \n    if (dCasing < 0.0) {\n        col *= 0.8 + 0.2 * bp.y;\n        \n        if (bp.x < -boxW + 0.1) {\n        } else {\n        }\n    }\n\n    col *= 1.0 - 0.1 * length(p);\n    \n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_ScrollingContent(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 col = vec3(-1.0);\n    \n    \n    vec2 bp;\n    float dCasing, boxW, boxH;\n    \n    if (dCasing < 0.0) {\n        col *= 0.8 + 0.2 * bp.y;\n        \n        if (bp.x < -boxW + 0.1) {\n        } else {\n            layer_Screen(bp, boxW, boxH, iTime, col);\n        }\n    }\n\n    col *= 1.0 - 0.1 * length(p);\n    \n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n","IMG_6876":"void layer_Background(in vec2 p, inout vec3 col) {\n    col = vec3(1.0); \n    \n    if (p.y > 0.85 && p.x > -1.2 && p.x < 0.2) {\n        if (fract(p.y * 30.0) > 0.3) col = vec3(0.2); \n    }\n    if (p.y > 0.75 && p.y <= 0.85 && p.x > -1.2 && p.x < 0.5) {\n        if (fract(p.y * 40.0) > 0.4) col = vec3(0.4);\n    }\n    \n    vec2 t1 = p - vec2(-0.8, 0.2);\n    if (abs(t1.x) < 0.3 && abs(t1.y) < 0.1) if (fract(t1.y * 35.0) > 0.3) col = vec3(0.4);\n    vec2 t2 = p - vec2(-0.8, -0.2);\n    if (abs(t2.x) < 0.3 && abs(t2.y) < 0.08) if (fract(t2.y * 35.0) > 0.3) col = vec3(0.4);\n    vec2 t3 = p - vec2(-0.6, -0.6);\n    if (abs(t3.x) < 0.3 && abs(t3.y) < 0.1) if (fract(t3.y * 35.0) > 0.3) col = vec3(0.4);\n    vec2 t4 = p - vec2(0.9, 0.6);\n    if (abs(t4.x) < 0.3 && abs(t4.y) < 0.1) if (fract(t4.y * 35.0) > 0.3) col = vec3(0.4);\n    vec2 t5 = p - vec2(0.9, -0.5);\n    if (abs(t5.x) < 0.25 && abs(t5.y) < 0.1) if (fract(t5.y * 35.0) > 0.3) col = vec3(0.4);\n}\n\nvoid layer_Protein(in vec2 p, in float iTime, inout vec3 col) {\n    float pulse = sin(iTime * 2.0) * 0.05;\n    float rot = iTime * 0.2;\n    mat2 rMat = mat2(cos(rot), -sin(rot), sin(rot), cos(rot));\n    vec2 pRot = rMat * (p - vec2(0.1, -0.1));\n    \n    float dProtein = length(pRot) - 0.5 + pulse;\n    \n    float noiseBase = sin(pRot.x * 10.0 + sin(pRot.y * 15.0 + iTime)) * cos(pRot.y * 12.0) * 0.1;\n    float noiseDetail = sin(pRot.x * 40.0) * cos(pRot.y * 35.0) * 0.03;\n    dProtein += noiseBase + noiseDetail;\n    \n    if (dProtein < 0.0) {\n        col = vec3(0.85, 0.85, 0.88);\n        float strands = abs(sin(pRot.x * 30.0 + sin(pRot.y * 20.0 + iTime*2.0)) * cos(pRot.y * 25.0));\n        col += vec3(0.15) * strands;\n        \n        col *= 0.6 + 0.4 * smoothstep(-0.2, 0.0, dProtein);\n        \n        vec2 grid = fract(pRot * 15.0 + vec2(0.0, iTime*0.5)) - 0.5;\n        vec2 id = floor(pRot * 15.0 + vec2(0.0, iTime*0.5));\n        float rand = fract(sin(dot(id, vec2(12.9898, 78.233))) * 43758.5453);\n        if (rand > 0.85 && dProtein < -0.05) {\n            float dDot = length(grid);\n            if (dDot < 0.15) {\n                if (rand > 0.95) col = vec3(0.9, 0.2, 0.2); \n                else col = vec3(0.2, 0.3, 0.8); \n            }\n        }\n    }\n}\n\nvoid layer_ZoomCircles(in vec2 p, in float iTime, inout vec3 col) {\n    vec3 pinkCol = vec3(0.95, 0.6, 0.7);\n    \n    vec2 c1 = p - vec2(0.3 + sin(iTime)*0.02, 0.4 + cos(iTime)*0.02);\n    float dC1 = length(c1) - 0.3;\n    if (abs(dC1) < 0.015) col = pinkCol;\n    if (dC1 < 0.0) {\n        col = vec3(0.9, 0.85, 0.85); \n        float strands = abs(sin(c1.x * 40.0 + sin(c1.y * 30.0 - iTime)) * cos(c1.y * 35.0));\n        col += vec3(0.1) * strands;\n        vec2 grid = fract(c1 * 20.0 - vec2(iTime*0.5)) - 0.5;\n        vec2 id = floor(c1 * 20.0 - vec2(iTime*0.5));\n        float rand = fract(sin(dot(id, vec2(12.9898, 78.233))) * 43758.5453);\n        if (rand > 0.7 && length(grid) < 0.2) col = vec3(0.2, 0.3, 0.8); \n    }\n    \n    vec2 c2 = p - vec2(-0.25 + cos(iTime*1.2)*0.02, 0.3 + sin(iTime*1.2)*0.02);\n    float dC2 = length(c2) - 0.15;\n    if (abs(dC2) < 0.01) col = pinkCol;\n    if (dC2 < 0.0) {\n        col = vec3(0.85);\n        if (length(c2 + vec2(0.05*cos(iTime))) < 0.03) col = vec3(0.2, 0.3, 0.8);\n        if (length(c2 - vec2(0.05*sin(iTime))) < 0.03) col = vec3(0.2, 0.3, 0.8);\n    }\n    \n    vec2 c3 = p - vec2(-0.35 + sin(iTime*0.8)*0.02, 0.0 + cos(iTime*0.8)*0.02);\n    float dC3 = length(c3) - 0.18;\n    if (abs(dC3) < 0.01) col = pinkCol;\n    if (dC3 < 0.0) {\n        col = vec3(0.85);\n        if (length(c3 + vec2(0.08, 0.02)*cos(iTime)) < 0.04) col = vec3(0.9, 0.2, 0.2); \n        if (length(c3 - vec2(0.02, 0.08)*sin(iTime)) < 0.04) col = vec3(0.2, 0.3, 0.8); \n    }\n\n    vec2 c4 = p - vec2(-0.15 + cos(iTime)*0.02, -0.3 + sin(iTime)*0.02);\n    float dC4 = length(c4) - 0.12;\n    if (abs(dC4) < 0.01) col = pinkCol;\n    if (dC4 < 0.0) {\n        col = vec3(0.85);\n        if (length(c4) < 0.03) col = vec3(0.2, 0.3, 0.8);\n    }\n}\n\nvoid layer_Lines(in vec2 p, in float iTime, inout vec3 col) {\n    vec3 pinkCol = vec3(0.95, 0.6, 0.7);\n    if (p.y > 0.6 && p.y < 0.605 && p.x > 0.4 && p.x < 0.6) {\n        col = mix(vec3(0.7), pinkCol, fract(p.x*10.0 - iTime*5.0));\n    }\n    if (p.x > -0.5 && p.x < -0.4 && p.y > 0.2 && p.y < 0.205) {\n        col = mix(vec3(0.7), pinkCol, fract(p.x*10.0 + iTime*5.0));\n    }\n}\n\nvoid layer_Vignette(in vec2 p, inout vec3 col) {\n    col *= 1.0 - 0.05 * length(p);\n}\n\nvec4 layer_Background(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 col = vec3(-1.0);\n    \n    layer_Background(p, col);\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_Protein(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 col = vec3(-1.0);\n    \n    layer_Protein(p, iTime, col);\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_ZoomCircles(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 col = vec3(-1.0);\n    \n    layer_ZoomCircles(p, iTime, col);\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_Lines(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 col = vec3(-1.0);\n    \n    layer_Lines(p, iTime, col);\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_Vignette(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 col = vec3(-1.0);\n    \n    layer_Vignette(p, col);\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n","IMG_6994":"void layer_Floor(in vec2 up_p, in float iTime, inout vec3 col) {\n    if (up_p.y < -0.2) {\n        vec3 tileWhite = vec3(0.85, 0.85, 0.88);\n        vec3 tileDirt = vec3(0.6, 0.6, 0.6);\n        \n        float gridX = fract(up_p.x * 2.0);\n        float gridY = fract((up_p.y + iTime*0.5) * 2.0);\n        \n        col = mix(tileWhite, tileDirt, 0.2 + 0.1 * sin(up_p.x * 10.0));\n        \n        if (gridX < 0.02 || gridY < 0.02) {\n            col = vec3(0.3);\n        }\n        \n        float shadow = exp(-length(up_p - vec2(0.0, -0.5)) * 3.0);\n        col *= 1.0 - 0.5 * shadow;\n    }\n}\n\nvoid layer_Wall(in vec2 up_p, in float iTime, inout vec3 col) {\n    if (up_p.y >= -0.2) {\n        col = vec3(0.7, 0.7, 0.7); \n        \n        if (up_p.y > -0.1 && up_p.y < 0.8 && abs(up_p.x) < 0.8) {\n            col = vec3(0.4, 0.5, 0.5);\n            float refl = sin((up_p.x + up_p.y)*10.0 + iTime*2.0);\n            col += vec3(0.1) * smoothstep(0.8, 1.0, refl);\n            \n            vec2 posterP = up_p;\n            posterP.y -= iTime * 0.2;\n            posterP.y = mod(posterP.y, 1.0) - 0.2;\n            \n            if (posterP.x > 0.1 && posterP.y > 0.1 && posterP.y < 0.5) {\n                col = vec3(0.1, 0.5, 0.3); \n                if (fract(posterP.y * 20.0) > 0.5 && posterP.x > 0.3) col = vec3(0.9); \n            }\n            if (posterP.x < -0.1 && posterP.y > 0.1 && posterP.y < 0.5) {\n                col = vec3(0.1, 0.3, 0.6); \n                if (fract(posterP.y * 25.0) > 0.6) col = vec3(0.9); \n            }\n        }\n    }\n}\n\nvoid layer_Figure(in vec2 up_p, in float iTime, inout vec3 col) {\n    float bounce = abs(sin(iTime * 4.0)) * 0.05;\n    \n    vec3 shirtCol = vec3(0.7, 0.15, 0.15); \n    vec3 shortsCol = vec3(0.45, 0.55, 0.65); \n    vec3 bootsCol = vec3(0.2, 0.45, 0.2); \n    vec3 skinCol = vec3(0.85, 0.65, 0.5); \n    \n    float legWalk1 = sin(iTime * 4.0) * 0.1;\n    float legWalk2 = sin(iTime * 4.0 + 3.1415) * 0.1;\n\n    vec2 lLegP = up_p - vec2(-0.15, -0.3 + bounce);\n    lLegP.y -= legWalk1;\n    float dLLeg = max(abs(lLegP.x) - 0.15, abs(lLegP.y) - 0.2);\n    \n    vec2 rLegP = up_p - vec2(0.25, -0.3 + bounce);\n    rLegP.y -= legWalk2;\n    float dRLeg = max(abs(rLegP.x) - 0.15, abs(rLegP.y) - 0.2);\n    \n    vec2 lBootP = up_p - vec2(-0.15, -0.65 + bounce);\n    lBootP.y -= legWalk1;\n    float dLBoot = max(abs(lBootP.x) - 0.12, abs(lBootP.y) - 0.25);\n    \n    vec2 rBootP = up_p - vec2(0.35, -0.6 + bounce);\n    rBootP.y -= legWalk2;\n    float dRBoot = length(rBootP - vec2(clamp(rBootP.x, -0.1, 0.2), 0.0)) - 0.15;\n    \n    vec2 torsoP = up_p - vec2(-0.1, 0.3 + bounce);\n    float dTorso = max(abs(torsoP.x) - 0.25, abs(torsoP.y) - 0.4);\n    \n    vec2 lArmP = up_p - vec2(-0.4, 0.1 + bounce);\n    float armSwing = sin(iTime * 4.0) * 0.1;\n    lArmP.x += armSwing;\n    float dLArm = max(abs(lArmP.x) - 0.08, abs(lArmP.y) - 0.25);\n    float dLHand = length(up_p - vec2(-0.45 + armSwing, -0.2 + bounce)) - 0.08;\n    \n    vec2 rArmP = up_p - vec2(0.2, 0.5 + bounce);\n    float dRArm = length(rArmP - vec2(clamp(rArmP.x, -0.1, 0.15), clamp(rArmP.y, -0.1, 0.2))) - 0.08;\n    float dRHand = length(up_p - vec2(0.4, 0.7 + bounce)) - 0.08;\n    \n    vec2 boxP = up_p - vec2(0.5, 0.6 + bounce);\n    vec2 boxRot = vec2(boxP.x * 0.8 + boxP.y * 0.6, -boxP.x * 0.6 + boxP.y * 0.8);\n    float dBox = max(abs(boxRot.x) - 0.2, abs(boxRot.y) - 0.2);\n    \n    if (dBox < 0.0) {\n        col = vec3(0.75, 0.6, 0.45); \n        if (abs(boxRot.x) < 0.02) col = vec3(0.6, 0.45, 0.3);\n    } else if (dTorso < 0.0 || dLArm < 0.0) {\n        col = shirtCol;\n        col *= 0.8 + 0.2 * up_p.x; \n    } else if (dLLeg < 0.0 || dRLeg < 0.0) {\n        col = shortsCol;\n        col *= 0.8 + 0.2 * sin(up_p.x * 50.0) * cos(up_p.y * 40.0);\n    } else if (dLBoot < 0.0 || dRBoot < 0.0) {\n        col = bootsCol;\n        col *= 0.7 + 0.3 * exp(-abs(up_p.x * 10.0));\n    } else if (dRArm < 0.0) {\n        col = skinCol;\n    } else if (dLHand < 0.0 || dRHand < 0.0) {\n        col = skinCol;\n    }\n    \n    if (length(up_p - vec2(-0.5 + armSwing, -0.2 + bounce)) < 0.06) {\n        col = vec3(0.3, 0.2, 0.1); \n    }\n}\n\nvoid layer_Vignette(in vec2 p, inout vec3 col) {\n    col *= 1.0 - 0.1 * length(p);\n}\n\nvec4 layer_Floor(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 col = vec3(-1.0);\n    vec2 up_p = vec2(p.y, -p.x);\n    \n    layer_Floor(up_p, iTime, col);\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_Wall(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 col = vec3(-1.0);\n    vec2 up_p = vec2(p.y, -p.x);\n    \n    layer_Wall(up_p, iTime, col);\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_Figure(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 col = vec3(-1.0);\n    vec2 up_p = vec2(p.y, -p.x);\n    \n    layer_Figure(up_p, iTime, col);\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_Vignette(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 col = vec3(-1.0);\n    vec2 up_p = vec2(p.y, -p.x);\n    \n    layer_Vignette(p, col);\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n","IMG_7211":"void layer_Background(in vec2 p, inout vec3 col, in vec3 bgDark) {\n    col = bgDark;\n}\n\nvoid layer_TopBar(in vec2 p, inout vec3 col) {\n    if (p.y > 0.9) {\n        col = vec3(0.12, 0.12, 0.12);\n        if (p.x < -0.3 && p.x > -0.5 && abs(p.y - 0.95) < 0.02) col = vec3(0.9);\n        if (p.x > 0.4 && p.x < 0.5 && abs(p.y - 0.95) < 0.02) col = vec3(0.9);\n    }\n}\n\nvoid layer_BottomBar(in vec2 p, inout vec3 col) {\n    if (p.y <= -0.5) {\n        col = vec3(0.15, 0.15, 0.15); \n        \n        if (p.y > -0.7 && p.y < -0.55) {\n            float dPill = max(abs(p.x) - 0.45, abs(p.y + 0.62) - 0.06);\n            if (dPill < 0.0) {\n                col = vec3(0.3, 0.3, 0.3);\n                if (p.x > -0.2 && p.x < 0.2 && abs(p.y + 0.62) < 0.015) col = vec3(0.9);\n                if (length(p - vec2(0.4, -0.62)) < 0.015) col = vec3(0.8);\n            }\n        }\n        if (p.y < -0.85) {\n            if (abs(p.y + 0.92) < 0.03) {\n                if (abs(p.x + 0.4) < 0.03) col = vec3(0.3, 0.6, 1.0); \n                if (abs(p.x + 0.2) < 0.03) col = vec3(0.3, 0.6, 1.0); \n                if (abs(p.x) < 0.03) col = vec3(0.3, 0.6, 1.0); \n                if (abs(p.x - 0.2) < 0.03) col = vec3(0.3, 0.6, 1.0); \n                if (abs(p.x - 0.4) < 0.03) col = vec3(0.3, 0.6, 1.0); \n            }\n        }\n    }\n}\n\nvoid layer_ScrollContent(in vec2 p, in float iTime, inout vec3 col, in vec3 bgDark) {\n    if (p.y <= 0.9 && p.y > -0.5) {\n        float scrollY = p.y + fract(iTime * 0.2) * 1.0;\n        float cy = mod(scrollY, 1.5) - 0.75; \n        \n        if (cy > 0.4) {\n            if (cy > 0.55 && cy < 0.65 && abs(p.x) < 0.2) {\n                col = vec3(1.0); \n                if (abs(cy - 0.6) < 0.01 && abs(p.x) < 0.18) col = bgDark; \n            }\n            if (length(vec2(p.x, cy) - vec2(0.4, 0.6)) < 0.05) col = vec3(0.9);\n            \n            float dSearch = max(abs(p.x) - 0.45, abs(cy - 0.45) - 0.06);\n            if (dSearch < 0.0) {\n                col = vec3(0.25, 0.25, 0.25); \n                if (length(vec2(p.x, cy) - vec2(-0.4, 0.45)) < 0.02) col = vec3(0.6);\n                if (p.x > -0.3 && p.x < 0.1 && abs(cy - 0.45) < 0.015) col = vec3(0.9);\n                if (length(vec2(p.x, cy) - vec2(0.4, 0.45)) < 0.015) col = vec3(0.6);\n            }\n        }\n        else if (cy > 0.28 && cy <= 0.4) {\n            if (abs(cy - 0.34) < 0.015) {\n                if (fract(p.x * 8.0) > 0.3) col = vec3(0.7);\n                if (p.x < -0.4) col = vec3(1.0);\n            }\n            if (cy > 0.29 && cy < 0.3 && p.x < -0.4 && p.x > -0.48) col = vec3(1.0);\n        }\n        else if (cy > 0.15 && cy <= 0.28) {\n            if (abs(cy - 0.2) < 0.02 && p.x > -0.45 && p.x < 0.3) {\n                col = vec3(0.9);\n                if (p.x < -0.35) col = vec3(1.0); \n            }\n        }\n        else if (cy > -0.7 && cy <= 0.15) {\n            float cardX = mod(p.x + 0.5, 0.5) - 0.25; \n            float cardIndex = floor((p.x + 0.5) / 0.5);\n            \n            if (cardIndex >= 0.0 && cardIndex < 2.0 && abs(cardX) < 0.23) {\n                col = vec3(0.2, 0.2, 0.22); \n                \n                if (cy > -0.35) {\n                    vec2 coverP = vec2(cardX, cy + 0.1); \n                    float dCover = max(abs(coverP.x) - 0.2, abs(coverP.y) - 0.23);\n                    \n                    if (dCover < 0.0) {\n                        vec3 coverBase = vec3(0.4, 0.38, 0.35); \n                        float noise = fract(sin(dot(coverP + vec2(iTime*0.1), vec2(12.9898, 78.233))) * 43758.5453);\n                        col = coverBase * (0.8 + 0.4 * noise);\n                        \n                        float dMan = max(abs(coverP.x) - 0.12, abs(coverP.y + 0.1) - 0.15); \n                        float dHat = length(coverP - vec2(0.0, 0.1)) - 0.08; \n                        float dBrim = max(abs(coverP.x) - 0.15, abs(coverP.y - 0.1) - 0.02);\n                        \n                        if (min(min(dMan, dHat), dBrim) < 0.0) {\n                            col = vec3(0.1); \n                        }\n                        \n                        if (abs(coverP.y + 0.05) < 0.08 && abs(coverP.x) < 0.15) {\n                            col = mix(col, vec3(0.8, 0.7, 0.5), 0.8); \n                            if (fract(coverP.y * 30.0) < 0.2) col = vec3(0.1); \n                            if (fract(coverP.x * 20.0) < 0.2) col = vec3(0.1); \n                        }\n                    }\n                } else {\n                    if (cy > -0.45 && cy < -0.4) col = vec3(0.5, 0.7, 1.0);\n                    if (cy > -0.5 && cy < -0.45 && p.x < 0.0) col = vec3(0.5, 0.7, 1.0); \n                    if (cy > -0.6 && cy < -0.55 && cardX < 0.0) col = vec3(1.0);\n                    if (cy > -0.65 && cy < -0.6 && cardX < 0.0) col = vec3(0.6);\n                }\n            }\n        }\n    }\n}\n\nvec4 layer_Background(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 bgDark = vec3(0.18, 0.18, 0.18); \n    vec3 col = vec3(-1.0);\n    \n    layer_Background(p, col, bgDark);\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_TopBar(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 bgDark = vec3(0.18, 0.18, 0.18); \n    vec3 col = vec3(-1.0);\n    \n    layer_TopBar(p, col);\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_BottomBar(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 bgDark = vec3(0.18, 0.18, 0.18); \n    vec3 col = vec3(-1.0);\n    \n    layer_BottomBar(p, col);\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_ScrollContent(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 bgDark = vec3(0.18, 0.18, 0.18); \n    vec3 col = vec3(-1.0);\n    \n    layer_ScrollContent(p, iTime, col, bgDark);\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n","IMG_7225":"#define segment(p, a, b) length(p - a - (b - a) * clamp(dot(p - a, b - a) / dot(b - a, b - a), 0.0, 1.0))\n\nvoid layer_Background(in vec2 bp, in float boxW, in float boxH, inout vec3 col) {\n    float shadow = max(abs(bp.x - 0.05) - boxW, abs(bp.y + 0.05) - boxH);\n    if (shadow < 0.0) col = vec3(0.02);\n}\n\nvoid layer_BoxBase(in vec2 bp, in vec2 p, in float boxW, in float boxH, inout vec3 col) {\n    vec3 greenBase = vec3(0.12, 0.35, 0.18);\n    float noise = fract(sin(dot(p, vec2(12.9898,78.233))) * 43758.5453);\n    col = greenBase + noise * 0.03;\n    \n    col *= 0.8 + 0.2 * smoothstep(boxW - 0.05, boxW, abs(bp.x));\n    col *= 0.8 + 0.2 * smoothstep(boxH - 0.05, boxH, abs(bp.y));\n    \n    if (boxH - abs(bp.y) < 0.03) col = vec3(0.7, 0.1, 0.1);\n}\n\nvoid layer_TextNHANG(in vec2 bp, inout vec3 col) {\n    vec2 t1 = bp - vec2(-0.2, 0.6);\n    float d1 = 1.0;\n    \n    d1 = min(d1, segment(t1, vec2(0.0, -0.05), vec2(0.0, 0.05)));\n    d1 = min(d1, segment(t1, vec2(0.0, 0.05), vec2(0.08, -0.05)));\n    d1 = min(d1, segment(t1, vec2(0.08, -0.05), vec2(0.08, 0.05)));\n    \n    d1 = min(d1, segment(t1, vec2(0.14, -0.05), vec2(0.14, 0.05)));\n    d1 = min(d1, segment(t1, vec2(0.14, 0.0), vec2(0.22, 0.0)));\n    d1 = min(d1, segment(t1, vec2(0.22, -0.05), vec2(0.22, 0.05)));\n    \n    d1 = min(d1, segment(t1, vec2(0.28, -0.05), vec2(0.32, 0.05)));\n    d1 = min(d1, segment(t1, vec2(0.32, 0.05), vec2(0.36, -0.05)));\n    \n    vec3 gold = vec3(0.9, 0.75, 0.3);\n    if (d1 < 0.01) col = gold;\n}\n\nvoid layer_TextTHANTAI(in vec2 bp, inout vec3 col) {\n    vec2 t2 = bp - vec2(-0.25, 0.45);\n    float d2 = 1.0;\n    \n    d2 = min(d2, segment(t2, vec2(0.0, 0.05), vec2(0.1, 0.05)));\n    d2 = min(d2, segment(t2, vec2(0.05, 0.05), vec2(0.05, -0.05)));\n    \n    d2 = min(d2, segment(t2, vec2(0.14, -0.05), vec2(0.14, 0.05)));\n    d2 = min(d2, segment(t2, vec2(0.14, 0.0), vec2(0.22, 0.0)));\n    d2 = min(d2, segment(t2, vec2(0.22, -0.05), vec2(0.22, 0.05)));\n    \n    if (d2 < 0.01) col = vec3(0.95, 0.85, 0.4); \n}\n\nvoid layer_GoldSeal(in vec2 bp, inout vec3 col) {\n    vec3 gold = vec3(0.9, 0.75, 0.3);\n    float sealD = length((bp - vec2(0.0, -0.1)) * vec2(1.0, 1.2)) - 0.25;\n    if (abs(sealD) < 0.01 || abs(sealD + 0.03) < 0.005) {\n        col = gold;\n    }\n    \n    vec2 dp = bp - vec2(0.0, -0.1);\n    float dragon = sin(dp.x * 30.0 + dp.y * 20.0) * sin(dp.x * 20.0 - dp.y * 30.0);\n    if (sealD < -0.03 && dragon > 0.5) col = mix(col, gold, 0.5);\n}\n\nvec4 layer_Background(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 col = vec3(-1.0);\n    \n    float boxW = 0.35, boxH = 0.85;\n    vec2 bp = p;\n    float box = max(abs(bp.x) - boxW, abs(bp.y) - boxH);\n    \n    if (box < 0.0) {\n    } else {\n        layer_Background(bp, boxW, boxH, col);\n    }\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_BoxBase(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 col = vec3(-1.0);\n    \n    float boxW = 0.35, boxH = 0.85;\n    vec2 bp = p;\n    float box = max(abs(bp.x) - boxW, abs(bp.y) - boxH);\n    \n    if (box < 0.0) {\n        layer_BoxBase(bp, p, boxW, boxH, col);\n    } else {\n    }\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_TextNHANG(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 col = vec3(-1.0);\n    \n    float boxW = 0.35, boxH = 0.85;\n    vec2 bp = p;\n    float box = max(abs(bp.x) - boxW, abs(bp.y) - boxH);\n    \n    if (box < 0.0) {\n        layer_TextNHANG(bp, col);\n    } else {\n    }\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_TextTHANTAI(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 col = vec3(-1.0);\n    \n    float boxW = 0.35, boxH = 0.85;\n    vec2 bp = p;\n    float box = max(abs(bp.x) - boxW, abs(bp.y) - boxH);\n    \n    if (box < 0.0) {\n        layer_TextTHANTAI(bp, col);\n    } else {\n    }\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_GoldSeal(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 col = vec3(-1.0);\n    \n    float boxW = 0.35, boxH = 0.85;\n    vec2 bp = p;\n    float box = max(abs(bp.x) - boxW, abs(bp.y) - boxH);\n    \n    if (box < 0.0) {\n        layer_GoldSeal(bp, col);\n    } else {\n    }\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n","IMG_7266":"#define segment(p, a, b) length(p - a - (b - a) * clamp(dot(p - a, b - a) / dot(b - a, b - a), 0.0, 1.0))\n\nvoid layer_Background(in vec2 p, inout vec3 col) {\n    float concreteNoise = fract(sin(dot(floor(p * 80.0), vec2(127.1, 311.7))) * 43758.5453);\n    col = vec3(0.55, 0.55, 0.56) + concreteNoise * 0.06 - 0.03;\n}\n\nvoid layer_CharacterShape(in vec2 p, out float mask, out float charShape) {\n    charShape = 1.0;\n    \n    charShape = min(charShape, segment(p, vec2(-0.4, 0.3), vec2(-0.5, -0.3)) - p.y * 0.02); \n    charShape = min(charShape, segment(p, vec2(-0.5, 0.1), vec2(-0.2, -0.1)) - 0.01);\n    charShape = min(charShape, segment(p, vec2(-0.35, -0.1), vec2(-0.35, -0.4)) - 0.015);\n    charShape = min(charShape, segment(p, vec2(-0.4, 0.1), vec2(-0.6, 0.0)) - 0.01);\n    \n    charShape = min(charShape, segment(p, vec2(-0.1, 0.3), vec2(0.4, 0.3)) - 0.01);\n    charShape = min(charShape, segment(p, vec2(-0.1, 0.1), vec2(0.4, 0.1)) - 0.01);\n    charShape = min(charShape, segment(p, vec2(-0.1, 0.3), vec2(-0.1, 0.1)) - 0.015);\n    charShape = min(charShape, segment(p, vec2(0.4, 0.3), vec2(0.4, 0.1)) - 0.015);\n    \n    charShape = min(charShape, segment(p, vec2(-0.1, -0.1), vec2(0.4, -0.1)) - 0.01);\n    charShape = min(charShape, segment(p, vec2(-0.1, -0.4), vec2(0.4, -0.4)) - 0.01);\n    charShape = min(charShape, segment(p, vec2(-0.1, -0.1), vec2(-0.1, -0.4)) - 0.015);\n    charShape = min(charShape, segment(p, vec2(0.4, -0.1), vec2(0.4, -0.4)) - 0.015);\n    charShape = min(charShape, segment(p, vec2(0.15, -0.1), vec2(0.15, -0.4)) - 0.01);\n    charShape = min(charShape, segment(p, vec2(-0.1, -0.25), vec2(0.4, -0.25)) - 0.01);\n\n    mask = 1.0 - smoothstep(0.04, 0.06, charShape);\n}\n\nvoid layer_Mosaic(in vec2 p, in float mask, in float charShape, inout vec3 col) {\n    if(mask > 0.0) {\n        vec2 shardP = p * 20.0;\n        vec2 g = floor(shardP);\n        vec2 f = fract(shardP);\n        float minDist = 1.0;\n        vec2 minId = vec2(0.0);\n        for(int y=-1; y<=1; y++) {\n            for(int x=-1; x<=1; x++) {\n                vec2 lattice = vec2(x, y);\n                vec2 rand = fract(sin(vec2(dot(g+lattice, vec2(127.1,311.7)), dot(g+lattice, vec2(269.5,183.3)))) * 43758.5453);\n                vec2 pt = lattice + rand - f;\n                float d = length(pt);\n                if(d < minDist) {\n                    minDist = d;\n                    minId = g + lattice;\n                }\n            }\n        }\n        \n        float grout = smoothstep(0.05, 0.1, minDist);\n        float bevel = smoothstep(0.1, 0.3, minDist);\n        \n        vec3 porcelainWhite = vec3(0.95);\n        vec3 porcelainBlue = vec3(0.1, 0.25, 0.65);\n        \n        float shardColorRand = fract(sin(dot(minId, vec2(12.9898, 78.233))) * 43758.5453);\n        vec3 tileColor = mix(porcelainWhite, porcelainBlue, step(0.6, shardColorRand));\n        \n        float crackle = fract(sin(dot(p*100.0, vec2(12.9, 78.2))) * 43758.0);\n        if(crackle > 0.95) tileColor *= 0.9;\n        \n        tileColor *= 0.8 + 0.2 * bevel;\n        \n        tileColor = mix(vec3(0.7, 0.65, 0.6), tileColor, grout);\n        \n        vec3 lightDir = normalize(vec3(1.0, 1.0, 1.0));\n        float spec = pow(max(0.0, dot(reflect(-lightDir, vec3(0.0, 0.0, 1.0)), vec3(0.0, 0.0, 1.0))), 32.0);\n        tileColor += spec * 0.3 * bevel;\n        \n        col = mix(col, vec3(0.2), 0.6 * smoothstep(0.0, 0.05, charShape + 0.02)); \n        col = mix(col, tileColor, mask);\n    }\n}\n\nvec4 layer_Background(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 col = vec3(-1.0);\n    \n    layer_Background(p, col);\n    \n    float mask, charShape;\n    \n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_CharacterShape(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 col = vec3(-1.0);\n    \n    \n    float mask, charShape;\n    layer_CharacterShape(p, mask, charShape);\n    \n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_Mosaic(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 col = vec3(-1.0);\n    \n    \n    float mask, charShape;\n    \n    layer_Mosaic(p, mask, charShape, col);\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n","IMG_7408":"#define segment(p, a, b) length(p - a - (b - a) * clamp(dot(p - a, b - a) / dot(b - a, b - a), 0.0, 1.0))\n\nvoid layer_Background(in vec2 p, inout vec3 col) {\n    col = vec3(0.1, 0.25, 0.12);\n    col += sin(p.x * 40.0) * sin(p.y * 40.0) * 0.02;\n    \n    if (p.y > 0.3 && abs(p.x) < 0.4) {\n        float roll = sin(p.x * 50.0 + p.y * 30.0);\n        col = mix(vec3(0.08, 0.08, 0.2), vec3(0.1, 0.1, 0.25), roll * 0.5 + 0.5);\n    }\n    if (p.y > 0.4 && p.x < -0.3) {\n        float roll = sin(p.x * 35.0);\n        col = mix(vec3(0.55, 0.5, 0.35), vec3(0.6, 0.55, 0.4), roll * 0.5 + 0.5);\n    }\n}\n\nvoid layer_BlueShirt(in vec2 p, inout vec3 col) {\n    vec2 bp = p - vec2(-0.3, -0.1);\n    float blueBody = length(max(abs(vec2(bp.x, bp.y + 0.2)) - vec2(0.4, 0.4), 0.0)) - 0.1;\n    float blueShoulders = length(vec2(bp.x, max(bp.y - 0.2, 0.0))) - 0.45;\n    float bs = max(blueBody, blueShoulders);\n    \n    if (bs < 0.0) {\n        vec3 bCol = vec3(0.55, 0.65, 0.85);\n        float wrinkle = sin(bp.x * 20.0 + bp.y * 10.0 + sin(bp.y * 5.0) * 2.0) * 0.05;\n        bCol += wrinkle;\n        \n        float collarL = segment(bp, vec2(0.0, 0.35), vec2(-0.15, 0.2));\n        float collarR = segment(bp, vec2(0.0, 0.35), vec2(0.15, 0.2));\n        if (min(collarL, collarR) < 0.05) {\n            bCol = vec3(0.5, 0.6, 0.8);\n            if (min(collarL, collarR) < 0.01) bCol *= 0.7; \n        }\n        \n        vec2 p1 = bp - vec2(-0.15, 0.05);\n        float pkt = max(abs(p1.x) - 0.08, abs(p1.y) - 0.08);\n        if (pkt < 0.0) {\n            bCol *= 0.95; \n            if (abs(p1.y - 0.08) < 0.005) bCol *= 0.8; \n            if (length(p1 - vec2(0.0, 0.04)) < 0.01) bCol = vec3(0.3, 0.4, 0.6);\n        }\n        \n        col = bCol;\n    }\n    else if (bs < 0.02) col *= 0.6;\n}\n\nvoid layer_WhiteShirt(in vec2 p, inout vec3 col) {\n    vec2 wp = p - vec2(0.25, 0.0);\n    float whiteBody = length(max(abs(vec2(wp.x, wp.y + 0.1)) - vec2(0.35, 0.45), 0.0)) - 0.1;\n    float whiteShoulders = length(vec2(wp.x, max(wp.y - 0.3, 0.0))) - 0.4;\n    float ws = max(whiteBody, whiteShoulders);\n    \n    if (ws < 0.0) {\n        vec3 wCol = vec3(0.9, 0.92, 0.95);\n        wCol *= 0.8 + 0.2 * smoothstep(0.3, 0.0, abs(wp.x));\n        float wrinkle = sin(wp.x * 20.0 + wp.y * 15.0) * 0.03;\n        wCol += wrinkle;\n        \n        for (float i = 0.0; i < 2.0; i++) {\n            vec2 epPos = vec2(-0.25 + i * 0.5, 0.35);\n            vec2 epLocal = wp - epPos;\n            float epShape = max(abs(epLocal.x) - 0.08, abs(epLocal.y) - 0.03);\n            if (epShape < 0.0) {\n                wCol = vec3(0.1, 0.1, 0.15); \n                float stripe = fract((wp.x - epPos.x) * 20.0);\n                if (stripe < 0.4) wCol = vec3(0.8, 0.7, 0.2);\n                \n                if (abs(epShape) < 0.005) wCol = vec3(0.8, 0.15, 0.1);\n                \n                if (length(epLocal - vec2(sign(i-0.5)*-0.05, 0.0)) < 0.015) {\n                    wCol = vec3(0.8, 0.8, 0.85);\n                }\n            }\n        }\n        \n        float wCollarL = segment(wp, vec2(0.0, 0.4), vec2(-0.15, 0.25));\n        float wCollarR = segment(wp, vec2(0.0, 0.4), vec2(0.15, 0.25));\n        if (min(wCollarL, wCollarR) < 0.06) {\n            wCol = vec3(0.85);\n            if (min(wCollarL, wCollarR) < 0.01) wCol *= 0.7;\n        }\n        \n        for (float i = 0.0; i < 2.0; i++) {\n            vec2 pktP = wp - vec2(-0.15 + i * 0.3, 0.05);\n            float pkt = max(abs(pktP.x) - 0.07, abs(pktP.y) - 0.08);\n            if (pkt < 0.0) {\n                wCol *= 0.95;\n                if (abs(pktP.y - 0.08) < 0.005) wCol *= 0.8; \n                if (length(pktP - vec2(0.0, 0.04)) < 0.01) wCol = vec3(0.8); \n            }\n        }\n        \n        col = wCol;\n    } else if (ws < 0.02) {\n        col *= 0.6; \n    }\n}\n\nvoid layer_StarBadge(in vec2 p, inout vec3 col) {\n    float blueBottom = max(abs(p.x - 0.5) - 0.35, abs(p.y + 0.6) - 0.25);\n    if (blueBottom < 0.0) {\n        col = mix(vec3(0.1, 0.2, 0.5), vec3(0.15, 0.25, 0.65), sin(p.x*20.0)*0.5+0.5);\n        vec2 starP = p - vec2(0.35, -0.5);\n        \n        float a = atan(starP.x, starP.y) + 3.14159;\n        float r = length(starP);\n        float seg = a * 5.0 / 6.28318;\n        float frac = fract(seg);\n        float id = floor(seg);\n        float ang = (id + 0.5) * 6.28318 / 5.0;\n        vec2 pnt = vec2(sin(ang), cos(ang)) * 0.08;\n        float starD = segment(starP, vec2(0.0), pnt);\n        \n        if (r < 0.06 && starD < 0.02 + r*0.2) {\n            col = vec3(0.9, 0.7, 0.1); \n            if (r < 0.025) col = vec3(0.8, 0.1, 0.1); \n        }\n    }\n}\n\nvec4 layer_Background(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 col = vec3(-1.0);\n    \n    layer_Background(p, col);\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_BlueShirt(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 col = vec3(-1.0);\n    \n    layer_BlueShirt(p, col);\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_WhiteShirt(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 col = vec3(-1.0);\n    \n    layer_WhiteShirt(p, col);\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_StarBadge(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 col = vec3(-1.0);\n    \n    layer_StarBadge(p, col);\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n","IMG_7409":"#define segment(p, a, b) length(p - a - (b - a) * clamp(dot(p - a, b - a) / dot(b - a, b - a), 0.0, 1.0))\n\nvoid layer_Background(in vec2 p, inout vec3 col) {\n    col = vec3(0.45, 0.45, 0.47);\n    float floorNoise = fract(sin(dot(floor(p * 80.0), vec2(127.1, 311.7))) * 43758.5453);\n    col += floorNoise * 0.05;\n}\n\nvoid layer_BackgroundGarments(in vec2 p, in float s1Body, in float s2Body, inout vec3 col) {\n    float greenArea = max(p.x - 0.5, p.y - 0.4);\n    if (greenArea < 0.0 && s1Body > 0.0 && s2Body > 0.0) {\n        if (p.x > 0.0 && p.y > 0.0) col = vec3(0.2, 0.45, 0.2); \n        if (p.y > 0.6) col = vec3(0.8, 0.15, 0.15); \n    }\n}\n\nvoid layer_WhiteShirt(in vec2 p, in float s1Body, out float s2Body, inout vec3 col) {\n    vec2 s2 = p - vec2(0.2, 0.0);\n    s2Body = length(max(abs(s2) - vec2(0.3, 0.4), 0.0)) - 0.1;\n    if (s2Body < 0.0 && s1Body > 0.0) {\n        vec3 s2Col = vec3(0.9, 0.92, 0.95);\n        s2Col += sin(s2.x * 15.0 + s2.y * 12.0) * 0.03;\n        \n        float tie = segment(s2, vec2(0.0, 0.4), vec2(0.0, -0.2));\n        if (tie < 0.03 + (0.3 - s2.y)*0.02) {\n            s2Col = vec3(0.1, 0.1, 0.15); \n        }\n        \n        for (float i = 0.0; i < 2.0; i++) {\n            vec2 epPos = vec2(-0.2 + i * 0.4, 0.25);\n            vec2 epL = s2 - epPos;\n            if (abs(epL.x) < 0.06 && abs(epL.y) < 0.025) {\n                s2Col = vec3(0.1);\n                if (fract(epL.x * 25.0) < 0.4) s2Col = vec3(0.8, 0.7, 0.2);\n            }\n        }\n        \n        float collarL = segment(s2, vec2(0.0, 0.35), vec2(-0.15, 0.25));\n        float collarR = segment(s2, vec2(0.0, 0.35), vec2(0.15, 0.25));\n        if (min(collarL, collarR) < 0.04) {\n            s2Col = vec3(0.8, 0.15, 0.15);\n        }\n        col = s2Col;\n    } else if (s2Body < 0.02 && s1Body > 0.0) {\n        col *= 0.6;\n    }\n}\n\nvoid layer_BlueShirt(in vec2 p, out float s1Body, inout vec3 col) {\n    vec2 s1 = p - vec2(-0.3, -0.2);\n    s1Body = length(max(abs(s1) - vec2(0.35, 0.35), 0.0)) - 0.1;\n    if (s1Body < 0.0) {\n        vec3 s1Col = vec3(0.55, 0.7, 0.85);\n        s1Col += sin(s1.x * 20.0 + s1.y * 10.0) * 0.04; \n        \n        for (float i = 0.0; i < 3.0; i++) {\n            vec2 epPos = vec2(-0.55 + i * 0.5, 0.05);\n            vec2 epL = s1 - epPos;\n            if (abs(epL.x) < 0.07 && abs(epL.y) < 0.03) {\n                s1Col = vec3(0.1, 0.1, 0.15);\n                if (fract(epL.x * 25.0) < 0.4) s1Col = vec3(0.8, 0.7, 0.2); \n            }\n        }\n        for (float i = 0.0; i < 4.0; i++) {\n            if (length(s1 - vec2(0.0, -0.3 + i * 0.15)) < 0.015) s1Col = vec3(0.8);\n        }\n        col = s1Col;\n    } else if (s1Body < 0.02) {\n        col *= 0.6; \n    }\n}\n\nvec4 layer_Background(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 col = vec3(-1.0);\n    \n    layer_Background(p, col);\n    \n    float s1Body, s2Body;\n    \n    // We need s1Body to process WhiteShirt and BackgroundGarments\n    vec2 s1 = p - vec2(-0.3, -0.2);\n    s1Body = length(max(abs(s1) - vec2(0.35, 0.35), 0.0)) - 0.1;\n    \n    // WhiteShirt needs s1Body and computes s2Body\n    \n    // BlueShirt is drawn on top\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_BackgroundGarments(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 col = vec3(-1.0);\n    \n    \n    float s1Body, s2Body;\n    \n    // We need s1Body to process WhiteShirt and BackgroundGarments\n    vec2 s1 = p - vec2(-0.3, -0.2);\n    s1Body = length(max(abs(s1) - vec2(0.35, 0.35), 0.0)) - 0.1;\n    \n    // WhiteShirt needs s1Body and computes s2Body\n    layer_BackgroundGarments(p, s1Body, s2Body, col);\n    \n    // BlueShirt is drawn on top\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_WhiteShirt(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 col = vec3(-1.0);\n    \n    \n    float s1Body, s2Body;\n    \n    // We need s1Body to process WhiteShirt and BackgroundGarments\n    vec2 s1 = p - vec2(-0.3, -0.2);\n    s1Body = length(max(abs(s1) - vec2(0.35, 0.35), 0.0)) - 0.1;\n    \n    // WhiteShirt needs s1Body and computes s2Body\n    layer_WhiteShirt(p, s1Body, s2Body, col);\n    \n    // BlueShirt is drawn on top\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_BlueShirt(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 col = vec3(-1.0);\n    \n    \n    float s1Body, s2Body;\n    \n    // We need s1Body to process WhiteShirt and BackgroundGarments\n    vec2 s1 = p - vec2(-0.3, -0.2);\n    s1Body = length(max(abs(s1) - vec2(0.35, 0.35), 0.0)) - 0.1;\n    \n    // WhiteShirt needs s1Body and computes s2Body\n    \n    // BlueShirt is drawn on top\n    layer_BlueShirt(p, s1Body, col);\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n","IMG_7410":"#define segment(p, a, b) length(p - a - (b - a) * clamp(dot(p - a, b - a) / dot(b - a, b - a), 0.0, 1.0))\n\nvoid layer_Background(in vec2 p, inout vec3 col) {\n    col = vec3(0.78, 0.78, 0.8);\n    if (p.y < -0.3) {\n        col = vec3(0.45, 0.45, 0.47); \n        vec2 bUV = p * vec2(10.0, 20.0);\n        bUV.x += step(1.0, mod(floor(bUV.y), 2.0)) * 0.5;\n        if (min(fract(bUV.x), fract(bUV.y)) < 0.05) col = vec3(0.35); \n    }\n}\n\nvoid layer_Shelves(in vec2 p, inout vec3 col) {\n    if (p.y > 0.4 && p.x > 0.1) {\n        col = vec3(0.4, 0.4, 0.45); \n        if (fract(p.y * 5.0) < 0.05) col = vec3(0.2);\n        \n        vec2 bootP = vec2(fract(p.x * 6.0) - 0.5, fract(p.y * 5.0) - 0.5);\n        float bootShape = max(abs(bootP.x) - 0.15, abs(bootP.y + 0.1) - 0.3);\n        bootShape = min(bootShape, length(max(abs(bootP - vec2(0.1, -0.4)) - vec2(0.15, 0.05), 0.0)) - 0.05);\n        \n        if (bootShape < 0.0) {\n            col = vec3(0.05, 0.05, 0.08); \n            if (length(bootP - vec2(-0.05, 0.1)) < 0.05) col += 0.1;\n        }\n    }\n}\n\nvoid layer_Mannequin(in vec2 p, inout vec3 col) {\n    vec2 mp = p - vec2(-0.1, -0.1);\n    float body = 1.0;\n    body = min(body, length(max(abs(mp) - vec2(0.15, 0.3), 0.0)) - 0.05);\n    body = min(body, segment(mp, vec2(-0.08, -0.3), vec2(-0.08, -0.7)) - 0.08);\n    body = min(body, segment(mp, vec2(0.08, -0.3), vec2(0.08, -0.7)) - 0.08);\n    body = min(body, segment(mp, vec2(-0.2, 0.25), vec2(-0.3, -0.1)) - 0.06);\n    body = min(body, segment(mp, vec2(0.2, 0.25), vec2(0.3, -0.1)) - 0.06);\n    \n    if (body < 0.0) {\n        vec3 mCol = vec3(0.3, 0.4, 0.2); \n        mCol *= 0.8 + 0.2 * smoothstep(0.15, 0.0, abs(mp.x));\n        mCol += sin(mp.x * 40.0 + mp.y * 20.0) * 0.02; \n        \n        if (abs(mp.y + 0.3) < 0.03 && abs(mp.x) < 0.2) {\n            mCol = vec3(0.1);\n            if (abs(mp.x) < 0.03) mCol = vec3(0.8, 0.7, 0.2); \n        }\n        \n        if (abs(mp.x) < 0.015 && fract(mp.y * 10.0) < 0.2 && mp.y > -0.3 && mp.y < 0.3) {\n            mCol = vec3(0.2, 0.25, 0.1);\n        }\n        col = mCol;\n    } else if (body < 0.02) {\n        col *= 0.6; \n    }\n    \n    vec2 hp = mp - vec2(0.0, 0.45);\n    float cap = length(max(abs(hp) - vec2(0.08, 0.05), 0.0)) - 0.02;\n    float brim = segment(hp, vec2(-0.1, -0.05), vec2(0.12, -0.07)) - 0.01;\n    if (min(cap, brim) < 0.0) {\n        col = vec3(0.28, 0.35, 0.18);\n        if (length(hp - vec2(0.0, 0.02)) < 0.02) col = vec3(0.8, 0.1, 0.1); \n    }\n    \n    float lShoe = length(max(abs(mp - vec2(-0.08, -0.75)) - vec2(0.06, 0.04), 0.0)) - 0.02;\n    float rShoe = length(max(abs(mp - vec2(0.08, -0.75)) - vec2(0.06, 0.04), 0.0)) - 0.02;\n    if (min(lShoe, rShoe) < 0.0) col = vec3(0.1);\n}\n\nvec4 layer_Background(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 col = vec3(-1.0);\n    \n    layer_Background(p, col);\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_Shelves(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 col = vec3(-1.0);\n    \n    layer_Shelves(p, col);\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_Mannequin(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 col = vec3(-1.0);\n    \n    layer_Mannequin(p, col);\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n","IMG_7680":"#define segment(p, a, b) length(p - a - (b - a) * clamp(dot(p - a, b - a) / dot(b - a, b - a), 0.0, 1.0))\n#define myCosh(x) (exp(x) + exp(-(x))) / 2.0\n\nvoid layer_Sky(in vec2 p, in float iTime, inout vec3 col) {\n    col = mix(vec3(0.02, 0.04, 0.1), vec3(0.1, 0.15, 0.25), p.y * 0.5 + 0.5);\n    \n    vec2 sUV = p * 20.0;\n    float starGrid = fract(sin(dot(floor(sUV), vec2(12.9898, 78.233))) * 43758.5453);\n    if (starGrid > 0.98) {\n        float twinkle = sin(iTime * 5.0 + starGrid * 100.0) * 0.5 + 0.5;\n        col += vec3(twinkle * 0.8) * exp(-length(fract(sUV) - 0.5) * 10.0);\n    }\n}\n\nvoid layer_Building(in vec2 p, inout vec3 col) {\n    if (p.y < -0.4) {\n        col = vec3(0.02, 0.03, 0.06);\n        if (fract(p.x * 4.0) < 0.1 && p.y > -0.5) col = vec3(0.6, 0.15, 0.15); \n    }\n}\n\nvoid layer_StringLights(in vec2 p, in float iTime, inout vec3 col) {\n    for (float i = 0.0; i < 4.0; i++) {\n        float wx = p.x - 0.3 * i + 0.5;\n        float bounce = sin(iTime * 2.0 + i) * 0.05;\n        float wireY = 0.6 - 0.5 * myCosh(wx * 2.0) + 0.5 + bounce;\n        \n        if (abs(p.y - wireY) < 0.005) {\n            col = vec3(0.1); \n        }\n        \n        for (int j = 0; j < 6; j++) {\n            float fj = float(j);\n            float bx = -1.2 + (fj / 5.0) * 2.5 + i * 0.2;\n            float bwx = bx - 0.3 * i + 0.5;\n            float by = 0.6 - 0.5 * myCosh(bwx * 2.0) + 0.5 + bounce - 0.08;\n            \n            vec2 bp = vec2(bx, by);\n            float swing = sin(iTime * 3.0 + fj * 0.5) * 0.03;\n            bp.x += swing;\n            \n            float d = length(p - bp);\n            \n            if (segment(p, vec2(bx, by + 0.08), vec2(bp.x, bp.y + 0.02)) < 0.004) col = vec3(0.1);\n            \n            vec3 bCol = vec3(1.0);\n            float c = mod(fj + i, 3.0);\n            if (c == 0.0) bCol = vec3(1.0, 0.2, 0.2); \n            else if (c == 1.0) bCol = vec3(1.0, 0.8, 0.2); \n            else bCol = vec3(0.9, 0.9, 1.0); \n            \n            float on = sin(iTime * 4.0 - fj * 1.5 - i * 2.0) * 0.5 + 0.5;\n            on = smoothstep(0.4, 0.6, on); \n            bCol *= 0.2 + 0.8 * on;\n            \n            float glow = exp(-d * 15.0);\n            col += bCol * glow * 1.5;\n            \n            if (d < 0.02) {\n                col = mix(col, bCol * 2.0, 0.8);\n                vec2 fp = p - bp;\n                if (abs(fp.x) < 0.002 && fp.y > -0.01 && fp.y < 0.01) col = vec3(2.0 * on);\n                if (abs(length(fp - vec2(0.0, 0.01)) - 0.005) < 0.001 && fp.y > 0.01) col = vec3(2.0 * on);\n            }\n        }\n    }\n}\n\nvoid layer_RedLantern(in vec2 p, in float iTime, inout vec3 col) {\n    vec2 lp = p - vec2(-0.7, 0.1);\n    \n    float angle = sin(iTime * 1.5) * 0.3; \n    mat2 rot = mat2(cos(angle), -sin(angle), sin(angle), cos(angle));\n    \n    lp.y -= 0.2; \n    lp = rot * lp;\n    lp.y += 0.2; \n    \n    float lanternBody = length(lp * vec2(1.0, 0.8)) - 0.15;\n    if (lanternBody < 0.0) {\n        col = vec3(0.8, 0.15, 0.15); \n        float ribs = abs(sin(lp.x * 40.0));\n        col = mix(col, vec3(0.9, 0.8, 0.2), smoothstep(0.8, 0.95, ribs) * 0.6);\n        col *= 0.7 + 0.3 * smoothstep(0.15, 0.0, length(lp));\n        if (abs(lp.y) > 0.17) col = vec3(0.8, 0.7, 0.2); \n    }\n    \n    float tasselSwing = sin(iTime * 1.5 + 1.0) * 0.1;\n    vec2 tp = lp - vec2(0.0, -0.18);\n    tp.x -= tp.y * tasselSwing * 5.0; \n    \n    if (tp.y < 0.0 && tp.y > -0.2 && abs(tp.x) < 0.02) {\n        vec3 tCol = vec3(0.8, 0.15, 0.15);\n        float threads = abs(fract(tp.x * 100.0) - 0.5);\n        tCol *= 0.6 + 0.4 * threads;\n        col = mix(col, tCol, 1.0); // Simple mix if drawn over background\n    }\n}\n\nvoid layer_Vignette(in vec2 p, inout vec3 col) {\n    col *= 1.0 - 0.3 * length(p);\n}\n\nvec4 layer_Sky(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 col = vec3(-1.0);\n    \n    layer_Sky(p, iTime, col);\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_Building(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 col = vec3(-1.0);\n    \n    layer_Building(p, col);\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_StringLights(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 col = vec3(-1.0);\n    \n    layer_StringLights(p, iTime, col);\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_RedLantern(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 col = vec3(-1.0);\n    \n    layer_RedLantern(p, iTime, col);\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_Vignette(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 col = vec3(-1.0);\n    \n    layer_Vignette(p, col);\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n","IMG_7681":"void layer_Background(in vec2 p, in float iTime, inout vec3 col) {\n    col = vec3(0.4, 0.7, 0.9);\n    col += 0.1 * sin(p.x * 5.0 + iTime * 0.2) * sin(p.y * 3.0);\n}\n\nvoid layer_TempleRoof(in vec2 p, in float iTime, inout vec3 col) {\n    if (p.x + p.y < -0.2) {\n        col = vec3(0.7, 0.3, 0.2); \n        vec2 tileP = p + vec2(iTime*0.05, 0.0);\n        if (fract(tileP.x * 10.0 - tileP.y * 10.0) < 0.1) col *= 0.8; \n        if (fract(tileP.x * 5.0 + tileP.y * 5.0) < 0.05) col *= 0.7;\n    }\n}\n\nvoid layer_Dragon(in vec2 p, in float iTime, inout vec3 col) {\n    vec3 dragonCol = vec3(0.9, 0.75, 0.2); \n    float isDragon = 0.0;\n    \n    vec2 dp = p;\n    dp.y += sin(dp.x * 4.0 + iTime * 2.0) * 0.05;\n    \n    float bodyCurve = sin(dp.x * 3.0) * 0.3;\n    if (abs(dp.y - bodyCurve - 0.1) < 0.1 && dp.x > -0.6 && dp.x < 0.8) {\n        isDragon = 1.0;\n        if (fract(dp.x * 20.0) < 0.2 || fract(dp.y * 20.0) < 0.2) {\n            dragonCol *= 0.8;\n        }\n    }\n    \n    vec2 headP = p - vec2(0.7, sin(0.7 * 3.0) * 0.3 + 0.1 + sin(0.7 * 4.0 + iTime * 2.0) * 0.05);\n    if (length(headP) < 0.15) {\n        isDragon = 1.0;\n        dragonCol = vec3(0.8, 0.6, 0.1);\n        if (length(headP - vec2(-0.05, 0.05)) < 0.03) dragonCol = vec3(1.0, 0.0, 0.0); \n        if (headP.y > 0.1 && headP.x > 0.0) dragonCol = vec3(0.6, 0.8, 0.3); \n    }\n    \n    if (abs(p.x - 0.2) < 0.05 && p.y > bodyCurve - 0.1 && p.y < bodyCurve + 0.1) {\n        isDragon = 1.0; \n    }\n    if (abs(p.x - (-0.3)) < 0.05 && p.y > bodyCurve - 0.1 && p.y < bodyCurve + 0.1) {\n        isDragon = 1.0; \n    }\n    \n    if (isDragon > 0.0 && fract(p.x * 8.0) < 0.15 && p.y > bodyCurve + 0.1) {\n        dragonCol = vec3(0.2, 0.6, 0.5); \n    }\n    \n    if (isDragon > 0.0) {\n        col = dragonCol;\n        col *= 0.8 + 0.4 * p.y;\n    }\n}\n\nvoid layer_Vignette(in vec2 p, inout vec3 col) {\n    col *= 1.0 - 0.1 * length(p);\n}\n\nvec4 layer_Background(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 col = vec3(-1.0);\n    \n    layer_Background(p, iTime, col);\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_TempleRoof(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 col = vec3(-1.0);\n    \n    layer_TempleRoof(p, iTime, col);\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_Dragon(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 col = vec3(-1.0);\n    \n    layer_Dragon(p, iTime, col);\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_Vignette(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 col = vec3(-1.0);\n    \n    layer_Vignette(p, col);\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n","IMG_7682":"#define segment(p, a, b) length(p - a - (b - a) * clamp(dot(p - a, b - a) / dot(b - a, b - a), 0.0, 1.0))\n\nvoid layer_Wall(in vec2 p, inout vec3 col) {\n    if (p.y > -0.5) {\n        col = vec3(0.85, 0.7, 0.55); \n        if (p.y > 0.35) col = vec3(0.35, 0.33, 0.3); \n        else if (p.y > 0.25) col = vec3(0.75, 0.45, 0.45); \n        \n        vec2 np = p * 10.0;\n        float n1 = fract(sin(dot(floor(np), vec2(12.9, 78.2))) * 43758.0);\n        float n2 = fract(sin(dot(floor(np*2.0), vec2(41.2, 13.5))) * 43758.0);\n        float stain = smoothstep(0.4, 0.6, n1 * n2);\n        col = mix(col, col * 0.7, stain * 0.5); \n    } else {\n        col = vec3(0.45, 0.15, 0.15); \n    }\n}\n\nvoid layer_WindowGrille(in vec2 p, inout vec3 col) {\n    if (p.x > -0.35 && p.x < 0.35 && p.y > -0.15 && p.y < 0.25) {\n        vec3 grilleCol = vec3(0.1, 0.4, 0.4);\n        \n        col = mix(col, vec3(0.1), 0.5);\n        \n        vec2 gp = fract(p * 15.0) - 0.5;\n        float lattice = min(abs(gp.x + gp.y), abs(gp.x - gp.y));\n        lattice = min(lattice, min(abs(gp.x), abs(gp.y)));\n        \n        if (lattice < 0.1) {\n            col = grilleCol;\n            if (lattice > 0.06) col *= 0.6;\n        }\n        \n        float frame = max(abs(p.x) - 0.35, abs(p.y - 0.05) - 0.2);\n        if (abs(frame) < 0.02) col = vec3(0.05, 0.3, 0.3);\n    }\n}\n\nvoid layer_Railing(in vec2 p, inout vec3 col) {\n    if (p.y > -0.55 && p.y < -0.25) {\n        col = vec3(0.75, 0.7, 0.5); \n        \n        if (abs(fract(p.x * 15.0) - 0.5) > 0.4) col = vec3(0.6, 0.55, 0.35);\n        if (abs(p.y - (-0.28)) < 0.015 || abs(p.y - (-0.52)) < 0.015) col = vec3(0.8, 0.75, 0.55);\n        \n        vec2 lp = vec2(fract(p.x * 5.0) - 0.5, (p.y + 0.4) * 3.0);\n        float lDist = length(lp);\n        float lAngle = atan(lp.x, lp.y);\n        \n        float petal1 = abs(sin(lAngle * 2.0)) * 0.4;\n        float petal2 = abs(cos(lAngle * 2.0)) * 0.3;\n        \n        if (lDist < petal1 || lDist < petal2) {\n            col = vec3(0.15, 0.45, 0.4); \n            if (abs(lDist - petal1) < 0.02 || abs(lDist - petal2) < 0.02) col = vec3(0.1, 0.3, 0.25);\n        }\n    }\n}\n\nvoid layer_Dragon(in vec2 p, inout vec3 col) {\n    vec2 dp = p - vec2(0.65, 0.5);\n    float dragon = 1.0;\n    \n    float bCurve = sin(dp.x * 15.0) * 0.1;\n    dragon = min(dragon, segment(dp, vec2(-0.2, -0.1), vec2(0.2, 0.1 + bCurve)));\n    dragon = min(dragon, segment(dp, vec2(0.2, 0.1 + bCurve), vec2(0.3, -0.05)));\n    \n    vec2 headP = dp - vec2(-0.2, -0.1);\n    float head = length(headP) - 0.05;\n    head = min(head, segment(headP, vec2(0.0), vec2(-0.08, -0.02))); \n    head = min(head, segment(headP, vec2(0.0), vec2(-0.05, 0.08))); \n    dragon = min(dragon, head);\n    \n    float scales = fract(dp.x * 30.0);\n    if (scales < 0.5 && dp.y > bCurve + 0.02 && dp.y < bCurve + 0.06 && dp.x > -0.1 && dp.x < 0.2) {\n        dragon = min(dragon, 0.0); \n    }\n    \n    if (dragon < 0.02) {\n        col = vec3(0.1, 0.3, 0.3); \n        if (dragon > 0.01) col = vec3(0.05, 0.2, 0.2); \n    }\n}\n\nvec4 layer_Wall(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 col = vec3(-1.0);\n    \n    layer_Wall(p, col);\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_WindowGrille(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 col = vec3(-1.0);\n    \n    layer_WindowGrille(p, col);\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_Railing(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 col = vec3(-1.0);\n    \n    layer_Railing(p, col);\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_Dragon(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 col = vec3(-1.0);\n    \n    layer_Dragon(p, col);\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n","IMG_7683":"void layer_BackgroundCourtyard(in vec2 p, in float iTime, inout vec3 col) {\n    if (p.y < -0.4) {\n        col = vec3(0.7, 0.6, 0.5); \n        vec2 tp = p * vec2(10.0, 5.0 / max(0.1, (p.y + 1.0)));\n        if (fract(tp.x) < 0.05 || fract(tp.y) < 0.05) col = vec3(0.5, 0.4, 0.3);\n    } else {\n        col = vec3(0.5, 0.7, 0.75); \n        \n        if (abs(p.x) > 0.3 && p.y > -0.4 && p.y < 0.4) {\n            col = vec3(0.3, 0.6, 0.55); \n            if (fract(p.x * 20.0) < 0.2) col *= 0.7; \n        }\n        \n        if (length(p - vec2(-0.2, -0.1)) < 0.2 || length(p - vec2(0.3, 0.0)) < 0.25) {\n            col = vec3(0.2, 0.4, 0.2); \n            col *= 0.8 + 0.2 * fract(sin(p.x*100.0 + iTime)*40.0); \n        }\n    }\n}\n\nvoid layer_TempleGateHeader(in vec2 p, in float iTime, inout vec3 col) {\n    if (p.y > 0.65) {\n        col = vec3(0.4, 0.1, 0.1); \n        if (p.y < 0.95 && p.y > 0.7) {\n            col = vec3(0.8, 0.7, 0.2); \n            if (abs(p.y - 0.82) < 0.08) {\n                if (fract(p.x * 5.0) < 0.7 && abs(p.x) < 0.8) {\n                    col = vec3(0.7, 0.15, 0.15); \n                }\n            }\n            col *= 0.9 + 0.1 * fract(sin(dot(p + iTime*0.05, vec2(12.9898, 78.233))) * 43758.5453);\n        }\n    }\n}\n\nvoid layer_TempleGatePillars(in vec2 p, inout vec3 col) {\n    float dPillarL = max(abs(p.x - (-0.7)) - 0.1, abs(p.y) - 0.65);\n    float dPillarR = max(abs(p.x - 0.7) - 0.1, abs(p.y) - 0.65);\n    \n    if (min(dPillarL, dPillarR) < 0.0) {\n        col = vec3(0.3, 0.15, 0.15); \n        if (fract(p.y * 30.0) < 0.1 || fract(p.x * 10.0 + (fract(p.y*15.0)>0.5?0.5:0.0)) < 0.1) {\n            col = vec3(0.2, 0.1, 0.1);\n        }\n        \n        if (abs(abs(p.x) - 0.7) < 0.06 && p.y < 0.6 && p.y > -0.6) {\n            col = vec3(0.9, 0.9, 0.9); \n            if (fract(p.y * 5.0) < 0.6 && abs(abs(p.x) - 0.7) < 0.03) {\n                if (fract(p.y * 25.0) < 0.5 || abs(abs(p.x) - 0.7) < 0.01) {\n                    col = vec3(0.05); \n                }\n            }\n        }\n    }\n}\n\nvoid layer_TempleGateGrid(in vec2 p, in float iTime, inout vec3 col, out float isGate) {\n    isGate = 0.0;\n    if (abs(p.x) < 0.6 && p.y > -0.65 && p.y < 0.65) {\n        vec3 gateCol = vec3(0.85, 0.75, 0.2); \n        gateCol += 0.1 * sin(p.y * 20.0 - iTime * 3.0) * sin(p.x * 20.0);\n        \n        if (abs(p.x) > 0.58) isGate = 1.0; \n        if (abs(p.y) > 0.63) isGate = 1.0;\n        if (abs(p.x) < 0.015) isGate = 1.0; \n        \n        if (abs(p.y - 0.4) < 0.01) isGate = 1.0;\n        if (abs(p.y - 0.2) < 0.01) isGate = 1.0;\n        if (abs(p.y - (-0.3)) < 0.01) isGate = 1.0;\n        if (abs(p.y - (-0.45)) < 0.01) isGate = 1.0;\n        \n        if (fract(p.x * 15.0) < 0.1 && p.y > 0.4) isGate = 1.0;\n        if (fract(p.x * 15.0) < 0.1 && p.y < -0.45) isGate = 1.0;\n        \n        if (p.y > -0.45 && p.y < -0.3) {\n            vec2 lp = vec2(fract(p.x * 5.0), p.y);\n            if (abs(length(lp - vec2(0.5, -0.4)) - 0.08) < 0.01) isGate = 1.0;\n            if (abs(lp.x - 0.5) < 0.01) isGate = 1.0;\n        }\n        \n        if (isGate > 0.0) col = gateCol;\n    }\n}\n\nvoid layer_RedChineseCharacters(in vec2 p, in float isGate, inout vec3 col) {\n    if (abs(p.x) < 0.6 && p.y > -0.65 && p.y < 0.65) {\n        if (p.y > -0.2 && p.y < 0.2) {\n            float isChar = 0.0;\n            vec2 cp = p;\n            cp.x = fract(p.x * 2.0) - 0.5; \n            \n            if (abs(cp.y) < 0.15 && abs(cp.x) < 0.15) {\n                if (abs(abs(cp.x) - 0.15) < 0.02) isChar = 1.0;\n                if (abs(abs(cp.y) - 0.15) < 0.02) isChar = 1.0;\n                if (abs(cp.y) < 0.02) isChar = 1.0;\n                if (abs(cp.x) < 0.02) isChar = 1.0;\n            }\n            \n            if (isChar > 0.0) {\n                col = vec3(0.7, 0.2, 0.2); \n            }\n        }\n    }\n}\n\nvoid layer_WhitePaperSign(in vec2 p, inout vec3 col) {\n    if (abs(p.x) < 0.6 && p.y > -0.65 && p.y < 0.65) {\n        float dSign = max(abs(p.x - 0.15) - 0.1, abs(p.y - 0.0) - 0.08);\n        if (dSign < 0.0) {\n            col = vec3(0.95); \n            if (fract(p.y * 20.0) < 0.3 && abs(p.y) < 0.06 && abs(p.x - 0.15) < 0.08) {\n                col = vec3(0.1); \n            }\n        }\n    }\n}\n\nvoid layer_Vignette(in vec2 p, inout vec3 col) {\n    col *= 1.0 - 0.1 * length(p);\n}\n\nvec4 layer_BackgroundCourtyard(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 col = vec3(-1.0);\n    \n    layer_BackgroundCourtyard(p, iTime, col);\n    \n    float isGate;\n    \n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_TempleGateHeader(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 col = vec3(-1.0);\n    \n    layer_TempleGateHeader(p, iTime, col);\n    \n    float isGate;\n    \n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_TempleGatePillars(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 col = vec3(-1.0);\n    \n    layer_TempleGatePillars(p, col);\n    \n    float isGate;\n    \n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_TempleGateGrid(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 col = vec3(-1.0);\n    \n    \n    float isGate;\n    layer_TempleGateGrid(p, iTime, col, isGate);\n    \n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_RedChineseCharacters(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 col = vec3(-1.0);\n    \n    \n    float isGate;\n    layer_RedChineseCharacters(p, isGate, col);\n    \n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_WhitePaperSign(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 col = vec3(-1.0);\n    \n    \n    float isGate;\n    layer_WhitePaperSign(p, col);\n    \n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_Vignette(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 col = vec3(-1.0);\n    \n    \n    float isGate;\n    \n    layer_Vignette(p, col);\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n","IMG_7684":"void layer_Background(in vec2 p, in float iTime, inout vec3 col) {\n    col = vec3(0.8, 0.8, 0.82); \n    \n    if (p.y > 0.7 && p.x > -0.5 && p.x < 0.6) {\n        col = vec3(0.6, 0.75, 0.4); \n        if (p.y > 0.85 && p.y < 0.95) {\n            col = vec3(0.6, 0.75, 0.4); \n            if (fract(p.x * 6.0) < 0.5 && p.x > -0.4 && p.x < 0.5) col = vec3(0.8, 0.2, 0.2);\n        }\n        if (p.y > 0.75 && p.y < 0.85) {\n            if (abs(p.x) < 0.2) col = vec3(0.2, 0.3, 0.5);\n        }\n    }\n    \n    if (p.y > 0.6 && p.y <= 0.7) {\n        col = vec3(0.5, 0.2, 0.2); \n        if (p.x < -0.4 || p.x > 0.5) col = vec3(0.8, 0.8, 0.82); \n        if (p.y > 0.65 && p.x < -0.1) {\n            if (fract(p.x * 10.0 + sin(p.x*20.0 + iTime)*0.5) < 0.3) col = vec3(0.3, 0.5, 0.4);\n        }\n    }\n}\n\nvoid layer_Facade(in vec2 p, in float iTime, inout vec3 col) {\n    if (p.y > -0.6 && p.y <= 0.6) {\n        col = vec3(0.4, 0.65, 0.65); \n        \n        if (p.x > 0.8) col = vec3(0.4, 0.2, 0.2); \n        \n        float dFrame = max(abs(p.x) - 0.5, abs(p.y) - 0.3);\n        if (dFrame < 0.0) {\n            col = vec3(0.15, 0.1, 0.1); \n            \n            vec3 grateCol = vec3(0.8, 0.7, 0.3); \n            float isGrate = 0.0;\n            \n            if (dFrame > -0.05) isGrate = 1.0;\n            \n            vec2 gridP = p;\n            gridP.x += sin(iTime + p.y * 5.0) * 0.02;\n            \n            if (fract(gridP.x * 5.0) < 0.05) isGrate = 1.0;\n            if (fract(gridP.y * 5.0) < 0.05) isGrate = 1.0;\n            vec2 gp = fract(gridP * 5.0);\n            if (abs(gp.x - 0.5) < 0.05 || abs(gp.y - 0.5) < 0.05) isGrate = 0.5;\n            \n            if (isGrate > 0.0) col = mix(col, grateCol, isGrate);\n            \n            if (abs(p.y) < 0.12 && abs(p.x) < 0.4) {\n                col = vec3(0.8, 0.7, 0.2); \n                \n                if (abs(p.x - (-0.25)) < 0.06 || abs(p.x) < 0.06 || abs(p.x - 0.25) < 0.06) {\n                    if (abs(p.y) < 0.08) {\n                        if (fract(p.y * 15.0) < 0.5 || fract(p.x * 20.0) < 0.5) {\n                            col = vec3(0.7, 0.2, 0.2); \n                        }\n                    }\n                }\n            }\n        }\n        \n        vec3 goldCol = vec3(0.75, 0.65, 0.2);\n        \n        vec2 lp = p - vec2(-0.7, 0.0);\n        float dL = length(max(abs(lp) - vec2(0.1, 0.3), 0.0)) - 0.05;\n        dL = min(dL, abs(length(lp - vec2(0.1, 0.2)) - 0.15) - 0.05); \n        dL = min(dL, abs(length(lp - vec2(0.1, -0.2)) - 0.1) - 0.04); \n        \n        vec2 rp = p - vec2(0.65, 0.0);\n        float dR = length(max(abs(rp) - vec2(0.1, 0.3), 0.0)) - 0.05;\n        dR = min(dR, abs(length(rp - vec2(-0.1, 0.2)) - 0.15) - 0.05);\n        dR = min(dR, abs(length(rp - vec2(-0.1, -0.2)) - 0.1) - 0.04);\n        \n        if (dL < 0.0 || dR < 0.0) {\n            col = goldCol;\n            col *= 0.7 + 0.3 * sin(p.x * 50.0 + p.y * 50.0 + iTime * 5.0);\n            if (p.y < 0.0) col *= 0.8;\n        }\n        \n        if (abs(p.x) < 0.15 && p.y > 0.3 && p.y < 0.5) {\n            if (length(p - vec2(0.0, 0.4)) < 0.1) col = goldCol;\n        }\n    }\n}\n\nvoid layer_BottomRoof(in vec2 p, inout vec3 col) {\n    if (p.y <= -0.6) {\n        vec2 tp = p;\n        tp.x = fract(tp.x * 10.0) - 0.5;\n        col = vec3(0.6, 0.3, 0.25); \n        \n        float arc = length(tp - vec2(0.0, 0.5)) - 0.5;\n        if (arc < 0.0) col *= 0.8;\n        if (abs(arc) < 0.05) col *= 0.6;\n        \n        if (p.y > -0.65) col = vec3(0.2, 0.4, 0.3);\n        \n        if (p.x < -0.8 && p.y > -0.8) col = vec3(0.9);\n        if (p.x > 0.8 && p.y > -0.8) {\n            col = vec3(0.2, 0.4, 0.3);\n            if (fract(p.x * 10.0 + p.y * 10.0) < 0.5) col = vec3(0.8, 0.7, 0.2);\n        }\n    }\n}\n\nvoid layer_Vignette(in vec2 p, inout vec3 col) {\n    col *= 1.0 - 0.1 * length(p);\n}\n\nvec4 layer_Background(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 col = vec3(-1.0);\n    \n    layer_Background(p, iTime, col);\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_Facade(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 col = vec3(-1.0);\n    \n    layer_Facade(p, iTime, col);\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_BottomRoof(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 col = vec3(-1.0);\n    \n    layer_BottomRoof(p, col);\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_Vignette(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 col = vec3(-1.0);\n    \n    layer_Vignette(p, col);\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n","IMG_7685":"void layer_BackgroundWall(in vec2 p, in float iTime, inout vec3 col) {\n    col = vec3(0.8, 0.65, 0.55); \n    \n    if (p.y > 0.6) {\n        col = vec3(0.4, 0.15, 0.15); \n        if (p.y < 0.62) col *= 0.5; \n    }\n    \n    if (p.x < -0.6 && p.y > 0.2) {\n        col = vec3(0.8, 0.8, 0.8); \n        if (p.x + 0.6 > p.y - 0.2) {\n            col = vec3(0.3, 0.3, 0.3); \n        }\n    }\n    \n    if (p.x > 0.8) {\n        col = vec3(0.6, 0.6, 0.55); \n        col *= 0.8 + 0.2 * fract(sin(p.x*100.0)*40.0 + iTime);\n        if (abs(p.x - 0.9) < 0.02) col *= 0.6;\n    }\n}\n\nvoid layer_CenterWindow(in vec2 p, inout vec3 col) {\n    float dCircle = length(p - vec2(0.0, 0.0)) - 0.25;\n    if (dCircle < 0.0) {\n        col = vec3(0.3, 0.1, 0.1); \n        \n        if (dCircle < -0.05) {\n            col = vec3(0.85); \n            vec2 cp = p;\n            if (abs(cp.x) < 0.15 && abs(cp.y) < 0.15) {\n                if (abs(cp.y) < 0.02 || abs(cp.x) < 0.02 || abs(cp.x + cp.y) < 0.03 || abs(cp.x - cp.y) < 0.03) {\n                    \n                } else {\n                    col = vec3(0.15); \n                }\n            } else if (dCircle < -0.06) {\n                col = vec3(0.15);\n            }\n        }\n    }\n}\n\nvoid layer_BalconyRailing(in vec2 p, in float iTime, inout vec3 col) {\n    if (p.y > -0.4 && p.y < 0.2) {\n        vec3 railCol = vec3(0.6, 0.5, 0.4); \n        float isRail = 0.0;\n        \n        if (abs(p.y - 0.2) < 0.01) isRail = 1.0;\n        if (abs(p.y - (-0.4)) < 0.01) isRail = 1.0;\n        \n        if (abs(p.x - (-0.8)) < 0.01) isRail = 1.0;\n        if (abs(p.x - 0.8) < 0.01) isRail = 1.0;\n        if (abs(p.x - (-0.3)) < 0.01) isRail = 1.0;\n        if (abs(p.x - 0.3) < 0.01) isRail = 1.0;\n        \n        if (max(abs(p.x) - 0.3, abs(p.y + 0.1) - 0.3) > 0.0 && max(abs(p.x) - 0.3, abs(p.y + 0.1) - 0.3) < 0.01) isRail = 1.0;\n        \n        vec2 lp = p;\n        lp.x = abs(p.x); \n        if (lp.x > 0.3 && lp.x < 0.8) {\n            vec2 lpc = lp - vec2(0.55, -0.2);\n            float breath = sin(iTime * 2.0 + lp.x * 5.0) * 0.02;\n            if (length(lpc) < 0.15 + breath && lpc.y > 0.0) {\n                if (fract(length(lpc)*10.0 - iTime*2.0) < 0.2) {\n                    col = mix(col, vec3(0.3, 0.5, 0.4), 0.8);\n                    isRail = 0.5;\n                }\n            }\n            if (abs(lpc.x) < 0.01 && lpc.y < 0.0) isRail = 1.0; \n        }\n        \n        if (isRail > 0.9) col = railCol;\n    }\n}\n\nvoid layer_BottomRedSection(in vec2 p, inout vec3 col) {\n    if (p.y < -0.4) {\n        col = vec3(0.4, 0.15, 0.15); \n        if (p.y > -0.45) col *= 0.5; \n        \n        if (abs(p.x) < 0.15) col *= 0.8;\n    }\n}\n\nvoid layer_CenterPost(in vec2 p, inout vec3 col) {\n    if (p.y < -0.4 && p.y > -1.0) {\n        float dPost = max(abs(p.x) - 0.1, abs(p.y + 0.7) - 0.3);\n        if (dPost < 0.0) {\n            col = vec3(0.8, 0.8, 0.8); \n            if (abs(abs(p.x) - 0.1) < 0.01) col *= 0.5;\n            \n            if (p.y > -0.6) {\n                col = vec3(0.7, 0.6, 0.2); \n                if (p.y > -0.5) col = vec3(0.2, 0.4, 0.3); \n                if (p.y > -0.45) col = vec3(0.8, 0.7, 0.2); \n                \n                col *= 0.5 + 0.5 * cos(p.x * 20.0);\n            }\n            \n            if (length(p - vec2(0.0, -0.8)) < 0.05) col = vec3(0.2);\n        }\n    }\n}\n\nvoid layer_Vignette(in vec2 p, inout vec3 col) {\n    col *= 1.0 - 0.1 * length(p);\n}\n\nvec4 layer_BackgroundWall(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 col = vec3(-1.0);\n    \n    layer_BackgroundWall(p, iTime, col);\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_CenterWindow(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 col = vec3(-1.0);\n    \n    layer_CenterWindow(p, col);\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_BalconyRailing(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 col = vec3(-1.0);\n    \n    layer_BalconyRailing(p, iTime, col);\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_BottomRedSection(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 col = vec3(-1.0);\n    \n    layer_BottomRedSection(p, col);\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_CenterPost(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 col = vec3(-1.0);\n    \n    layer_CenterPost(p, col);\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_Vignette(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 col = vec3(-1.0);\n    \n    layer_Vignette(p, col);\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n","IMG_7686":"void layer_Background(in vec2 p, inout vec3 col) {\n    col = vec3(0.8, 0.65, 0.55); \n    if (p.y > 0.4) col = vec3(0.4, 0.15, 0.15);\n    if (p.y > 0.8) col = vec3(0.7);\n}\n\nvoid layer_LotusGrate(in vec2 p, in float iTime, inout vec3 col) {\n    if (p.x > 0.5 && p.y > 0.4) {\n        vec2 gp = p - vec2(0.8, 0.7);\n        float isGrate = 0.0;\n        float pulse = sin(iTime * 3.0 + length(gp)*10.0) * 0.02;\n        if (abs(length(gp - vec2(0.0, -0.2)) - 0.3 + pulse) < 0.02 && gp.y > 0.0) isGrate = 1.0;\n        if (abs(length(gp - vec2(-0.2, -0.1)) - 0.2 + pulse) < 0.02) isGrate = 1.0;\n        if (abs(length(gp - vec2(0.2, -0.1)) - 0.2 + pulse) < 0.02) isGrate = 1.0;\n        \n        if (isGrate > 0.0) col = vec3(0.2, 0.4, 0.3); \n    }\n}\n\nvoid layer_ForegroundRoof(in vec2 p, inout vec3 col, out float roofLine) {\n    roofLine = -0.5 + p.x * 0.4;\n    if (p.y < roofLine) {\n        col = vec3(0.6, 0.3, 0.2); \n        \n        float tx = p.x * 10.0 - p.y * 5.0; \n        if (fract(tx) < 0.2) col *= 0.5; \n        else {\n            float fx = fract(tx) - 0.5;\n            col *= 1.0 - fx*fx*2.0;\n        }\n        \n        if (p.y > roofLine - 0.05) col *= 0.8;\n    }\n}\n\nvoid layer_CentralOrnateFeature(in vec2 p, in float iTime, in float roofLine, inout vec3 col) {\n    float dBase = 1.0;\n    dBase = min(dBase, abs(length(p - vec2(-0.4, -0.6)) - 0.2) - 0.05);\n    dBase = min(dBase, abs(length(p - vec2(0.4, -0.3)) - 0.15) - 0.05);\n    dBase = min(dBase, max(abs(p.x - 0.1) - 0.2, abs(p.y - (-0.3)) - 0.15));\n    \n    if (p.y < roofLine - 0.1) dBase = 1.0;\n    \n    if (dBase < 0.0) {\n        col = vec3(0.9); \n        col *= 0.8 + 0.2 * sin(p.y * 20.0 - iTime * 2.0);\n        if (dBase > -0.02) col *= 0.6;\n    }\n    \n    vec2 fp = p - vec2(0.1, -0.1);\n    float dFinial = max(abs(fp.x) - 0.15, abs(fp.y) - 0.1); \n    dFinial = min(dFinial, length(fp - vec2(0.0, 0.15)) - 0.15); \n    dFinial = min(dFinial, length(fp - vec2(0.0, 0.35)) - 0.05); \n    \n    dFinial = min(dFinial, max(abs(fp.x) - 0.2, abs(fp.y - 0.15) - 0.05));\n    \n    if (dFinial < 0.0) {\n        col = vec3(0.2, 0.3, 0.2); \n        \n        if (fp.y < -0.05 || fp.y > 0.25 || abs(fp.x) > 0.12) {\n            col = vec3(0.8, 0.6, 0.2); \n            if (abs(fp.x) < 0.02) col = vec3(0.9, 0.8, 0.4); \n            if (abs(fp.y - (-0.08)) < 0.02) col *= 0.5; \n        } else {\n            col = vec3(0.2, 0.4, 0.3);\n            col *= 1.0 - length(fp - vec2(0.0, 0.15)) * 4.0;\n            if (length(fp - vec2(-0.05, 0.2)) < 0.03) col = vec3(0.6, 0.8, 0.7);\n        }\n        \n        if (fp.y < 0.0 && fp.y > -0.1 && abs(fp.x) < 0.1) {\n            float rotTime = iTime * 1.5;\n            if (abs(length(fp - vec2(0.0, -0.05)) - 0.04) < 0.01) col = vec3(0.8, 0.7, 0.2);\n            if (fract(atan(fp.x, fp.y + 0.05) * 1.27 + rotTime) < 0.1 && length(fp - vec2(0.0, -0.05)) < 0.04) col = vec3(0.8, 0.7, 0.2);\n        }\n    }\n}\n\nvoid layer_Vignette(in vec2 p, inout vec3 col) {\n    col *= 1.0 - 0.1 * length(p);\n}\n\nvec4 layer_Background(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 col = vec3(-1.0);\n    \n    layer_Background(p, col);\n    \n    float roofLine;\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_LotusGrate(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 col = vec3(-1.0);\n    \n    layer_LotusGrate(p, iTime, col);\n    \n    float roofLine;\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_ForegroundRoof(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 col = vec3(-1.0);\n    \n    \n    float roofLine;\n    layer_ForegroundRoof(p, col, roofLine);\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_CentralOrnateFeature(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 col = vec3(-1.0);\n    \n    \n    float roofLine;\n    layer_CentralOrnateFeature(p, iTime, roofLine, col);\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_Vignette(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 col = vec3(-1.0);\n    \n    \n    float roofLine;\n    layer_Vignette(p, col);\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n","IMG_7694":"void layer_Background(in vec2 p, in float iTime, inout vec3 col) {\n    col = vec3(0.5, 0.5, 0.5); \n    \n    vec2 gp = p * 8.0;\n    float pattern = abs(sin(length(fract(gp) - 0.5) * 10.0 - iTime * 2.0));\n    pattern = min(pattern, abs(sin((gp.x + gp.y)*5.0 + iTime)));\n    col *= 0.8 + 0.2 * pattern;\n    \n    if (p.x < -0.4) {\n        col = vec3(0.65, 0.6, 0.55); \n        col *= 0.8 + 0.2 * sin(p.y * 10.0);\n    }\n}\n\nvoid layer_GreenDoor(in vec2 p, inout vec3 col) {\n    if (p.x < -0.6) {\n        col = vec3(0.1, 0.3, 0.15); \n        if (abs(p.x - (-0.8)) < 0.15) {\n            float py = fract(p.y * 2.0) - 0.5;\n            if (abs(py) < 0.4) {\n                col = vec3(0.15, 0.35, 0.2);\n                if (abs(abs(py) - 0.4) < 0.02 || abs(abs(p.x - (-0.8)) - 0.15) < 0.02) {\n                    col = vec3(0.8);\n                }\n            }\n        }\n    }\n}\n\nvoid layer_RedTable(in vec2 p, inout vec3 col) {\n    if (p.x > 0.1 && p.y > -0.3 && p.y < 0.8) {\n        float dTable = max(abs(p.x - 0.6) - 0.4, abs(p.y - 0.25) - 0.45);\n        dTable = length(max(abs(vec2(p.x - 0.6, p.y - 0.25)) - vec2(0.3, 0.35), 0.0)) - 0.1;\n        \n        if (dTable < 0.0) {\n            col = vec3(0.7, 0.2, 0.2); \n            col += 0.2 * smoothstep(0.0, -0.2, dTable) * sin(p.y * 5.0 + p.x * 10.0);\n            \n            if (p.x > 0.8 && p.y > -0.5 && p.y < -0.3) col = vec3(0.6, 0.15, 0.15); \n            if (p.x > 0.8 && p.y > 0.8 && p.y < 0.9) col = vec3(0.6, 0.15, 0.15); \n        }\n    }\n}\n\nvoid layer_Chairs(in vec2 p, in float iTime, inout vec3 col) {\n    vec2 cPos[4];\n    cPos[0] = vec2(-0.1, 0.5); \n    cPos[1] = vec2(-0.2, 0.0); \n    cPos[2] = vec2(-0.25, -0.5); \n    cPos[3] = vec2(-0.5, -0.7); \n    \n    vec3 cCol[4];\n    cCol[0] = vec3(0.1, 0.3, 0.8); \n    cCol[1] = vec3(0.1, 0.3, 0.8); \n    cCol[2] = vec3(0.1, 0.3, 0.8); \n    cCol[3] = vec3(0.8, 0.2, 0.2); \n    \n    for (int i = 0; i < 4; i++) {\n        vec2 cp = p - cPos[i];\n        \n        float dBack = length(max(abs(cp - vec2(-0.1, 0.0)) - vec2(0.15, 0.15), 0.0)) - 0.05;\n        float dSeat = length(max(abs(cp - vec2(0.2, 0.0)) - vec2(0.1, 0.15), 0.0)) - 0.05;\n        \n        float dChair = min(dBack, dSeat);\n        float dConn = length(max(abs(cp - vec2(0.05, 0.0)) - vec2(0.05, 0.15), 0.0)) - 0.02;\n        dChair = min(dChair, dConn);\n        \n        if (dChair < 0.0) {\n            col = cCol[i];\n            \n            if (cp.x < 0.0 && cp.x > -0.2 && abs(cp.y) < 0.1) {\n                col = vec3(0.9);\n                float pulse = sin(iTime * 3.0 + float(i) * 1.0) * 0.005;\n                if (length(cp - vec2(-0.1, 0.0)) < 0.02 + pulse) col = vec3(0.1);\n                if (length(cp - vec2(-0.1, 0.05)) < 0.015 + pulse) col = vec3(0.1);\n                if (length(cp - vec2(-0.1, -0.05)) < 0.015 + pulse) col = vec3(0.1);\n                if (length(cp - vec2(-0.15, 0.0)) < 0.015 + pulse) col = vec3(0.1);\n            }\n            \n            col *= 0.7 + 0.3 * smoothstep(0.0, -0.05, dChair);\n            if (cp.x > 0.2) col *= 0.8; \n        }\n        \n        if (cp.x > 0.1 && cp.x < 0.4 && abs(cp.y + 0.25) < 0.02) col = cCol[i] * 0.7;\n        if (cp.x > 0.1 && cp.x < 0.4 && abs(cp.y - 0.25) < 0.02) col = cCol[i] * 0.7;\n    }\n}\n\nvoid layer_Vignette(in vec2 p, inout vec3 col) {\n    col *= 1.0 - 0.1 * length(p);\n}\n\nvec4 layer_Background(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 col = vec3(-1.0);\n    \n    layer_Background(p, iTime, col);\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_GreenDoor(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 col = vec3(-1.0);\n    \n    layer_GreenDoor(p, col);\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_RedTable(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 col = vec3(-1.0);\n    \n    layer_RedTable(p, col);\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_Chairs(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 col = vec3(-1.0);\n    \n    layer_Chairs(p, iTime, col);\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_Vignette(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 col = vec3(-1.0);\n    \n    layer_Vignette(p, col);\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n","IMG_9792":"void layer_SkyTrees(in vec2 p, in float iTime, inout vec3 col) {\n    if (p.y > 0.3) {\n        col = vec3(0.6, 0.7, 0.8); \n        \n        float treeNoise = fract(sin(p.x * 50.0 + p.y * 30.0 + iTime * 0.5) * 43758.5453);\n        float dTree = length(vec2(p.x, max(0.0, p.y - 0.6))) + treeNoise * 0.1;\n        if (dTree < 0.8 || p.y > 0.5 + sin(p.x * 10.0)*0.2) {\n            col = vec3(0.2, 0.35, 0.2); \n            col += vec3(0.1, 0.1, 0.0) * treeNoise;\n        }\n    }\n}\n\nvoid layer_WallRoof(in vec2 p, inout vec3 col) {\n    if (p.y > 0.1 && p.y <= 0.3) {\n        col = vec3(0.6, 0.55, 0.5); \n        \n        if (p.y > 0.2) {\n            col = vec3(0.3, 0.3, 0.32); \n            if (fract(p.x * 20.0 + p.y * 10.0) < 0.2) col *= 0.8;\n            if (fract(p.y * 15.0) < 0.1) col = vec3(0.2); \n        } else {\n            if (fract(p.x * 15.0) < 0.05 || fract(p.y * 20.0) < 0.1) col *= 0.8;\n        }\n    }\n}\n\nvoid layer_Ground(in vec2 p, in float iTime, inout vec3 col) {\n    if (p.y <= 0.1) {\n        col = vec3(0.55, 0.5, 0.45); \n        float dirtNoise = fract(sin(p.x * 100.0 + p.y * 100.0 + iTime * 0.5) * 43758.5453);\n        col *= 0.9 + 0.2 * dirtNoise;\n    }\n}\n\nvoid layer_FlowersLamppost(in vec2 p, inout vec3 col) {\n    if (p.x < -0.8 && p.y > -0.2 && p.y < 0.2) {\n        float f = length(fract(p * 15.0) - 0.5);\n        if (f < 0.3) col = vec3(0.8, 0.3, 0.6); \n    }\n    if (abs(p.x - (-0.9)) < 0.02 && p.y > -0.1) col = vec3(0.1); \n}\n\nvoid layer_RockCairn(in vec2 p, in float iTime, inout vec3 col) {\n    float cairnWidth = 0.8 - p.y * 0.6;\n    if (p.y > -0.6 && p.y < 0.8 && abs(p.x) < cairnWidth) {\n        vec3 rockColBase = vec3(0.5, 0.5, 0.5);\n        \n        vec2 rp = p * vec2(8.0, 10.0);\n        vec2 i_rp = floor(rp);\n        vec2 f_rp = fract(rp);\n        \n        float minDist = 1.0;\n        vec2 closestPoint = vec2(0.0);\n        \n        for (int y= -1; y <= 1; y++) {\n            for (int x= -1; x <= 1; x++) {\n                vec2 neighbor = vec2(float(x), float(y));\n                vec2 pt = fract(sin(vec2(dot(i_rp + neighbor, vec2(127.1, 311.7)), dot(i_rp + neighbor, vec2(269.5, 183.3)))) * 43758.5453);\n                pt = 0.5 + 0.5 * sin(iTime * 1.5 + 6.2831 * pt);\n                vec2 diff = neighbor + pt - f_rp;\n                float dist = length(diff);\n                \n                if (dist < minDist) {\n                    minDist = dist;\n                    closestPoint = pt;\n                }\n            }\n        }\n        \n        vec3 rockCol = mix(vec3(0.4, 0.4, 0.4), vec3(0.6, 0.55, 0.5), closestPoint.x);\n        rockCol = mix(rockCol, vec3(0.4, 0.45, 0.4), closestPoint.y);\n        \n        vec3 c = rockCol;\n        c *= 1.0 - minDist * 1.5; \n        \n        float edgeDist = abs(p.x) / cairnWidth;\n        c *= 1.0 - pow(edgeDist, 3.0) * 0.6;\n        \n        if (p.y < -0.4) c *= smoothstep(-0.6, -0.4, p.y);\n        col = c;\n    }\n}\n\nvoid layer_Vignette(in vec2 p, inout vec3 col) {\n    col *= 1.0 - 0.1 * length(p);\n}\n\nvec4 layer_SkyTrees(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 col = vec3(-1.0);\n    \n    layer_SkyTrees(p, iTime, col);\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_WallRoof(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 col = vec3(-1.0);\n    \n    layer_WallRoof(p, col);\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_Ground(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 col = vec3(-1.0);\n    \n    layer_Ground(p, iTime, col);\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_FlowersLamppost(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 col = vec3(-1.0);\n    \n    layer_FlowersLamppost(p, col);\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_RockCairn(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 col = vec3(-1.0);\n    \n    layer_RockCairn(p, iTime, col);\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_Vignette(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 col = vec3(-1.0);\n    \n    layer_Vignette(p, col);\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n","IMG_9812":"void layer_BackgroundWoodPanels(in vec2 p, in float iTime, inout vec3 col) {\n    col = vec3(0.45, 0.35, 0.25); \n    \n    float planks = fract(p.x * 2.0);\n    if (planks < 0.02) col *= 0.5; \n    \n    float grain = fract(sin(p.x * 50.0 + iTime * 0.1) * 43758.5453 + p.y * 2.0 - iTime * 0.5);\n    col *= 0.8 + 0.2 * grain;\n}\n\nvoid layer_LeftObjects(in vec2 p, inout vec3 col) {\n    if (p.x < -0.6 && p.y > 0.2) {\n        col = vec3(0.85, 0.85, 0.8); \n        if (p.x < -0.9 || p.y < 0.25) col = vec3(0.4, 0.3, 0.2); \n        if (p.x > -0.85 && p.x < -0.65 && p.y > 0.6 && p.y < 0.8) {\n            col = vec3(0.6); \n        }\n    }\n    \n    if (p.x < -0.4 && p.y < 0.2) {\n        col = vec3(0.4, 0.25, 0.15); \n        if (p.y > 0.15) col = vec3(0.3, 0.2, 0.1);\n        if (p.x > -0.45) col = vec3(0.3, 0.2, 0.1);\n        if (length(vec2(p.x + 0.5, p.y + 0.1)) < 0.02) col = vec3(0.8, 0.7, 0.5); \n        if (length(vec2(p.x + 0.5, p.y - 0.1)) < 0.02) col = vec3(0.8, 0.7, 0.5);\n    }\n}\n\nvoid layer_BottomSurface(in vec2 p, inout vec3 col) {\n    if (p.y < -0.6) {\n        col = vec3(0.2, 0.25, 0.25); \n        if (p.y > -0.65 && p.y < -0.6) {\n            col = vec3(0.15); \n            if (fract(p.x * 5.0) < 0.1) col = vec3(0.7, 0.7, 0.75); \n        }\n    }\n}\n\nvoid layer_CenterWoodWheelObject(in vec2 p, in float iTime, inout vec3 col) {\n    float dBase = max(abs(p.x) - 0.25, abs(p.y + 0.4) - 0.1);\n    if (dBase < 0.0) {\n        col = vec3(0.25, 0.15, 0.1); \n        col *= 0.8 + 0.2 * fract(sin(p.x * 20.0)*10.0 + iTime*0.5); \n        \n        if (p.y > -0.32) col = mix(col, vec3(0.5, 0.4, 0.3), 0.5);\n    }\n    \n    if (abs(p.x) < 0.02 && p.y > -0.3 && p.y < -0.15) {\n        col = vec3(0.5, 0.5, 0.5); \n    }\n    \n    float wheelDist = length(p - vec2(0.0, 0.1));\n    if (wheelDist < 0.35) {\n        float edgeNoise = sin(atan(p.y - 0.1, p.x) * 10.0) * 0.02 + sin(atan(p.y - 0.1, p.x) * 3.0) * 0.03;\n        if (wheelDist <= 0.35 + edgeNoise) {\n            if (wheelDist < 0.04) {\n                col = vec3(0.1); \n            } else {\n                col = vec3(0.4, 0.3, 0.2); \n                \n                float grain2 = fract(sin(p.y * 30.0 + iTime * 0.5) * 43758.5453 + p.x * 2.0 - iTime);\n                col *= 0.7 + 0.3 * grain2;\n                \n                if (fract(p.y * 12.0 + sin(p.x*5.0 - iTime)*0.1) < 0.05) col *= 0.4;\n                if (abs(p.y - 0.1) < 0.01 && abs(p.x) > 0.1) col *= 0.3; \n                \n                col *= 0.5 + 0.5 * smoothstep(0.35, 0.0, wheelDist); \n                col += vec3(0.1, 0.1, 0.0) * max(0.0, p.y - 0.1);\n            }\n        }\n    }\n}\n\nvoid layer_Vignette(in vec2 p, inout vec3 col) {\n    col *= 1.0 - 0.2 * length(p);\n}\n\nvec4 layer_BackgroundWoodPanels(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 col = vec3(-1.0);\n    \n    layer_BackgroundWoodPanels(p, iTime, col);\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_LeftObjects(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 col = vec3(-1.0);\n    \n    layer_LeftObjects(p, col);\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_BottomSurface(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 col = vec3(-1.0);\n    \n    layer_BottomSurface(p, col);\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_CenterWoodWheelObject(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 col = vec3(-1.0);\n    \n    layer_CenterWoodWheelObject(p, iTime, col);\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_Vignette(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 col = vec3(-1.0);\n    \n    layer_Vignette(p, col);\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n","IMG_9813":"void layer_Background(in vec2 p, in float iTime, inout vec3 col) {\n    col = vec3(0.65, 0.65, 0.68);\n    \n    float brush = fract(sin(p.x * 500.0 + iTime * 0.5) * 43758.5453);\n    brush += fract(sin(p.x * 300.0 + p.y * 10.0 + iTime * 0.5) * 43758.5453) * 0.5;\n    col *= 0.9 + 0.1 * brush;\n    \n    float lightPulse = sin(iTime * 1.0) * 0.1;\n    col += vec3(0.2) * smoothstep(0.5 + lightPulse, 1.0 + lightPulse, p.y);\n    col += vec3(0.3, 0.25, 0.1) * smoothstep(0.4, 0.0, abs(p.y - 0.2 - lightPulse*0.5));\n    col -= vec3(0.1, 0.0, 0.1) * smoothstep(0.5, 0.0, abs(p.y + 0.3 - lightPulse));\n    col += vec3(0.0, 0.15, 0.0) * smoothstep(0.4, 0.0, abs(p.y + 0.1 - lightPulse*0.8));\n}\n\nvoid layer_MainGraphics(in vec2 p, in float iTime, inout vec3 col) {\n    if (p.y > -0.1 && p.y < 0.6) {\n        float spacing = 0.6;\n        float x_idx = floor((p.x + spacing * 1.5) / spacing);\n        \n        if (x_idx >= 0.0 && x_idx < 3.0) {\n            vec2 char_p = p;\n            char_p.x = mod(p.x + spacing * 0.5, spacing) - spacing * 0.5;\n            char_p.y -= 0.25;\n            char_p.y += sin(iTime * 2.0 + x_idx * 1.5) * 0.02;\n            \n            float dChar = 1.0;\n            \n            float dHat = length(vec2(char_p.x, (char_p.y - 0.2) * 2.5)) - 0.12;\n            dChar = min(dChar, dHat);\n            \n            float dHeadTop = abs(length(vec2(char_p.x, char_p.y - 0.05)) - 0.2) - 0.04;\n            if (char_p.y > 0.05) dChar = min(dChar, dHeadTop);\n            \n            float dEyeL = length(vec2(char_p.x + 0.08, char_p.y)) - 0.04;\n            float dEyeR = length(vec2(char_p.x - 0.08, char_p.y)) - 0.04;\n            float dEyes = min(dEyeL, dEyeR);\n            dEyes = max(dEyes, -(length(vec2(char_p.x + 0.08, char_p.y + 0.01)) - 0.02));\n            dEyes = max(dEyes, -(length(vec2(char_p.x - 0.08, char_p.y + 0.01)) - 0.02));\n            dChar = min(dChar, dEyes);\n            \n            float dSwoop = abs(sin(char_p.x * 10.0) * 0.1 - (char_p.y + 0.15)) - 0.04;\n            if (char_p.y < 0.0 && char_p.x > -0.25 && char_p.x < 0.25) {\n                dChar = min(dChar, dSwoop);\n            }\n            \n            if (length(vec2(char_p.x, char_p.y + 0.1)) < 0.15 && char_p.y > -0.1) dChar = min(dChar, 0.0);\n            \n            if (char_p.y < -0.25) dChar = 1.0;\n            \n            if (dChar < 0.0) {\n                col = vec3(0.08); \n            }\n        }\n    }\n}\n\nvoid layer_TextOpeningHours(in vec2 p, inout vec3 col) {\n    if (abs(p.y + 0.3) < 0.05 && abs(p.x) < 0.4) {\n        float textPat = sin(p.x * 200.0);\n        if (textPat > 0.5 && abs(p.y + 0.28) < 0.015) col = vec3(0.1);\n        \n        float numPat = sin(p.x * 150.0);\n        if (numPat > 0.3 && abs(p.y + 0.35) < 0.025) {\n            if (abs(p.x) > 0.05) col = vec3(0.1); \n        }\n        if (abs(p.x) < 0.02 && abs(p.y + 0.35) < 0.01) col = vec3(0.1); \n    }\n}\n\nvoid layer_BottomTextLogos(in vec2 p, in float iTime, inout vec3 col) {\n    if (abs(p.y + 0.6) < 0.1 && abs(p.x) < 0.3) {\n        float textPat = sin(p.x * 250.0);\n        if (textPat > 0.5 && abs(p.y + 0.58) < 0.01) col = vec3(0.1);\n        \n        if (abs(p.y + 0.65) < 0.04) {\n            float spacing = 0.12;\n            float x_idx = floor((p.x + spacing * 1.5) / spacing);\n            if (x_idx >= 0.0 && x_idx < 3.0) {\n                vec2 lp = p;\n                lp.x = mod(p.x + spacing * 0.5, spacing) - spacing * 0.5;\n                lp.y += 0.65;\n                lp.y += sin(iTime * 3.0 + x_idx * 2.0) * 0.005;\n                \n                float dLogo = length(max(abs(lp) - vec2(0.02, 0.01), 0.0)) - 0.02;\n                if (dLogo < 0.0) col = vec3(0.1);\n                \n                if (length(lp) < 0.015) col = vec3(0.65); \n            }\n        }\n    }\n}\n\nvoid layer_Vignette(in vec2 p, inout vec3 col) {\n    col *= 1.0 - 0.2 * length(p);\n}\n\nvec4 layer_Background(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 col = vec3(-1.0);\n    \n    layer_Background(p, iTime, col);\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_MainGraphics(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 col = vec3(-1.0);\n    \n    layer_MainGraphics(p, iTime, col);\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_TextOpeningHours(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 col = vec3(-1.0);\n    \n    layer_TextOpeningHours(p, col);\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_BottomTextLogos(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 col = vec3(-1.0);\n    \n    layer_BottomTextLogos(p, iTime, col);\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_Vignette(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 col = vec3(-1.0);\n    \n    layer_Vignette(p, col);\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n","kite_garment_sketch":"float hash(vec2 p) { return fract(sin(dot(p, vec2(12.9898, 78.233))) * 43758.5453); }\n\nfloat sketchLine(vec2 p, vec2 a, vec2 b, float thickness, float iTime) {\n    vec2 pa = p - a, ba = b - a;\n    float h = clamp( dot(pa,ba)/dot(ba,ba), 0.0, 1.0 );\n    float d = length( pa - ba*h );\n    float jitter = (hash(vec2(h * 100.0, iTime*0.0)) - 0.5) * 0.02;\n    return smoothstep(thickness + abs(jitter), thickness*0.2, d);\n}\n\nvoid layer_Background(in vec2 p, inout vec3 col) {\n    col = vec3(0.96, 0.96, 0.93);\n    float grain = hash(p * 500.0);\n    col -= 0.05 * grain; \n}\n\nvoid layer_PersonHead(in vec2 p, float t, inout float lineMap) {\n    vec2 headPos = vec2(p.x, (p.y - 0.7)*1.4);\n    float headDist = abs(length(headPos) - 0.15);\n    lineMap += smoothstep(t+0.01, t*0.5, headDist + (hash(p*50.0)-0.5)*0.02);\n}\n\nvoid layer_ShouldersNeck(in vec2 p, float t, float iTime, inout float lineMap) {\n    lineMap += sketchLine(p, vec2(-0.15, 0.55), vec2(0.15, 0.55), t, iTime);\n    lineMap += sketchLine(p, vec2(-0.05, 0.55), vec2(-0.05, 0.7), t, iTime);\n    lineMap += sketchLine(p, vec2(0.05, 0.55), vec2(0.05, 0.7), t, iTime);\n}\n\nvoid layer_CentralGarmentSpine(in vec2 p, float t, float iTime, inout float lineMap) {\n    lineMap += sketchLine(p, vec2(0.0, 0.55), vec2(0.0, -0.3), t * 1.5, iTime);\n}\n\nvoid layer_KiteWingTopEdge(in vec2 p, float t, float iTime, inout float lineMap) {\n    lineMap += sketchLine(p, vec2(0.0, 0.3), vec2(-0.8, -0.1), t, iTime);\n    lineMap += sketchLine(p, vec2(0.0, 0.3), vec2(0.8, -0.1), t, iTime);\n}\n\nvoid layer_InternalArchYoke(in vec2 p, float t, inout float lineMap) {\n    float archY = p.y - (0.2 - p.x*p.x*0.4);\n    if (abs(p.x) < 0.65 && p.y > 0.0 && p.y < 0.35) {\n        lineMap += smoothstep(t+0.01, t*0.5, abs(archY) + (hash(p*50.0)-0.5)*0.02);\n    }\n}\n\nvoid layer_KiteWingBottomEdge(in vec2 p, float t, float iTime, inout float lineMap) {\n    lineMap += sketchLine(p, vec2(-0.8, -0.1), vec2(0.0, -0.3), t, iTime);\n    lineMap += sketchLine(p, vec2(0.8, -0.1), vec2(0.0, -0.3), t, iTime);\n}\n\nvoid layer_Sleeves(in vec2 p, float t, float iTime, inout float lineMap) {\n    lineMap += sketchLine(p, vec2(-0.8, -0.1), vec2(-1.0, -0.3), t, iTime);\n    lineMap += sketchLine(p, vec2(0.8, -0.1), vec2(1.0, -0.3), t, iTime);\n    lineMap += sketchLine(p, vec2(-0.6, -0.3), vec2(-0.8, -0.5), t, iTime);\n    lineMap += sketchLine(p, vec2(0.6, -0.3), vec2(0.8, -0.5), t, iTime);\n}\n\nvoid layer_Legs(in vec2 p, float t, float iTime, inout float lineMap) {\n    lineMap += sketchLine(p, vec2(-0.15, -0.3), vec2(-0.25, -1.0), t, iTime);\n    lineMap += sketchLine(p, vec2(0.15, -0.3), vec2(0.25, -1.0), t, iTime);\n    lineMap += sketchLine(p, vec2(-0.02, -0.4), vec2(-0.05, -1.0), t, iTime);\n    lineMap += sketchLine(p, vec2(0.02, -0.4), vec2(0.05, -1.0), t, iTime);\n}\n\nvoid layer_ForbesText(in vec2 p, float t, inout float lineMap) {\n    if (abs(p.x) < 0.3 && abs(p.y - 0.05) < 0.1) {\n        float squiggle = sin(p.x * 40.0) * 0.04 + (p.y - 0.05);\n        lineMap += smoothstep(t*1.5, t*0.5, abs(squiggle));\n    }\n}\n\nvec4 layer_Background(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 col = vec3(-1.0);\n    layer_Background(p, col);\n\n    float lineMap = 0.0;\n    float t = 0.008; \n\n\n    col = mix(col, vec3(0.1, 0.1, 0.15), clamp(lineMap, 0.0, 1.0));\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_PersonHead(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 col = vec3(-1.0);\n\n    float lineMap = 0.0;\n    float t = 0.008; \n\n    layer_PersonHead(p, t, lineMap);\n\n    col = mix(col, vec3(0.1, 0.1, 0.15), clamp(lineMap, 0.0, 1.0));\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_ShouldersNeck(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 col = vec3(-1.0);\n\n    float lineMap = 0.0;\n    float t = 0.008; \n\n    layer_ShouldersNeck(p, t, iTime, lineMap);\n\n    col = mix(col, vec3(0.1, 0.1, 0.15), clamp(lineMap, 0.0, 1.0));\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_CentralGarmentSpine(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 col = vec3(-1.0);\n\n    float lineMap = 0.0;\n    float t = 0.008; \n\n    layer_CentralGarmentSpine(p, t, iTime, lineMap);\n\n    col = mix(col, vec3(0.1, 0.1, 0.15), clamp(lineMap, 0.0, 1.0));\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_KiteWingTopEdge(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 col = vec3(-1.0);\n\n    float lineMap = 0.0;\n    float t = 0.008; \n\n    layer_KiteWingTopEdge(p, t, iTime, lineMap);\n\n    col = mix(col, vec3(0.1, 0.1, 0.15), clamp(lineMap, 0.0, 1.0));\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_InternalArchYoke(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 col = vec3(-1.0);\n\n    float lineMap = 0.0;\n    float t = 0.008; \n\n    layer_InternalArchYoke(p, t, lineMap);\n\n    col = mix(col, vec3(0.1, 0.1, 0.15), clamp(lineMap, 0.0, 1.0));\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_KiteWingBottomEdge(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 col = vec3(-1.0);\n\n    float lineMap = 0.0;\n    float t = 0.008; \n\n    layer_KiteWingBottomEdge(p, t, iTime, lineMap);\n\n    col = mix(col, vec3(0.1, 0.1, 0.15), clamp(lineMap, 0.0, 1.0));\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_Sleeves(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 col = vec3(-1.0);\n\n    float lineMap = 0.0;\n    float t = 0.008; \n\n    layer_Sleeves(p, t, iTime, lineMap);\n\n    col = mix(col, vec3(0.1, 0.1, 0.15), clamp(lineMap, 0.0, 1.0));\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_Legs(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 col = vec3(-1.0);\n\n    float lineMap = 0.0;\n    float t = 0.008; \n\n    layer_Legs(p, t, iTime, lineMap);\n\n    col = mix(col, vec3(0.1, 0.1, 0.15), clamp(lineMap, 0.0, 1.0));\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_ForbesText(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 col = vec3(-1.0);\n\n    float lineMap = 0.0;\n    float t = 0.008; \n\n    layer_ForbesText(p, t, lineMap);\n\n    col = mix(col, vec3(0.1, 0.1, 0.15), clamp(lineMap, 0.0, 1.0));\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n","laser_engraver":"void layer_OpticalBreadboard(in vec2 p, in vec2 uv, inout vec3 col) {\n    vec2 st = vec2(p.x / (abs(p.y - 0.8) + 0.3), 1.0 / (abs(p.y - 0.8) + 0.3));\n    st *= 4.0; \n    \n    float ang = 0.15;\n    st = mat2(cos(ang), -sin(ang), sin(ang), cos(ang)) * st;\n\n    vec2 id = floor(st);\n    vec2 f = fract(st);\n\n    col = vec3(0.65, 0.68, 0.72); \n    \n    float brush = fract(sin(dot(uv * vec2(0.5, 200.0), vec2(12.98, 78.23))) * 43758.5);\n    col *= 0.9 + 0.1 * brush;\n\n    float dHole = length(f - 0.5);\n    \n    if (dHole < 0.18) {\n        if (dHole < 0.06) {\n            col = vec3(0.1); \n        } else {\n            float thread = sin(dHole * 150.0);\n            col = vec3(0.5) * (0.8 + 0.2 * thread);\n        }\n        col *= 0.7 + 0.6 * smoothstep(-0.2, 0.2, (f.x - 0.5) + (f.y - 0.5));\n    }\n}\n\nvoid layer_LaserDot(in vec2 p, in float iTime, inout vec3 col) {\n    float t = iTime * 2.0;\n    vec2 laserPos = vec2(sin(t)*0.4, -0.2 + cos(t*0.5)*0.3);\n    float dLaser = length(p - laserPos);\n    \n    vec3 laserCol = vec3(1.0, 0.2, 0.1);\n    float glow = exp(-dLaser * 15.0);\n    float intense = exp(-dLaser * 80.0);\n    \n    col += laserCol * glow * 1.8;\n    col += vec3(1.0) * intense * 2.5;\n}\n\nvoid layer_Vignette(in vec2 p, inout vec3 col) {\n    col *= smoothstep(1.5, 0.2, length(vec2(p.x, p.y + 0.5)));\n}\n\nvec4 layer_OpticalBreadboard(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 col = vec3(-1.0);\n\n    layer_OpticalBreadboard(p, uv, col);\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_LaserDot(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 col = vec3(-1.0);\n\n    layer_LaserDot(p, iTime, col);\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_Vignette(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 col = vec3(-1.0);\n\n    layer_Vignette(p, col);\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n","lottery_ticket":"float smin(float a, float b, float k) { \n    float h = clamp(0.5+0.5*(b-a)/k, 0.0, 1.0); \n    return mix(b, a, h) - k*h*(1.0-h); \n}\n\nvoid layer_Background(in vec2 p, inout vec3 col) {\n    vec3 pinkCol = mix(vec3(1.0, 0.6, 0.7), vec3(1.0, 0.8, 0.9), p.y);\n    pinkCol += 0.05 * sin(p.x * 200.0) * cos(p.y * 200.0);\n    col = pinkCol;\n}\n\nvoid layer_BlueEdges(in vec2 p, inout vec3 col, out bool isEdge) {\n    isEdge = false;\n    if (p.x < 0.1 || p.x > 0.9) {\n        isEdge = true;\n        vec3 edgeCol = vec3(0.1, 0.4, 0.8);\n        float dots = step(0.8, sin(p.y * 200.0)) * step(0.8, sin(p.x * 400.0));\n        col = mix(edgeCol, vec3(1.0, 0.9, 0.2), dots);\n    }\n}\n\nvoid layer_RedNumbers(in vec2 p, inout vec3 col) {\n    if (p.x > 0.15 && p.x < 0.35) {\n        float numStack = fract(p.y * 6.0);\n        float block = step(0.2, numStack) * step(numStack, 0.8) * step(0.2, p.x) * step(p.x, 0.3);\n        float hole = step(0.4, numStack) * step(numStack, 0.6) * step(0.22, p.x) * step(p.x, 0.28);\n        col = mix(col, vec3(0.9, 0.1, 0.1), clamp(block - hole, 0.0, 1.0));\n    }\n}\n\nvoid layer_DeityFigure(in vec2 p, inout vec3 col) {\n    vec2 center = p - vec2(0.6, 0.4);\n    float r = length(center);\n    float a = atan(center.y, center.x);\n    \n    float halo = smoothstep(0.3, 0.2, r + 0.05 * sin(a * 15.0));\n    vec3 gold = vec3(0.9, 0.8, 0.1);\n    col = mix(col, gold, halo * 0.5);\n    \n    float robes = smoothstep(0.2, 0.18, length(center + vec2(0.0, 0.1)) + 0.05 * sin(a * 5.0));\n    col = mix(col, vec3(0.8, 0.1, 0.1), robes);\n    \n    float details = smoothstep(0.1, 0.08, length(center - vec2(0.1, -0.1))) + \n                    smoothstep(0.08, 0.06, length(center - vec2(-0.15, 0.05)));\n    col = mix(col, gold, clamp(details, 0.0, 1.0));\n}\n\nvoid layer_TextOverlay(in vec2 p, inout vec3 col) {\n    if (p.x > 0.75 && p.x < 0.85) {\n        float textBar = step(0.2, p.y) * step(p.y, 0.8);\n        float letters = step(0.5, sin(p.y * 150.0));\n        col = mix(col, vec3(0.85, 0.1, 0.2), textBar * letters);\n    }\n}\n\nvoid layer_TopSection(in vec2 p, inout vec3 col) {\n    if (p.y > 0.8) {\n        col = mix(col, vec3(0.95, 0.9, 0.8), 0.5);\n        float logo = smoothstep(0.1, 0.08, length(p - vec2(0.5, 0.9)));\n        float innerLogo = smoothstep(0.08, 0.06, length(p - vec2(0.5, 0.9)));\n        col = mix(col, vec3(0.8, 0.1, 0.1), logo - innerLogo);\n    }\n}\n\nvec4 layer_Background(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv;\n    \n    vec3 col = vec3(-1.0);\n    bool isEdge = false;\n    \n    layer_Background(p, col);\n    \n    if (!isEdge) {\n    }\n    \n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_BlueEdges(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv;\n    \n    vec3 col = vec3(-1.0);\n    bool isEdge = false;\n    \n    layer_BlueEdges(p, col, isEdge);\n    \n    if (!isEdge) {\n    }\n    \n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_RedNumbers(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv;\n    \n    vec3 col = vec3(-1.0);\n    bool isEdge = false;\n    \n    \n    if (!isEdge) {\n        layer_RedNumbers(p, col);\n    }\n    \n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_DeityFigure(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv;\n    \n    vec3 col = vec3(-1.0);\n    bool isEdge = false;\n    \n    \n    if (!isEdge) {\n        layer_DeityFigure(p, col);\n    }\n    \n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_TextOverlay(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv;\n    \n    vec3 col = vec3(-1.0);\n    bool isEdge = false;\n    \n    \n    if (!isEdge) {\n        layer_TextOverlay(p, col);\n    }\n    \n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_TopSection(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv;\n    \n    vec3 col = vec3(-1.0);\n    bool isEdge = false;\n    \n    \n    if (!isEdge) {\n        layer_TopSection(p, col);\n    }\n    \n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n","metal_gate":"void layer_BackgroundTiles(in vec2 uv, in float gateWidth, in vec2 p, inout vec3 col) {\n    if (abs(p.x) > gateWidth) {\n        vec2 tileUV = uv * vec2(8.0, 12.0);\n        vec2 cell = fract(tileUV);\n        vec2 id = floor(tileUV);\n        \n        float edge = smoothstep(0.0, 0.05, cell.x) * smoothstep(1.0, 0.95, cell.x) *\n                     smoothstep(0.0, 0.05, cell.y) * smoothstep(1.0, 0.95, cell.y);\n        \n        float noise = fract(sin(dot(id, vec2(12.9898, 78.233))) * 43758.5453);\n        vec3 baseCol = mix(vec3(0.7, 0.35, 0.2), vec3(0.85, 0.5, 0.3), noise);\n        \n        col = mix(vec3(0.1), baseCol, edge);\n    }\n}\n\nvoid layer_GateBackground(in float gateWidth, in vec2 p, inout vec3 col) {\n    if (abs(p.x) <= gateWidth) {\n        col = vec3(0.05, 0.05, 0.06);\n    }\n}\n\nvoid layer_MetalGateStructure(in vec2 p, in float gateWidth, inout float metal, inout vec3 col) {\n    if (abs(p.x) <= gateWidth) {\n        float div0 = step(abs(p.x), 0.02);\n        float divL1 = step(abs(p.x + 0.6), 0.02);\n        float divR1 = step(abs(p.x - 0.6), 0.02);\n        float divL2 = step(abs(p.x + 1.0), 0.04);\n        float divR2 = step(abs(p.x - 1.0), 0.04);\n        \n        metal = max(metal, max(max(div0, divL1), max(divR1, max(divL2, divR2))));\n        \n        if (abs(p.x) < 0.6) {\n            float hBars = step(abs(fract(p.y * 15.0) - 0.5), 0.2);\n            float vBars = step(abs(fract(p.x * 6.0) - 0.5), 0.1);\n            \n            metal = max(metal, hBars);\n            if (vBars > 0.5 && hBars < 0.5) {\n                metal = max(metal, 0.8);\n            }\n        }\n        \n        if (abs(p.x) >= 0.6 && abs(p.x) < 1.0) {\n            vec2 panelP = vec2(abs(p.x) - 0.8, p.y);\n            \n            float mesh = step(0.8, sin(panelP.x * 250.0) * sin(panelP.y * 250.0));\n            if (mesh > 0.5 && metal < 0.5) col = mix(col, vec3(0.3), 0.5);\n            \n            vec2 cell = fract(panelP * vec2(1.0, 2.5)) - 0.5;\n            \n            float arc1 = step(abs(length(cell - vec2(-0.5, 0.0)) - 0.5), 0.03);\n            float arc2 = step(abs(length(cell - vec2(0.5, 0.0)) - 0.5), 0.03);\n            \n            metal = max(metal, max(arc1, arc2));\n            \n            float panelHBar = step(abs(fract(panelP.y * 2.5) - 0.5), 0.02);\n            metal = max(metal, panelHBar);\n        }\n    }\n}\n\nvoid layer_GateMaterial(in vec2 p, in float metal, inout vec3 col) {\n    if (metal > 0.0) {\n        vec3 metalCol = vec3(0.5, 0.55, 0.6);\n        metalCol *= 0.6 + 0.4 * sin(p.x * 20.0 + p.y * 20.0);\n        col = metal == 0.8 ? metalCol * 0.6 : metalCol;\n    }\n}\n\nvec4 layer_BackgroundTiles(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n    \n    vec3 col = vec3(-1.0);\n    float gateWidth = 1.0;\n    float metal = 0.0;\n    \n    layer_BackgroundTiles(uv, gateWidth, p, col);\n    \n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_GateBackground(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n    \n    vec3 col = vec3(-1.0);\n    float gateWidth = 1.0;\n    float metal = 0.0;\n    \n    layer_GateBackground(gateWidth, p, col);\n    \n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_MetalGateStructure(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n    \n    vec3 col = vec3(-1.0);\n    float gateWidth = 1.0;\n    float metal = 0.0;\n    \n    layer_MetalGateStructure(p, gateWidth, metal, col);\n    \n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_GateMaterial(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n    \n    vec3 col = vec3(-1.0);\n    float gateWidth = 1.0;\n    float metal = 0.0;\n    \n    layer_GateMaterial(p, metal, col);\n    \n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n","mother_of_pearl":"float hash(vec2 p) { return fract(sin(dot(p, vec2(12.9898, 78.233))) * 43758.5453); }\nvec2 hash2(vec2 p) { return fract(sin(vec2(dot(p,vec2(127.1,311.7)),dot(p,vec2(269.5,183.3))))*43758.5453); }\n\nfloat voronoi(vec2 x) {\n    vec2 n = floor(x); vec2 f = fract(x); float m = 8.0;\n    for(int j=-1; j<=1; j++)\n    for(int i=-1; i<=1; i++) {\n        vec2 g = vec2(float(i),float(j));\n        vec2 o = hash2(n + g);\n        vec2 r = g - f + o;\n        m = min(m, dot(r,r));\n    }\n    return sqrt(m);\n}\n\nfloat sdCircle(vec2 p, float r) { return length(p) - r; }\nfloat sdEllipse(vec2 p, vec2 r) {\n    float k0 = length(p/r); float k1 = length(p/(r*r));\n    return k0*(k0-1.0)/k1;\n}\nfloat sdBox(vec2 p, vec2 b) {\n    vec2 d = abs(p) - b; return length(max(d, 0.0)) + min(max(d.x, d.y), 0.0);\n}\nfloat sdFanTop(vec2 p, float flip) {\n    vec2 cp = p - vec2(flip*0.6, 0.6);\n    float d = length(cp) - 0.25;\n    float angle = atan(cp.y, cp.x);\n    d -= 0.03 * sin(angle * 14.0);\n    return max(d, -(cp.y + 0.1));\n}\n\nvec4 layer(vec4 bg, float d, vec3 col) {\n    float a = smoothstep(0.005, 0.0, d);\n    float outline = smoothstep(0.02, 0.005, abs(d));\n    vec3 finalCol = mix(col, vec3(0.0), outline);\n    return mix(bg, vec4(finalCol, 1.0), a);\n}\n\nvoid layer_Background(in vec2 uv, inout vec4 col) {\n    vec3 bgCol = vec3(0.12, 0.5, 0.7);\n    float n1 = hash(uv*15.0);\n    float n2 = hash(uv*25.0);\n    if (n1 > 0.94 && n2 > 0.4) {\n        bgCol = mix(bgCol, vec3(0.85, 0.9, 0.85), smoothstep(0.94, 0.95, n1));\n    }\n    col = vec4(bgCol, 1.0);\n}\n\nvoid layer_Halo(in vec2 p, in vec3 cYel, inout vec4 col) {\n    col = layer(col, sdCircle(p - vec2(0.0, 0.3), 0.5), cYel);\n}\n\nvoid layer_SideFigures(in vec2 p, in vec3 shellWht, in vec3 cRed, in vec3 cSkin, inout vec4 col) {\n    float dSideBody = min(sdEllipse(p - vec2(-0.7, -0.1), vec2(0.2, 0.5)), sdEllipse(p - vec2(0.7, -0.1), vec2(0.2, 0.5)));\n    col = layer(col, max(dSideBody, -(p.y+0.6)), shellWht);\n    \n    float dFanTop = min(sdFanTop(p, 1.0), sdFanTop(p, -1.0));\n    col = layer(col, dFanTop, shellWht);\n    float dFanStem = min(sdBox(p - vec2(-0.6, 0.1), vec2(0.02, 0.4)), sdBox(p - vec2(0.6, 0.1), vec2(0.02, 0.4)));\n    col = layer(col, dFanStem, cRed);\n    float dFanBulb = min(sdCircle(p - vec2(-0.6, 0.4), 0.08), sdCircle(p - vec2(0.6, 0.4), 0.08));\n    col = layer(col, dFanBulb, cRed);\n    \n    float dSideFace = min(sdCircle(p - vec2(-0.7, 0.25), 0.12), sdCircle(p - vec2(0.7, 0.25), 0.12));\n    col = layer(col, dSideFace, cSkin);\n    \n    float dSideHair = min(sdCircle(p - vec2(-0.7, 0.3), 0.13), sdCircle(p - vec2(0.7, 0.3), 0.13));\n    dSideHair = max(dSideHair, p.y - 0.32);\n    col = layer(col, dSideHair, vec3(0.1));\n    \n    float dSideBun = min(sdCircle(p - vec2(-0.7, 0.45), 0.06), sdCircle(p - vec2(0.7, 0.45), 0.06));\n    col = layer(col, dSideBun, vec3(0.1));\n}\n\nvoid layer_MainFigure(in vec2 p, in vec3 shellRed, in vec3 shellGrn, in vec3 shellWht, in vec3 cRed, in vec3 cYel, in vec3 cSkin, inout vec4 col) {\n    col = layer(col, sdBox(p - vec2(0.0, -0.8), vec2(0.45, 0.05)), cRed);\n    col = layer(col, sdBox(p - vec2(0.0, -0.7), vec2(0.5, 0.05)), cYel);\n    \n    float dBody = sdEllipse(p - vec2(0.0, -0.2), vec2(0.45, 0.5));\n    dBody = max(dBody, -(p.y + 0.65));\n    col = layer(col, dBody, shellRed);\n    \n    float dSleeves = min(sdEllipse(p - vec2(-0.4, -0.2), vec2(0.15, 0.4)), sdEllipse(p - vec2(0.4, -0.2), vec2(0.15, 0.4)));\n    col = layer(col, max(dSleeves, -(p.y + 0.6)), shellGrn);\n    \n    col = layer(col, sdEllipse(p - vec2(0.0, -0.15), vec2(0.12, 0.2)), shellWht);\n    col = layer(col, sdCircle(p - vec2(0.0, 0.0), 0.05), cYel);\n    \n    col = layer(col, sdEllipse(p - vec2(0.0, 0.15), vec2(0.3, 0.15)), shellGrn);\n    col = layer(col, sdEllipse(p - vec2(0.0, 0.2), vec2(0.2, 0.1)), cRed);\n    \n    col = layer(col, sdCircle(p - vec2(0.0, 0.4), 0.16), cSkin);\n    \n    float dHeadDress = sdCircle(p - vec2(0.0, 0.58), 0.2);\n    dHeadDress = max(dHeadDress, -(p.y - 0.5));\n    col = layer(col, dHeadDress, shellGrn);\n    col = layer(col, sdCircle(p - vec2(0.0, 0.7), 0.05), cRed);\n    col = layer(col, sdCircle(p - vec2(-0.15, 0.65), 0.04), cYel);\n    col = layer(col, sdCircle(p - vec2(0.15, 0.65), 0.04), cYel);\n}\n\nvoid layer_FacialFeatures(in vec2 p, inout vec4 col) {\n    float eyes = min(sdCircle(p - vec2(-0.06, 0.42), 0.015), sdCircle(p - vec2(0.06, 0.42), 0.015));\n    col.rgb = mix(col.rgb, vec3(0.1), smoothstep(0.01, 0.0, eyes));\n    float mouth = sdEllipse(p - vec2(0.0, 0.32), vec2(0.03, 0.015));\n    col.rgb = mix(col.rgb, vec3(0.8, 0.2, 0.2), smoothstep(0.01, 0.0, mouth));\n    \n    float eyesSide = min(\n        min(sdCircle(p - vec2(-0.73, 0.27), 0.01), sdCircle(p - vec2(-0.67, 0.27), 0.01)),\n        min(sdCircle(p - vec2(0.67, 0.27), 0.01), sdCircle(p - vec2(0.73, 0.27), 0.01))\n    );\n    col.rgb = mix(col.rgb, vec3(0.1), smoothstep(0.01, 0.0, eyesSide));\n    float mouthSide = min(sdEllipse(p - vec2(-0.7, 0.18), vec2(0.02, 0.01)), sdEllipse(p - vec2(0.7, 0.18), vec2(0.02, 0.01)));\n    col.rgb = mix(col.rgb, vec3(0.8, 0.2, 0.2), smoothstep(0.01, 0.0, mouthSide));\n}\n\nvec4 layer_Background(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n    p *= 1.1;\n    \n    vec4 col = vec4(-1.0);\n    layer_Background(uv, col);\n    \n    vec3 cYel = vec3(0.85, 0.85, 0.2);\n    vec3 cRed = vec3(0.8, 0.15, 0.15);\n    vec3 cGrn = vec3(0.2, 0.6, 0.4);\n    vec3 cWht = vec3(0.9, 0.9, 0.85);\n    vec3 cSkin = vec3(0.95, 0.8, 0.7);\n    \n    float v = voronoi(p * 20.0);\n    vec3 irid = 0.5 + 0.5 * cos(v * 10.0 + p.xyx * 3.0 + vec3(0, 2, 4));\n    \n    vec3 shellWht = mix(cWht, irid, 0.5) * (0.8 + 0.3 * v);\n    vec3 shellRed = mix(cRed, irid*cRed*2.0, 0.4) * (0.8 + 0.3 * v);\n    vec3 shellGrn = mix(cGrn, irid*cGrn*2.0, 0.4) * (0.8 + 0.3 * v);\n    \n    float pattern = smoothstep(0.05, 0.0, abs(fract(v*2.5) - 0.5));\n    shellRed = mix(shellRed, vec3(0.9, 0.8, 0.4), pattern * 0.4);\n\n    \n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_Halo(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n    p *= 1.1;\n    \n    vec4 col = vec4(-1.0);\n    \n    vec3 cYel = vec3(0.85, 0.85, 0.2);\n    vec3 cRed = vec3(0.8, 0.15, 0.15);\n    vec3 cGrn = vec3(0.2, 0.6, 0.4);\n    vec3 cWht = vec3(0.9, 0.9, 0.85);\n    vec3 cSkin = vec3(0.95, 0.8, 0.7);\n    \n    float v = voronoi(p * 20.0);\n    vec3 irid = 0.5 + 0.5 * cos(v * 10.0 + p.xyx * 3.0 + vec3(0, 2, 4));\n    \n    vec3 shellWht = mix(cWht, irid, 0.5) * (0.8 + 0.3 * v);\n    vec3 shellRed = mix(cRed, irid*cRed*2.0, 0.4) * (0.8 + 0.3 * v);\n    vec3 shellGrn = mix(cGrn, irid*cGrn*2.0, 0.4) * (0.8 + 0.3 * v);\n    \n    float pattern = smoothstep(0.05, 0.0, abs(fract(v*2.5) - 0.5));\n    shellRed = mix(shellRed, vec3(0.9, 0.8, 0.4), pattern * 0.4);\n\n    layer_Halo(p, cYel, col);\n    \n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_SideFigures(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n    p *= 1.1;\n    \n    vec4 col = vec4(-1.0);\n    \n    vec3 cYel = vec3(0.85, 0.85, 0.2);\n    vec3 cRed = vec3(0.8, 0.15, 0.15);\n    vec3 cGrn = vec3(0.2, 0.6, 0.4);\n    vec3 cWht = vec3(0.9, 0.9, 0.85);\n    vec3 cSkin = vec3(0.95, 0.8, 0.7);\n    \n    float v = voronoi(p * 20.0);\n    vec3 irid = 0.5 + 0.5 * cos(v * 10.0 + p.xyx * 3.0 + vec3(0, 2, 4));\n    \n    vec3 shellWht = mix(cWht, irid, 0.5) * (0.8 + 0.3 * v);\n    vec3 shellRed = mix(cRed, irid*cRed*2.0, 0.4) * (0.8 + 0.3 * v);\n    vec3 shellGrn = mix(cGrn, irid*cGrn*2.0, 0.4) * (0.8 + 0.3 * v);\n    \n    float pattern = smoothstep(0.05, 0.0, abs(fract(v*2.5) - 0.5));\n    shellRed = mix(shellRed, vec3(0.9, 0.8, 0.4), pattern * 0.4);\n\n    layer_SideFigures(p, shellWht, cRed, cSkin, col);\n    \n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_MainFigure(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n    p *= 1.1;\n    \n    vec4 col = vec4(-1.0);\n    \n    vec3 cYel = vec3(0.85, 0.85, 0.2);\n    vec3 cRed = vec3(0.8, 0.15, 0.15);\n    vec3 cGrn = vec3(0.2, 0.6, 0.4);\n    vec3 cWht = vec3(0.9, 0.9, 0.85);\n    vec3 cSkin = vec3(0.95, 0.8, 0.7);\n    \n    float v = voronoi(p * 20.0);\n    vec3 irid = 0.5 + 0.5 * cos(v * 10.0 + p.xyx * 3.0 + vec3(0, 2, 4));\n    \n    vec3 shellWht = mix(cWht, irid, 0.5) * (0.8 + 0.3 * v);\n    vec3 shellRed = mix(cRed, irid*cRed*2.0, 0.4) * (0.8 + 0.3 * v);\n    vec3 shellGrn = mix(cGrn, irid*cGrn*2.0, 0.4) * (0.8 + 0.3 * v);\n    \n    float pattern = smoothstep(0.05, 0.0, abs(fract(v*2.5) - 0.5));\n    shellRed = mix(shellRed, vec3(0.9, 0.8, 0.4), pattern * 0.4);\n\n    layer_MainFigure(p, shellRed, shellGrn, shellWht, cRed, cYel, cSkin, col);\n    \n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_FacialFeatures(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n    p *= 1.1;\n    \n    vec4 col = vec4(-1.0);\n    \n    vec3 cYel = vec3(0.85, 0.85, 0.2);\n    vec3 cRed = vec3(0.8, 0.15, 0.15);\n    vec3 cGrn = vec3(0.2, 0.6, 0.4);\n    vec3 cWht = vec3(0.9, 0.9, 0.85);\n    vec3 cSkin = vec3(0.95, 0.8, 0.7);\n    \n    float v = voronoi(p * 20.0);\n    vec3 irid = 0.5 + 0.5 * cos(v * 10.0 + p.xyx * 3.0 + vec3(0, 2, 4));\n    \n    vec3 shellWht = mix(cWht, irid, 0.5) * (0.8 + 0.3 * v);\n    vec3 shellRed = mix(cRed, irid*cRed*2.0, 0.4) * (0.8 + 0.3 * v);\n    vec3 shellGrn = mix(cGrn, irid*cGrn*2.0, 0.4) * (0.8 + 0.3 * v);\n    \n    float pattern = smoothstep(0.05, 0.0, abs(fract(v*2.5) - 0.5));\n    shellRed = mix(shellRed, vec3(0.9, 0.8, 0.4), pattern * 0.4);\n\n    layer_FacialFeatures(p, col);\n    \n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n","pavement_stripe":"float hash(vec2 p) { return fract(sin(dot(p, vec2(12.9898, 78.233))) * 43758.5453); }\n\nvoid layer_CentralStripe(in vec2 pathUV, inout vec3 col, out bool inStripe) {\n    inStripe = false;\n    if (abs(pathUV.x + 0.2) < 0.15) {\n        inStripe = true;\n        vec3 stripeCol = vec3(0.5);\n        float speckle = fract(sin(dot(pathUV * 500.0, vec2(12.98, 78.23))) * 43758.54);\n        if (speckle > 0.75) stripeCol = vec3(0.9);\n        else if (speckle < 0.2) stripeCol = vec3(0.15);\n        else stripeCol = vec3(0.45);\n        \n        float seams = smoothstep(0.0, 0.05, abs(fract(pathUV.y * 3.0) - 0.5));\n        stripeCol *= 0.5 + 0.5 * seams;\n        \n        col = stripeCol;\n    }\n}\n\nvoid layer_PinwheelPaving(in vec2 tiledUV, in bool inStripe, inout vec3 col) {\n    if (!inStripe) {\n        vec2 id = floor(tiledUV);\n        vec2 f = fract(tiledUV);\n        \n        int part = 0;\n        if (f.x > 0.333 && f.x < 0.666 && f.y > 0.333 && f.y < 0.666) part = 0;\n        else if (f.x < 0.666 && f.y < 0.333) part = 1;\n        else if (f.x > 0.666 && f.y < 0.666) part = 2;\n        else if (f.x > 0.333 && f.y > 0.666) part = 3;\n        else if (f.x < 0.333 && f.y > 0.333) part = 4;\n        \n        vec3 brickCol;\n        vec3 greenBrick = vec3(0.4, 0.52, 0.45);\n        vec3 beigeSquare = vec3(0.7, 0.68, 0.62);\n        \n        if (part == 0) brickCol = beigeSquare;\n        else brickCol = greenBrick;\n        \n        vec2 brickId;\n        if (part == 0) brickId = id + vec2(0.5, 0.5);\n        else if (part == 1) brickId = id + vec2(0.3, 0.1);\n        else if (part == 2) brickId = id + vec2(0.8, 0.3);\n        else if (part == 3) brickId = id + vec2(0.6, 0.8);\n        else if (part == 4) brickId = id + vec2(0.1, 0.6);\n        \n        float n = hash(brickId);\n        brickCol *= 0.85 + 0.3 * n;\n        \n        float tex = hash(tiledUV * 15.0);\n        brickCol = mix(brickCol, vec3(0.2), tex * 0.15);\n        \n        float dEdge = 1.0;\n        if (part == 0) dEdge = min(min(f.x - 0.333, 0.666 - f.x), min(f.y - 0.333, 0.666 - f.y));\n        else if (part == 1) dEdge = min(min(f.x, 0.666 - f.x), min(f.y, 0.333 - f.y));\n        else if (part == 2) dEdge = min(min(f.x - 0.666, 1.0 - f.x), min(f.y, 0.666 - f.y));\n        else if (part == 3) dEdge = min(min(f.x - 0.333, 1.0 - f.x), min(f.y - 0.666, 1.0 - f.y));\n        else if (part == 4) dEdge = min(min(f.x, 0.333 - f.x), min(f.y - 0.333, 1.0 - f.y));\n        \n        float grout = smoothstep(0.01, 0.03, dEdge);\n        col = mix(vec3(0.3, 0.3, 0.3), brickCol, grout);\n        \n        col += 0.1 * smoothstep(0.02, 0.06, dEdge) * grout;\n    }\n}\n\nvoid layer_Vignette(in vec2 st, inout vec3 col) {\n    col *= smoothstep(0.0, 1.0, 1.5 - st.y);\n}\n\nvec4 layer_CentralStripe(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv;\n    \n    vec2 st = p * 2.0 - 1.0;\n    st.x *= iResolution.x / iResolution.y;\n    vec2 pathUV = vec2(st.x / (abs(st.y - 1.2) + 0.1), 1.0 / (abs(st.y - 1.2) + 0.1));\n    float angle = 0.2;\n    mat2 rot = mat2(cos(angle), -sin(angle), sin(angle), cos(angle));\n    vec2 tiledUV = rot * pathUV * 8.0;\n    \n    vec3 col = vec3(-1.0);\n    bool inStripe = false;\n    \n    layer_CentralStripe(pathUV, col, inStripe);\n    \n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_PinwheelPaving(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv;\n    \n    vec2 st = p * 2.0 - 1.0;\n    st.x *= iResolution.x / iResolution.y;\n    vec2 pathUV = vec2(st.x / (abs(st.y - 1.2) + 0.1), 1.0 / (abs(st.y - 1.2) + 0.1));\n    float angle = 0.2;\n    mat2 rot = mat2(cos(angle), -sin(angle), sin(angle), cos(angle));\n    vec2 tiledUV = rot * pathUV * 8.0;\n    \n    vec3 col = vec3(-1.0);\n    bool inStripe = false;\n    \n    layer_PinwheelPaving(tiledUV, inStripe, col);\n    \n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_Vignette(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv;\n    \n    vec2 st = p * 2.0 - 1.0;\n    st.x *= iResolution.x / iResolution.y;\n    vec2 pathUV = vec2(st.x / (abs(st.y - 1.2) + 0.1), 1.0 / (abs(st.y - 1.2) + 0.1));\n    float angle = 0.2;\n    mat2 rot = mat2(cos(angle), -sin(angle), sin(angle), cos(angle));\n    vec2 tiledUV = rot * pathUV * 8.0;\n    \n    vec3 col = vec3(-1.0);\n    bool inStripe = false;\n    \n    layer_Vignette(st, col);\n    \n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n","plastic_pallet":"void layer_PalletStructure(in vec2 p, in vec2 f, in vec2 nF, in vec2 fineUV, out float plastic, out float node, out float beams, out float nHole, out float nIndents, out float hBeam, out float vBeam, out float isFineGrid) {\n    node = step(abs(nF.x - 0.5), 0.15) * step(abs(nF.y - 0.5), 0.15);\n    nHole = step(length(nF - 0.5), 0.04);\n    nIndents = step(length(abs(nF - 0.5) - 0.09), 0.025);\n    \n    hBeam = step(abs(f.y - 0.5), 0.1) * step(abs(nF.x - 0.5), 0.5);\n    vBeam = step(abs(f.x - 0.5), 0.1) * step(abs(nF.y - 0.5), 0.5);\n    beams = max(hBeam, vBeam);\n    \n    float fineGrid = step(fineUV.x, 0.25) + step(fineUV.y, 0.25);\n    fineGrid = clamp(fineGrid, 0.0, 1.0);\n    \n    float h1 = step(0.18, f.x) * step(f.x, 0.4) * step(0.18, f.y) * step(f.y, 0.4);\n    float h2 = step(0.6, f.x) * step(f.x, 0.82) * step(0.18, f.y) * step(f.y, 0.4);\n    float h3 = step(0.18, f.x) * step(f.x, 0.4) * step(0.6, f.y) * step(f.y, 0.82);\n    float h4 = step(0.6, f.x) * step(f.x, 0.82) * step(0.6, f.y) * step(f.y, 0.82);\n    float allBigHoles = h1 + h2 + h3 + h4;\n    \n    plastic = max(node, beams);\n    isFineGrid = 0.0;\n    if (plastic < 0.5) {\n        if (allBigHoles < 0.5) {\n            plastic = fineGrid;\n            isFineGrid = plastic;\n        }\n    }\n    plastic *= (1.0 - nHole);\n}\n\nvoid layer_PalletMaterial(in vec2 p, in vec2 f, in vec2 nF, in vec2 fineUV, in float plastic, in float node, in float beams, in float nHole, in float nIndents, in float hBeam, in float vBeam, in float isFineGrid, in vec3 palletCol, inout vec3 col) {\n    if (plastic > 0.5) {\n        col = palletCol;\n        \n        if (node > 0.5 && nHole < 0.5) {\n            if (nIndents > 0.5) {\n                col *= 0.4;\n            } else {\n                col *= smoothstep(0.04, 0.06, length(nF - 0.5));\n                float dX = abs(abs(nF.x - 0.5) - 0.15);\n                float dY = abs(abs(nF.y - 0.5) - 0.15);\n                col *= smoothstep(0.0, 0.015, min(dX, dY)) * 0.3 + 0.7;\n            }\n        } else if (beams > 0.5) {\n            float bX = min(abs(f.y - 0.4), abs(f.y - 0.6));\n            float bY = min(abs(f.x - 0.4), abs(f.x - 0.6));\n            float distToEdge = beams == hBeam ? bX : bY;\n            if (hBeam > 0.5 && vBeam > 0.5) distToEdge = min(bX, bY);\n            col *= smoothstep(0.0, 0.02, distToEdge) * 0.4 + 0.6;\n        } else if (isFineGrid > 0.5) {\n            col *= 0.75;\n            float gX = min(fineUV.x, 0.25 - fineUV.x);\n            float gY = min(fineUV.y, 0.25 - fineUV.y);\n            col *= smoothstep(0.0, 0.1, max(gX, gY)) * 0.5 + 0.5;\n        }\n        \n        float tex = fract(sin(dot(p*100.0, vec2(12.98, 78.23))) * 43758.54);\n        col *= 0.95 + 0.05 * tex;\n    }\n}\n\nvoid layer_BackgroundHoles(in vec2 p, in float plastic, in vec3 palletCol, inout vec3 col) {\n    if (plastic <= 0.5) {\n        vec2 p2 = p + vec2(0.1, -0.1);\n        vec2 f2 = fract(p2);\n        vec2 nF2 = fract(p2 + 0.5);\n        float b2 = max(step(abs(f2.y - 0.5), 0.1), step(abs(f2.x - 0.5), 0.1));\n        float n2 = step(abs(nF2.x - 0.5), 0.15) * step(abs(nF2.y - 0.5), 0.15);\n        if (max(b2, n2) > 0.5) {\n            col = palletCol * 0.25;\n        }\n    }\n}\n\nvec4 layer_PalletStructure(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 3.0;\n    p.x *= iResolution.x / iResolution.y;\n    \n    vec2 id = floor(p);\n    vec2 f = fract(p);\n    vec2 nF = fract(p + 0.5);\n    vec2 fineUV = fract(p * 12.0);\n    \n    float plastic = 0.0;\n    float node, beams, nHole, nIndents, hBeam, vBeam, isFineGrid;\n    layer_PalletStructure(p, f, nF, fineUV, plastic, node, beams, nHole, nIndents, hBeam, vBeam, isFineGrid);\n    \n    vec3 palletCol = vec3(0.65, 0.72, 0.2);\n    vec3 col = vec3(-1.0);\n    \n    \n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_PalletMaterial(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 3.0;\n    p.x *= iResolution.x / iResolution.y;\n    \n    vec2 id = floor(p);\n    vec2 f = fract(p);\n    vec2 nF = fract(p + 0.5);\n    vec2 fineUV = fract(p * 12.0);\n    \n    float plastic = 0.0;\n    float node, beams, nHole, nIndents, hBeam, vBeam, isFineGrid;\n    \n    vec3 palletCol = vec3(0.65, 0.72, 0.2);\n    vec3 col = vec3(-1.0);\n    \n    layer_PalletMaterial(p, f, nF, fineUV, plastic, node, beams, nHole, nIndents, hBeam, vBeam, isFineGrid, palletCol, col);\n    \n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_BackgroundHoles(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 3.0;\n    p.x *= iResolution.x / iResolution.y;\n    \n    vec2 id = floor(p);\n    vec2 f = fract(p);\n    vec2 nF = fract(p + 0.5);\n    vec2 fineUV = fract(p * 12.0);\n    \n    float plastic = 0.0;\n    float node, beams, nHole, nIndents, hBeam, vBeam, isFineGrid;\n    \n    vec3 palletCol = vec3(0.65, 0.72, 0.2);\n    vec3 col = vec3(-1.0);\n    \n    layer_BackgroundHoles(p, plastic, palletCol, col);\n    \n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n","porcelain_mosaic":"vec2 hash2(vec2 p) {\n    p = vec2(dot(p,vec2(127.1,311.7)), dot(p,vec2(269.5,183.3)));\n    return fract(sin(p)*43758.5453);\n}\n\nfloat sdSegment(vec2 p, vec2 a, vec2 b, float r) {\n    vec2 pa = p - a, ba = b - a;\n    float h = clamp(dot(pa,ba)/dot(ba,ba), 0.0, 1.0);\n    return length(pa - ba*h) - r;\n}\n\nvoid layer_BackgroundCement(in vec2 p, inout vec3 col) {\n    col = vec3(0.55, 0.56, 0.58);\n    float noise = fract(sin(dot(p*100.0, vec2(12.9898, 78.233))) * 43758.5453);\n    col -= noise * 0.1;\n}\n\nvoid layer_CharacterMask(in vec2 p, out float d) {\n    float top = abs(length(p - vec2(0.0, 0.6)) - 0.2) - 0.05;\n    float topCrescent = max(top, -p.y + 0.6);\n    \n    float h1 = sdSegment(p, vec2(-0.4, 0.4), vec2(0.4, 0.4), 0.08);\n    h1 = min(h1, sdSegment(p, vec2(-0.4, 0.4), vec2(-0.6, 0.6), 0.06));\n    h1 = min(h1, sdSegment(p, vec2(0.4, 0.4), vec2(0.6, 0.6), 0.06));\n    \n    float h2 = sdSegment(p, vec2(-0.5, 0.0), vec2(0.5, 0.0), 0.08);\n    float v1 = sdSegment(p, vec2(0.0, 0.2), vec2(0.0, -0.2), 0.08);\n    \n    float b1 = sdSegment(p, vec2(-0.1, -0.1), vec2(-0.6, -0.6), 0.08);\n    float b2 = sdSegment(p, vec2(0.1, -0.1), vec2(0.6, -0.6), 0.08);\n    \n    d = min(topCrescent, min(h1, min(h2, min(v1, min(b1, b2)))));\n}\n\nvoid layer_MosaicTiles(in vec2 p, out float edgeDist, out vec2 closestId) {\n    vec2 grid = p * 12.0;\n    vec2 id = floor(grid);\n    vec2 f = fract(grid);\n    \n    vec2 res = vec2(8.0);\n    vec2 mr;\n    \n    for(int j=-1; j<=1; j++)\n    for(int i=-1; i<=1; i++) {\n        vec2 b = vec2(i, j);\n        vec2 pt = hash2(id + b);\n        vec2 r = b + pt - f;\n        float dist = dot(r,r);\n        if(dist < res.x) {\n            res.x = dist;\n            mr = r;\n            closestId = id + b;\n        }\n    }\n    \n    res.x = 8.0;\n    for(int j=-2; j<=2; j++)\n    for(int i=-2; i<=2; i++) {\n        vec2 b = vec2(i, j);\n        vec2 pt = hash2(id + b);\n        vec2 r = b + pt - f;\n        if(dot(mr-r,mr-r) > 0.00001) {\n            res.x = min(res.x, dot( 0.5*(mr+r), normalize(r-mr) ));\n        }\n    }\n    edgeDist = res.x;\n}\n\nvoid layer_TileMaterial(in vec2 p, in vec2 closestId, inout vec3 col) {\n    col = vec3(0.95);\n    float pattern = sin(closestId.x * 15.0 + p.x * 40.0) * cos(closestId.y * 15.0 + p.y * 40.0);\n    pattern += 0.5 * sin(length(p - closestId*0.1) * 100.0);\n    \n    if (pattern > 0.5) {\n        col = vec3(0.1, 0.2, 0.7);\n    }\n}\n\nvoid layer_GroutAndBevel(in float d, in float edgeDist, inout vec3 col) {\n    if (edgeDist < 0.05) {\n        col = vec3(0.85);\n    }\n    col *= 0.8 + 0.2 * smoothstep(0.0, -0.05, d);\n}\n\nvoid layer_DropShadow(in float d, inout vec3 col) {\n    float shadow = smoothstep(0.0, 0.05, d);\n    col = mix(col * 0.6, col, shadow);\n}\n\nvec4 layer_BackgroundCement(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n    \n    vec3 col = vec3(-1.0);\n    \n    layer_BackgroundCement(p, col);\n    \n    float d;\n    \n    if (d < 0.0) {\n        float edgeDist;\n        vec2 closestId;\n        \n        if (edgeDist >= 0.05) {\n        }\n    } else {\n    }\n    \n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_CharacterMask(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n    \n    vec3 col = vec3(-1.0);\n    \n    \n    float d;\n    layer_CharacterMask(p, d);\n    \n    if (d < 0.0) {\n        float edgeDist;\n        vec2 closestId;\n        \n        if (edgeDist >= 0.05) {\n        }\n    } else {\n    }\n    \n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_MosaicTiles(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n    \n    vec3 col = vec3(-1.0);\n    \n    \n    float d;\n    \n    if (d < 0.0) {\n        float edgeDist;\n        vec2 closestId;\n        layer_MosaicTiles(p, edgeDist, closestId);\n        \n        if (edgeDist >= 0.05) {\n        }\n    } else {\n    }\n    \n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_TileMaterial(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n    \n    vec3 col = vec3(-1.0);\n    \n    \n    float d;\n    \n    if (d < 0.0) {\n        float edgeDist;\n        vec2 closestId;\n        \n        if (edgeDist >= 0.05) {\n            layer_TileMaterial(p, closestId, col);\n        }\n    } else {\n    }\n    \n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_GroutandBevel(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n    \n    vec3 col = vec3(-1.0);\n    \n    \n    float d;\n    \n    if (d < 0.0) {\n        float edgeDist;\n        vec2 closestId;\n        \n        if (edgeDist >= 0.05) {\n        }\n        layer_GroutAndBevel(d, edgeDist, col);\n    } else {\n    }\n    \n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_DropShadow(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n    \n    vec3 col = vec3(-1.0);\n    \n    \n    float d;\n    \n    if (d < 0.0) {\n        float edgeDist;\n        vec2 closestId;\n        \n        if (edgeDist >= 0.05) {\n        }\n    } else {\n        layer_DropShadow(d, col);\n    }\n    \n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n","red_book":"void layer_Wall(in vec2 p, inout vec3 col) {\n    col = vec3(0.85, 0.9, 0.9);\n}\n\nvoid layer_Floor(in vec2 p, inout vec3 col) {\n    if (p.y < -0.3) {\n        vec2 floorUV = vec2(p.x / (p.y + 0.6), 1.0 / (p.y + 0.6));\n        floorUV *= 6.0;\n        \n        vec2 id = floor(floorUV);\n        if (mod(id.y, 2.0) > 0.5) floorUV.x += 0.5;\n        vec2 f = fract(floorUV);\n        float hole = smoothstep(0.35, 0.25, length(f - 0.5));\n        \n        vec3 fCol = vec3(0.55, 0.6, 0.55); \n        fCol = mix(fCol, vec3(0.1), hole);\n        \n        float shadow = smoothstep(-0.1, 0.4, abs(p.x - 0.1));\n        shadow *= smoothstep(-0.3, -0.6, p.y);\n        fCol *= 0.4 + 0.6 * shadow;\n        \n        col = fCol;\n    }\n}\n\nvoid layer_RedBook(in vec2 p, inout vec3 col) {\n    float coverX = 0.3;\n    float coverY = 0.5;\n    float skew = 0.05;\n    \n    float cover = smoothstep(0.01, 0.0, max(abs(p.x - 0.1) - coverX, abs(p.y - 0.1) - coverY));\n    \n    float skewX = p.x - (p.y - 0.1) * skew;\n    float spine = smoothstep(0.01, 0.0, max(abs(skewX + 0.25) - 0.05, abs(p.y - 0.12) - coverY));\n    \n    float backEdge = smoothstep(0.01, 0.0, max(abs(skewX + 0.28) - 0.08, abs(p.y - 0.14) - coverY));\n\n    if (backEdge > 0.5) {\n        col = vec3(0.5, 0.05, 0.1); \n    }\n    if (spine > 0.5) {\n        col = vec3(0.95);\n        col *= 0.8 + 0.2 * smoothstep(-0.3, -0.2, skewX); \n        if (mod(p.y * 10.0, 1.0) < 0.1 && abs(p.y) < 0.3) col *= 0.5;\n    }\n    if (cover > 0.5) {\n        col = vec3(0.85, 0.1, 0.2); \n        \n        float tY1 = abs(p.y - 0.0);\n        float tX1 = abs(p.x - 0.1);\n        if (tY1 < 0.05 && tX1 < 0.2) {\n            if (mod(p.x * 15.0, 1.0) < 0.75) col = vec3(1.0); \n        }\n        float tY2 = abs(p.y + 0.1);\n        float tX2 = abs(p.x - 0.05);\n        if (tY2 < 0.05 && tX2 < 0.15) {\n            if (mod(p.x * 15.0, 1.0) < 0.75) col = vec3(1.0); \n        }\n        \n        if (abs(p.y + 0.18) < 0.005 && abs(p.x - 0.1) < 0.18) {\n            col = vec3(1.0);\n        }\n    }\n}\n\nvec4 layer_Wall(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 col = vec3(-1.0);\n\n    layer_Wall(p, col);\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_Floor(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 col = vec3(-1.0);\n\n    layer_Floor(p, col);\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_RedBook(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 2.0 - 1.0;\n    p.x *= iResolution.x / iResolution.y;\n\n    vec3 col = vec3(-1.0);\n\n    layer_RedBook(p, col);\n\n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n","sticker_wall":"float hash(vec2 p) { return fract(sin(dot(p, vec2(12.9898, 78.233))) * 43758.5453); }\n\nvoid layer_MetalPanels(in vec2 p, inout vec3 col) {\n    col = vec3(0.75, 0.76, 0.78);\n    float seams = step(abs(fract(p.x * 4.0) - 0.5), 0.01);\n    col = mix(col, vec3(0.4), seams);\n    \n    float dirt = hash(floor(p * 200.0)) * 0.1;\n    col -= dirt * (1.0 - p.y);\n}\n\nvoid layer_SmallStickers(in vec2 p, inout vec3 col) {\n    vec2 grid = p * vec2(10.0, 15.0);\n    vec2 id = floor(grid);\n    vec2 f = fract(grid);\n    \n    float n = hash(id);\n    if (n > 0.6) {\n        float w = 0.2 + 0.2 * hash(id + 1.0);\n        float h = 0.15 + 0.15 * hash(id + 2.0);\n        vec2 center = vec2(0.5) + 0.2 * vec2(hash(id + 3.0), hash(id + 4.0)) - 0.1;\n        \n        vec2 dist = abs(f - center);\n        if (dist.x < w && dist.y < h) {\n            vec3 stickerCol = vec3(0.95, 0.95, 0.9);\n            \n            float lines = step(abs(fract((f.y - center.y) * 15.0) - 0.5), 0.2);\n            vec3 textColor = hash(id + 5.0) > 0.5 ? vec3(0.1, 0.2, 0.7) : vec3(0.1, 0.6, 0.3);\n            \n            float textMask = step(dist.x, w - 0.05) * step(dist.y, h - 0.05);\n            col = mix(stickerCol, textColor, lines * textMask);\n        }\n    }\n}\n\nvoid layer_WarningSign(in vec2 p, inout vec3 col) {\n    if (p.x > 0.35 && p.x < 0.65 && p.y > 0.7 && p.y < 0.9) {\n        vec3 signCol = vec3(0.98);\n        \n        float text1 = step(abs(p.y - 0.85), 0.02) * step(abs(p.x - 0.5), 0.1);\n        float text2 = step(abs(p.y - 0.78), 0.015) * step(abs(p.x - 0.5), 0.12);\n        float text3 = step(abs(p.y - 0.74), 0.015) * step(abs(p.x - 0.5), 0.12);\n        \n        float boltX = abs(p.x - 0.4);\n        float bolt = step(boltX, 0.02 - (p.y - 0.8)*0.1) * step(p.y, 0.88) * step(0.72, p.y);\n        \n        col = mix(signCol, vec3(0.8, 0.1, 0.1), clamp(text1 + text2 + text3 + bolt, 0.0, 1.0));\n    }\n}\n\nvec4 layer_MetalPanels(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv;\n    \n    vec3 col = vec3(-1.0);\n    \n    layer_MetalPanels(p, col);\n    \n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_SmallStickers(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv;\n    \n    vec3 col = vec3(-1.0);\n    \n    layer_SmallStickers(p, col);\n    \n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_WarningSign(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv;\n    \n    vec3 col = vec3(-1.0);\n    \n    layer_WarningSign(p, col);\n    \n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n","woven_chair":"void layer_BackgroundFloor(in vec2 uv, inout vec3 col) {\n    col = vec3(0.6, 0.55, 0.45) * (0.8 + 0.2 * fract(sin(floor(uv.x*20.0)*13.0 + floor(uv.y*20.0)*37.0)*43758.0));\n}\n\nvoid layer_WeaveStructure(in vec2 id, in vec2 f, out float isVerticalTop, out float vStrap, out float hStrap) {\n    isVerticalTop = mod(id.x + id.y, 2.0);\n    float gap = 0.05;\n    vStrap = step(gap, f.x) * step(f.x, 1.0 - gap);\n    hStrap = step(gap, f.y) * step(f.y, 1.0 - gap);\n}\n\nvoid layer_WeaveMaterial(in vec2 f, in float isVerticalTop, in float vStrap, in float hStrap, inout vec3 col) {\n    vec3 blueStrap = vec3(0.15, 0.3, 0.6);\n    vec3 darkBlue = vec3(0.05, 0.15, 0.4);\n    \n    float vTex = sin(f.y * 50.0) * 0.5 + 0.5;\n    float hTex = sin(f.x * 50.0) * 0.5 + 0.5;\n    \n    if (isVerticalTop > 0.5) {\n        if (vStrap > 0.0) {\n            col = mix(darkBlue, blueStrap, vTex) * (0.6 + 0.4 * sin(f.x * 3.1415));\n        } else if (hStrap > 0.0) {\n            col = mix(darkBlue, blueStrap, hTex) * (0.6 + 0.4 * sin(f.y * 3.1415)) * 0.6;\n        }\n    } else {\n        if (hStrap > 0.0) {\n            col = mix(darkBlue, blueStrap, hTex) * (0.6 + 0.4 * sin(f.y * 3.1415));\n        } else if (vStrap > 0.0) {\n            col = mix(darkBlue, blueStrap, vTex) * (0.6 + 0.4 * sin(f.x * 3.1415)) * 0.6;\n        }\n    }\n    \n    if (isVerticalTop > 0.5 && vStrap > 0.0 && hStrap > 0.0) {\n        col *= smoothstep(0.0, 0.2, f.y) * smoothstep(1.0, 0.8, f.y);\n    } else if (isVerticalTop < 0.5 && hStrap > 0.0 && vStrap > 0.0) {\n        col *= smoothstep(0.0, 0.2, f.x) * smoothstep(1.0, 0.8, f.x);\n    }\n}\n\nvec4 layer_BackgroundFloor(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 10.0;\n    p.x *= iResolution.x / iResolution.y;\n    \n    vec2 id = floor(p);\n    vec2 f = fract(p);\n    \n    vec3 col = vec3(-1.0);\n    \n    layer_BackgroundFloor(uv, col);\n    \n    float isVerticalTop, vStrap, hStrap;\n    \n    \n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_WeaveStructure(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 10.0;\n    p.x *= iResolution.x / iResolution.y;\n    \n    vec2 id = floor(p);\n    vec2 f = fract(p);\n    \n    vec3 col = vec3(-1.0);\n    \n    \n    float isVerticalTop, vStrap, hStrap;\n    layer_WeaveStructure(id, f, isVerticalTop, vStrap, hStrap);\n    \n    \n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n\nvec4 layer_WeaveMaterial(vec2 _uv){\n\n    vec2 uv = gl_FragCoord.xy / iResolution.xy;\n    vec2 p = uv * 10.0;\n    p.x *= iResolution.x / iResolution.y;\n    \n    vec2 id = floor(p);\n    vec2 f = fract(p);\n    \n    vec3 col = vec3(-1.0);\n    \n    \n    float isVerticalTop, vStrap, hStrap;\n    \n    layer_WeaveMaterial(f, isVerticalTop, vStrap, hStrap, col);\n    \n\n  vec3 _rgb = vec3(col);\n  return vec4(clamp(_rgb,0.0,1.0), step(0.0, max(_rgb.r, max(_rgb.g, _rgb.b))));\n}\n"};
window.LAYER_SOURCES = {"1011":["Background","ChairShadow","WovenChair"],"1121":["Background","AbstractCar","AbstractText","StreetGround","ScooterAndRider"],"0995":["Background","BalconyWall","Clothesline","Clothes","BirdCage","PlantPot"],"blue_chair":["Background","Frame","Mesh","FloralDesign","Lighting"],"breeze_blocks":["StoneBase","BreezeBlocks","HolesBackground","WallLighting"],"checkered_wood":["DarkWood","LightWood","GrooveandBevel"],"emblem_wall":["Background","Emblem","Shadow","Ledge"],"flat_grey_tiles":["StoneTiles","Grout","Bevel"],"flower_vase":["Background","WoodStripes","Watermark","Diamonds","Shelf","Vases","Branches","Flowers"],"glass_cutouts":["GlassBackground","DropShadows","PaperCutouts"],"grey_tiles":["TiledBox","Pillar","Ground"],"IMG_0398":["Floor","CeilingGrid","BackgroundForest","GlowingScreens","WoodenSculpture"],"IMG_0415":["GravelFloor","Stones","WhiteCubes","BrickWall","DarkWindow"],"IMG_0417":["Background","Pedestal","Stone"],"IMG_0418":["Wall","RedPallet","GreenPallets"],"IMG_0419":["GreenTiles","BumpyLine","StreetWall","DistanceFade"],"IMG_0586":["InstagramUI","BackgroundWall","PerforatedTable","RedBook"],"IMG_0767":["Background","TextShape","TextShadow","TextMaterial"],"IMG_0824":["Sky","LeftBuilding","RightBuilding","Street","Leaves","Motorcycle"],"IMG_0825":["Background","PersonPants","PersonShirt","PersonHead","Motorcycle","GreenBasket"],"IMG_0826":["SurroundingWall","BlueTiles","ScissorGate","RightPillar"],"IMG_0965":["BlueTiles","Bird","Flowers","SmallCloud"],"IMG_0967":["BrickWall","WindowSlats","WindowFrames","Vignette"],"IMG_0973":["OuterWall","DarkBackground","Floor","Gate","Objects"],"IMG_0986":["WoodTexture","Signboard","GoldArea","Shadow"],"IMG_0992":["Wall","Floor","Shelves","FloatingObjects"],"IMG_0995":["Background","BalconyWall","Clothesline","BirdCage","Plant"],"IMG_1011":["WallTiles","FloorTiles","ChairShadow","WovenChair"],"IMG_1014":["Floor","Chair","ChairLeg"],"IMG_1061":["Background","RedBar","Text2K4","GodOfWealth","BottomNumbers","DarkEdges"],"IMG_1072":["Background","Cart","LotteryTickets","Woman","Man"],"IMG_1077":["MetallicPlate","Holes","ColoredPencils","LaserBeam"],"IMG_1121":["Poster","Street","Scooter","Rider"],"IMG_1126":["Background","ChromeExhaust","SuspensionSpring","CarbonFiberPlates"],"IMG_1151":["Road","MotorcycleParts","TrousersLeg","FootAndShoe"],"IMG_1183":["RollerDoor","Posters","Scooter"],"IMG_1185":["WoodenFloor","MainBodyWrapped","FrontWheel","BackPartWrapping"],"IMG_1213":["Background","GoldenFrame","SceneSky","SceneMountain","SceneOcean","SceneBoat","SceneBalcony"],"IMG_1240":["Signboard","TextGIAI","TextNUOCMIA","SugarcaneStalks","SugarcaneLeaves"],"IMG_1241":["Ground","Chair","LicensePlate","Vignette"],"IMG_1276":["Walls","TerracottaRoof","CourtyardTarp","Railings","Tree","FallingLeaves","Shadows"],"IMG_1300":["Background","VestBody","Pouches","ArmAttachments","Collar","Shadows"],"IMG_1301":["Sky","Flag"],"IMG_1306":["BackgroundRoad","MetalFrame","BlueCloth","PlasticBags","Shadows"],"IMG_1308":["Background","Head","KiteGarment","Shading","TextForbes"],"IMG_1309":["Background","Kite","KiteTail","Shadows"],"IMG_1323":["SignBox","TextALOC","TextCHAOLONG","TreeBark","NeonReflections"],"IMG_1333":["Background","Hood","Raincoat","Hands","Shoes","Floor"],"IMG_1348":["BackgroundWalls","GodRays","Pillar","Headpiece","VeilAndEmblem"],"IMG_1399":["StoreInterior","CheckeredFloor","MainSubjectTorso","SkinArmsNeck","Shorts","BareLegs","Head","Phone"],"IMG_1415":["RunwayBackground","ModelSharedFeatures","ModelLeft","ModelCenter","ModelRight"],"IMG_1426":["Sky","Clouds","Birds","Rocks","Plants","Vignette"],"IMG_1449":["MosaicWall","GoldFrame","Padding","WoodFrame","InsideWall"],"IMG_1621":["Background","Floor","WoodRack","ChairTable","Motorcycle"],"IMG_1644":["Background","TextElements","TopImpact","BottomImpact","Hardhat","FamilyGroup"],"IMG_1663":["SignArea","Pavement","DecorativeIronGate","RedWarningSign"],"IMG_1714":["StorefrontAlley","WhiteWall","DecorativeVentBlock"],"IMG_1718":["Background","IronGate","HouseSign","Plant","VinesLeaves"],"IMG_1720":["Background","GroundPath","RightWallColumn","WhiteGateStructure","TopFoliage","Motorcycle"],"IMG_1721":["BackgroundWall","BottomStone","TopTrim","StrepText","CartoonCharacter"],"IMG_1725":["StorefrontStructure","LeftDisplayPanel","UpperDisplayCase","LowerDisplayCase","CurvyWoodenDividers","LowerItems"],"IMG_1847":["Background","CarvedSymbol","TopLouvers","BottomSurface"],"IMG_2148":["Sky","Road","Buildings","PowerLines","ScooterRider","Bundles"],"IMG_2249":["Ceiling","LatticeStructure","SideWalls"],"IMG_2250":["Ceiling","StoneBase","LatticeWall","SidePillars"],"IMG_2263":["Background","Ground","BluePillar","BlueGateStructure","Fence"],"IMG_2450":["Background","HeadStructure","LabelTexts"],"IMG_2477":["Background","TopCertificate","BottomCertificate"],"IMG_2586":["Background","TableBenches"],"IMG_2700":["Background","TrashBin","MainSignboard","SignFrameCart"],"IMG_2703":["Background","FibonacciTrendLines","HorizontalPriceLevels","Candlesticks","RightSidebar"],"IMG_2745":["Background","LanternWire","Lantern","Tassel"],"IMG_2768":["AltarCloth","Floor","Feet"],"IMG_2773":["WallBase","Awning","Symbol","LeavesOverlay"],"IMG_2982":["Background","ShrineStructure","FloorShadow"],"IMG_3105":["Sky","RoofBehind","Cat","MainWall","ExposedBrick"],"IMG_3714":["Asphalt","ArrowShape","FeetClothes"],"IMG_3922":["Background","TextLetters","FoodPicture"],"IMG_4080":["Fabric","RedSymbols","WoodSlats","Ground"],"IMG_4742":["Street","Person","Bags","Motorcycle"],"IMG_4782":["Wall","Floor","Window","Table","Chair","Figure","GridBorders"],"IMG_4783":["Wall","Floor","TV","Plant","Furniture","Figure","GridBorders"],"IMG_4784":["Background","BluePatternedDress","GreySuit","MaroonDress","BlackFloralDress","BlackFormalAoDai"],"IMG_5181":["ConcreteBackground","Card"],"IMG_5206":["Background","LeftSign","GoldenBat"],"IMG_5211":["Background","TopReflection","Leaf","SpecularHighlight"],"IMG_5212":["Sky","GreenBuilding","Crane","WhiteBuilding","Roof","TerraceWalls","TerraceFloor","Balustrade"],"IMG_5214":["Background","Vase"],"IMG_5215":["Background","Smoke","Object"],"IMG_5225":["ImageCenter","TopArea","BottomArea"],"IMG_5251":["Fabric","Woman","Flowers","Hat"],"IMG_5320":["Background","Tripod","HangingPlant","SwingingHead","SwingingString"],"IMG_5326":["Background","Sign","Leaves"],"IMG_5346":["Banner","Background","Plants"],"IMG_5506":["TopSection","BottomPhoto","Lasers","Foreground"],"IMG_5517":["Ground","PillarBase","TopColumn","DecorativeTile"],"IMG_5538":["Background","MotorcycleWheel","RedPanel","Seat","GrabRail"],"IMG_5569":["Background","RockSpeaker","SpeakerHoles","Logo","Cable"],"IMG_6550":["Background","Roof","Symbols","Gate","Fence","Noise"],"IMG_6551":["Wall","Railing","Window","RoofandDragon"],"IMG_6574":["Sky","Trees","Ground","Poles","Windows","Moto","Noise"],"IMG_6609":["Background","LatticePanels","CenterPanel"],"IMG_6693":["Shelves","SideObject","Figure","Shadow"],"IMG_6694":["Shelves","SideObject","Figure","Shadow"],"IMG_6695":["Background","Hat","Face","Torso","Arms","Pants","Shadow"],"IMG_6697":["Panel","TornPoster","TopCenterPoster","Stickers"],"IMG_6755":["Background","Shadow","Cutout","Vignette"],"IMG_6786":["Background","Ticket","Hand"],"IMG_6793":["Background","Casing","Screen","ScrollingContent"],"IMG_6876":["Background","Protein","ZoomCircles","Lines","Vignette"],"IMG_6994":["Floor","Wall","Figure","Vignette"],"IMG_7211":["Background","TopBar","BottomBar","ScrollContent"],"IMG_7225":["Background","BoxBase","TextNHANG","TextTHANTAI","GoldSeal"],"IMG_7266":["Background","CharacterShape","Mosaic"],"IMG_7408":["Background","BlueShirt","WhiteShirt","StarBadge"],"IMG_7409":["Background","BackgroundGarments","WhiteShirt","BlueShirt"],"IMG_7410":["Background","Shelves","Mannequin"],"IMG_7680":["Sky","Building","StringLights","RedLantern","Vignette"],"IMG_7681":["Background","TempleRoof","Dragon","Vignette"],"IMG_7682":["Wall","WindowGrille","Railing","Dragon"],"IMG_7683":["BackgroundCourtyard","TempleGateHeader","TempleGatePillars","TempleGateGrid","RedChineseCharacters","WhitePaperSign","Vignette"],"IMG_7684":["Background","Facade","BottomRoof","Vignette"],"IMG_7685":["BackgroundWall","CenterWindow","BalconyRailing","BottomRedSection","CenterPost","Vignette"],"IMG_7686":["Background","LotusGrate","ForegroundRoof","CentralOrnateFeature","Vignette"],"IMG_7694":["Background","GreenDoor","RedTable","Chairs","Vignette"],"IMG_9792":["SkyTrees","WallRoof","Ground","FlowersLamppost","RockCairn","Vignette"],"IMG_9812":["BackgroundWoodPanels","LeftObjects","BottomSurface","CenterWoodWheelObject","Vignette"],"IMG_9813":["Background","MainGraphics","TextOpeningHours","BottomTextLogos","Vignette"],"kite_garment_sketch":["Background","PersonHead","ShouldersNeck","CentralGarmentSpine","KiteWingTopEdge","InternalArchYoke","KiteWingBottomEdge","Sleeves","Legs","ForbesText"],"laser_engraver":["OpticalBreadboard","LaserDot","Vignette"],"lottery_ticket":["Background","BlueEdges","RedNumbers","DeityFigure","TextOverlay","TopSection"],"metal_gate":["BackgroundTiles","GateBackground","MetalGateStructure","GateMaterial"],"mother_of_pearl":["Background","Halo","SideFigures","MainFigure","FacialFeatures"],"pavement_stripe":["CentralStripe","PinwheelPaving","Vignette"],"plastic_pallet":["PalletStructure","PalletMaterial","BackgroundHoles"],"porcelain_mosaic":["BackgroundCement","CharacterMask","MosaicTiles","TileMaterial","GroutandBevel","DropShadow"],"red_book":["Wall","Floor","RedBook"],"sticker_wall":["MetalPanels","SmallStickers","WarningSign"],"woven_chair":["BackgroundFloor","WeaveStructure","WeaveMaterial"]};
window.LAYER_METADATA = {"1011":{"title":"Woven Blue Folding Chair","layers":[{"name":"Background","keywords":["background","floor","wall","tiles","stone","grey","beige","brown","brick"]},{"name":"Chair Shadow","keywords":["shadow","dark","chair shadow"]},{"name":"Woven Chair","keywords":["chair","folding chair","woven","fabric","blue","metallic","frame","silver"]}]},"1121":{"title":"Abstract Red Scooter","layers":[{"name":"Background","keywords":["background","dark","poster","grey","blue"]},{"name":"Abstract Car","keywords":["car","red","gradient","poster"]},{"name":"Abstract Text","keywords":["text","lines","poster","white"]},{"name":"Street Ground","keywords":["street","ground","pavement","gravel","grey"]},{"name":"Scooter And Rider","keywords":["scooter","rider","red","shiny","helmet","mask","shirt"]}]},"0995":{"title":"Balcony with Clothes and Bird Cage","layers":[{"name":"Background","keywords":["background","wall","plaster","texture","grey"]},{"name":"Balcony Wall","keywords":["balcony","wall","teal","gradient"]},{"name":"Clothesline","keywords":["clothesline","wire","sagging","curve","black"]},{"name":"Clothes","keywords":["clothes","shirt","hanging","sway","wind","red","blue","yellow","wrinkles"]},{"name":"Bird Cage","keywords":["bird","cage","bamboo","bars","hook","brown","shadow"]},{"name":"Plant Pot","keywords":["plant","pot","terracotta","leaves","vines","green"]}]},"blue_chair":{"title":"Blue Plastic Chair Pattern","layers":[{"name":"Background","keywords":["dark","grey","background","behind"]},{"name":"Frame","keywords":["plastic","blue","curved","backrest","boundary","bevel"]},{"name":"Mesh","keywords":["mesh","diamond","holes","pattern","diagonal","threads"]},{"name":"Floral Design","keywords":["flower","floral","petals","center","stems","leaves","branch"]},{"name":"Lighting","keywords":["lighting","specular","highlight","top-down","shiny"]}]},"breeze_blocks":{"title":"Breeze Blocks Pattern","layers":[{"name":"Stone Base","keywords":["stone","base","grey","grout","ledge","horizon"]},{"name":"Breeze Blocks","keywords":["terracotta","blocks","frame","mortar","curved"]},{"name":"Holes Background","keywords":["holes","sky","background","interior","shadow"]},{"name":"Wall Lighting","keywords":["lighting","darkens","edges","curved wall"]}]},"checkered_wood":{"title":"Checkered Wood Pattern","layers":[{"name":"Dark Wood","keywords":["dark","wood","vertical","grain","reddish","brown"]},{"name":"Light Wood","keywords":["light","wood","horizontal","beige","fine"]},{"name":"Groove and Bevel","keywords":["groove","bevel","edge","3D","shadow","highlight"]}]},"emblem_wall":{"title":"Decorative Emblem Wall","layers":[{"name":"Background","keywords":["granite","stone","wall","grey","speckles"]},{"name":"Emblem","keywords":["emblem","metal","dark","stone","cross","crown","frame","bevel"]},{"name":"Shadow","keywords":["drop shadow","shadow","depth"]},{"name":"Ledge","keywords":["ledge","table","marble","red","bottom"]}]},"flat_grey_tiles":{"title":"Flat Grey Stone Tiles","layers":[{"name":"Stone Tiles","keywords":["stone","tiles","grey","alternating","bands","texture","noise"]},{"name":"Grout","keywords":["grout","lines","spacing"]},{"name":"Bevel","keywords":["bevel","highlight","shadow","3D","edge"]}]},"flower_vase":{"title":"Flower Vase","layers":[{"name":"Background","keywords":["background","base","tile","color"]},{"name":"Wood Stripes","keywords":["wood","stripes","grain","brown"]},{"name":"Watermark","keywords":["leaf","watermark","subtle"]},{"name":"Diamonds","keywords":["diamonds","pattern","bevel"]},{"name":"Shelf","keywords":["shelf","wood","table","ledge"]},{"name":"Vases","keywords":["vases","ceramic","lines","specular","highlight"]},{"name":"Branches","keywords":["branches","twigs","stem","wood"]},{"name":"Flowers","keywords":["flowers","magnolias","petals","shadow"]}]},"glass_cutouts":{"title":"Glass Window with Paper Cutouts","layers":[{"name":"Glass Background","keywords":["glass","blocks","blue","grid","grout"]},{"name":"Drop Shadows","keywords":["shadow","depth","paper"]},{"name":"Paper Cutouts","keywords":["paper","cutouts","clouds","flowers","birds","swallows"]}]},"grey_tiles":{"title":"3D Grey Stone Tiles","layers":[{"name":"TiledBox","keywords":["box","tiles","grey","grout","stone","cube"]},{"name":"Pillar","keywords":["pillar","concrete","top","solid","grey"]},{"name":"Ground","keywords":["ground","floor","flower","pattern","tiles"]}]},"IMG_0398":{"title":"Shader: IMG_0398","layers":[{"name":"Floor","keywords":["floor","wood","plank","noise","projection"]},{"name":"Ceiling Grid","keywords":["ceiling","grid","light","perspective"]},{"name":"Background Forest","keywords":["forest","trees","silhouette","moon","night"]},{"name":"Glowing Screens","keywords":["screens","glow","wave","animation","middle"]},{"name":"Wooden Sculpture","keywords":["sculpture","wood","curves","shadow"]}]},"IMG_0415":{"title":"Shader: IMG_0415","layers":[{"name":"Gravel Floor","keywords":["gravel","floor","texture","noise"]},{"name":"Stones","keywords":["stones","rocks","shapes"]},{"name":"White Cubes","keywords":["cubes","white","plinths","stripes"]},{"name":"Brick Wall","keywords":["brick","wall","background"]},{"name":"Dark Window","keywords":["dark","window","opening"]}]},"IMG_0417":{"title":"Shader: IMG_0417","layers":[{"name":"Background","keywords":["background","wall","floor","gray","backdrop"]},{"name":"Pedestal","keywords":["pedestal","base","stone","column","black"]},{"name":"Stone","keywords":["stone","gem","sphere","white","top"]}]},"IMG_0418":{"title":"Shader: IMG_0418","layers":[{"name":"Wall","keywords":["wall","texture","grey","background"]},{"name":"Red Pallet","keywords":["pallet","red","back","holes"]},{"name":"Green Pallets","keywords":["pallet","green","front","glow","animation","holes"]}]},"IMG_0419":{"title":"Shader: IMG_0419","layers":[{"name":"Green Tiles","keywords":["green","tiles","pavement","grout","squares"]},{"name":"Bumpy Line","keywords":["bumpy","line","middle","pebbles","grid"]},{"name":"Street Wall","keywords":["street","wall","background"]},{"name":"Distance Fade","keywords":["distance","fade","perspective"]}]},"IMG_0586":{"title":"Shader: IMG_0586","layers":[{"name":"Instagram UI","keywords":["instagram","ui","header","footer","message","circles"]},{"name":"Background Wall","keywords":["wall","background","grey"]},{"name":"Perforated Table","keywords":["table","metal","perforated","grid","holes"]},{"name":"Red Book","keywords":["book","red","layout now","text","cover"]}]},"IMG_0767":{"title":"Shader: IMG_0767","layers":[{"name":"Background","keywords":["background","yellow","wavy","animation"]},{"name":"Text Shape","keywords":["text","shape","sdf","lines"]},{"name":"Text Shadow","keywords":["shadow","text","red","pink","gradient","animated"]},{"name":"Text Material","keywords":["text","material","border","white","core","blue","highlight"]}]},"IMG_0824":{"title":"Shader: IMG_0824","layers":[{"name":"Sky","keywords":["sky","blue","background"]},{"name":"Left Building","keywords":["building","left","grey","shutter","door"]},{"name":"Right Building","keywords":["building","right","blue","perspective"]},{"name":"Street","keywords":["street","pavement","asphalt","wet","reflection","rain"]},{"name":"Leaves","keywords":["leaves","tree","green","sway","animation"]},{"name":"Motorcycle","keywords":["motorcycle","silhouette","black"]}]},"IMG_0825":{"title":"Shader: IMG_0825","layers":[{"name":"Background","keywords":["background","street","sidewalk","barrier","wall"]},{"name":"Person Pants","keywords":["pants","purple","pattern","animated"]},{"name":"Person Shirt","keywords":["shirt","high-vis","plaid","orange","yellow","reflective"]},{"name":"Person Head","keywords":["head","face","hat","hard hat","yellow"]},{"name":"Motorcycle","keywords":["motorcycle","scooter","black","white"]},{"name":"Green Basket","keywords":["basket","bag","green"]}]},"IMG_0826":{"title":"Shader: IMG_0826","layers":[{"name":"Surrounding Wall","keywords":["wall","grey","background"]},{"name":"Blue Tiles","keywords":["blue","tiles","grout","above gate"]},{"name":"Scissor Gate","keywords":["gate","scissor","lattice","metal","animation","stretch"]},{"name":"Right Pillar","keywords":["pillar","cables","right","dark"]}]},"IMG_0965":{"title":"Shader: IMG_0965","layers":[{"name":"Blue Tiles","keywords":["blue","tiles","grout","background"]},{"name":"Bird","keywords":["bird","paper","shadow","parallax","white","blue details"]},{"name":"Flowers","keywords":["flower","cloud","paper","shadow","painted","green","yellow"]},{"name":"Small Cloud","keywords":["cloud","flower","small","white"]}]},"IMG_0967":{"title":"Shader: IMG_0967","layers":[{"name":"Brick Wall","keywords":["brick","wall","red","texture","surrounding"]},{"name":"Window Slats","keywords":["window","slats","blinds","animation","reflection","light"]},{"name":"Window Frames","keywords":["window","frames","vertical","center"]},{"name":"Vignette","keywords":["vignette","shadow","edge"]}]},"IMG_0973":{"title":"Shader: IMG_0973","layers":[{"name":"Outer Wall","keywords":["wall","outer","grey","texture"]},{"name":"Dark Background","keywords":["background","dark","behind gate"]},{"name":"Floor","keywords":["floor","perspective","moving","tiles","animation"]},{"name":"Gate","keywords":["gate","bars","circles","pattern","metallic","animation"]},{"name":"Objects","keywords":["objects","red","blue","pulse","animation"]}]},"IMG_0986":{"title":"Shader: IMG_0986","layers":[{"name":"Wood Texture","keywords":["wood","texture","animated","background"]},{"name":"Signboard","keywords":["signboard","green","red border","text","pulsating","animation"]},{"name":"Gold Area","keywords":["gold","sparkling","highlight","animation"]},{"name":"Shadow","keywords":["shadow","drop shadow"]}]},"IMG_0992":{"title":"Shader: IMG_0992","layers":[{"name":"Wall","keywords":["wall","gradient","background"]},{"name":"Floor","keywords":["floor","perspective","animated","panning","tiles"]},{"name":"Shelves","keywords":["shelves","items","pulse","left"]},{"name":"Floating Objects","keywords":["floating","objects","purple","green","undulating","animation"]}]},"IMG_0995":{"title":"Shader: IMG_0995","layers":[{"name":"Background","keywords":["background","wall","plaster","texture"]},{"name":"Balcony Wall","keywords":["balcony","wall","teal","gradient"]},{"name":"Clothesline","keywords":["clothesline","clothes","shirts","hanging","sway","animation"]},{"name":"Bird Cage","keywords":["bird","cage","bamboo","hook"]},{"name":"Plant","keywords":["plant","pot","vines","leaves","terracotta"]}]},"IMG_1011":{"title":"Shader: IMG_1011","layers":[{"name":"Wall Tiles","keywords":["wall","tiles","pattern","beige","brown","animation"]},{"name":"Floor Tiles","keywords":["floor","tiles","stone","grey","animation"]},{"name":"Chair Shadow","keywords":["shadow","chair","dark"]},{"name":"Woven Chair","keywords":["chair","woven","blue","metal","frame","silver","fabric","holes"]}]},"IMG_1014":{"title":"Shader: IMG_1014","layers":[{"name":"Floor","keywords":["floor","stone","texture","background"]},{"name":"Chair","keywords":["chair","plastic","blue","floral","cutout","embossed","reflection","animation"]},{"name":"Chair Leg","keywords":["chair leg","cylinder","blue"]}]},"IMG_1061":{"title":"Shader: IMG_1061","layers":[{"name":"Background","keywords":["background","pink","pattern"]},{"name":"Red Bar","keywords":["red bar","text","gold","shine","animation"]},{"name":"Text 2K4","keywords":["text","2K4","red","outline"]},{"name":"God Of Wealth","keywords":["god","wealth","figure","gold","red","green","glimmer"]},{"name":"Bottom Numbers","keywords":["numbers","bottom","red","grid"]},{"name":"Dark Edges","keywords":["dark","edges","brown","fabric","texture"]}]},"IMG_1072":{"title":"Shader: IMG_1072","layers":[{"name":"Background","keywords":["background","street","wall","gate","flowers","yellow","wind","animation"]},{"name":"Cart","keywords":["cart","blue","shine","animation"]},{"name":"Lottery Tickets","keywords":["lottery","tickets","white","pink","rectangles","grid"]},{"name":"Woman","keywords":["woman","green shirt","black pants","fabric","wrinkles","animation"]},{"name":"Man","keywords":["man","dark shirt"]}]},"IMG_1077":{"title":"Shader: IMG_1077","layers":[{"name":"Metallic Plate","keywords":["metal","plate","reflection","animation","background"]},{"name":"Holes","keywords":["holes","grid","dark"]},{"name":"Colored Pencils","keywords":["pencils","colors","orange","blue","peach","texture"]},{"name":"Laser Beam","keywords":["laser","beam","spot","red","glow","animation"]}]},"IMG_1121":{"title":"Shader: IMG_1121","layers":[{"name":"Poster","keywords":["poster","dark","blue","grey","red","car","text","abstract"]},{"name":"Street","keywords":["street","gravel","pavement"]},{"name":"Scooter","keywords":["scooter","red","shine","reflection","animation"]},{"name":"Rider","keywords":["rider","shirt","grey","helmet","white","mask","blue","folds","fabric"]}]},"IMG_1126":{"title":"Shader: IMG_1126","layers":[{"name":"Background","keywords":["motorcycle","body","dark","background"]},{"name":"Chrome Exhaust","keywords":["exhaust","chrome","silver","reflection","shine","animation"]},{"name":"Suspension Spring","keywords":["suspension","spring","coil","metal","highlight"]},{"name":"Carbon Fiber Plates","keywords":["carbon fiber","plates","silver","texture","pattern","animation"]}]},"IMG_1151":{"title":"Shader: IMG_1151","layers":[{"name":"Road","keywords":["road","asphalt","background","texture"]},{"name":"Motorcycle Parts","keywords":["motorcycle","tail light","red","license plate","pulse","animation"]},{"name":"Trousers Leg","keywords":["trousers","pants","leg","pattern","floral","fabric","fold","animation"]},{"name":"Foot And Shoe","keywords":["foot","shoe","brown","leather","shine"]}]},"IMG_1183":{"title":"Shader: IMG_1183","layers":[{"name":"Roller Door","keywords":["roller door","metal","grooves","dirt","background"]},{"name":"Posters","keywords":["posters","paper","text","torn","flutter","animation"]},{"name":"Scooter","keywords":["scooter","handlebar","plastic","grip","shine"]}]},"IMG_1185":{"title":"Shader: IMG_1185","layers":[{"name":"Wooden Floor","keywords":["wood","floor","planks","grain","background"]},{"name":"Main Body Wrapped","keywords":["motorcycle","cardboard","box","tape","text","red","blue"]},{"name":"Front Wheel","keywords":["wheel","tire","rim","spokes","silver"]},{"name":"Back Part Wrapping","keywords":["wrapping","paper","white","blue","pattern","tape"]}]},"IMG_1213":{"title":"Shader: IMG_1213","layers":[{"name":"Background","keywords":["background","base"]},{"name":"Golden Frame","keywords":["frame","gold","lattice","shadow"]},{"name":"Scene Sky","keywords":["scene","sky","gradient","clouds","animation"]},{"name":"Scene Mountain","keywords":["scene","mountain"]},{"name":"Scene Ocean","keywords":["scene","ocean","waves","parallax","glints","animation"]},{"name":"Scene Boat","keywords":["scene","boat","sailing","waves","mast","sail","animation"]},{"name":"Scene Balcony","keywords":["scene","balcony","railing","flowers","wind","rustle","animation"]}]},"IMG_1240":{"title":"Shader: IMG_1240","layers":[{"name":"Signboard","keywords":["signboard","dark","frame"]},{"name":"Text GIAI","keywords":["text","green","giai"]},{"name":"Text NUOC MIA","keywords":["text","red","nuoc mia","outline"]},{"name":"Sugarcane Stalks","keywords":["sugarcane","stalks","bamboo","segments","green"]},{"name":"Sugarcane Leaves","keywords":["sugarcane","leaves","green","vein"]}]},"IMG_1241":{"title":"Shader: IMG_1241","layers":[{"name":"Ground","keywords":["ground","floor","texture","dirt","grime","background"]},{"name":"Chair","keywords":["chair","seat","backrest","legs","plastic","wear","dirt"]},{"name":"License Plate","keywords":["license plate","blue","border","text","shimmer","animation","string"]},{"name":"Vignette","keywords":["vignette","darken","effect"]}]},"IMG_1276":{"title":"Shader: IMG_1276","layers":[{"name":"Walls","keywords":["walls","grey","background"]},{"name":"Terracotta Roof","keywords":["roof","terracotta","tiles","3D","background"]},{"name":"Courtyard Tarp","keywords":["tarp","fabric","flap","folds","animation"]},{"name":"Railings","keywords":["railings","bars","metal","highlight"]},{"name":"Tree","keywords":["tree","trunk","branches","wood","bark","leaves","rustle","animation"]},{"name":"Falling Leaves","keywords":["leaves","falling","spin","animation"]},{"name":"Shadows","keywords":["shadows","darken"]}]},"IMG_1300":{"title":"Shader: IMG_1300","layers":[{"name":"Background","keywords":["background","white","grey"]},{"name":"Vest Body","keywords":["vest","tactical","fabric","texture","molle","webbing","pads"]},{"name":"Pouches","keywords":["pouches","zipper","pockets"]},{"name":"Arm Attachments","keywords":["arm","attachments","protectors","straps"]},{"name":"Collar","keywords":["collar","hood","folds","fabric"]},{"name":"Shadows","keywords":["shadows","darken"]}]},"IMG_1301":{"title":"Shader: IMG_1301","layers":[{"name":"Sky","keywords":["sky","cyan","blue","background"]},{"name":"Flag","keywords":["flag","purple","spikes","waves","cross","texture","animation"]}]},"IMG_1306":{"title":"Shader: IMG_1306","layers":[{"name":"Background Road","keywords":["road","street","city","buildings","trees","car lights","background"]},{"name":"Metal Frame","keywords":["frame","metal","bars","rusty","grey"]},{"name":"Blue Cloth","keywords":["cloth","tarp","blue","folds","wind","animation","knot"]},{"name":"Plastic Bags","keywords":["bags","plastic","white","translucent","wind","animation"]},{"name":"Shadows","keywords":["shadows","darken"]}]},"IMG_1308":{"title":"Shader: IMG_1308","layers":[{"name":"Background","keywords":["background","paper","texture"]},{"name":"Head","keywords":["head","hair","outline","ink"]},{"name":"Kite Garment","keywords":["garment","kite","spine","shoulders","sleeves","legs","ink","lines"]},{"name":"Shading","keywords":["shading","crosshatching","ink","animation"]},{"name":"Text Forbes","keywords":["text","forbes","ink","letters"]}]},"IMG_1309":{"title":"Shader: IMG_1309","layers":[{"name":"Background","keywords":["background","white","studio"]},{"name":"Kite","keywords":["kite","diamond","colors","paper","texture","wind","animation","sticks","bamboo"]},{"name":"Kite Tail","keywords":["kite","tail","brown","folds","flap","animation"]},{"name":"Shadows","keywords":["shadows","drop shadow"]}]},"IMG_1323":{"title":"Shader: IMG_1323","layers":[{"name":"Sign Box","keywords":["sign","box","tilted","yellow","orange","border","red"]},{"name":"Text A LOC","keywords":["text","red","a loc","glow"]},{"name":"Text CHAO LONG","keywords":["text","blue","chao long","glow"]},{"name":"Tree Bark","keywords":["tree","bark","background"]},{"name":"Neon Reflections","keywords":["neon","reflections","glow","background","animation"]}]},"IMG_1333":{"title":"Shader: IMG_1333","layers":[{"name":"Background","keywords":["background","white","grey","vignette"]},{"name":"Hood","keywords":["hood","head","face","hat","fabric","wind","animation"]},{"name":"Raincoat","keywords":["raincoat","jacket","pants","sleeves","buttons","belt","epaulets","collar","texture","plastic","flutter","animation"]},{"name":"Hands","keywords":["hands","skin"]},{"name":"Shoes","keywords":["shoes","black","shine"]},{"name":"Floor","keywords":["floor","tiles","shadow","background"]}]},"IMG_1348":{"title":"Shader: IMG_1348","layers":[{"name":"Background Walls","keywords":["background","walls","temple"]},{"name":"God Rays","keywords":["rays","light","volumetric","animation","window"]},{"name":"Pillar","keywords":["pillar","column","dragon","relief","carved","clouds","red","gold"]},{"name":"Headpiece","keywords":["headpiece","wood","grain","glimmer","animation","horns"]},{"name":"Veil And Emblem","keywords":["veil","fabric","face","eyes","blink","mouth","emblem","divine eye","tracking","animation","hands","grip"]}]},"IMG_1399":{"title":"Shader: IMG_1399","layers":[{"name":"Store Interior","keywords":["store","interior","brick","wall","racks","clothes","background"]},{"name":"Checkered Floor","keywords":["floor","checkered","perspective","background"]},{"name":"Main Subject Torso","keywords":["subject","torso","shirt","striped","snoopy","pocket","collar","buttons"]},{"name":"Skin Arms Neck","keywords":["skin","arms","neck","hands"]},{"name":"Shorts","keywords":["shorts","striped","clothing"]},{"name":"Bare Legs","keywords":["legs","skin"]},{"name":"Head","keywords":["head","hair","ears","face"]},{"name":"Phone","keywords":["phone","camera","reflection","flash"]}]},"IMG_1415":{"title":"Shader: IMG_1415","layers":[{"name":"Runway Background","keywords":["runway","background","gradient","panels"]},{"name":"Model Shared Features","keywords":["model","head","skin","hair","sunglasses","lips","arms"]},{"name":"Model Left","keywords":["model 1","left","puffy top","pants","belt bag","animation"]},{"name":"Model Center","keywords":["model 2","center","dress","parachute","folds","wind","animation"]},{"name":"Model Right","keywords":["model 3","right","cape","poncho","skirt","belt bag"]}]},"IMG_1426":{"title":"Shader: IMG_1426","layers":[{"name":"Sky","keywords":["sky","blue","painted"]},{"name":"Clouds","keywords":["clouds","painted","drifting","texture"]},{"name":"Birds","keywords":["birds","flock","painted","animation"]},{"name":"Rocks","keywords":["rocks","peaks","texture","crevice","crack","roots","bonsai"]},{"name":"Plants","keywords":["plants","ferns","leaves","animation","rustle"]},{"name":"Vignette","keywords":["vignette"]}]},"IMG_1449":{"title":"Shader: IMG_1449","layers":[{"name":"Mosaic Wall","keywords":["mosaic","wall","glassy","tiles","sparkle","background"]},{"name":"Gold Frame","keywords":["frame","gold","ornate"]},{"name":"Padding","keywords":["padding","leather","quilted","silver","buttons","diamond"]},{"name":"Wood Frame","keywords":["frame","wood","grain","center"]},{"name":"Inside Wall","keywords":["wall","grungy","wires","blue","black","exposed"]}]},"IMG_1621":{"title":"Shader: IMG_1621","layers":[{"name":"Background","keywords":["background","wall","cafe","store front","ivy","leaves","animation"]},{"name":"Floor","keywords":["floor","tiles","perspective","animation"]},{"name":"Wood Rack","keywords":["wood","rack","display","carvings","ornamental"]},{"name":"Chair Table","keywords":["chair","table","green","background"]},{"name":"Motorcycle","keywords":["motorcycle","honda cub","wheels","spokes","red","seat","leather","engine","exhaust","pipe","animation"]}]},"IMG_1644":{"title":"Shader: IMG_1644","layers":[{"name":"Background","keywords":["background","blue","white"]},{"name":"Text Elements","keywords":["text","graphic","red","green","animation"]},{"name":"Top Impact","keywords":["impact","hammer","burst","falling","motion lines","animation"]},{"name":"Bottom Impact","keywords":["impact","block","burst","falling","motion lines","animation"]},{"name":"Hardhat","keywords":["hardhat","helmet","yellow","safety"]},{"name":"Family Group","keywords":["family","father","mother","child","safety"]}]},"IMG_1663":{"title":"Shader: IMG_1663","layers":[{"name":"Sign Area","keywords":["sign","blue","top","background"]},{"name":"Pavement","keywords":["pavement","concrete","pebbles","bottom","background"]},{"name":"Decorative Iron Gate","keywords":["iron","gate","decorative","patterns","circles","diamonds","bricks","mesh"]},{"name":"Red Warning Sign","keywords":["warning","sign","red","yellow","text"]}]},"IMG_1714":{"title":"Shader: IMG_1714","layers":[{"name":"Storefront Alley","keywords":["storefront","alley","person","motorcycle","shadow"]},{"name":"White Wall","keywords":["wall","white","grunge","crack"]},{"name":"Decorative Vent Block","keywords":["vent","block","decorative","pattern","grid","holes","animation"]}]},"IMG_1718":{"title":"Shader: IMG_1718","layers":[{"name":"Background","keywords":["wall","white","concrete","path","background"]},{"name":"Iron Gate","keywords":["gate","iron","blue","ornate","pattern","star","cross","animation"]},{"name":"House Sign","keywords":["sign","house","number","brass","gold"]},{"name":"Plant","keywords":["plant","palm","pot","stem","leaves","animation"]},{"name":"Vines Leaves","keywords":["vines","leaves","overhanging","sky","top"]}]},"IMG_1720":{"title":"Shader: IMG_1720","layers":[{"name":"Background","keywords":["wall","white","grey","background"]},{"name":"Ground Path","keywords":["ground","path","pavement","brick","animation"]},{"name":"Right Wall Column","keywords":["wall","column","stone","texture"]},{"name":"White Gate Structure","keywords":["gate","white","structure","sunburst","fan","swirls","rust","borna","sign"]},{"name":"Top Foliage","keywords":["foliage","leaves","overhanging","green","animation"]},{"name":"Motorcycle","keywords":["motorcycle","scooter","fairing","seat","wheels","mirror"]}]},"IMG_1721":{"title":"Shader: IMG_1721","layers":[{"name":"Background Wall","keywords":["wall","background","siding"]},{"name":"Bottom Stone","keywords":["stone","bottom","background"]},{"name":"Top Trim","keywords":["trim","top","dark","animation"]},{"name":"Strep Text","keywords":["text","strep"]},{"name":"Cartoon Character","keywords":["character","cartoon","body","head","eyes","mouth","arms","animation"]}]},"IMG_1725":{"title":"Shader: IMG_1725","layers":[{"name":"Storefront Structure","keywords":["storefront","frame","wood"]},{"name":"Left Display Panel","keywords":["panel","menu","text","chinese","animation"]},{"name":"Upper Display Case","keywords":["display","case","horse","rider","sky","water","mountain","tree","animation"]},{"name":"Lower Display Case","keywords":["display","case","people","scene","room","figures","table"]},{"name":"Curvy Wooden Dividers","keywords":["dividers","wood","curvy","wavy"]},{"name":"Lower Items","keywords":["items","basket","bowl","lower"]}]},"IMG_1847":{"title":"Shader: IMG_1847","layers":[{"name":"Background","keywords":["wall","grey","stone","concrete","texture"]},{"name":"Carved Symbol","keywords":["carving","symbol","stone","frame","highlight","shadow"]},{"name":"Top Louvers","keywords":["louvers","vents","top","white","black"]},{"name":"Bottom Surface","keywords":["surface","shelf","table","bottom","speckle","granite"]}]},"IMG_2148":{"title":"Shader: IMG_2148","layers":[{"name":"Sky","keywords":["sky","background"]},{"name":"Road","keywords":["road","parallax","scrolling","speed","lines","dashed"]},{"name":"Buildings","keywords":["buildings","parallax","background","windows","shadow"]},{"name":"Power Lines","keywords":["power lines","swooping"]},{"name":"Scooter Rider","keywords":["scooter","rider","bouncing","physics","headlight","wheels","helmet","jacket"]},{"name":"Bundles","keywords":["bundles","bouncing","bags","woven","ropes","smoke","exhaust"]}]},"IMG_2249":{"title":"Shader: IMG_2249","layers":[{"name":"Ceiling","keywords":["ceiling","top","texture","noise"]},{"name":"Lattice Structure","keywords":["lattice","brick","mortar","hole","glass","reflection","light","sweep","animation"]},{"name":"Side Walls","keywords":["side","walls","texture","bounding"]}]},"IMG_2250":{"title":"Shader: IMG_2250","layers":[{"name":"Ceiling","keywords":["ceiling","top"]},{"name":"Stone Base","keywords":["stone","base","floor","ledge","pattern"]},{"name":"Lattice Wall","keywords":["lattice","wall","brick","curve","sky","clouds","animation","shadow"]},{"name":"Side Pillars","keywords":["side","pillars","railing","blue"]}]},"IMG_2263":{"title":"Shader: IMG_2263","layers":[{"name":"Background","keywords":["trees","sky","leaves","animation","background"]},{"name":"Ground","keywords":["ground","asphalt"]},{"name":"Blue Pillar","keywords":["pillar","blue","stone","sign"]},{"name":"Blue Gate Structure","keywords":["gate","blue","structure","rings","pattern","animation"]},{"name":"Fence","keywords":["fence","mesh","diamond"]}]},"IMG_2450":{"title":"Shader: IMG_2450","layers":[{"name":"Background","keywords":["paper","texture","background"]},{"name":"Head Structure","keywords":["head","outline","hair","brain","veins","heart","animation","ear"]},{"name":"Label Texts","keywords":["label","text","scribble","title","lines"]}]},"IMG_2477":{"title":"Shader: IMG_2477","layers":[{"name":"Background","keywords":["paper","base","texture"]},{"name":"Top Certificate","keywords":["certificate","top","red","border","diplome","stamp","signature","animation"]},{"name":"Bottom Certificate","keywords":["certificate","bottom","green","border","leaves","photo","portrait","signature","animation"]}]},"IMG_2586":{"title":"Shader: IMG_2586","layers":[{"name":"Background","keywords":["wall","metal","gate","floor","brick","perspective","animation"]},{"name":"Table Benches","keywords":["table","bench","isometric","perspective","chessboard","tiles","shine","animation"]}]},"IMG_2700":{"title":"Shader: IMG_2700","layers":[{"name":"Background","keywords":["night","street","tree","lights","glow","animation","background"]},{"name":"Trash Bin","keywords":["trash","bin","green"]},{"name":"Main Signboard","keywords":["signboard","text","neon","pulse","cursive","oval"]},{"name":"Sign Frame Cart","keywords":["frame","cart","wheel","bicycle","spokes","animation"]}]},"IMG_2703":{"title":"Shader: IMG_2703","layers":[{"name":"Background","keywords":["white","grid","background"]},{"name":"Fibonacci Trend Lines","keywords":["fibonacci","trend","lines","intersections","animation"]},{"name":"Horizontal Price Levels","keywords":["horizontal","price","levels","shifting"]},{"name":"Candlesticks","keywords":["candlesticks","moving","prices","wicks","body","scrolling","volatility"]},{"name":"Right Sidebar","keywords":["sidebar","price","axis","ticks","label"]}]},"IMG_2745":{"title":"Shader: IMG_2745","layers":[{"name":"Background","keywords":["ceiling","wall","tile","red","peeling","paint","mortar"]},{"name":"Lantern Wire","keywords":["wire","lantern","swing","animation"]},{"name":"Lantern","keywords":["lantern","light","flicker","ribs","characters","scrolling","ring","decorative"]},{"name":"Tassel","keywords":["tassel","strands","swaying","fringe","gold","cap"]}]},"IMG_2768":{"title":"Shader: IMG_2768","layers":[{"name":"Altar Cloth","keywords":["altar","cloth","silk","shimmer","folds","patterns","characters","tassels"]},{"name":"Floor","keywords":["floor","perspective","tiles","mat","lighting","flash"]},{"name":"Feet","keywords":["feet","bobbing","toes","pants"]}]},"IMG_2773":{"title":"Shader: IMG_2773","layers":[{"name":"Wall Base","keywords":["wall","tile","red","texture","animation"]},{"name":"Awning","keywords":["awning","stripes","perspective","shadows"]},{"name":"Symbol","keywords":["symbol","circle","yellow","yin-yang","trigram","nodes","pulse"]},{"name":"Leaves Overlay","keywords":["leaves","overlay","wind","shadows","animation"]}]},"IMG_2982":{"title":"Shader: IMG_2982","layers":[{"name":"Background","keywords":["dark","background"]},{"name":"Shrine Structure","keywords":["shrine","roof","latticework","interior","figures","arch","railing","cracks","weathering","lighting"]},{"name":"Floor Shadow","keywords":["shadow","floor"]}]},"IMG_3105":{"title":"Shader: IMG_3105","layers":[{"name":"Sky","keywords":["sky","clouds","trees","animation"]},{"name":"Roof Behind","keywords":["roof","behind"]},{"name":"Cat","keywords":["cat","fur","eyes","tail","animation"]},{"name":"Main Wall","keywords":["wall","texture","grungy","peeling","paint","shadow"]},{"name":"Exposed Brick","keywords":["brick","broken","wall","mortar","dirt"]}]},"IMG_3714":{"title":"Shader: IMG_3714","layers":[{"name":"Asphalt","keywords":["asphalt","scroll","noise","manhole"]},{"name":"Arrow Shape","keywords":["arrow","shape","paint","cracks"]},{"name":"Feet Clothes","keywords":["feet","clothes","walking","shoes","animation"]}]},"IMG_3922":{"title":"Shader: IMG_3922","layers":[{"name":"Background","keywords":["yellow","sweep","light","blue","edge","animation"]},{"name":"Text Letters","keywords":["text","red","letters","pulse","sdf","hand-written"]},{"name":"Food Picture","keywords":["food","picture","bowl","noodles","steam","animation"]}]},"IMG_4080":{"title":"Shader: IMG_4080","layers":[{"name":"Fabric","keywords":["fabric","yellow-green","wave","wrinkles","animation"]},{"name":"Red Symbols","keywords":["symbols","red","square","star","insect","wings","text"]},{"name":"Wood Slats","keywords":["wood","slats","top right"]},{"name":"Ground","keywords":["ground","grey","noise"]}]},"IMG_4742":{"title":"Shader: IMG_4742","layers":[{"name":"Street","keywords":["street","scroll","noise"]},{"name":"Person","keywords":["person","body","legs","clothes","hat","shoes","animation"]},{"name":"Bags","keywords":["bags","basket","plaid","swing"]},{"name":"Motorcycle","keywords":["motorcycle","headlight"]}]},"IMG_4782":{"title":"Shader: IMG_4782","layers":[{"name":"Wall","keywords":["wall","beige","lighting","animation"]},{"name":"Floor","keywords":["floor","wooden","lines"]},{"name":"Window","keywords":["window","clouds","animation"]},{"name":"Table","keywords":["table","legs","top","tablecloth","lace"]},{"name":"Chair","keywords":["chair"]},{"name":"Figure","keywords":["figure","sparkles","gold","clothes"]},{"name":"Grid Borders","keywords":["grid","borders"]}]},"IMG_4783":{"title":"Shader: IMG_4783","layers":[{"name":"Wall","keywords":["wall","white","blue","panel"]},{"name":"Floor","keywords":["floor","tiled","reflection","sweeping"]},{"name":"TV","keywords":["tv","painting","screen","flicker","animation"]},{"name":"Plant","keywords":["plant","pot","leaves","swaying"]},{"name":"Furniture","keywords":["furniture","table","legs","cloth","black"]},{"name":"Figure","keywords":["figure","black","gold","pattern","animation"]},{"name":"Grid Borders","keywords":["grid","borders"]}]},"IMG_4784":{"title":"Shader: IMG_4784","layers":[{"name":"Background","keywords":["white","background"]},{"name":"Blue Patterned Dress","keywords":["blue","dress","pattern","shimmer"]},{"name":"Grey Suit","keywords":["grey","suit","shirt","tie","sitting"]},{"name":"Maroon Dress","keywords":["maroon","dress","wrinkles","animation"]},{"name":"Black Floral Dress","keywords":["black","floral","dress","pattern","animation"]},{"name":"Black Formal Ao Dai","keywords":["black","formal","ao dai","silver","pattern","shimmer"]}]},"IMG_5181":{"title":"Shader: IMG_5181","layers":[{"name":"Concrete Background","keywords":["concrete","noise","shadow","sweeping"]},{"name":"Card","keywords":["card","grid","border","orange","flash","numbers","scanner","laser"]}]},"IMG_5206":{"title":"Shader: IMG_5206","layers":[{"name":"Background","keywords":["wall","panel","texture","weave","animation"]},{"name":"Left Sign","keywords":["sign","reflection","text","rivets"]},{"name":"Golden Bat","keywords":["bat","sculpture","gold","animation","eyes","glow"]}]},"IMG_5211":{"title":"Shader: IMG_5211","layers":[{"name":"Background","keywords":["background","grey","glass","reflection","animation","line"]},{"name":"Top Reflection","keywords":["reflection","white","blue","blocks","swaying"]},{"name":"Leaf","keywords":["leaf","green","veins","stem","gold","decorations","calligraphy","grapes","vines","trim"]},{"name":"Specular Highlight","keywords":["specular","highlight","glass"]}]},"IMG_5212":{"title":"Shader: IMG_5212","layers":[{"name":"Sky","keywords":["sky","blue","clouds","animation"]},{"name":"Green Building","keywords":["building","green","netting","grid"]},{"name":"Crane","keywords":["crane","yellow","swaying","animation"]},{"name":"White Building","keywords":["building","white","windows","glass","glimmer","sign"]},{"name":"Roof","keywords":["roof","brown","tiles","texture"]},{"name":"Terrace Walls","keywords":["walls","beige","window","pergola","columns"]},{"name":"Terrace Floor","keywords":["floor","terrace","concrete","perspective","shadows"]},{"name":"Balustrade","keywords":["balustrade","wall","pillars","band","gold","pattern"]}]},"IMG_5214":{"title":"Shader: IMG_5214","layers":[{"name":"Background","keywords":["background","gradient","light","shadows"]},{"name":"Vase","keywords":["vase","bronze","profile","decorations","grapes","leaves"]}]},"IMG_5215":{"title":"Shader: IMG_5215","layers":[{"name":"Background","keywords":["panels","text","lines"]},{"name":"Smoke","keywords":["smoke","animation","drift"]},{"name":"Object","keywords":["object","base","cage","cap","embers","rings","spikes","rotation"]}]},"IMG_5225":{"title":"Shader: IMG_5225","layers":[{"name":"Image Center","keywords":["dome","perspective","foliage","people","animation"]},{"name":"Top Area","keywords":["text","blinking","animation"]},{"name":"Bottom Area","keywords":["slider","animation","heart","pulsing"]}]},"IMG_5251":{"title":"Shader: IMG_5251","layers":[{"name":"Fabric","keywords":["fabric","waving"]},{"name":"Woman","keywords":["woman","face","hair","lip","hand"]},{"name":"Flowers","keywords":["flowers","fuchsia","pulsing","leaf"]},{"name":"Hat","keywords":["hat","shadow","gleam","rings"]}]},"IMG_5320":{"title":"Shader: IMG_5320","layers":[{"name":"Background","keywords":["background","walls","floor","reflection","text"]},{"name":"Tripod","keywords":["tripod","pole","legs"]},{"name":"Hanging Plant","keywords":["plant","leaves","rustling"]},{"name":"Swinging Head","keywords":["head","mannequin","swinging","hair","face","eyes","blinking"]},{"name":"Swinging String","keywords":["string","swinging"]}]},"IMG_5326":{"title":"Shader: IMG_5326","layers":[{"name":"Background","keywords":["background","noise"]},{"name":"Sign","keywords":["sign","red","P","outline","arrow","animation"]},{"name":"Leaves","keywords":["leaves","green","shading","vein","sway"]}]},"IMG_5346":{"title":"Shader: IMG_5346","layers":[{"name":"Banner","keywords":["banner","red","XIII","text","glint","animation"]},{"name":"Background","keywords":["background","wall","floor","pattern"]},{"name":"Plants","keywords":["plants","pots","leaves","sway","flowers"]}]},"IMG_5506":{"title":"Shader: IMG_5506","layers":[{"name":"Top Section","keywords":["UI","text","logo","pulsing","animation"]},{"name":"Bottom Photo","keywords":["photo","noise"]},{"name":"Lasers","keywords":["lasers","sweep","animation"]},{"name":"Foreground","keywords":["DJ","equipment","screens","glare"]}]},"IMG_5517":{"title":"Shader: IMG_5517","layers":[{"name":"Ground","keywords":["ground","arc","radial","lines"]},{"name":"Pillar Base","keywords":["pillar","base","tiled","grout","specular","rolling"]},{"name":"Top Column","keywords":["column","smooth"]},{"name":"Decorative Tile","keywords":["decorative","tile","flower","rotating"]}]},"IMG_5538":{"title":"Shader: IMG_5538","layers":[{"name":"Background","keywords":["background","noise"]},{"name":"Motorcycle Wheel","keywords":["wheel","tire","spokes","animation","rotating"]},{"name":"Red Panel","keywords":["panel","red","highlights","decal","key"]},{"name":"Seat","keywords":["seat","black","text"]},{"name":"Grab Rail","keywords":["grab","rail","black","tube"]}]},"IMG_5569":{"title":"Shader: IMG_5569","layers":[{"name":"Background","keywords":["background","floor","noise"]},{"name":"Rock Speaker","keywords":["rock","speaker","texture","lighting","moss"]},{"name":"Speaker Holes","keywords":["speaker","holes","mesh","vibration","animation"]},{"name":"Logo","keywords":["logo","TIC"]},{"name":"Cable","keywords":["cable"]}]},"IMG_6550":{"title":"Shader: IMG_6550","layers":[{"name":"Background","keywords":["wall","pink","maroon","trim"]},{"name":"Roof","keywords":["roof","tile","shimmer","animation"]},{"name":"Symbols","keywords":["symbol","maroon"]},{"name":"Gate","keywords":["gate","yellow","metal","shine","rotation","animation"]},{"name":"Fence","keywords":["fence","green","sliding"]},{"name":"Noise","keywords":["noise","overlay"]}]},"IMG_6551":{"title":"Shader: IMG_6551","layers":[{"name":"Wall","keywords":["wall","noise"]},{"name":"Railing","keywords":["railing","balcony","lotus","animation","bloom"]},{"name":"Window","keywords":["window","frame","grille","pattern","scrolling","animation"]},{"name":"Roof and Dragon","keywords":["roof","dragon","tiles","flowing","shimmering","gold"]}]},"IMG_6574":{"title":"Shader: IMG_6574","layers":[{"name":"Sky","keywords":["sky","gradient"]},{"name":"Trees","keywords":["trees","swaying","animation","noise"]},{"name":"Ground","keywords":["ground","dots","animation"]},{"name":"Poles","keywords":["poles","shading"]},{"name":"Windows","keywords":["windows","blue"]},{"name":"Moto","keywords":["moto","animation","wheels","body","fairing","tail","pipe"]},{"name":"Noise","keywords":["noise","overlay"]}]},"IMG_6609":{"title":"Shader: IMG_6609","layers":[{"name":"Background","keywords":["background","green","wavy","corrugated","floor","vignette"]},{"name":"Lattice Panels","keywords":["lattice","panels","shifting","animation","frame"]},{"name":"Center Panel","keywords":["center","painting","horse","person","flowing","hair","cloth"]}]},"IMG_6693":{"title":"Shader: IMG_6693","layers":[{"name":"Shelves","keywords":["shelves","store","colors","animation"]},{"name":"Side Object","keywords":["side","object"]},{"name":"Figure","keywords":["figure","breathing","shirt","pants","arms","head","animation"]},{"name":"Shadow","keywords":["shadow","figure"]}]},"IMG_6694":{"title":"Shader: IMG_6694","layers":[{"name":"Shelves","keywords":["shelves","store","noise","items","flowing"]},{"name":"Side Object","keywords":["side","object"]},{"name":"Figure","keywords":["figure","breathing","shirt","pants","arms","head","folds","animation"]},{"name":"Shadow","keywords":["shadow","figure"]}]},"IMG_6695":{"title":"Shader: IMG_6695","layers":[{"name":"Background","keywords":["background","blur","noise","drifting"]},{"name":"Hat","keywords":["hat","white"]},{"name":"Face","keywords":["face","skin","tone"]},{"name":"Torso","keywords":["torso","uniform","blue","orange","collar","metallic","sweep"]},{"name":"Arms","keywords":["arms","hands","cuffs"]},{"name":"Pants","keywords":["pants","trim"]},{"name":"Shadow","keywords":["shadow"]}]},"IMG_6697":{"title":"Shader: IMG_6697","layers":[{"name":"Panel","keywords":["panel","seams","dirt","stains"]},{"name":"Torn Poster","keywords":["poster","torn","edges","fluttering","grid"]},{"name":"Top Center Poster","keywords":["poster","top","center","lightning","animation"]},{"name":"Stickers","keywords":["stickers","green","blue"]}]},"IMG_6755":{"title":"Shader: IMG_6755","layers":[{"name":"Background","keywords":["background","noise","light","spots"]},{"name":"Shadow","keywords":["shadow","pulsing","arrow"]},{"name":"Cutout","keywords":["cutout","figure","arrow","outline","fill"]},{"name":"Vignette","keywords":["vignette"]}]},"IMG_6786":{"title":"Shader: IMG_6786","layers":[{"name":"Background","keywords":["background","wood","table","grain","animation"]},{"name":"Ticket","keywords":["ticket","bend","float","grid","numbers","shading"]},{"name":"Hand","keywords":["hand","fingers","tapping","animation","skin"]}]},"IMG_6793":{"title":"Shader: IMG_6793","layers":[{"name":"Background","keywords":["background","white","vignette"]},{"name":"Casing","keywords":["casing","grey","box"]},{"name":"Screen","keywords":["screen","led","grid","border","rgb"]},{"name":"Scrolling Content","keywords":["scrolling","logo","text","animation"]}]},"IMG_6876":{"title":"Shader: IMG_6876","layers":[{"name":"Background","keywords":["background","white","lines","text","blocks"]},{"name":"Protein","keywords":["protein","structure","pulsating","rotating","dots","flowing"]},{"name":"Zoom Circles","keywords":["zoom","circles","floating","pink"]},{"name":"Lines","keywords":["lines","flowing","pink"]},{"name":"Vignette","keywords":["vignette"]}]},"IMG_6994":{"title":"Shader: IMG_6994","layers":[{"name":"Floor","keywords":["floor","tiles","panning","shadow"]},{"name":"Wall","keywords":["wall","reflection","posters","parallax"]},{"name":"Figure","keywords":["figure","walking","bounce","box","arms","legs","boots"]},{"name":"Vignette","keywords":["vignette"]}]},"IMG_7211":{"title":"Shader: IMG_7211","layers":[{"name":"Background","keywords":["background","dark"]},{"name":"Top Bar","keywords":["top","bar","status","static"]},{"name":"Bottom Bar","keywords":["bottom","bar","static"]},{"name":"Scroll Content","keywords":["scroll","content","header","menu","tabs","ads","cards","book"]}]},"IMG_7225":{"title":"Shader: IMG_7225","layers":[{"name":"Background","keywords":["background","shadow"]},{"name":"Box Base","keywords":["box","green","noise","bevel","edges","red"]},{"name":"Text NHANG","keywords":["text","nhang","gold","typography"]},{"name":"Text THAN TAI","keywords":["text","than","tai","yellow"]},{"name":"Gold Seal","keywords":["gold","seal","ornate","dragon","motif"]}]},"IMG_7266":{"title":"Shader: IMG_7266","layers":[{"name":"Background","keywords":["background","concrete","noise"]},{"name":"Character Shape","keywords":["character","shape","fortune","calligraphic"]},{"name":"Mosaic","keywords":["mosaic","voronoi","shards","porcelain","crackle","shadow"]}]},"IMG_7408":{"title":"Shader: IMG_7408","layers":[{"name":"Background","keywords":["background","fabric","rolls"]},{"name":"Blue Shirt","keywords":["blue","shirt","wrinkles","collar","pockets"]},{"name":"White Shirt","keywords":["white","shirt","epaulettes","collar","pockets"]},{"name":"Star Badge","keywords":["star","badge","gold","red"]}]},"IMG_7409":{"title":"Shader: IMG_7409","layers":[{"name":"Background","keywords":["background","concrete","grey","floor","noise"]},{"name":"Background Garments","keywords":["garments","green","tarp","red","shirt"]},{"name":"White Shirt","keywords":["white","shirt","tie","epaulettes","collar"]},{"name":"Blue Shirt","keywords":["blue","shirt","wrinkles","epaulettes","buttons"]}]},"IMG_7410":{"title":"Shader: IMG_7410","layers":[{"name":"Background","keywords":["background","walls","floors","grout"]},{"name":"Shelves","keywords":["shelves","boots","leather"]},{"name":"Mannequin","keywords":["mannequin","uniform","cap","shoes","buttons","belt"]}]},"IMG_7680":{"title":"Shader: IMG_7680","layers":[{"name":"Sky","keywords":["sky","night","stars","starfield","twinkle"]},{"name":"Building","keywords":["building","silhouette","roof"]},{"name":"String Lights","keywords":["string","lights","wire","bulbs","glow","blinking"]},{"name":"Red Lantern","keywords":["red","lantern","pendulum","swing","tassel"]},{"name":"Vignette","keywords":["vignette"]}]},"IMG_7681":{"title":"Shader: IMG_7681","layers":[{"name":"Background","keywords":["background","sky","clouds"]},{"name":"Temple Roof","keywords":["temple","roof","tiles"]},{"name":"Dragon","keywords":["dragon","scales","head","claws","accents"]},{"name":"Vignette","keywords":["vignette"]}]},"IMG_7682":{"title":"Shader: IMG_7682","layers":[{"name":"Wall","keywords":["wall","peeling","paint","texture","weathered"]},{"name":"Window Grille","keywords":["window","grille","lattice","teal"]},{"name":"Railing","keywords":["railing","lotus","petals","gold"]},{"name":"Dragon","keywords":["dragon","silhouette","teal"]}]},"IMG_7683":{"title":"Shader: IMG_7683","layers":[{"name":"Background Courtyard","keywords":["background","courtyard","floor","wall","plants","shutters"]},{"name":"Temple Gate Header","keywords":["temple","gate","header","sign","gold"]},{"name":"Temple Gate Pillars","keywords":["temple","gate","pillars","brick","signs"]},{"name":"Temple Gate Grid","keywords":["temple","gate","grid","yellow","metal"]},{"name":"Red Chinese Characters","keywords":["red","chinese","characters"]},{"name":"White Paper Sign","keywords":["white","paper","sign","text"]},{"name":"Vignette","keywords":["vignette"]}]},"IMG_7684":{"title":"Shader: IMG_7684","layers":[{"name":"Background","keywords":["background","sky","building","roof"]},{"name":"Facade","keywords":["facade","window","sign","dragons","gold"]},{"name":"Bottom Roof","keywords":["bottom","roof","tiles","red","green"]},{"name":"Vignette","keywords":["vignette"]}]},"IMG_7685":{"title":"Shader: IMG_7685","layers":[{"name":"Background Wall","keywords":["background","wall","sky","roof"]},{"name":"Center Window","keywords":["center","window","vent","circle","text"]},{"name":"Balcony Railing","keywords":["balcony","railing","lotus","metal"]},{"name":"Bottom Red Section","keywords":["bottom","red","section"]},{"name":"Center Post","keywords":["center","post","ornate","gold"]},{"name":"Vignette","keywords":["vignette"]}]},"IMG_7686":{"title":"Shader: IMG_7686","layers":[{"name":"Background","keywords":["background","wall","ceiling","red","trim"]},{"name":"Lotus Grate","keywords":["lotus","grate","green","metal"]},{"name":"Foreground Roof","keywords":["foreground","roof","red","tiles"]},{"name":"Central Ornate Feature","keywords":["central","ornate","finial","gold","green","sphere"]},{"name":"Vignette","keywords":["vignette"]}]},"IMG_7694":{"title":"Shader: IMG_7694","layers":[{"name":"Background","keywords":["background","ground","wall","concrete"]},{"name":"Green Door","keywords":["green","door","panels"]},{"name":"Red Table","keywords":["red","table","legs"]},{"name":"Chairs","keywords":["chairs","blue","red","seat","flower"]},{"name":"Vignette","keywords":["vignette"]}]},"IMG_9792":{"title":"Shader: IMG_9792","layers":[{"name":"Sky Trees","keywords":["sky","trees","leaves","background"]},{"name":"Wall Roof","keywords":["wall","roof","tiles","brick"]},{"name":"Ground","keywords":["ground","dirt","sand"]},{"name":"Flowers Lamppost","keywords":["flowers","pink","lamppost","pole"]},{"name":"Rock Cairn","keywords":["rock","cairn","mound","voronoi"]},{"name":"Vignette","keywords":["vignette"]}]},"IMG_9812":{"title":"Shader: IMG_9812","layers":[{"name":"Background Wood Panels","keywords":["background","wood","panels","grain"]},{"name":"Left Objects","keywords":["picture","frame","leather","trunks"]},{"name":"Bottom Surface","keywords":["bottom","surface","trunk","rivets"]},{"name":"Center Wood Wheel Object","keywords":["center","wood","wheel","metal","rod"]},{"name":"Vignette","keywords":["vignette"]}]},"IMG_9813":{"title":"Shader: IMG_9813","layers":[{"name":"Background","keywords":["background","brushed","metal","reflections"]},{"name":"Main Graphics","keywords":["graphics","characters","repeating","black"]},{"name":"Text Opening Hours","keywords":["text","opening","hours"]},{"name":"Bottom Text Logos","keywords":["bottom","text","logos"]},{"name":"Vignette","keywords":["vignette"]}]},"kite_garment_sketch":{"title":"Kite Garment Sketch","layers":[{"name":"Background","keywords":["paper","background","texture","grain"]},{"name":"Person Head","keywords":["head","person","sketch"]},{"name":"Shoulders Neck","keywords":["shoulders","neck","lines"]},{"name":"Central Garment Spine","keywords":["central","garment","spine","line"]},{"name":"Kite Wing Top Edge","keywords":["kite","wing","top","edge"]},{"name":"Internal Arch Yoke","keywords":["internal","arch","yoke"]},{"name":"Kite Wing Bottom Edge","keywords":["kite","wing","bottom","edge"]},{"name":"Sleeves","keywords":["sleeves","lines"]},{"name":"Legs","keywords":["legs","lines"]},{"name":"Forbes Text","keywords":["forbes","text","squiggle"]}]},"laser_engraver":{"title":"Laser Engraver","layers":[{"name":"Optical Breadboard","keywords":["optical","breadboard","aluminum","brushed","holes"]},{"name":"Laser Dot","keywords":["laser","dot","red","glowing"]},{"name":"Vignette","keywords":["vignette","background"]}]},"lottery_ticket":{"title":"Lottery Ticket","layers":[{"name":"Background","keywords":["ticket","pink","gradient","pattern"]},{"name":"Blue Edges","keywords":["blue","edges","yellow","dots","stars"]},{"name":"Red Numbers","keywords":["red","numbers","digits"]},{"name":"Deity Figure","keywords":["deity","gold","halo","robes","coins","ingots"]},{"name":"Text Overlay","keywords":["text","overlay","red","bars"]},{"name":"Top Section","keywords":["top","date","logo","stamp"]}]},"metal_gate":{"title":"Metal Gate","layers":[{"name":"Background Tiles","keywords":["tiles","orange","brown","bevel","wall"]},{"name":"Gate Background","keywords":["dark","background","behind","gate"]},{"name":"Metal Gate Structure","keywords":["metal","gate","structure","bars","grid","mesh","arches","panels"]},{"name":"Gate Material","keywords":["metal","shading","gray","recessed"]}]},"mother_of_pearl":{"title":"Mother of Pearl Art","layers":[{"name":"Background","keywords":["blue","chipped","background"]},{"name":"Halo","keywords":["halo","circle","yellow","gold"]},{"name":"Side Figures","keywords":["side","figures","fans","hair","buns","faces"]},{"name":"Main Figure","keywords":["main","figure","body","sleeves","hands","collar","face","headdress","jewel"]},{"name":"Facial Features","keywords":["eyes","mouth","facial","features"]}]},"pavement_stripe":{"title":"Pavement Stripe","layers":[{"name":"Central Stripe","keywords":["stripe","central","speckle","noise","cement","aggregate","seams"]},{"name":"Pinwheel Paving","keywords":["pinwheel","paving","pattern","brick","green","beige","grout","bevel","stone"]},{"name":"Vignette","keywords":["vignette","fade","distance","perspective"]}]},"plastic_pallet":{"title":"Plastic Pallet","layers":[{"name":"Pallet Structure","keywords":["structure","nodes","beams","grid","holes"]},{"name":"Pallet Material","keywords":["plastic","pallet","yellow-green","bevel","texture"]},{"name":"Background Holes","keywords":["holes","background","dark","underneath"]}]},"porcelain_mosaic":{"title":"Porcelain Mosaic","layers":[{"name":"Background Cement","keywords":["cement","grey","noise","background"]},{"name":"Character Mask","keywords":["character","mask","shape","abstract"]},{"name":"Mosaic Tiles","keywords":["voronoi","shards","tiles","mosaic"]},{"name":"Tile Material","keywords":["porcelain","surface","white","blue","patterns"]},{"name":"Grout and Bevel","keywords":["grout","bevel","edge","shadow"]},{"name":"Drop Shadow","keywords":["drop","shadow","outside"]}]},"red_book":{"title":"Red Book on Perforated Surface","layers":[{"name":"Wall","keywords":["wall","background"]},{"name":"Floor","keywords":["floor","perforated","metal","shadow"]},{"name":"Red Book","keywords":["red","book","cover","spine","text","typography"]}]},"sticker_wall":{"title":"Sticker Wall","layers":[{"name":"Metal Panels","keywords":["metal","panels","seams","dirt","grime","wall"]},{"name":"Small Stickers","keywords":["stickers","ads","paper","text","random"]},{"name":"Warning Sign","keywords":["warning","sign","electrical","red","text","lightning"]}]},"woven_chair":{"title":"Woven Chair","layers":[{"name":"Background Floor","keywords":["background","floor","tiles","brown"]},{"name":"Weave Structure","keywords":["weave","structure","straps","vertical","horizontal","checkered"]},{"name":"Weave Material","keywords":["weave","material","blue","ribbed","texture","shading","shadows","overlap"]}]}};
window.LAYER_PHOTOS = {"1011":"moodboard_htmls/assets/IMG_1011.jpg","1121":"moodboard_htmls/assets/IMG_1121.jpg","0995":"moodboard_htmls/assets/IMG_0995.jpg","IMG_0398":"moodboard_htmls/assets/IMG_0398.jpg","IMG_0415":"moodboard_htmls/assets/IMG_0415.jpg","IMG_0417":"moodboard_htmls/assets/IMG_0417.jpg","IMG_0418":"moodboard_htmls/assets/IMG_0418.jpg","IMG_0419":"moodboard_htmls/assets/IMG_0419.jpg","IMG_0586":"moodboard_htmls/assets/IMG_0586.jpg","IMG_0767":"moodboard_htmls/assets/IMG_0767.jpg","IMG_0824":"moodboard_htmls/assets/IMG_0824.jpg","IMG_0825":"moodboard_htmls/assets/IMG_0825.jpg","IMG_0826":"moodboard_htmls/assets/IMG_0826.jpg","IMG_0965":"moodboard_htmls/assets/IMG_0965.jpg","IMG_0967":"moodboard_htmls/assets/IMG_0967.jpg","IMG_0973":"moodboard_htmls/assets/IMG_0973.jpg","IMG_0986":"moodboard_htmls/assets/IMG_0986.jpg","IMG_0992":"moodboard_htmls/assets/IMG_0992.jpg","IMG_0995":"moodboard_htmls/assets/IMG_0995.jpg","IMG_1011":"moodboard_htmls/assets/IMG_1011.jpg","IMG_1014":"moodboard_htmls/assets/IMG_1014.jpg","IMG_1061":"moodboard_htmls/assets/IMG_1061.jpg","IMG_1072":"moodboard_htmls/assets/IMG_1072.jpg","IMG_1077":"moodboard_htmls/assets/IMG_1077.jpg","IMG_1121":"moodboard_htmls/assets/IMG_1121.jpg","IMG_1126":"moodboard_htmls/assets/IMG_1126.jpg","IMG_1151":"moodboard_htmls/assets/IMG_1151.jpg","IMG_1183":"moodboard_htmls/assets/IMG_1183.jpg","IMG_1185":"moodboard_htmls/assets/IMG_1185.jpg","IMG_1213":"moodboard_htmls/assets/IMG_1213.jpg","IMG_1240":"moodboard_htmls/assets/IMG_1240.jpg","IMG_1241":"moodboard_htmls/assets/IMG_1241.jpg","IMG_1276":"moodboard_htmls/assets/IMG_1276.jpg","IMG_1300":"moodboard_htmls/assets/IMG_1300.jpg","IMG_1301":"moodboard_htmls/assets/IMG_1301.jpg","IMG_1306":"moodboard_htmls/assets/IMG_1306.jpg","IMG_1308":"moodboard_htmls/assets/IMG_1308.jpg","IMG_1309":"moodboard_htmls/assets/IMG_1309.jpg","IMG_1323":"moodboard_htmls/assets/IMG_1323.jpg","IMG_1333":"moodboard_htmls/assets/IMG_1333.jpg","IMG_1348":"moodboard_htmls/assets/IMG_1348.jpg","IMG_1399":"moodboard_htmls/assets/IMG_1399.jpg","IMG_1415":"moodboard_htmls/assets/IMG_1415.jpg","IMG_1426":"moodboard_htmls/assets/IMG_1426.jpg","IMG_1449":"moodboard_htmls/assets/IMG_1449.jpg","IMG_1621":"moodboard_htmls/assets/IMG_1621.jpg","IMG_1644":"moodboard_htmls/assets/IMG_1644.jpg","IMG_1663":"moodboard_htmls/assets/IMG_1663.jpg","IMG_1714":"moodboard_htmls/assets/IMG_1714.jpg","IMG_1718":"moodboard_htmls/assets/IMG_1718.jpg","IMG_1720":"moodboard_htmls/assets/IMG_1720.jpg","IMG_1721":"moodboard_htmls/assets/IMG_1721.jpg","IMG_1725":"moodboard_htmls/assets/IMG_1725.jpg","IMG_1847":"moodboard_htmls/assets/IMG_1847.jpg","IMG_2148":"moodboard_htmls/assets/IMG_2148.jpg","IMG_2249":"moodboard_htmls/assets/IMG_2249.jpg","IMG_2250":"moodboard_htmls/assets/IMG_2250.jpg","IMG_2263":"moodboard_htmls/assets/IMG_2263.jpg","IMG_2450":"moodboard_htmls/assets/IMG_2450.jpg","IMG_2477":"moodboard_htmls/assets/IMG_2477.jpg","IMG_2586":"moodboard_htmls/assets/IMG_2586.jpg","IMG_2700":"moodboard_htmls/assets/IMG_2700.jpg","IMG_2703":"moodboard_htmls/assets/IMG_2703.jpg","IMG_2745":"moodboard_htmls/assets/IMG_2745.jpg","IMG_2768":"moodboard_htmls/assets/IMG_2768.jpg","IMG_2773":"moodboard_htmls/assets/IMG_2773.jpg","IMG_2982":"moodboard_htmls/assets/IMG_2982.jpg","IMG_3105":"moodboard_htmls/assets/IMG_3105.jpg","IMG_3714":"moodboard_htmls/assets/IMG_3714.jpg","IMG_3922":"moodboard_htmls/assets/IMG_3922.jpg","IMG_4080":"moodboard_htmls/assets/IMG_4080.jpg","IMG_4742":"moodboard_htmls/assets/IMG_4742.jpg","IMG_4782":"moodboard_htmls/assets/IMG_4782.jpg","IMG_4783":"moodboard_htmls/assets/IMG_4783.jpg","IMG_4784":"moodboard_htmls/assets/IMG_4784.jpg","IMG_5181":"moodboard_htmls/assets/IMG_5181.jpg","IMG_5206":"moodboard_htmls/assets/IMG_5206.jpg","IMG_5211":"moodboard_htmls/assets/IMG_5211.jpg","IMG_5212":"moodboard_htmls/assets/IMG_5212.jpg","IMG_5214":"moodboard_htmls/assets/IMG_5214.jpg","IMG_5215":"moodboard_htmls/assets/IMG_5215.jpg","IMG_5225":"moodboard_htmls/assets/IMG_5225.jpg","IMG_5251":"moodboard_htmls/assets/IMG_5251.jpg","IMG_5320":"moodboard_htmls/assets/IMG_5320.jpg","IMG_5326":"moodboard_htmls/assets/IMG_5326.jpg","IMG_5346":"moodboard_htmls/assets/IMG_5346.jpg","IMG_5506":"moodboard_htmls/assets/IMG_5506.jpg","IMG_5517":"moodboard_htmls/assets/IMG_5517.jpg","IMG_5538":"moodboard_htmls/assets/IMG_5538.jpg","IMG_5569":"moodboard_htmls/assets/IMG_5569.jpg","IMG_6550":"moodboard_htmls/assets/IMG_6550.jpg","IMG_6551":"moodboard_htmls/assets/IMG_6551.jpg","IMG_6574":"moodboard_htmls/assets/IMG_6574.jpg","IMG_6609":"moodboard_htmls/assets/IMG_6609.jpg","IMG_6693":"moodboard_htmls/assets/IMG_6693.jpg","IMG_6694":"moodboard_htmls/assets/IMG_6694.jpg","IMG_6695":"moodboard_htmls/assets/IMG_6695.jpg","IMG_6697":"moodboard_htmls/assets/IMG_6697.jpg","IMG_6755":"moodboard_htmls/assets/IMG_6755.jpg","IMG_6786":"moodboard_htmls/assets/IMG_6786.jpg","IMG_6793":"moodboard_htmls/assets/IMG_6793.jpg","IMG_6876":"moodboard_htmls/assets/IMG_6876.jpg","IMG_6994":"moodboard_htmls/assets/IMG_6994.jpg","IMG_7211":"moodboard_htmls/assets/IMG_7211.jpg","IMG_7225":"moodboard_htmls/assets/IMG_7225.jpg","IMG_7266":"moodboard_htmls/assets/IMG_7266.jpg","IMG_7408":"moodboard_htmls/assets/IMG_7408.jpg","IMG_7409":"moodboard_htmls/assets/IMG_7409.jpg","IMG_7410":"moodboard_htmls/assets/IMG_7410.jpg","IMG_7680":"moodboard_htmls/assets/IMG_7680.jpg","IMG_7681":"moodboard_htmls/assets/IMG_7681.jpg","IMG_7682":"moodboard_htmls/assets/IMG_7682.jpg","IMG_7683":"moodboard_htmls/assets/IMG_7683.jpg","IMG_7684":"moodboard_htmls/assets/IMG_7684.jpg","IMG_7685":"moodboard_htmls/assets/IMG_7685.jpg","IMG_7686":"moodboard_htmls/assets/IMG_7686.jpg","IMG_7694":"moodboard_htmls/assets/IMG_7694.jpg","IMG_9792":"moodboard_htmls/assets/IMG_9792.jpg","IMG_9812":"moodboard_htmls/assets/IMG_9812.jpg","IMG_9813":"moodboard_htmls/assets/IMG_9813.jpg"};
